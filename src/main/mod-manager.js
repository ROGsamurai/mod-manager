const path = require('path');
const fs = require('fs-extra');
const extractZip = require('extract-zip');
const Seven = require('node-7z');
const sevenBin = require('7zip-bin');
const Store = require('electron-store');

const store = new Store({ name: 'mod-manager' });

// The game executable — used to verify the configured game folder is correct
// before anything is installed into it.
const GAME_EXE = 'Card Shop Simulator.exe';

const TARGETS = {
  game_root: { label: 'Game Root Folder', relative: '' },
  bepinex:   { label: 'BepInEx', relative: 'BepInEx' },
  plugins:   { label: 'BepInEx / plugins', relative: 'BepInEx/plugins' },
  patchers:  { label: 'BepInEx / patchers', relative: 'BepInEx/patchers' },
  config:    { label: 'BepInEx / config', relative: 'BepInEx/config' },
};

// Known core/framework mods that should be auto-locked
const CORE_MOD_NAMES = [
  'bepinex', 'bepinex with configuration manager', 'configurationmanager',
  'enhancedprefabloader', 'enhanced prefab loader',
  'phone overhaul', 'phone - overhaul',
  'grading overhaul',
  'expansion', 'expansions',
  'pokemon plushies', 'pokemon gameboy games', 'pokemon n64 games',
  'pokemon statues', 'custom furniture', 'custom tv',
  'theme decks', 'adventures manga',
];

class ModManager {
  constructor() {
    this.gamePath = store.get('gamePath', null);
    this.portablePath = null;
    this.mods = new Map();
    this.onProgress = null; // Set from main.js to send progress to renderer
    // Staged archives the user removed that were locked by another process at the
    // time (OneDrive sync / antivirus). They are hidden from the staged list and
    // retried on every refresh and on quit — see removeFromStaging.
    this._pendingDeletes = new Set();
    this._loadDb();
    this._migrateModData();
  }

  _isKnownCore(name) {
    const lower = (name || '').toLowerCase().replace(/\s+/g, ' ').trim();
    return CORE_MOD_NAMES.some(c => lower.includes(c));
  }

  /**
   * Find a known mod DB entry by name (case-insensitive). Returns null if
   * no match. Used during install to apply per-mod hints like `excludePaths`
   * (files the mod zip happens to include but which belong to a different,
   * standalone mod — e.g. shared phone-app icons).
   */
  _findKnownModByName(name) {
    if (!name) return null;
    const lower = name.toLowerCase();
    return this._getKnownModsDB().find(k => k.name.toLowerCase() === lower) || null;
  }

  /**
   * Global file-path exclude rules. Paths matching ANY of these patterns are
   * stripped from any mod's tracked files list (install, import, and scan).
   * Use this for files that:
   *   - belong to a different standalone mod (e.g. shared phone-app icons)
   *   - are auto-regenerated by BepInEx (configs, cache)
   *   - should never be deleted when uninstalling a mod
   * The files still exist on disk — they're just not OWNED by the mod.
   */
  _getGlobalExcludePatterns() {
    return [
      // BepInEx auto-regenerates config files on first game launch. Tracking
      // them causes ghost-uninstall behavior (delete → regenerate → still there).
      /^BepInEx\/config\/[^/]+\.(cfg|csv)$/i,
      // BepInEx cache is transient and rebuilt on launch.
      /^BepInEx\/cache\//i,
      // NOTE: We used to also exclude shared phone-app icons (DXPokemon,
      // HHPokemon, EGOShop, Pokemon Shop, Yu-Gi-Oh Shop) here so individual
      // content mods wouldn't "own" them. That was the right call before
      // reference counting existed — uninstalling any one DraX/HH/iiTzSamurai
      // pack would delete shared icons every other pack also needs. Now we
      // let every mod claim the icons its archive actually ships, and ref
      // counting at uninstall/toggle time keeps the shared files alive until
      // the LAST claimant is removed. Cleaner, no DB curation required.
    ];
  }

  /**
   * Apply global exclude patterns + per-mod excludePaths to a tracked files
   * list. Called during install, import, and scan to keep the tracked list
   * surgical so uninstall never touches files owned by other mods.
   *
   * IMPORTANT: a mod is exempt from any pattern that matches its own
   * signature file(s). Example: the "00 Pokemon Shop Phone App" mod has sig
   * "BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/IBG.png" — that
   * path matches the global exclude for shared Pokemon Shop icons, but the
   * exclude exists precisely to redirect ownership of those icons TO this mod.
   * So we must not strip them from this mod's own tracked list.
   */
  _applyExcludeRules(files, knownEntry) {
    if (!Array.isArray(files) || files.length === 0) return files;
    const patterns = this._getGlobalExcludePatterns();
    const perModExcludes = knownEntry?.excludePaths?.length
      ? new Set(knownEntry.excludePaths.map(p => p.toLowerCase()))
      : null;
    // Build the set of patterns this mod is EXEMPT from — any pattern that
    // matches one of this mod's own signature files OR content paths. The mod
    // is the rightful owner of anything its signature or contentPaths cover,
    // so those files should always be tracked.
    const exemptPatterns = new Set();
    const exemptionPaths = [
      ...(knownEntry?.signature || []),
      ...(knownEntry?.contentPaths || []),
    ];
    if (exemptionPaths.length) {
      for (const pat of patterns) {
        // A pattern matches a contentPath if the pattern would match any
        // plausible file inside it — we test by appending a placeholder.
        const hit = exemptionPaths.some(p => {
          if (pat.test(p)) return true;
          // For folder-rooted paths, test a sample file inside
          return pat.test(p + '/sample.png') || pat.test(p + '.dll');
        });
        if (hit) exemptPatterns.add(pat);
      }
    }
    return files.filter(f => {
      const lower = f.toLowerCase();
      if (perModExcludes && perModExcludes.has(lower)) return false;
      for (const pat of patterns) {
        if (exemptPatterns.has(pat)) continue;
        if (pat.test(f)) return false;
      }
      return true;
    });
  }

  /**
   * Resolve an installedFiles list against the on-disk state. Each entry is a
   * gamePath-relative path; any entry containing a '*' is treated as a simple
   * glob (single-segment wildcard) and expanded to whatever files actually
   * match on disk. Non-glob entries are passed through as-is and filtered to
   * those that exist. Returns the materialized file list — what the mod
   * actually owns right now.
   *
   * Used by install, scan, and migration to turn DB declarations like
   *   'BepInEx/plugins/Binder Overhaul/v*.txt'
   * into whatever version sidecar the current install wrote
   *   'BepInEx/plugins/Binder Overhaul/v*.txt'
   * so tracked mods stay accurate across user updates.
   */
  _resolveInstalledFiles(installedFiles) {
    if (!Array.isArray(installedFiles) || installedFiles.length === 0) return [];
    const out = [];
    const seen = new Set();
    for (const entry of installedFiles) {
      if (!entry || typeof entry !== 'string') continue;
      if (entry.includes('*')) {
        // Expand simple single-segment glob: "<dir>/<pattern>" where pattern
        // contains '*'. The glob is confined to one directory (no '**' recursion).
        const slash = entry.lastIndexOf('/');
        const dirRel = slash < 0 ? '' : entry.substring(0, slash);
        const pattern = slash < 0 ? entry : entry.substring(slash + 1);
        const dirFull = dirRel ? path.join(this.gamePath, dirRel) : this.gamePath;
        try {
          if (!fs.existsSync(dirFull) || !fs.statSync(dirFull).isDirectory()) continue;
          // Convert pattern to regex: escape regex chars, then replace \* with .*
          const regexSrc = '^' + pattern
            .split('*')
            .map(s => s.replace(/[.+?^${}()|[\]\\]/g, '\\$&'))
            .join('.*') + '$';
          const re = new RegExp(regexSrc, 'i');
          for (const name of fs.readdirSync(dirFull)) {
            if (!re.test(name)) continue;
            const full = path.join(dirFull, name);
            if (!fs.statSync(full).isFile()) continue;
            const relPath = dirRel ? dirRel + '/' + name : name;
            const key = relPath.toLowerCase();
            if (!seen.has(key)) { seen.add(key); out.push(relPath); }
          }
        } catch {}
      } else {
        try {
          if (fs.existsSync(path.join(this.gamePath, entry))) {
            const key = entry.toLowerCase();
            if (!seen.has(key)) { seen.add(key); out.push(entry); }
          }
        } catch {}
      }
    }
    return out;
  }

  /**
   * Reference counting for cross-mod file ownership.
   *
   * Returns a Set of lowercased gamePath-relative file paths that are claimed
   * by tracked mods OTHER than `excludeModId`. Used by uninstallMod and the
   * disable branch of toggleMod to make file removal precise: when DraX SV01
   * and DraX SV02 both ship "BepInEx/plugins/Phone - Overhaul/App Images/
   * DXPokemon/Clear.png", uninstalling SV01 must NOT delete that file because
   * SV02 still needs it. The shared file only gets cleaned up when the LAST
   * mod claiming it is uninstalled.
   *
   * All tracked mods (enabled AND disabled) count as claimants. A disabled mod
   * still expects its shared files to be present when it's re-enabled, so we
   * preserve them. Files unique to a single mod cleanly come and go with that
   * mod; shared files persist until every claimant is gone.
   *
   * Normalizes every comparison to gamePath-relative + lowercase. For mods
   * whose tracked files are extractTo-relative (non-scatter installs, plugin/
   * patcher/config targets), this method joins them to extractTo and converts
   * back to gamePath-relative before adding to the set, so cross-mod matches
   * work regardless of how each mod stores its files internally.
   */
  _filesClaimedByOtherMods(excludeModId) {
    const claimed = new Set();
    if (!this.gamePath) return claimed;
    const gp = this.gamePath.toLowerCase();
    for (const [id, mod] of this.mods) {
      if (id === excludeModId) continue;
      if (!Array.isArray(mod.files) || mod.files.length === 0) continue;
      const isScatter = (mod.targetKey === 'game_root' || mod.targetKey === 'bepinex');
      const base = isScatter
        ? (mod.targetKey === 'bepinex' ? path.join(this.gamePath, 'BepInEx') : this.gamePath)
        : (mod.extractTo || this.gamePath);
      for (const f of mod.files) {
        if (!f || typeof f !== 'string') continue;
        // Determine absolute path using the same rules toggle/uninstall do.
        let abs;
        if (mod.targetKey === 'bepinex' && /^BepInEx[\\/]/i.test(f)) {
          abs = path.join(this.gamePath, f);
        } else {
          abs = path.join(base, f);
        }
        // Convert back to gamePath-relative for the canonical key
        let rel = path.relative(this.gamePath, abs).replace(/\\/g, '/');
        if (rel) claimed.add(rel.toLowerCase());
      }
    }
    return claimed;
  }

  /**
   * Companion to _filesClaimedByOtherMods that returns the canonical
   * gamePath-relative key for one of `mod`'s tracked files. Used when iterating
   * a mod's files during uninstall/toggle to compare against the claim set.
   */
  _canonicalFileKey(mod, relFile) {
    const isScatter = (mod.targetKey === 'game_root' || mod.targetKey === 'bepinex');
    const base = isScatter
      ? (mod.targetKey === 'bepinex' ? path.join(this.gamePath, 'BepInEx') : this.gamePath)
      : (mod.extractTo || this.gamePath);
    let abs;
    if (mod.targetKey === 'bepinex' && /^BepInEx[\\/]/i.test(relFile)) {
      abs = path.join(this.gamePath, relFile);
    } else {
      abs = path.join(base, relFile);
    }
    return path.relative(this.gamePath, abs).replace(/\\/g, '/').toLowerCase();
  }

  _migrateModData() {
    let changed = false;
    const migrationVersion = store.get('migrationVersion', 0);

    for (const [id, mod] of this.mods) {
      if (mod.filename && (!mod.version || mod.version === '')) {
        const parsed = this._parseNexusFilename(mod.filename);
        if (parsed.version) { mod.version = parsed.version; changed = true; }
        if (parsed.name && mod.name === mod.filename.replace(/\.(zip|rar|7z)$/i, '')) {
          mod.name = parsed.name; changed = true;
        }
      }
    }

    // v6: Re-evaluate core status — only known core mods get locked
    if (migrationVersion < 6) {
      for (const [id, mod] of this.mods) {
        const shouldBeCore = this._isKnownCore(mod.name);
        if (mod.core !== shouldBeCore) {
          mod.core = shouldBeCore;
          changed = true;
        }
      }
      store.set('migrationVersion', 6);
    }

    // v7: Mark mods with _prefabloader folders as core
    if (migrationVersion < 7) {
      for (const [id, mod] of this.mods) {
        if (!mod.core && mod.files && mod.files.some(f => f.toLowerCase().includes('_prefabloader'))) {
          mod.core = true;
          changed = true;
        }
      }
      store.set('migrationVersion', 7);
    }

    // v8: Strip trailing version numbers from mod names
    if (migrationVersion < 8) {
      for (const [id, mod] of this.mods) {
        if (mod.version && mod.name) {
          const cleaned = mod.name.replace(/\s+v?\d+(\.\d+)*\s*$/i, '').trim();
          if (cleaned !== mod.name && cleaned.length > 0) {
            mod.name = cleaned;
            changed = true;
          }
        }
      }
      store.set('migrationVersion', 8);
    }

    // v9: Retroactively apply exclude rules to tracked file lists AND drop
    // "foreign" files that belong to another known mod's signature folder.
    // Fixes mods that were installed/imported before excludePaths existed:
    // they had shared icons, BepInEx configs, or other non-owned files
    // erroneously tracked. Also fixes the pre-v10 file-slurping bug where
    // top-level plugin DLLs walked all of BepInEx/plugins/ and grabbed
    // every sibling mod's files. Dropping those here means a subsequent
    // uninstall won't delete files that belong to other mods.
    // NOTE: v11 below re-runs this logic with an improved filter (handles
    // sig-companion files like .json sidecars). Most users will bypass v9
    // on first launch because v11 supersedes it. We keep the block intact
    // for robustness — running it twice is idempotent.
    if (migrationVersion < 9) {
      this._retroactivelyCleanModFiles();
      changed = true; // helper modifies in-place, mark dirty
      store.set('migrationVersion', 9);
    }

    // v10: Fix auto-group assignments that were made with corrupted file lists
    // from the pre-v9 scan bug (where top-level plugin DLLs walked all of
    // BepInEx/plugins/ and claimed every sibling mod's files). A mod whose
    // current files DON'T match its auto-group's rule anymore is removed
    // from that group. Then we also run auto-grouping over any mod that's
    // currently ungrouped, so Collection Tracker and any other mods imported
    // under an older build get slotted into the correct group.
    if (migrationVersion < 10) {
      const rules = this._autoGroupRules();
      const groups = this.getModGroups();
      let groupsChanged = false;
      // Pass A: remove mods from auto-groups whose rule no longer matches
      for (const group of groups) {
        const rule = rules.find(r => r.groupName === group.name);
        if (!rule) continue;
        const keepIds = [];
        for (const modId of group.modIds) {
          const mod = this.mods.get(modId);
          if (!mod) continue;
          const fileList = Array.isArray(mod.files) ? mod.files : [];
          const nameMatches = rule.namePattern && mod.name && rule.namePattern.test(mod.name);
          const pathMatches = rule.pathPattern && fileList.some(f => rule.pathPattern.test(f));
          const nameInSet = rule.names && mod.name && rule.names.has(mod.name.toLowerCase());
          if (nameMatches || pathMatches || nameInSet) {
            keepIds.push(modId);
          } else {
            groupsChanged = true;
          }
        }
        group.modIds = keepIds;
      }
      if (groupsChanged) store.set('modGroups', groups);
      // Pass B: any mod not in a group that matches a rule — auto-assign it.
      // This picks up Collection Tracker (imported as "CollectionTracker" under
      // older builds), and any mod whose DB entry was added in a newer version.
      for (const [id, mod] of this.mods) {
        const fileList = Array.isArray(mod.files) ? mod.files : [];
        this._autoAssignGroupByName(id, mod.name, fileList);
      }
      store.set('migrationVersion', 10);
    }

    // v11: Re-run the file-cleanup helper with improved logic that handles
    // sig-companion files (e.g. `.json` sidecar of another mod's signature).
    // Fixes mods imported under v1.0.3 earlier builds where the cleanup filter
    // didn't catch sibling mods' sidecar files in shared-parent layouts like
    // DXCardSets_prefabLoader/ (DraX SV/XY card-set addons).
    if (migrationVersion < 11) {
      this._retroactivelyCleanModFiles();
      changed = true;
      store.set('migrationVersion', 11);
    }

    // v12: Merge duplicate mod entries that share a name (case-insensitive).
    // Pre-v1.0.3 the scanner could create a phantom duplicate when the folder
    // name on disk had different casing than the DB signature — e.g. Phase 1
    // matched "EndGame Overhaul" (from DB) while Phase 2 then saw the
    // "Endgame Overhaul" folder on disk and imported it as a separate mod.
    // This migration merges such pairs into a single entry: files are unioned
    // and the duplicate id is removed from any groups it was placed in.
    if (migrationVersion < 12) {
      const byNameLower = new Map(); // lowerName → canonical id (first seen)
      const mergeIds = [];
      for (const [id, mod] of this.mods) {
        const lower = (mod.name || '').toLowerCase();
        if (!lower) continue;
        if (byNameLower.has(lower)) {
          mergeIds.push({ dupId: id, canonicalId: byNameLower.get(lower) });
        } else {
          byNameLower.set(lower, id);
        }
      }
      if (mergeIds.length > 0) {
        for (const { dupId, canonicalId } of mergeIds) {
          const dup = this.mods.get(dupId);
          const canonical = this.mods.get(canonicalId);
          if (!dup || !canonical) continue;
          // Merge files (union, case-insensitive dedup)
          const mergedFiles = Array.isArray(canonical.files) ? [...canonical.files] : [];
          const seen = new Set(mergedFiles.map(f => f.toLowerCase()));
          for (const f of (dup.files || [])) {
            if (!seen.has(f.toLowerCase())) {
              mergedFiles.push(f);
              seen.add(f.toLowerCase());
            }
          }
          canonical.files = mergedFiles;
          canonical.fileCount = mergedFiles.length;
          // If canonical lacks metadata (e.g. version) but dup has it, copy over
          if (!canonical.version && dup.version) canonical.version = dup.version;
          if (!canonical.filename && dup.filename) canonical.filename = dup.filename;
          this.mods.delete(dupId);
        }
        // Clean groups: replace any dupId references with canonicalId, dedup modIds
        const dupMap = new Map(mergeIds.map(m => [m.dupId, m.canonicalId]));
        const groups = this.getModGroups();
        let groupsChanged = false;
        for (const group of groups) {
          const newIds = [];
          const seenIds = new Set();
          for (const mid of group.modIds) {
            const mapped = dupMap.get(mid) || mid;
            if (!seenIds.has(mapped)) {
              newIds.push(mapped);
              seenIds.add(mapped);
            }
          }
          if (newIds.length !== group.modIds.length ||
              newIds.some((v, i) => v !== group.modIds[i])) {
            group.modIds = newIds;
            groupsChanged = true;
          }
        }
        if (groupsChanged) store.set('modGroups', groups);
        changed = true;
      }
      store.set('migrationVersion', 12);
    }

    // v13: Re-walk the sig folder of every tracked known mod and union any
    // on-disk files that got missed by earlier builds' tracking. Fixes mods
    // whose tracked list lost files (e.g. the `.json` sidecar of an HH card-
    // set prefab) due to a bug in an earlier scanner/migration. We only walk
    // a mod's own signature folder — never a shared container — and we drop
    // anything that lands in another mod's exclusive sig folder, is another
    // mod's exact sig, or matches a global exclude pattern.
    if (migrationVersion < 13) {
      if (this.gamePath && fs.existsSync(this.gamePath)) {
        const knownDB = this._getKnownModsDB();
        const knownSigFiles = new Map(); // lower-sig → mod-name-lower
        const knownSigFolders = [];
        for (const k of knownDB) {
          for (const sig of k.signature) {
            knownSigFiles.set(sig.toLowerCase(), k.name.toLowerCase());
            const parts = sig.split('/');
            if (parts.length < 2) continue;
            const folder = parts.slice(0, -1).join('/').toLowerCase();
            if (folder === 'bepinex/plugins' || folder === 'bepinex' || folder === '') continue;
            knownSigFolders.push({ folder, name: k.name.toLowerCase() });
          }
        }

        for (const [id, mod] of this.mods) {
          const known = this._findKnownModByName(mod.name);
          if (!known) continue;
          const ownName = (mod.name || '').toLowerCase();

          // AUTHORITATIVE TRACKING: if the DB declares installedFiles, replace
          // this mod's tracked list with that list (filtered to on-disk files).
          // Fixes mods whose tracked list was corrupted by pre-v1.0.3 scanner
          // guesswork — e.g. Phone Overhaul ending up with 41 files from other
          // mods' content, when it should have exactly 35 from its own zip.
          if (Array.isArray(known.installedFiles) && known.installedFiles.length > 0) {
            const authoritative = this._resolveInstalledFiles(known.installedFiles);
            // Compare to existing (order-insensitive, case-insensitive)
            const prevSet = new Set((mod.files || []).map(f => f.toLowerCase()));
            const newSet = new Set(authoritative.map(f => f.toLowerCase()));
            let differs = prevSet.size !== newSet.size;
            if (!differs) {
              for (const v of newSet) if (!prevSet.has(v)) { differs = true; break; }
            }
            if (differs) {
              mod.files = authoritative;
              mod.fileCount = authoritative.length;
              // installedFiles are gamePath-relative. If this mod was installed
              // via a non-scatter target (plugins/patchers/config), its extractTo
              // is a subfolder and its old `files` were extractTo-relative.
              // Mixing those with gamePath-relative paths would break toggle/uninstall
              // logic that does `path.join(extractTo, file)`. Normalize to game_root:
              // set extractTo=gamePath, targetKey=game_root, clear looseDlls flag.
              if (mod.targetKey !== 'game_root' && mod.targetKey !== 'bepinex') {
                mod.targetKey = 'game_root';
                mod.targetLabel = 'Game Root Folder';
                mod.extractTo = this.gamePath;
                if (mod.looseDlls) delete mod.looseDlls;
              }
              changed = true;
            }
            continue;
          }

          const currentFiles = new Set((mod.files || []).map(f => f.toLowerCase()));
          let changedAny = false;

          for (const sig of known.signature) {
            const parts = sig.split('/');
            if (parts.length < 2) continue;
            const sigParent = parts.slice(0, -1).join('/');
            const sigParentLower = sigParent.toLowerCase();
            // Skip shared containers — walking BepInEx/plugins would be disastrous
            if (sigParentLower === 'bepinex/plugins' || sigParentLower === 'bepinex' || sigParentLower === '') {
              // Still union in the exact sig file if it exists on disk
              const sigFull = path.join(this.gamePath, sig);
              try {
                if (fs.existsSync(sigFull) && !currentFiles.has(sig.toLowerCase())) {
                  mod.files = mod.files || [];
                  mod.files.push(sig);
                  currentFiles.add(sig.toLowerCase());
                  changedAny = true;
                }
              } catch {}
              continue;
            }
            // Walk the sig's parent folder
            const modRoot = path.join(this.gamePath, sigParent);
            let walkedFiles = [];
            try {
              if (fs.existsSync(modRoot) && fs.statSync(modRoot).isDirectory()) {
                walkedFiles = this._walkDir(modRoot, this.gamePath);
              }
            } catch { continue; }

            for (const f of walkedFiles) {
              const lower = f.toLowerCase();
              if (currentFiles.has(lower)) continue;
              // Skip another mod's exact sig
              const owner = knownSigFiles.get(lower);
              if (owner && owner !== ownName) continue;
              // Skip sidecar of another mod's sig
              let isForeignSidecar = false;
              for (const [otherSig, otherName] of knownSigFiles) {
                if (otherName === ownName) continue;
                if (lower.startsWith(otherSig + '.') || lower.startsWith(otherSig + '/')) {
                  isForeignSidecar = true; break;
                }
              }
              if (isForeignSidecar) continue;
              // Skip if under another mod's exclusive folder
              let isForeignFolder = false;
              for (const { folder, name: otherName } of knownSigFolders) {
                if (otherName === ownName) continue;
                if (lower.startsWith(folder + '/')) { isForeignFolder = true; break; }
              }
              if (isForeignFolder) continue;
              // Skip if globally excluded (unless this mod owns the pattern,
              // e.g. 00 Pokemon Shop Phone App owns the shared-icon paths).
              // _applyExcludeRules handles owner-exemption via the mod's own
              // signature files.
              if (this._applyExcludeRules([f], known).length === 0) continue;
              // Add!
              mod.files = mod.files || [];
              mod.files.push(f);
              currentFiles.add(lower);
              changedAny = true;
            }
          }

          // Also walk each declared contentPath (cross-folder mod content).
          // Same filters apply to avoid claiming another mod's files.
          if (Array.isArray(known.contentPaths)) {
            for (const cp of known.contentPaths) {
              const cpFull = path.join(this.gamePath, cp);
              let walked = [];
              try {
                if (!fs.existsSync(cpFull)) continue;
                const stat = fs.statSync(cpFull);
                if (stat.isDirectory()) {
                  walked = this._walkDir(cpFull, this.gamePath);
                } else if (stat.isFile()) {
                  walked = [cp];
                }
              } catch { continue; }
              for (const f of walked) {
                const lower = f.toLowerCase();
                if (currentFiles.has(lower)) continue;
                const owner = knownSigFiles.get(lower);
                if (owner && owner !== ownName) continue;
                let isForeignSidecar = false;
                for (const [otherSig, otherName] of knownSigFiles) {
                  if (otherName === ownName) continue;
                  if (lower.startsWith(otherSig + '.') || lower.startsWith(otherSig + '/')) {
                    isForeignSidecar = true; break;
                  }
                }
                if (isForeignSidecar) continue;
                // For contentPaths we DO want files inside other mods' folders
                // if they fall inside our declared contentPath — so skip the
                // foreign-folder check here.
                if (this._applyExcludeRules([f], known).length === 0) continue;
                mod.files = mod.files || [];
                mod.files.push(f);
                currentFiles.add(lower);
                changedAny = true;
              }
            }
          }
          if (changedAny) {
            mod.fileCount = mod.files.length;
            changed = true;
          }
        }
      }
      store.set('migrationVersion', 13);
    }

    // v14: re-run the authoritative-tracking pass from v13 with two fixes:
    //   (1) for mods whose DB entry declares installedFiles, normalize the
    //       tracked extractTo/targetKey to game_root when the mod was
    //       installed via a non-scatter target (plugins/patchers/config).
    //       Without this, toggle/uninstall compute wrong paths for loose-DLL
    //       mods like CashOrCard and silently fail.
    //   (2) same normalization for any mods that DID get v13 applied but
    //       still have the extractTo/files path mismatch.
    if (migrationVersion < 14) {
      if (this.gamePath && fs.existsSync(this.gamePath)) {
        for (const [, mod] of this.mods) {
          const known = this._findKnownModByName(mod.name);
          if (!known || !Array.isArray(known.installedFiles) || known.installedFiles.length === 0) continue;
          const authoritative = this._resolveInstalledFiles(known.installedFiles);
          // Always re-sync the file list so it matches what's on disk
          const prevSet = new Set((mod.files || []).map(f => f.toLowerCase()));
          const newSet = new Set(authoritative.map(f => f.toLowerCase()));
          let differs = prevSet.size !== newSet.size;
          if (!differs) {
            for (const v of newSet) if (!prevSet.has(v)) { differs = true; break; }
          }
          const needsNormalize = mod.targetKey !== 'game_root' && mod.targetKey !== 'bepinex';
          if (differs || needsNormalize) {
            mod.files = authoritative;
            mod.fileCount = authoritative.length;
            if (needsNormalize) {
              mod.targetKey = 'game_root';
              mod.targetLabel = 'Game Root Folder';
              mod.extractTo = this.gamePath;
              if (mod.looseDlls) delete mod.looseDlls;
            }
            changed = true;
          }
        }
      }
      store.set('migrationVersion', 14);
    }

    // v15: force a re-sync of authoritative tracking from DB installedFiles.
    // The DB was fixed in v1.0.3 to include missing prefab sig binaries for
    // 183 entries (DraX, HellHound, hololive, Brainrot, Sports-series, Lorcana,
    // Riftbound, Sorcery, Grand Archive, MetaZoo, Shadowverse, Arknights,
    // BlazBlue, Cardfight Vanguard, Fire Emblem Cipher, Union Arena, Final
    // Fantasy, 40K, Gundam, GPK, Grails, Chaotic, Music Battlers, Pokedoodles,
    // Road Trip, Schwarz, hBP, Unset MTG, and more). Existing installs stopped
    // at v14 still have the old truncated file list on disk — this migration
    // adopts the fresh DB list so toggle/uninstall moves both the prefab binary
    // and its .json sidecar. Same normalization logic as v14 applies.
    if (migrationVersion < 15) {
      if (this.gamePath && fs.existsSync(this.gamePath)) {
        for (const [, mod] of this.mods) {
          const known = this._findKnownModByName(mod.name);
          if (!known || !Array.isArray(known.installedFiles) || known.installedFiles.length === 0) continue;
          const authoritative = this._resolveInstalledFiles(known.installedFiles);
          const prevSet = new Set((mod.files || []).map(f => f.toLowerCase()));
          const newSet = new Set(authoritative.map(f => f.toLowerCase()));
          let differs = prevSet.size !== newSet.size;
          if (!differs) {
            for (const v of newSet) if (!prevSet.has(v)) { differs = true; break; }
          }
          const needsNormalize = mod.targetKey !== 'game_root' && mod.targetKey !== 'bepinex';
          if (differs || needsNormalize) {
            mod.files = authoritative;
            mod.fileCount = authoritative.length;
            if (needsNormalize) {
              mod.targetKey = 'game_root';
              mod.targetLabel = 'Game Root Folder';
              mod.extractTo = this.gamePath;
              if (mod.looseDlls) delete mod.looseDlls;
            }
            changed = true;
          }
        }
      }
      store.set('migrationVersion', 15);
    }

    // v16: re-run authoritative sync. DB was corrected again to restore
    // gamePath-relative paths for 6 mods that regressed during the last
    // re-bake pass (EnhancedPrefabLoader, TextureReplacer, Auto Pricer,
    // Fast Opening Pack, TCGSimPC, Worker Shelf Access). Existing installs
    // stuck at v15 still track the old bad paths — this adopts the fixed list.
    if (migrationVersion < 16) {
      if (this.gamePath && fs.existsSync(this.gamePath)) {
        for (const [, mod] of this.mods) {
          const known = this._findKnownModByName(mod.name);
          if (!known || !Array.isArray(known.installedFiles) || known.installedFiles.length === 0) continue;
          const authoritative = this._resolveInstalledFiles(known.installedFiles);
          const prevSet = new Set((mod.files || []).map(f => f.toLowerCase()));
          const newSet = new Set(authoritative.map(f => f.toLowerCase()));
          let differs = prevSet.size !== newSet.size;
          if (!differs) {
            for (const v of newSet) if (!prevSet.has(v)) { differs = true; break; }
          }
          const needsNormalize = mod.targetKey !== 'game_root' && mod.targetKey !== 'bepinex';
          if (differs || needsNormalize) {
            mod.files = authoritative;
            mod.fileCount = authoritative.length;
            if (needsNormalize) {
              mod.targetKey = 'game_root';
              mod.targetLabel = 'Game Root Folder';
              mod.extractTo = this.gamePath;
              if (mod.looseDlls) delete mod.looseDlls;
            }
            changed = true;
          }
        }
      }
      store.set('migrationVersion', 16);
    }

    // v17: installedFiles now support glob patterns (like "v*.txt") to handle
    // version-stamped sidecars that Munchmatoast mods + Holographic/Phone
    // Overhaul ship. Old installs tracked the specific versioned filename
    // (e.g. "v2.0.10.txt") which goes stale on update. Re-resolve against the
    // DB's glob patterns so the mod now owns whatever version sidecar is
    // actually on disk, and leftover/stale paths drop off.
    if (migrationVersion < 17) {
      if (this.gamePath && fs.existsSync(this.gamePath)) {
        for (const [, mod] of this.mods) {
          const known = this._findKnownModByName(mod.name);
          if (!known || !Array.isArray(known.installedFiles) || known.installedFiles.length === 0) continue;
          const authoritative = this._resolveInstalledFiles(known.installedFiles);
          const prevSet = new Set((mod.files || []).map(f => f.toLowerCase()));
          const newSet = new Set(authoritative.map(f => f.toLowerCase()));
          let differs = prevSet.size !== newSet.size;
          if (!differs) {
            for (const v of newSet) if (!prevSet.has(v)) { differs = true; break; }
          }
          if (differs) {
            mod.files = authoritative;
            mod.fileCount = authoritative.length;
            changed = true;
          }
        }
      }
      store.set('migrationVersion', 17);
    }

    // v18: re-sync after adding YuGiOh DB entries (00 YuGiOh Shop Phone App,
    // 01 Legend Of Blue Eyes White Dragon Expansion) plus the new Yu-Gi-Oh
    // Shop shared-icon exclude pattern. Existing installs that imported
    // these mods as untracked-unknown entries before v1.0.5 will have stale
    // file lists; this re-applies authoritative tracking + auto-grouping.
    if (migrationVersion < 18) {
      if (this.gamePath && fs.existsSync(this.gamePath)) {
        for (const [, mod] of this.mods) {
          const known = this._findKnownModByName(mod.name);
          if (!known || !Array.isArray(known.installedFiles) || known.installedFiles.length === 0) continue;
          const authoritative = this._resolveInstalledFiles(known.installedFiles);
          const prevSet = new Set((mod.files || []).map(f => f.toLowerCase()));
          const newSet = new Set(authoritative.map(f => f.toLowerCase()));
          let differs = prevSet.size !== newSet.size;
          if (!differs) {
            for (const v of newSet) if (!prevSet.has(v)) { differs = true; break; }
          }
          if (differs) {
            mod.files = authoritative;
            mod.fileCount = authoritative.length;
            changed = true;
          }
        }
      }
      store.set('migrationVersion', 18);
    }

    // v19: Rename YuGiOh DB entries — the three Real TCG Overhaul YuGiOh
    // accessories were originally added as "01 Yu Gi Oh Manga", "02 Yu Gi Oh
    // Starter Decks", "03 Yu Gi Oh Structure Decks". Renamed to the cleaner
    // canonical form "Yu-Gi-Oh Manga" / "Yu-Gi-Oh Starter Decks" / "Yu-Gi-Oh
    // Structure Decks". Existing installs that tracked the old names need
    // their .name field updated so _findKnownModByName matches the DB and
    // future migrations / scans pick them up correctly.
    if (migrationVersion < 19) {
      const renames = new Map([
        ['01 yu gi oh manga', 'Yu-Gi-Oh Manga'],
        ['02 yu gi oh starter decks', 'Yu-Gi-Oh Starter Decks'],
        ['03 yu gi oh structure decks', 'Yu-Gi-Oh Structure Decks'],
      ]);
      for (const [, mod] of this.mods) {
        const newName = renames.get((mod.name || '').toLowerCase());
        if (newName && mod.name !== newName) {
          mod.name = newName;
          changed = true;
        }
      }
      store.set('migrationVersion', 19);
    }

    // v20: Repair entries created from a duplicate download. Before the
    // duplicate-suffix fix, installing "Mod-612-3-4-2-1777790483 (1).zip"
    // produced an entry named after the raw filename tail
    // ("BepInEx with Configuration Manager-2-5-4-23-2-1726589034") with no
    // version, sitting alongside the correctly named one. Re-parse those names
    // and merge them into the real entry.
    if (migrationVersion < 20) {
      const looksLikeRawTail = (n) => /-\d+(?:-\d+)+$/.test(n || '');
      const byBase = new Map();
      for (const [id, mod] of this.mods) {
        if (!looksLikeRawTail(mod.name)) byBase.set(this._baseName(mod.name), id);
      }
      for (const [id, mod] of Array.from(this.mods)) {
        if (!looksLikeRawTail(mod.name)) continue;
        const parsed = this._parseNexusFilename(`${mod.name}.zip`);
        const canonicalId = byBase.get(this._baseName(parsed.name));
        const canonical = canonicalId && canonicalId !== id ? this.mods.get(canonicalId) : null;
        if (canonical) {
          // Same mod installed twice — fold the stray entry's files into the
          // real one so nothing becomes untracked, then drop it.
          const merged = Array.isArray(canonical.files) ? [...canonical.files] : [];
          const seen = new Set(merged.map(f => f.toLowerCase()));
          for (const f of (mod.files || [])) {
            if (!seen.has(f.toLowerCase())) { merged.push(f); seen.add(f.toLowerCase()); }
          }
          canonical.files = merged;
          canonical.fileCount = merged.length;
          if (!canonical.version && mod.version) canonical.version = mod.version;
          this.mods.delete(id);
          // Drop the dead id from any group it was placed in.
          const groups = this.getModGroups();
          let groupsChanged = false;
          for (const g of groups) {
            const kept = g.modIds.filter(mid => mid !== id);
            if (kept.length !== g.modIds.length) { g.modIds = kept; groupsChanged = true; }
          }
          if (groupsChanged) store.set('modGroups', groups);
        } else if (parsed.name && parsed.name !== mod.name) {
          // No twin — just give it its proper name and version back.
          mod.name = parsed.name;
          if (!mod.version && parsed.version) mod.version = parsed.version;
          byBase.set(this._baseName(mod.name), id);
        }
        changed = true;
      }
      store.set('migrationVersion', 20);
    }

    if (changed) this._saveDb();
  }

  /**
   * Retroactively scrubs every tracked mod's `files` list:
   *   (1) applies global exclude patterns (shared icons, BepInEx configs/cache)
   *   (2) drops files that ARE another known mod's exact signature
   *   (3) drops files that are SIDECARS of another mod's signature
   *       (e.g. `foo.json` next to another mod's `foo` sig file, or files
   *       nested inside a folder named after another mod's sig path)
   *   (4) drops files in another mod's exclusive sig folder
   * Used by migrations v9 and v11. Idempotent — safe to run multiple times.
   */
  _retroactivelyCleanModFiles() {
    const knownDB = this._getKnownModsDB();
    const knownSigFolders = [];
    const knownSigFiles = new Map(); // lower-case-path → mod-name-lower
    for (const k of knownDB) {
      for (const sig of k.signature) {
        const parts = sig.split('/');
        if (parts.length < 2) continue;
        const folder = parts.slice(0, -1).join('/').toLowerCase();
        knownSigFiles.set(sig.toLowerCase(), k.name.toLowerCase());
        // Skip shared containers — those aren't foreign-folder markers.
        if (folder === 'bepinex/plugins' || folder === 'bepinex' || folder === '') continue;
        knownSigFolders.push({ folder, name: k.name.toLowerCase() });
      }
    }

    for (const [id, mod] of this.mods) {
      if (!Array.isArray(mod.files) || mod.files.length === 0) continue;
      const known = this._findKnownModByName(mod.name);
      const ownName = (mod.name || '').toLowerCase();
      const ownFolders = new Set();
      if (known) {
        for (const sig of known.signature) {
          const parts = sig.split('/');
          if (parts.length < 2) continue;
          const folder = parts.slice(0, -1).join('/').toLowerCase();
          if (folder === 'bepinex/plugins' || folder === 'bepinex' || folder === '') continue;
          ownFolders.add(folder);
        }
      }

      const before = mod.files.length;
      let filtered = this._applyExcludeRules(mod.files, known);
      filtered = filtered.filter(f => {
        const lower = f.toLowerCase();

        // Drop if file is another mod's exact signature, or is a sibling
        // companion of another mod's signature (e.g. .json/.meta sidecar).
        // This must come BEFORE the "own folder" keep check, because in the
        // shared-parent case (DXCardSets_prefabLoader, HH EPL Pokemons) the
        // "own folder" is the shared parent and every sibling mod's files
        // are also under it.
        const sigOwner = knownSigFiles.get(lower);
        if (sigOwner && sigOwner !== ownName) return false;
        for (const [otherSigLower, otherName] of knownSigFiles) {
          if (otherName === ownName) continue;
          // starts with "other-sig." → e.g. "...sv-02pal.json" next to "...sv-02pal"
          // starts with "other-sig/" → files nested under a sig-named folder
          if (lower.startsWith(otherSigLower + '.') ||
              lower.startsWith(otherSigLower + '/')) return false;
        }

        // Keep files inside this mod's own sig folders
        for (const ownFolder of ownFolders) {
          if (lower.startsWith(ownFolder + '/') || lower === ownFolder) return true;
        }
        // Drop files inside another mod's exclusive sig folder
        for (const { folder, name: otherName } of knownSigFolders) {
          if (otherName === ownName) continue;
          if (lower.startsWith(folder + '/')) return false;
        }
        return true;
      });
      if (filtered.length !== before) {
        mod.files = filtered;
        mod.fileCount = filtered.length;
      }
    }
  }

  setPortablePath(p) {
    this.portablePath = p;
    try {
      fs.ensureDirSync(this.getStagingPath());
      fs.ensureDirSync(this._getDisabledPath());
    } catch (err) {
      // If we can't create folders next to the exe (read-only location),
      // fall back to user's app data directory
      const fallback = require('electron').app?.getPath('userData') || p;
      this.portablePath = fallback;
      try {
        fs.ensureDirSync(this.getStagingPath());
        fs.ensureDirSync(this._getDisabledPath());
      } catch (e) {
        console.error('Failed to create staging folders:', e.message);
      }
    }
    // Clean up any stale temp folders left behind by crashes
    this._cleanStaleTempFolders();
    // Stage the private 7za copy now, while the portable unpack dir is still
    // guaranteed to exist, rather than on first archive operation.
    try { this._get7zBin(true); } catch { /* non-fatal — resolved lazily later */ }
  }

  /** Remove leftover temp extraction folders from previous crashes.
   *  Temp dirs are created in getStagingPath() with these prefixes:
   *    _temp_extract_ (install flow)
   *    _zip_temp_     (extract-zip fallback)
   *    _7z_temp_      (node-7z fallback)
   *  Legacy locations (old versions put them in gamePath or BepInEx/) are
   *  still swept to clean up after upgrade. */
  _cleanStaleTempFolders() {
    const PREFIXES = ['_temp_extract_', '_zip_temp_', '_7z_temp_'];
    const sweep = (dir) => {
      try {
        if (!fs.existsSync(dir)) return;
        for (const entry of fs.readdirSync(dir)) {
          if (PREFIXES.some(p => entry.startsWith(p))) {
            try { fs.removeSync(path.join(dir, entry)); } catch {}
          }
        }
      } catch {}
    };
    sweep(this.getStagingPath());
    // Legacy locations — old versions put 7z temp folders in the game dir.
    if (this.gamePath) {
      sweep(this.gamePath);
      sweep(path.join(this.gamePath, 'BepInEx'));
    }
  }

  getStagingPath() { return path.join(this.portablePath || '.', 'staging'); }
  _getDisabledPath() { return path.join(this.portablePath || '.', 'disabled-mods'); }

  /**
   * Normalize a chosen game folder to the one that actually contains the game
   * executable.
   *
   * Xbox Game Pass installs look like:
   *   <install root>\gamelaunchhelper.exe
   *   <install root>\Content\Card Shop Simulator.exe
   * Mods (BepInEx, doorstop, plugins) must sit next to the EXE — i.e. in
   * Content\ — or BepInEx never loads. Users naturally browse to the install
   * root, which would install every mod one level too high and silently do
   * nothing in game. If the selected folder has no exe but Content\ does, use
   * Content\ instead.
   */
  _normalizeGamePath(p) {
    if (!p) return p;
    const EXE = 'Card Shop Simulator.exe';
    try {
      if (fs.existsSync(path.join(p, EXE))) return p;
      const content = path.join(p, 'Content');
      if (fs.existsSync(path.join(content, EXE))) return content;
    } catch {}
    return p;
  }

  setGamePath(p) {
    const resolved = this._normalizeGamePath(p);
    this.gamePath = resolved;
    store.set('gamePath', resolved);
    return resolved;
  }
  getGamePath() { return this.gamePath; }

  getTargetPath(key) {
    if (!this.gamePath) return null;
    const t = TARGETS[key];
    return t ? (t.relative ? path.join(this.gamePath, t.relative) : this.gamePath) : null;
  }

  getTargets() {
    return Object.entries(TARGETS).map(([key, val]) => ({ key, label: val.label, fullPath: this.getTargetPath(key) }));
  }

  isBepInExInstalled() {
    if (!this.gamePath) return false;
    return fs.existsSync(path.join(this.gamePath, 'BepInEx', 'core')) && fs.existsSync(path.join(this.gamePath, 'winhttp.dll'));
  }

  /**
   * Is "Card Shop Simulator.exe" actually present at the configured game path?
   *
   * This is the check that catches a wrong game folder BEFORE mods are written
   * into it. Installing to a folder the game never reads looks like it worked —
   * files copy fine — but nothing loads in game, which is nearly impossible for
   * a user to diagnose. Case-insensitive so it behaves the same on Linux.
   */
  isGameExePresent(gamePath) {
    const dir = gamePath || this.gamePath;
    if (!dir) return false;
    try {
      if (fs.existsSync(path.join(dir, GAME_EXE))) return true;
      // Linux is case-sensitive; match the filename regardless of case.
      const want = GAME_EXE.toLowerCase();
      return fs.readdirSync(dir, { withFileTypes: true })
        .some(e => e.isFile() && e.name.toLowerCase() === want);
    } catch {
      return false;
    }
  }

  getBepInExStatus() {
    if (!this.gamePath) return { installed: false, gameFound: false, reason: 'Game path not set.' };
    // Verify the game itself first — a wrong folder makes every later check and
    // every install meaningless.
    const gameFound = this.isGameExePresent();
    if (!gameFound) {
      return {
        installed: false,
        gameFound: false,
        gameMissing: true,
        reason: `"${GAME_EXE}" was not found in the selected folder. Point the game location at the folder containing it (for Xbox Game Pass that is the "Content" folder), or mods will install where the game cannot load them.`,
      };
    }
    if (!this.isBepInExInstalled()) return { installed: false, gameFound: true, reason: 'BepInEx not found. Download it and install with Game Root target.', ...this._duplicateStatus() };
    return { installed: true, gameFound: true, ...this._duplicateStatus() };
  }

  /**
   * Duplicate-plugin summary for the live Mod Status panel. Folded into
   * getBepInExStatus so it refreshes on the same events as the game and BepInEx
   * checks (startup, install, uninstall, path change) instead of only when
   * someone opens the Health Check.
   */
  _duplicateStatus() {
    try {
      const groups = this.scanDuplicatePluginDlls();
      return {
        duplicateCount: groups.reduce((n, g) => n + g.strays.length, 0),
        duplicateDlls: groups.map(g => g.dll),
      };
    } catch (err) {
      console.warn('[status] duplicate scan failed:', err.message);
      return { duplicateCount: 0, duplicateDlls: [] };
    }
  }

  /** Detailed health check — verifies all critical BepInEx files.
   *  Every fs call is guarded: if a directory is locked (OneDrive/antivirus)
   *  or disappears mid-check, the result is reported as 'warn' instead of
   *  crashing the main process. */
  /**
   * Find plugin DLLs that exist in more than one place under BepInEx/plugins.
   *
   * BepInEx loads one plugin per GUID and skips the rest, so a duplicate leaves
   * the mod listed and enabled in the F1 config menu while the copy that
   * actually loaded may be looking for its assets somewhere else entirely — the
   * failure a user hit with TextureReplacer.dll present both in plugins/ and in
   * plugins/TextureReplacer/. Nothing in the game or the manager showed it.
   *
   * Detection is independent of who installed the copies: hand-installs and
   * leftovers from other tools are exactly the cases that need catching.
   * Shared libraries (0Harmony and friends) legitimately appear many times and
   * are never reported.
   */
  scanDuplicatePluginDlls() {
    const pluginsDir = path.join(this.gamePath || '', 'BepInEx', 'plugins');
    if (!this.gamePath || !fs.existsSync(pluginsDir)) return [];

    const byName = new Map();
    const walk = (dir) => {
      let entries = [];
      try { entries = fs.readdirSync(dir, { withFileTypes: true }); } catch { return; }
      for (const e of entries) {
        const full = path.join(dir, e.name);
        if (e.isDirectory()) { walk(full); continue; }
        if (!/\.dll$/i.test(e.name)) continue;
        const key = e.name.toLowerCase();
        if (ModManager.SHARED_DLLS.has(key)) continue;
        if (!byName.has(key)) byName.set(key, []);
        byName.get(key).push(full);
      }
    };
    walk(pluginsDir);

    // Which tracked mod, if any, owns each path.
    const owner = new Map();
    for (const [, m] of this.mods) {
      for (const abs of this._absFilesOf(m)) owner.set(path.resolve(abs).toLowerCase(), m.name);
    }

    const ident = (v) => String(v || '').toLowerCase().replace(/[^a-z0-9]/g, '');
    const groups = [];
    for (const [key, paths] of byName) {
      if (paths.length < 2) continue;
      const stem = ident(key.replace(/\.dll$/i, ''));
      const score = (p) => {
        const rel = path.relative(pluginsDir, p).replace(/\\/g, '/');
        const depth = rel.split('/').length - 1;
        let sc = 0;
        // A copy inside a folder named after the DLL is the canonical layout.
        if (depth > 0 && ident(rel.split('/')[0]) === stem) sc += 100;
        if (owner.has(path.resolve(p).toLowerCase())) sc += 40;   // a mod claims it
        if (depth > 0) sc += 10;                                   // in a folder beats plugins root
        let mtime = 0;
        try { mtime = fs.statSync(p).mtimeMs; } catch {}
        return { sc, mtime };
      };
      const scored = paths.map(p => ({ path: p, ...score(p) }))
        .sort((a, b) => (b.sc - a.sc) || (b.mtime - a.mtime));
      groups.push({
        dll: path.basename(paths[0]),
        keep: scored[0].path,
        strays: scored.slice(1).map(x => ({
          path: x.path,
          owner: owner.get(path.resolve(x.path).toLowerCase()) || null,
        })),
      });
    }
    return groups;
  }

  /**
   * Delete the redundant copies found by scanDuplicatePluginDlls and untrack
   * them from whichever mod claimed them, so the DB does not keep pointing at
   * files that are gone.
   */
  async removeDuplicatePluginDlls() {
    const groups = this.scanDuplicatePluginDlls();
    const removed = [];
    for (const g of groups) {
      for (const stray of g.strays) {
        try {
          await fs.remove(stray.path);
          removed.push(stray.path);
          console.log(`[health] removed duplicate plugin DLL: ${stray.path} (kept ${g.keep})`);
        } catch (err) {
          console.warn(`[health] could not remove ${stray.path}:`, err.message);
        }
      }
    }
    if (removed.length > 0) {
      const gone = new Set(removed.map(p => path.resolve(p).toLowerCase()));
      for (const [, m] of this.mods) {
        if (!Array.isArray(m.files) || m.files.length === 0) continue;
        const abs = this._absFilesOf(m);
        const keptFiles = m.files.filter((_, i) => !gone.has(path.resolve(abs[i]).toLowerCase()));
        if (keptFiles.length !== m.files.length) {
          m.files = keptFiles;
          m.fileCount = keptFiles.length;
        }
      }
      this._saveDb();
    }
    return removed;
  }

  bepinexHealthCheck() {
    if (!this.gamePath) return { ok: false, checks: [{ file: 'Game Path', status: 'missing', detail: 'Game path not set' }] };
    const checks = [];
    const safeExists = (p) => { try { return fs.existsSync(p); } catch { return false; } };
    const safeStat = (p) => { try { return fs.statSync(p); } catch { return null; } };
    const safeReaddir = (p) => { try { return fs.readdirSync(p); } catch { return null; } };

    const winhttp = path.join(this.gamePath, 'winhttp.dll');
    const doorstop = path.join(this.gamePath, 'doorstop_config.ini');
    const winhttpStat = safeStat(winhttp);
    checks.push({ file: 'winhttp.dll', status: safeExists(winhttp) ? 'ok' : 'missing',
      detail: safeExists(winhttp) ? (winhttpStat ? `${(winhttpStat.size / 1024).toFixed(0)} KB` : 'Present (size unavailable)') : 'BepInEx loader hook missing. Reinstall BepInEx.' });
    checks.push({ file: 'doorstop_config.ini', status: safeExists(doorstop) ? 'ok' : 'missing',
      detail: safeExists(doorstop) ? 'Present' : 'Doorstop config missing. Reinstall BepInEx.' });
    if (safeExists(doorstop)) {
      try {
        const content = fs.readFileSync(doorstop, 'utf8');
        if (!content.includes('BepInEx') && !content.includes('bepinex')) {
          checks[checks.length - 1].status = 'warn';
          checks[checks.length - 1].detail = 'File exists but does not reference BepInEx — may be corrupted.';
        }
      } catch {}
    }
    const bepDir = path.join(this.gamePath, 'BepInEx');
    checks.push({ file: 'BepInEx/', status: safeExists(bepDir) ? 'ok' : 'missing',
      detail: safeExists(bepDir) ? 'Present' : 'Main BepInEx folder missing. Reinstall BepInEx.' });
    const coreDir = path.join(bepDir, 'core');
    const coreList = safeExists(coreDir) ? safeReaddir(coreDir) : null;
    checks.push({ file: 'BepInEx/core/', status: safeExists(coreDir) ? (coreList ? 'ok' : 'warn') : 'missing',
      detail: safeExists(coreDir) ? (coreList ? `${coreList.length} files` : 'Present (cannot read — file locked)') : 'Core framework DLLs missing. Reinstall BepInEx.' });
    if (coreList) {
      const hasBepDll = coreList.some(f => f.toLowerCase().includes('bepinex') && f.endsWith('.dll'));
      if (!hasBepDll) checks.push({ file: 'BepInEx.dll', status: 'missing', detail: 'Core BepInEx DLL not found in BepInEx/core/. Reinstall BepInEx.' });
    }
    const pluginsDir = path.join(bepDir, 'plugins');
    const pluginsList = safeExists(pluginsDir) ? safeReaddir(pluginsDir) : null;
    checks.push({ file: 'BepInEx/plugins/', status: safeExists(pluginsDir) ? (pluginsList ? 'ok' : 'warn') : 'warn',
      detail: safeExists(pluginsDir) ? (pluginsList ? `${pluginsList.length} items` : 'Present (cannot read — file locked)') : 'Plugins folder missing — created when first mod is installed.' });
    // Duplicate plugin DLLs — invisible from inside the game, and the reason a
    // mod can be installed, enabled and completely inert.
    try {
      const dupes = this.scanDuplicatePluginDlls();
      if (dupes.length > 0) {
        const names = dupes.map(d => d.dll).join(', ');
        const count = dupes.reduce((n, d) => n + d.strays.length, 0);
        checks.push({ file: 'Duplicate plugins', status: 'warn', fixable: 'duplicate-dlls',
          detail: `${count} redundant cop${count === 1 ? 'y' : 'ies'} of ${names}. BepInEx loads only one, so the mod can appear enabled while doing nothing.` });
      } else {
        checks.push({ file: 'Duplicate plugins', status: 'ok', detail: 'No duplicate plugin DLLs' });
      }
    } catch (err) {
      checks.push({ file: 'Duplicate plugins', status: 'warn', detail: 'Check failed: ' + err.message });
    }

    const configDir = path.join(bepDir, 'config');
    const configList = safeExists(configDir) ? safeReaddir(configDir) : null;
    checks.push({ file: 'BepInEx/config/', status: safeExists(configDir) ? (configList ? 'ok' : 'warn') : 'warn',
      detail: safeExists(configDir) ? (configList ? `${configList.filter(f => f.endsWith('.cfg')).length} config files` : 'Present (cannot read — file locked)') : 'Config folder missing — created after first game launch with BepInEx.' });
    return { ok: checks.every(c => c.status !== 'missing'), checks };
  }

  /** Strip trailing version numbers from a name for fuzzy matching */
  _baseName(name) {
    return (name || '')
      .toLowerCase()
      .replace(/\s+/g, ' ')
      // Strip a trailing variant tag in ()/[] first — "(HQ Base Game Sprites)",
      // "[Optional]" — so different downloads of the same mod collapse to one
      // base key for overwrite/dedup detection.
      .replace(/(?:\s*[([][^()[\]]*[)\]])+\s*$/, '')
      // Then strip a trailing version — but ONLY a dotted number ("1.1.6", "3.3")
      // or a 'v'-prefixed one ("v2", "v1.1.6"). A bare trailing integer must be
      // kept: it is part of the name, not a version — "Pokemon Expansions -
      // Generation 1/2/3" are three DIFFERENT mods and must not collapse to one,
      // and "Base Set 2" is a name, not "Base Set" v2. This mirrors the version
      // rule in _parseNexusFilename so the two stay consistent.
      .replace(/\s*(?:v\d+(?:\.\d+)*|\d+(?:\.\d+)+)\s*$/i, '')
      // Finally, normalise separators. The two Nexus filename formats disagree
      // about them for the same mod: "Phone - Overhaul-685-1-7-2-....zip" parses
      // to "Phone Overhaul" (the old format turns its hyphens into spaces) while
      // "Phone - Overhaul 685 1.9.0 ....zip" keeps "Phone - Overhaul". Without
      // this they are two different base keys, so one mod showed as two rows in
      // Downloaded Mods and updating it created a second Installed Mods entry
      // instead of replacing the first.
      .replace(/[\s\-_]+/g, ' ')
      .trim();
  }

  /** Compare two version strings. Returns 1 if a > b, -1 if a < b, 0 if equal */
  _compareVersions(a, b) {
    const pa = String(a).split('.').map(Number);
    const pb = String(b).split('.').map(Number);
    for (let i = 0; i < Math.max(pa.length, pb.length); i++) {
      const na = pa[i] || 0, nb = pb[i] || 0;
      if (na > nb) return 1;
      if (na < nb) return -1;
    }
    return 0;
  }

  getStagedFiles() {
    const dir = this.getStagingPath();
    if (!fs.existsSync(dir)) return [];

    // Retry anything that was locked when the user removed it — so it vanishes
    // for good the moment OneDrive/antivirus lets go, with no action needed.
    this.sweepPendingDeletes();

    // Installed versions lookup
    const installed = new Map();
    for (const mod of this.mods.values()) {
      const key = this._baseName(mod.name);
      if (key) installed.set(key, { version: mod.version || '', installedAt: mod.installedAt || '', archiveMtime: mod.archiveMtime || 0, filename: mod.filename || '' });
    }

    // List directory entries. If the whole readdir fails (rare — usually permissions
    // on the staging root itself), return an empty list rather than throwing up
    // through chokidar and crashing the main process.
    let entries;
    try {
      entries = fs.readdirSync(dir)
        .filter(f => /\.(zip|rar|7z)$/i.test(f))
        // Files the user already removed but that were locked at the time stay on
        // disk until the lock clears — never show them again.
        .filter(f => !this._pendingDeletes.has(f));
    } catch (err) {
      console.warn('[getStagedFiles] readdir failed:', err.message);
      return [];
    }

    // Parse all staged archives. Wrap stat in try/catch so one unreadable file
    // (e.g. OneDrive online-only placeholder, file locked by AV, file mid-sync)
    // doesn't poison the whole list.
    const allFiles = [];
    for (const f of entries) {
      const parsed = this._parseNexusFilename(f);
      let size = 0;
      let mtime = 0;
      let statError = null;
      try {
        const st = fs.statSync(path.join(dir, f));
        size = st.size;
        mtime = st.mtimeMs;
      } catch (err) {
        // Keep the file in the list with size 0 so the user can see and delete it.
        // Record WHY so the UI can say "unreadable" instead of silently showing
        // "0 B" — a file the manager can list but cannot stat is almost always
        // locked, mid-sync (OneDrive), or on a path Windows considers too long,
        // and that same failure is what blocks deleting and installing it.
        statError = err.code || err.message || 'unknown';
        console.warn(`[getStagedFiles] stat failed for ${f}:`, statError);
      }
      allFiles.push({
        filename: f,
        size,
        mtime,
        statError,
        parsedName: parsed.name,
        parsedVersion: parsed.version,
        base: this._baseName(parsed.name),
      });
    }

    // Group by base name
    const groups = new Map();
    for (const f of allFiles) {
      const key = f.base || f.filename;
      if (!groups.has(key)) groups.set(key, []);
      groups.get(key).push(f);
    }

    // For each group: newest is the main entry, older ones become downgrade options
    const result = [];
    for (const [base, files] of groups) {
      // Sort by version (desc). When versions tie or are both missing — common
      // now that Nexus filenames may omit the version, and for same-version
      // variants like base vs "(HQ Base Game Sprites)" — fall back to the file's
      // modified time (newest download first) so the most recent archive wins.
      files.sort((a, b) => {
        const cmp = this._compareVersions(b.parsedVersion || '0', a.parsedVersion || '0');
        if (cmp !== 0) return cmp;
        return (b.mtime || 0) - (a.mtime || 0);
      });
      const newest = files[0];
      const olderFiles = files.slice(1);

      const inst = installed.get(base);
      const installedVersion = inst ? inst.version : undefined;
      // Is this staged archive a newer download than the one the current copy
      // was installed from? Prefer an archive-date-to-archive-date comparison
      // (the mtime of the archive we installed, captured at install time). Fall
      // back to the install timestamp for mods installed before that was tracked.
      let installedRefMs = NaN;
      if (inst) {
        if (inst.archiveMtime) installedRefMs = inst.archiveMtime;
        else if (inst.installedAt) installedRefMs = Date.parse(inst.installedAt);
      }
      // A duplicate download of the archive this mod was installed from
      // ("Mod (2).zip") is byte-identical to what is already installed, but its
      // mtime is whenever the browser wrote it — always newer. Without this it
      // read as an "Update" to the same version it already had. Compare names
      // with the copy suffix stripped so a copy is correctly a Re-install.
      const canonName = (f) => {
        const ext = path.extname(f || '');
        return this._stripDuplicateSuffix((f || '').slice(0, f.length - ext.length)).toLowerCase();
      };
      const sameDownload = !!(inst && inst.filename) && canonName(newest.filename) === canonName(inst.filename);
      const newerDownload = !sameDownload && !!newest.mtime && !isNaN(installedRefMs) && newest.mtime > installedRefMs + 1000;
      let status = 'new';
      if (inst !== undefined && newest.parsedVersion && installedVersion) {
        const cmp = this._compareVersions(newest.parsedVersion, installedVersion);
        if (cmp > 0) status = 'update';
        else if (cmp < 0) status = newerDownload ? 'update' : 'downgrade';
        // Same version string: a newer archive (re-release or a different variant
        // like "(HQ Base Game Sprites)") is an update — the file changed even
        // though the version didn't.
        else status = newerDownload ? 'update' : 'reinstall';
      } else if (inst !== undefined) {
        // Versions can't decide (one or both missing). Use the archive date: a
        // newer download of the same mod is an update; otherwise a reinstall.
        status = newerDownload ? 'update' : 'reinstall';
      }
      result.push({
        filename: newest.filename,
        size: newest.size,
        statError: newest.statError || null,
        parsedName: newest.parsedName,
        parsedVersion: newest.parsedVersion,
        status,
        installedVersion: installedVersion || null,
        olderVersions: olderFiles.map(f => ({ filename: f.filename, version: f.parsedVersion })),
      });
    }

    return result;
  }

  /**
   * Parse a mod archive filename into { name, version }, supporting BOTH the
   * legacy Nexus download format and the newer simplified format.
   *
   * OLD format: "ModName-ModID-Version-Timestamp.zip"
   *   "BepInEx with Configuration Manager-2-5-4-23-2-1726589034.zip"
   *     → name "BepInEx with Configuration Manager", version "5.4.23.2"
   *   "EnhancedPrefabLoader-496-6-0-0-1775691005.zip"
   *     → name "EnhancedPrefabLoader", version "6.0.0"
   *
   * NEW format (Nexus dropped the "-modID-version-timestamp" suffix): the file
   * arrives as the human-readable mod name plus an optional trailing version:
   *   "Collection Tracker v1.1.6.zip" → name "Collection Tracker", version "1.1.6"
   *   "Collection Tracker 1.1.6.zip"  → name "Collection Tracker", version "1.1.6"
   *   "Yu-Gi-Oh Expansions v3.0.zip"  → name "Yu-Gi-Oh Expansions", version "3.0"
   *   "RTCGO Custom TV.zip"           → name "RTCGO Custom TV",     version ""
   *
   * Getting the NAME right matters beyond display: it drives known-DB matching,
   * core detection, auto-grouping, and same-mod detection on reinstall. A name
   * left as "Collection Tracker v1.1.6" would fail to match the DB's
   * "Collection Tracker" and break all of those.
   */
  /**
   * Strip the suffix a browser adds when the same file is downloaded twice:
   * "Mod-612-3-4-2-1777790483 (1).zip", " (2)", and Windows' " - Copy".
   *
   * This has to happen before anything else is parsed. The old-format detector
   * keys off a trailing 9-10 digit timestamp, and " (1)" pushes that group out
   * of last place — so the whole filename fell through to the new-format parser,
   * which produced a different name ("Grading Overhaul-612-3-4-2-1777790") with
   * no version. That is a different base key, so the second download installed
   * as a SECOND mod instead of overwriting the first, and users ended up with
   * two entries for one mod.
   *
   * Only a purely numeric parenthesised group is stripped. Nexus optional-file
   * tags are words — "(HQ Base Game Sprites)", "(2k Textures)" — and are left
   * for the variant-tag handling further down.
   */
  _stripDuplicateSuffix(base) {
    let out = base;
    let prev;
    do {
      prev = out;
      out = out
        .replace(/\s*\((\d{1,3})\)\s*$/, '')          // "name (1)", "name (12)"
        .replace(/\s*-\s*Copy\s*$/i, '')               // "name - Copy"
        .trim();
    } while (out !== prev && out.length > 0);
    return out || base;
  }

  _parseNexusFilename(filename) {
    const base = this._stripDuplicateSuffix(filename.replace(/\.(zip|rar|7z)$/i, ''));
    const parts = base.split('-');

    // Detect the OLD format. Its signature is hyphen-delimited groups ending in
    // either a 9-10 digit timestamp, or a run (>=2) of numeric-only groups
    // (modID + version segments). A plain hyphenated display name like
    // "Yu-Gi-Oh Expansions v3.0" has no such all-numeric tail, so it won't
    // false-trigger here and falls through to the NEW-format parser below.
    const last = parts[parts.length - 1];
    const hasTimestamp = /^\d{9,10}$/.test(last);
    let numericTail = 0;
    for (let i = parts.length - 1; i >= 0; i--) {
      if (/^\d+$/.test(parts[i])) numericTail++; else break;
    }
    const looksOld = hasTimestamp || (parts.length >= 3 && numericTail >= 2);

    if (looksOld) {
      const op = parts.slice();
      if (hasTimestamp) op.pop();
      // Name = leading non-numeric groups; first numeric = modID; rest = version.
      let nameEnd = 0;
      for (let i = 0; i < op.length; i++) {
        if (/^\d+$/.test(op[i])) { nameEnd = i; break; }
        nameEnd = i + 1;
      }
      const nameParts = op.slice(0, nameEnd);
      const numericParts = op.slice(nameEnd);
      let name = nameParts.join(' ') || base;
      let version = '';
      if (numericParts.length >= 2) version = numericParts.slice(1).join('.');
      else if (numericParts.length === 1) version = numericParts[0];
      name = name.replace(/_/g, ' ').replace(/\s+/g, ' ').trim();
      name = name.replace(/\.(zip|rar|7z)$/i, '').trim();
      if (version) name = name.replace(/\s+v?\d+(\.\d+)*\s*$/i, '').trim();
      return { name: name || base, version: version || '' };
    }

    // NEW format: first peel an optional trailing variant tag in parentheses or
    // brackets — "(HQ Base Game Sprites)", "[Optional]", "(2k Textures)". These
    // are Nexus optional-file markers for the SAME mod, so we drop them from the
    // name: "Collection Tracker v1.1.6 (HQ Base Game Sprites)" must reduce to
    // "Collection Tracker" so it overwrites the plain "Collection Tracker"
    // instead of installing alongside it as a phantom second copy.
    let work = base.replace(/(?:\s*[([][^()[\]]*[)\]])+\s*$/, '').trim();
    let version = '';

    // Nexus's latest download format appends "<modID> <version> [timestamp] <hash>"
    // to the name, space-separated, with a random alphanumeric download hash last
    // and (in newer builds) an ISO-ish build timestamp before it:
    //   "Enhanced Binder 1116 1.0.0 KV1sEKw3T"                    → name, id, ver, hash
    //   "Collection Tracker v1.1.7 867 1.1.7 cF8ECcmKb"           → name+vVer, id, ver, hash
    //   "Enhanced Binder 1116 1.0.5 2026-07-07T22-24Z 3ti183gmM"  → name, id, ver, timestamp, hash
    // Strip that suffix from the right. The hash is only stripped when the token
    // before it is recognizable metadata (version, number, or timestamp), so a
    // mod name that merely ends in a long alphanumeric word isn't mistaken for a
    // hash. Each metadata token is popped at most once, so a mod name that ends
    // in a bare number (e.g. "Base Set 2") keeps that number.
    // A download hash is a random 8–16 char alphanumeric token. Nexus hashes
    // either contain a digit ("KV1sEKw3T") or have an "internal capital" — a
    // lowercase letter followed later by an uppercase ("uGRhSuxST", "cF8ECcmKb").
    // Natural trailing words ("Remastered", "Extender") have at most a leading
    // capital, so requiring a digit OR an internal capital strips real hashes
    // without eating ordinary name words that follow a number.
    const isHashTok = (tk) => !!tk && tk.length >= 8 && tk.length <= 16 && /^[A-Za-z0-9]+$/.test(tk) && (/\d/.test(tk) || /[a-z].*[A-Z]/.test(tk));
    const isVerTok  = (tk) => !!tk && /^\d+(\.\d+)+$/.test(tk);                        // dotted version, e.g. 1.0.5
    const isIntTok  = (tk) => !!tk && /^\d+$/.test(tk);                               // mod ID / bare number
    const isTsTok   = (tk) => !!tk && /^\d{4}-\d{2}-\d{2}(t[\dhms.:\-]*z?)?$/i.test(tk); // 2026-07-07 or 2026-07-07T22-24Z
    const isMetaTok = (tk) => isVerTok(tk) || isIntTok(tk) || isTsTok(tk);
    const tokens = work.split(/\s+/);
    if (tokens.length >= 3 && isHashTok(tokens[tokens.length - 1]) && isMetaTok(tokens[tokens.length - 2])) {
      tokens.pop();                                                                     // random download hash
      if (isTsTok(tokens[tokens.length - 1])) tokens.pop();                             // optional build timestamp
      if (isVerTok(tokens[tokens.length - 1]) || isIntTok(tokens[tokens.length - 1])) version = tokens.pop(); // version
      if (isIntTok(tokens[tokens.length - 1])) tokens.pop();                            // mod ID
      work = tokens.join(' ');
    }

    // Then pull an optional trailing version token off what's left. Require a
    // dotted number ("1.1.6") OR a 'v' prefix ("v2") so we never strip a
    // legitimate trailing integer that's part of the name (e.g. "Base Set 2").
    // This also strips an embedded "vX.Y.Z" left in the name by the hash format
    // (e.g. "Collection Tracker v1.1.7" after the id/ver/hash suffix is removed).
    let name = work;
    const m = work.match(/^(.*?)[\s_-]+v?(\d+(?:\.\d+)+)\s*$/i)  // "Name v1.1.6" / "Name 1.1.6"
           || work.match(/^(.*?)[\s_-]+v(\d+)\s*$/i);            // "Name v2"
    if (m) { name = m[1]; if (!version) version = m[2]; }
    name = name.replace(/_/g, ' ').replace(/\s+/g, ' ').trim();
    name = name.replace(/\.(zip|rar|7z)$/i, '').trim();
    return { name: name || base, version: version || '' };
  }

  /** How many versions of the same mod to keep in the staging folder. */
  static get STAGING_KEEP_VERSIONS() { return 2; }

  /**
   * Delete stale archives from the staging folder.
   *
   * Two passes: duplicate downloads of the same file ("Mod (1).zip") are
   * reduced to one, then only the newest STAGING_KEEP_VERSIONS versions of each
   * mod are kept.
   *
   * Grouping is by mod, and retention is by VERSION rather than by file: a mod
   * with a plain download plus an optional-file variant of the same version
   * (e.g. "(HQ Base Game Sprites)") keeps both, since those are one version in
   * two pieces. Anything the parser can't read a version out of is left alone —
   * a file we can't rank is not a file we should delete.
   *
   * The archive of a currently installed version is never removed, so Re-install
   * keeps working for whatever the user is actually running.
   */
  pruneStagingVersions() {
    const dir = this.getStagingPath();
    if (!fs.existsSync(dir)) return [];

    const installedFilenames = new Set();
    for (const [, m] of this.mods) {
      if (m.filename) installedFilenames.add(m.filename.toLowerCase());
    }

    let entries = [];
    try { entries = fs.readdirSync(dir); } catch { return []; }

    const removed = [];
    const renamed = [];

    // ── Pass 1: drop duplicate downloads of the SAME file ──────────────────
    // "Mod.zip", "Mod (1).zip" and "Mod (2).zip" are one download fetched three
    // times. Version retention alone never removes them, because they are all
    // the same version. Files are only treated as copies when their names are
    // identical once the browser's suffix is stripped, so an optional-file
    // variant ("... (HQ Base Game Sprites).zip") is never mistaken for a copy.
    // The extension is part of the key too — a .zip and a .7z of the same mod
    // are different downloads, not copies.
    const copies = new Map(); // canonical "stem.ext" -> [filenames]
    for (const filename of entries) {
      if (!/\.(zip|rar|7z)$/i.test(filename)) continue;
      const ext = path.extname(filename).toLowerCase();
      const stem = this._stripDuplicateSuffix(filename.slice(0, -ext.length));
      const key = `${stem.toLowerCase()}${ext}`;
      if (!copies.has(key)) copies.set(key, []);
      copies.get(key).push(filename);
    }
    const stemOf = (f) => f.slice(0, f.length - path.extname(f).length);
    const isCopyName = (f) => this._stripDuplicateSuffix(stemOf(f)) !== stemOf(f);

    for (const [key, group] of copies) {
      // Keep the installed one if that is the copy on record, otherwise the
      // name with no suffix, otherwise the first alphabetically.
      const keep =
        group.find(f => installedFilenames.has(f.toLowerCase())) ||
        group.find(f => !isCopyName(f)) ||
        [...group].sort()[0];
      for (const filename of group) {
        if (filename === keep) continue;
        try {
          fs.removeSync(path.join(dir, filename));
          removed.push(filename);
          console.log(`[staging] pruned duplicate download: ${filename} (kept ${keep})`);
        } catch (err) {
          console.warn(`[staging] could not prune ${filename}:`, err.message);
        }
      }
      // A copy dropped into the folder by hand keeps its "(1)" / "- Copy" name,
      // which parses as a different mod and shows up as a phantom Update or
      // Downgrade row. Rename the survivor to the proper name so the suffix
      // never survives in the staging folder — whether the manager put the file
      // there or the user did. Renaming rather than deleting matters when the
      // copy is the ONLY archive of that mod: deleting it would take the mod out
      // of Downloaded Mods entirely.
      if (isCopyName(keep)) {
        const target = `${this._stripDuplicateSuffix(stemOf(keep))}${path.extname(keep)}`;
        const targetPath = path.join(dir, target);
        try {
          if (fs.existsSync(targetPath)) {
            fs.removeSync(path.join(dir, keep));
            removed.push(keep);
            console.log(`[staging] pruned duplicate download: ${keep} (kept ${target})`);
          } else {
            fs.renameSync(path.join(dir, keep), targetPath);
            renamed.push({ from: keep, to: target });
            // Keep the DB honest: a mod installed from the copy still records
            // the old name, and that name is what protects its archive from
            // version pruning and drives the Re-install row.
            let dbChanged = false;
            for (const [, mod] of this.mods) {
              if (mod.filename === keep) { mod.filename = target; dbChanged = true; }
            }
            if (dbChanged) this._saveDb();
            console.log(`[staging] renamed hand-added copy: ${keep} -> ${target}`);
          }
        } catch (err) {
          console.warn(`[staging] could not normalise ${keep}:`, err.message);
        }
      }
    }
    entries = entries
      .filter(f => !removed.includes(f))
      .map(f => (renamed.find(r => r.from === f)?.to) || f);

    // ── Pass 2: keep only the newest STAGING_KEEP_VERSIONS versions ────────
    // mod base key -> version string -> [filenames]
    const groups = new Map();
    for (const filename of entries) {
      if (!/\.(zip|rar|7z)$/i.test(filename)) continue;
      const parsed = this._parseNexusFilename(filename);
      if (!parsed?.version) continue;          // unrankable — leave it
      const key = this._baseName(parsed.name);
      if (!key) continue;
      if (!groups.has(key)) groups.set(key, new Map());
      const byVersion = groups.get(key);
      if (!byVersion.has(parsed.version)) byVersion.set(parsed.version, []);
      byVersion.get(parsed.version).push(filename);
    }

    for (const [key, byVersion] of groups) {
      if (byVersion.size <= ModManager.STAGING_KEEP_VERSIONS) continue;
      const ordered = [...byVersion.keys()].sort((a, b) => this._compareVersions(b, a));
      for (const version of ordered.slice(ModManager.STAGING_KEEP_VERSIONS)) {
        for (const filename of byVersion.get(version)) {
          if (installedFilenames.has(filename.toLowerCase())) {
            console.log(`[staging] keeping ${filename} — it is the installed version`);
            continue;
          }
          try {
            fs.removeSync(path.join(dir, filename));
            removed.push(filename);
            console.log(`[staging] pruned old version: ${filename} (${key} v${version})`);
          } catch (err) {
            console.warn(`[staging] could not prune ${filename}:`, err.message);
          }
        }
      }
    }
    return removed;
  }

  async addToStaging(filePaths) {
    const dir = this.getStagingPath();
    fs.ensureDirSync(dir);

    // Identity of a download, ignoring the browser's "(1)" / "- Copy" suffix.
    const canonKey = (filename) => {
      const ext = path.extname(filename).toLowerCase();
      return `${this._stripDuplicateSuffix(filename.slice(0, -ext.length)).toLowerCase()}${ext}`;
    };
    const staged = new Map(); // canonical key -> filename already in staging
    try {
      for (const f of fs.readdirSync(dir)) {
        if (/\.(zip|rar|7z)$/i.test(f)) staged.set(canonKey(f), f);
      }
    } catch {}

    const added = [];
    const skipped = [];
    const replaced = [];
    for (const src of filePaths) {
      const filename = path.basename(src);
      const displayName = this._parseNexusFilename(filename).name || filename;

      // Import under the CANONICAL name: "Mod (1).zip" is written as "Mod.zip",
      // overwriting what is already staged. A re-download is meant to replace
      // the copy in Downloaded Mods, and normalising the name here means the
      // browser's suffix never reaches the staging folder in the first place.
      const ext = path.extname(filename);
      const destName = `${this._stripDuplicateSuffix(filename.slice(0, filename.length - ext.length))}${ext}`;
      const dest = path.join(dir, destName);
      const wasThere = fs.existsSync(dest);

      // A differently-named copy of the same download already staged (e.g. a
      // "(1)" file from an older build) would otherwise survive alongside the
      // canonical name.
      const twin = staged.get(canonKey(filename));
      if (twin && twin !== destName) {
        try { fs.removeSync(path.join(dir, twin)); } catch {}
      }

      try {
        await fs.move(src, dest, { overwrite: true });
      } catch {
        // Move fails across drives — fall back to copy + delete
        await fs.copy(src, dest, { overwrite: true });
        try { await fs.remove(src); } catch {}
      }

      if (wasThere || (twin && twin !== destName)) {
        replaced.push({ filename: destName, of: twin || destName, name: displayName });
        console.log(`[staging] ${destName} replaced the copy already in staging`);
      } else {
        added.push(destName);
      }
      staged.set(canonKey(destName), destName);
    }

    // A newly added version can push an older one past the retention limit.
    const pruned = this.pruneStagingVersions();
    return { added, skipped, replaced, pruned };
  }

  async removeFromStaging(f) {
    const dir = this.getStagingPath();
    // Sweep any leftovers from a previous rename-fallback delete (see below).
    try {
      for (const e of fs.readdirSync(dir)) {
        if (/\.pending-delete-\d+$/.test(e)) { try { fs.rmSync(path.join(dir, e), { force: true }); } catch {} }
      }
    } catch {}

    // Resolve the name against the real directory listing. getStagedFiles() builds
    // the UI list from readdir, so readdir is the source of truth for "is this row
    // still here". If a file can be listed but not stat'ed or deleted via the path
    // we build, the name we were handed doesn't match the on-disk entry (encoding
    // normalization, a trailing space, a >MAX_PATH path).
    let real = f;
    try {
      const entries = fs.readdirSync(dir);
      real = entries.find(e => e === f)
        || entries.find(e => e.normalize('NFC') === String(f).normalize('NFC'))
        || entries.find(e => e.toLowerCase() === String(f).toLowerCase())
        || f;
    } catch { /* fall through with the given name */ }

    const p = path.join(dir, real);
    const stillListed = () => {
      try { return fs.readdirSync(dir).includes(real); } catch { return false; }
    };
    if (!stillListed()) { this._pendingDeletes.delete(real); return { success: true }; }

    // Windows refuses unlink on a read-only file with EPERM — clear the flag first.
    try { fs.chmodSync(p, 0o666); } catch {}

    // Retry with a real async wait, NOT a synchronous spin. A busy-wait blocks the
    // main process's event loop, so any in-flight handle release (a 7za child
    // exiting, a stream closing) can never be processed while we spin — the retries
    // are guaranteed to see the same lock that caused the first failure. Awaiting
    // yields the loop so those handles actually get released between attempts.
    let lastErr = null;
    const delays = [0, 120, 300, 700];
    for (const wait of delays) {
      if (wait) await new Promise(r => setTimeout(r, wait));
      try {
        fs.rmSync(p, { force: true, maxRetries: 3, retryDelay: 100 });
      } catch (err) {
        lastErr = err;
      }
      if (!stillListed()) { this._pendingDeletes.delete(real); return { success: true }; }
    }

    // Last resort: Windows will often let a locked file be RENAMED even when it
    // refuses to delete it. Move it out of the way so it leaves the staged list
    // immediately, and clean it up on the next sweep.
    try {
      const parked = path.join(dir, `${real}.pending-delete-${Date.now()}`);
      fs.renameSync(p, parked);
      try { fs.rmSync(parked, { force: true }); } catch { /* swept later */ }
      if (!stillListed()) { this._pendingDeletes.delete(real); return { success: true }; }
    } catch (err) {
      lastErr = err || lastErr;
    }

    // The file is locked by something outside this process (OneDrive sync, antivirus,
    // a deny ACL) — nothing we can do from in here will force it right now. Rather
    // than leaving a row the user can't get rid of, mark it deleted: it is filtered
    // out of the staged list immediately, retried automatically on every refresh,
    // and swept again on quit. From the user's point of view it disappears at once
    // and the file goes as soon as the lock clears.
    this._pendingDeletes.add(real);
    const code = lastErr && (lastErr.code || '');
    return {
      success: true,
      deferred: true,
      error: null,
      note: `"${real}" is locked by another process${code ? ` (${code})` : ''} — it has been removed from the list and will be deleted automatically as soon as the lock clears.`,
    };
  }

  /**
   * Retry any deletions that were blocked earlier (locked by OneDrive/antivirus).
   * Cheap and silent — called on every staged-list refresh and on quit, so a file
   * the user removed disappears for good the moment the lock is released.
   */
  sweepPendingDeletes() {
    if (!this._pendingDeletes || this._pendingDeletes.size === 0) return;
    const dir = this.getStagingPath();
    for (const name of Array.from(this._pendingDeletes)) {
      const target = path.join(dir, name);
      try {
        try { fs.chmodSync(target, 0o666); } catch {}
        fs.rmSync(target, { force: true });
      } catch { /* still locked — try again next time */ }
      let gone = false;
      try { gone = !fs.readdirSync(dir).includes(name); } catch { gone = true; }
      if (gone) this._pendingDeletes.delete(name);
    }
  }

  clearStaging() {
    const dir = this.getStagingPath();
    if (!fs.existsSync(dir)) return { success: true };
    const errors = [];
    for (const f of fs.readdirSync(dir)) {
      if (!/\.(zip|rar|7z)$/i.test(f)) continue;
      try { fs.removeSync(path.join(dir, f)); }
      catch (err) { errors.push(`${f}: ${err.code || err.message}`); }
    }
    if (errors.length) return { success: false, error: `Could not remove ${errors.length} file(s): ${errors.slice(0, 3).join('; ')}` };
    return { success: true };
  }

  /**
   * List entries in a ZIP archive with a 7z fallback.
   * node-stream-zip rejects zips whose entries use backslash path separators
   * (common in zips made by Windows tools) as "Malicious entry" — so when that
   * happens, fall back to the bundled 7za binary which handles them fine.
   * Returns [{ path: 'forward/slashed/path', size: Number, isDir: Boolean }, ...]
   */
  /**
   * Run `7za l` and resolve the raw entry list, guaranteeing the child process
   * and its stream are torn down before resolving.
   *
   * node-7z spawns 7za.exe, which holds an OPEN HANDLE on the archive while it
   * runs. If the stream is left dangling (no destroy, no kill) the child can
   * outlive the call, and on Windows that handle makes the .zip undeletable
   * (EPERM) until the whole app exits — which is exactly the "the ✕ button only
   * works after I close the manager" symptom. Always reap the child.
   */
  /**
   * True when a node-7z listing entry is a directory.
   *
   * node-7z reports the attribute column as `attributes` ("D...." for a folder).
   * This used to read `e.attr`, which is always undefined — so EVERY entry in a
   * .7z or .rar looked like a file. A mod packed as a single folder therefore
   * looked like it had a root-level file, single-folder detection failed, and
   * the installer wrapped it in a second folder of the same name:
   * plugins/StorageShelfExpanded/StorageShelfExpanded/. Zip archives were fine
   * because node-stream-zip reports isDirectory directly.
   */
  _sevenEntryIsDir(e) {
    const attrs = e?.attributes || e?.attr || '';
    if (attrs.startsWith('D')) return true;
    const f = e?.file || '';
    return f.endsWith('/') || f.endsWith('\\');
  }

  _sevenList(archivePath, bin) {
    return this._run7z(bin, b => this._sevenListOnce(archivePath, b));
  }

  _sevenListOnce(archivePath, bin) {
    return new Promise((resolve, reject) => {
      const entries = [];
      let settled = false;
      let s;
      const teardown = () => {
        try { if (s && s._childProcess && !s._childProcess.killed) s._childProcess.kill(); } catch {}
        try { if (s && typeof s.destroy === 'function') s.destroy(); } catch {}
        try { if (s && typeof s.removeAllListeners === 'function') s.removeAllListeners(); } catch {}
      };
      const finish = (err, val) => {
        if (settled) return;
        settled = true;
        teardown();
        if (err) reject(err); else resolve(val);
      };
      try {
        s = Seven.list(archivePath, { $bin: bin });
      } catch (err) {
        return finish(err);
      }
      s.on('data', e => entries.push(e));
      s.on('end', () => finish(null, entries));
      s.on('error', err => finish(err));
    });
  }

  async _listZipEntries(archivePath) {
    try {
      const StreamZip = require('node-stream-zip');
      // Deliberately the callback API, NOT StreamZip.async. The async wrapper's
      // close() does `const zip = await this[propZip]` first, so when an archive
      // fails to open (0-byte, truncated, corrupt, still downloading) that promise
      // is already rejected and close() throws WITHOUT ever releasing the file
      // descriptor. On Windows the leaked handle leaves the .zip locked: deleting
      // it silently becomes a "delete pending" that only completes when the app
      // exits — so the file appears undeletable and reports no error. The
      // callback API's close() is safe and idempotent even when the open failed.
      const entriesObj = await new Promise((resolve, reject) => {
        const zip = new StreamZip({ file: archivePath, storeEntries: true });
        let settled = false;
        const finish = (err, val) => {
          if (settled) return;
          settled = true;
          try { zip.close(); } catch { /* already closed */ }
          if (err) reject(err); else resolve(val);
        };
        zip.on('ready', () => { let e; try { e = zip.entries(); } catch (err) { return finish(err); } finish(null, e); });
        zip.on('error', err => finish(err));
      });
      const result = [];
      for (const [name, entry] of Object.entries(entriesObj)) {
        result.push({ path: name.replace(/\\/g, '/'), size: entry.size, isDir: entry.isDirectory });
      }
      return result;
    } catch (err) {
      if (!/Malicious entry/.test(err.message || '')) throw err;
      // Fallback: 7za handles Windows-style zips (backslash paths) correctly
      const result = [];
      for (const e of await this._sevenList(archivePath)) {
        result.push({
          path: (e.file || '').replace(/\\/g, '/'),
          size: e.size || 0,
          isDir: this._sevenEntryIsDir(e),
        });
      }
      return result;
    }
  }

  /**
   * Mods that must never be installed, with the reason shown to the user.
   *
   * Matching is by ARCHIVE CONTENTS first (renaming a download must not defeat
   * the block) and by mod name second. Each entry in `signatures` is a group of
   * path fragments that must ALL be present; any group matching blocks the
   * archive. Fragments match a full path, a trailing path segment, or a
   * substring, so both normal and "VORTEX VERSION" packagings are caught.
   */
  static get BLOCKED_MODS() {
    return [
      {
        id: 'bepinex-pack',
        name: 'BepInEx Pack for TCG Card Shop Simulator',
        reason: 'This is the plain BepInEx pack. It has no Configuration Manager and none of the settings this game\'s mods rely on.',
        useInstead: 'Pre-configured BepInEx with Configuration Manager',
        namePatterns: [/\bbepinex\s*pack\b/i],
        // A stock BepInEx distribution ships version.dll next to winhttp.dll.
        // The pre-configured pack does not, which is what keeps this rule from
        // catching the one everybody should actually be using.
        signatures: [['version.dll', 'winhttp.dll', 'bepinex/core/bepinex.dll']],
      },
      {
        id: 'create-cards',
        name: 'Create Cards',
        reason: 'Known to break the game.',
        useInstead: 'Enhanced Prefab Loader',
        namePatterns: [/^\s*create\s*cards\b/i],
        signatures: [
          ['createcardspreloader.dll'],
          ['createcards/createcards.dll'],
        ],
      },
      {
        id: 'card-configurator',
        name: 'Card Configurator',
        reason: 'Known to break the game.',
        useInstead: 'Enhanced Prefab Loader',
        namePatterns: [/^\s*card\s*configurator\b/i],
        signatures: [['cardconfigurator/cardconfigurator.dll']],
      },
      {
        id: 'card-expansion-mod',
        name: 'Card Expansion Mod',
        reason: 'Known to break the game.',
        useInstead: 'Enhanced Prefab Loader',
        namePatterns: [/^\s*card\s*expansion\s*mod\b/i],
        signatures: [
          ['tcgshopexpansionmod.dll'],
          ['customexpansionpackimages/'],
        ],
      },
      {
        id: 'new-cards-mod',
        name: 'New Cards Mod',
        reason: 'Known to break the game.',
        useInstead: 'Enhanced Prefab Loader',
        namePatterns: [/^\s*new\s*cards\s*mod\b/i],
        signatures: [
          ['tcgshopnewcardsmod.dll'],
          ['tcgshopnewcardsmodpreloader.dll'],
        ],
      },
    ];
  }

  /**
   * Check a staged archive against BLOCKED_MODS.
   * Returns { name, reason, useInstead, matchedBy } or null.
   */
  _findBlockedMod(filename, entries) {
    const paths = (entries || [])
      .filter(e => e && !e.isDir && e.path)
      .map(e => String(e.path).replace(/\\/g, '/').toLowerCase());
    const parsedName = this._parseNexusFilename(filename)?.name || '';
    const hay = `${parsedName} ${filename}`.replace(/[_-]+/g, ' ').toLowerCase();

    // Never block the pre-configured pack, whatever else it looks like.
    if (/pre[\s-]*configured|configuration\s*manager/i.test(hay)) return null;

    const hasFragment = (frag) =>
      paths.some(p => p === frag || p.endsWith('/' + frag) || p.includes(frag));

    for (const rule of ModManager.BLOCKED_MODS) {
      const bySignature = (rule.signatures || []).some(group => group.every(hasFragment));
      const byName = (rule.namePatterns || []).some(re => re.test(hay));
      if (bySignature || byName) {
        return {
          id: rule.id,
          name: rule.name,
          reason: rule.reason,
          useInstead: rule.useInstead || null,
          matchedBy: bySignature ? 'contents' : 'name',
        };
      }
    }
    return null;
  }

  async peekArchive(filename) {
    const archivePath = path.join(this.getStagingPath(), filename);
    const entries = [];
    if (/\.zip$/i.test(filename)) {
      const zipEntries = await this._listZipEntries(archivePath);
      for (const e of zipEntries) entries.push(e);
    } else if (/\.rar$/i.test(filename)) {
      const rarEntries = await this._listRar(archivePath);
      for (const e of rarEntries) entries.push({ path: e.file, size: e.size, isDir: e.isDir });
    } else {
      for (const e of await this._sevenList(archivePath)) {
        entries.push({ path: e.file, size: e.size || 0, isDir: this._sevenEntryIsDir(e) });
      }
    }
    const suggestedTarget = this._detectTarget(entries);
    const security = this._scanSecurity(entries, filename);
    const blockedMod = this._findBlockedMod(filename, entries);
    return { entries, suggestedTarget, security, blockedMod };
  }

  /** Security scan — checks archive contents for threats before installation */
  _scanSecurity(entries, filename) {
    const blocked = [];
    const warnings = [];

    // Mods verified safe by the developer — skip all warnings
    const VERIFIED_MODS = [
      'fast pack opening', 'fast opening pack', 'fastpackopening',
      'rtcgo custom tv', 'rtcgo custom furniture', 'custom tv', 'custom furniture', 'furniture_prefabloader',
      'base set theme decks', 'jungle theme decks', 'fossil theme decks',
      'base set 2 theme decks', 'team rocket theme decks',
      'gym heroes theme decks', 'gym challenge theme decks',
      'pokemon adventures manga', 'pokemon statues', 'pokemon shop textures',
      'rtcgo shop textures', 'pokemon n64 games', 'pokemon gameboy games',
      'pokemon plushies', 'neo expansions', 'base expansions', 'gym expansions',
      'texturereplacer', 'collection tracker', 'enhancedprefabloader',
      'bepinex with configuration manager',
    ];
    const fnLower = (filename || '').toLowerCase();
    const isVerified = VERIFIED_MODS.some(v => fnLower.includes(v));
    if (isVerified) {
      const nonDirEntries = entries.filter(e => !e.isDir);
      return { safe: true, blocked: [], warnings: [], scanned: nonDirEntries.length, verified: true };
    }

    // Script/executable files blocked in mods
    const BLOCKED_EXT = ['.exe', '.msi', '.bat', '.cmd', '.ps1', '.vbs', '.vbe',
      '.wsh', '.wsf', '.scr', '.com', '.reg', '.inf', '.hta', '.pif', '.cpl'];

    // Known safe executables that some mods legitimately bundle as tools
    const SAFE_EXES = ['ffmpeg.exe', 'yt-dlp.exe', 'deno.exe'];

    // System DLLs that mods should never replace
    const SYSTEM_DLLS = ['kernel32.dll','user32.dll','ntdll.dll','advapi32.dll','shell32.dll',
      'gdi32.dll','ole32.dll','oleaut32.dll','ws2_32.dll','crypt32.dll','msvcrt.dll',
      'comctl32.dll','comdlg32.dll','shlwapi.dll','secur32.dll','psapi.dll',
      'mswsock.dll','dnsapi.dll','iphlpapi.dll','rpcrt4.dll','setupapi.dll',
      'version.dll','wintrust.dll','urlmon.dll','wininet.dll','wldap32.dll'];

    // Game executable — must never be overwritten
    const GAME_EXE = 'card shop simulator.exe';

    // Known safe DLLs that BepInEx mods commonly include
    const SAFE_DLLS = ['winhttp.dll', 'doorstop_config.ini', '0harmony.dll',
      'bepinex.dll', 'bepinex.preloader.dll', 'bepinex.core.dll',
      'mono.cecil.dll', 'mono.cecil.rocks.dll', 'mono.cecil.pdb.dll',
      'monomod.runtimedetour.dll', 'monomod.utils.dll', 'newtonsoft.json.dll'];

    // Detect a legitimate pre-configured BepInEx / Unity Doorstop pack. Doorstop
    // is loaded via a proxy DLL that Windows auto-loads from the game root — the
    // usual names are winhttp.dll AND version.dll. version.dll is otherwise a
    // classic DLL-hijack target, so we only treat it as the safe loader when the
    // archive clearly IS a BepInEx pack: it ships the Doorstop marker/config or a
    // BepInEx/core layout. (winhttp.dll is already in SAFE_DLLS for the same
    // reason.) Outside that context, version.dll stays blocked below.
    const lowerPaths = entries.filter(e => !e.isDir).map(e => e.path.replace(/\\/g, '/').toLowerCase());
    const isBepInExPack =
      lowerPaths.some(p => p === '.doorstop_version' || p.endsWith('/.doorstop_version')) ||
      lowerPaths.some(p => p.split('/').pop() === 'doorstop_config.ini') ||
      lowerPaths.some(p => /(^|\/)bepinex\/core\//.test(p)) ||
      (lowerPaths.some(p => /(^|\/)bepinex\//.test(p)) &&
       lowerPaths.some(p => { const n = p.split('/').pop(); return n === 'winhttp.dll' || n === 'version.dll'; }));

    for (const entry of entries) {
      if (entry.isDir) continue;
      const p = entry.path.replace(/\\/g, '/');
      const name = p.split('/').pop().toLowerCase();
      const ext = '.' + name.split('.').pop();

      // 1. Path traversal — BLOCKED
      if (p.includes('../') || p.includes('..\\')) {
        blocked.push(`Path traversal detected: "${p}" — could write files outside game folder`);
        continue;
      }
      // 2. Dangerous executables/scripts — BLOCKED (unless whitelisted)
      if (BLOCKED_EXT.includes(ext)) {
        if (SAFE_EXES.includes(name)) {
          // Known safe tool — just warn
          warnings.push(`Contains bundled tool: "${name}" — known safe executable`);
        } else {
          blocked.push(`Dangerous file type: "${name}" (${ext}) — executables are not allowed in mods`);
        }
        continue;
      }
      // 3. Game executable overwrite — BLOCKED
      if (name === GAME_EXE) {
        blocked.push(`Attempts to overwrite game executable: "${name}"`);
        continue;
      }
      // 4. System DLL replacement — BLOCKED, except the BepInEx/Doorstop proxy
      //    loader (version.dll) at the game root inside a genuine BepInEx pack.
      if (SYSTEM_DLLS.includes(name)) {
        const isRootLoader = !p.includes('/'); // proxy loader must sit at game root
        if (name === 'version.dll' && isRootLoader && isBepInExPack) {
          warnings.push(`Contains BepInEx loader "version.dll" (Unity Doorstop proxy) — expected for a pre-configured BepInEx pack.`);
          continue;
        }
        blocked.push(`Contains system DLL: "${name}" — this could compromise your system`);
        continue;
      }
      // 5. DLL in root that isn't a known safe BepInEx DLL — WARNING
      if (ext === '.dll' && !p.includes('/') && !SAFE_DLLS.includes(name)) {
        warnings.push(`Loose DLL at archive root: "${name}" — verify this is from a trusted source`);
      }
      // 7. Double extension (e.g., mod.dll.bat, readme.txt.cmd)
      const parts = name.split('.');
      if (parts.length > 2) {
        const secondLast = '.' + parts[parts.length - 2];
        if (BLOCKED_EXT.includes(secondLast) || BLOCKED_EXT.includes(ext)) {
          if (!SAFE_EXES.includes(name)) {
            blocked.push(`Suspicious double extension: "${name}"`);
          }
        }
      }
    }

    // 8. Archive with no actual mod content — just executables
    const nonDirEntries = entries.filter(e => !e.isDir);
    const dllCount = nonDirEntries.filter(e => e.path.toLowerCase().endsWith('.dll')).length;
    const dataCount = nonDirEntries.filter(e => {
      const ext = e.path.split('.').pop().toLowerCase();
      return ['png','jpg','jpeg','json','txt','cfg','xml','yaml','yml','asset','bundle','prefab','mat','mesh','wav','ogg','mp3'].includes(ext);
    }).length;

    if (nonDirEntries.length > 0 && dllCount === 0 && dataCount === 0 && blocked.length === 0) {
      warnings.push('Archive contains no recognizable mod files (.dll or data files). Verify this is a valid mod.');
    }

    return {
      safe: blocked.length === 0,
      blocked,
      warnings,
      scanned: nonDirEntries.length,
    };
  }

  /**
   * Auto-detect the correct extract target based on archive contents.
   *
   * Rule 1: Contains BepInEx/ folder (at root or inside a wrapper) → game_root
   * Rule 2: Contains plugins/ AND/OR patchers/ at root             → bepinex
   * Rule 3: Contains a named folder with .dll                      → plugins
   * Rule 4: Contains loose .dll at root                            → plugins
   */
  _detectTarget(entries) {
    const paths = entries.map(e => (e.path || '').replace(/\\/g, '/'));

    // Rule 1: Has a BepInEx/ folder (case-insensitive) at root or one level deep
    const bepinexInfo = this._findBepInExInArchive(paths);
    if (bepinexInfo.found) {
      return 'game_root';
    }

    // Rule 2: Has plugins/ or patchers/ at root
    const hasPluginsDir = paths.some(p => /^plugins(\/|$)/i.test(p));
    const hasPatchersDir = paths.some(p => /^patchers(\/|$)/i.test(p));
    if (hasPluginsDir || hasPatchersDir) {
      return 'bepinex';
    }

    // Rule 3: Named folder containing a .dll
    const topFolders = new Set();
    const dllsInFolders = new Set();
    for (const p of paths) {
      const parts = p.split('/');
      if (parts.length >= 2) {
        topFolders.add(parts[0]);
        if (p.endsWith('.dll')) dllsInFolders.add(parts[0]);
      }
    }
    if (dllsInFolders.size > 0) {
      return 'plugins';
    }

    // Rule 4: Loose .dll at root level
    const hasRootDll = paths.some(p => !p.includes('/') && p.endsWith('.dll'));
    if (hasRootDll) {
      return 'plugins';
    }

    // Fallback
    return 'plugins';
  }

  /**
   * Find BepInEx folder in archive paths — handles:
   * - BepInEx/ at root (direct)
   * - SomeWrapper/BepInEx/ (one level deep, e.g. "GPK 1986/BepInEx/")
   * - Case insensitive (bepinex, BepInEx, BEPINEX)
   * Returns { found, prefix } where prefix is the wrapper folder to strip ('' if at root)
   */
  _findBepInExInArchive(paths) {
    // Check root level first — match "BepInEx/..." or just "BepInEx" directory entry
    if (paths.some(p => /^bepinex(\/|$)/i.test(p))) {
      return { found: true, prefix: '' };
    }
    // Check one level deep: WrapperFolder/BepInEx/...
    for (const p of paths) {
      const match = p.match(/^([^/]+)\/bepinex(\/|$)/i);
      if (match) {
        return { found: true, prefix: match[1] };
      }
    }
    return { found: false, prefix: '' };
  }

  /**
   * Absolute on-disk paths of a mod's tracked files. Mirrors the base-path rules
   * uninstallMod uses: scatter installs are relative to the game folder (or
   * BepInEx/), everything else to the mod's own extractTo.
   */
  _absFilesOf(mod) {
    const files = Array.isArray(mod.files) ? mod.files : [];
    let base;
    if (mod.looseDlls) base = mod.extractTo;
    else if (mod.targetKey === 'game_root') base = this.gamePath;
    else if (mod.targetKey === 'bepinex') base = path.join(this.gamePath, 'BepInEx');
    else base = mod.extractTo;
    if (!base) return [];
    return files.filter(Boolean).map(f => path.join(base, f));
  }

  /**
   * The subfolder a mod's DLL must live in, taken from the curated known-mods
   * list. TextureReplacer.dll only works from BepInEx/plugins/TextureReplacer/;
   * dropped straight into BepInEx/plugins/ it still loads and still appears in
   * the F1 config menu, but resolves its texture folders next to itself and so
   * finds nothing — and if both copies exist BepInEx loads one and skips the
   * other by GUID, which is impossible to spot from the game.
   *
   * Returns the folder name when the DB says this mod's DLL belongs in one.
   */
  _requiredPluginFolder(modName, dllNames) {
    const known = this._findKnownModByName(modName);
    const paths = [...(known?.installedFiles || []), ...(known?.signature || [])];
    const wanted = new Set(dllNames.map(d => path.basename(d).toLowerCase()));
    for (const raw of paths) {
      const m = /^bepinex\/plugins\/([^/]+)\/([^/]+\.dll)$/i.exec(String(raw).replace(/\\/g, '/'));
      if (m && wanted.has(m[2].toLowerCase())) return m[1];
    }
    return null;
  }

  /** DLLs shared by many mods — never treat these as a mod's own plugin assembly. */
  static get SHARED_DLLS() {
    return new Set(['0harmony.dll', 'harmony.dll', 'newtonsoft.json.dll', 'mono.cecil.dll',
      'bepinex.dll', 'bepinex.core.dll', 'bepinex.preloader.dll', 'unityengine.dll']);
  }

  /**
   * After installing, delete stray copies of this mod's own plugin DLL that are
   * sitting elsewhere under BepInEx/plugins — the exact situation a user hit
   * with TextureReplacer.dll present both in plugins/ and in plugins/
   * TextureReplacer/. BepInEx loads one and skips the other, so the mod looks
   * installed and enabled while doing nothing.
   *
   * Deliberately narrow:
   *   - only under BepInEx/plugins (never core/ or patchers/)
   *   - only DLLs named after the mod itself, never shared libraries like
   *     0Harmony.dll that legitimately appear in several places
   *   - never a file another tracked mod claims
   */
  async _sweepDuplicatePluginDlls(mod) {
    const pluginsRoot = path.join(this.gamePath, 'BepInEx', 'plugins');
    if (!fs.existsSync(pluginsRoot)) return [];

    const ours = this._absFilesOf(mod).filter(p => /\.dll$/i.test(p));
    if (ours.length === 0) return [];

    // Only the mod's own assembly: name matches the mod, or its install folder.
    const ident = (v) => String(v || '').toLowerCase().replace(/[^a-z0-9]/g, '');
    const idents = new Set([ident(mod.name), ident(path.basename(mod.extractTo || ''))].filter(Boolean));
    const keep = new Set();
    const targets = new Set();
    for (const p of ours) {
      const base = path.basename(p);
      keep.add(path.resolve(p).toLowerCase());
      if (ModManager.SHARED_DLLS.has(base.toLowerCase())) continue;
      if (idents.has(ident(base.replace(/\.dll$/i, '')))) targets.add(base.toLowerCase());
    }
    if (targets.size === 0) return [];

    const claimed = new Set();
    for (const [id, m] of this.mods) {
      if (id === mod.id) continue;
      for (const p of this._absFilesOf(m)) claimed.add(path.resolve(p).toLowerCase());
    }

    const found = [];
    const walk = (dir) => {
      let entries = [];
      try { entries = fs.readdirSync(dir, { withFileTypes: true }); } catch { return; }
      for (const e of entries) {
        const full = path.join(dir, e.name);
        if (e.isDirectory()) walk(full);
        else if (targets.has(e.name.toLowerCase())) found.push(full);
      }
    };
    walk(pluginsRoot);

    const removed = [];
    for (const f of found) {
      const abs = path.resolve(f).toLowerCase();
      if (keep.has(abs)) continue;
      if (claimed.has(abs)) {
        console.warn(`[install] duplicate ${path.basename(f)} at ${f} belongs to another tracked mod — left alone`);
        continue;
      }
      try {
        await fs.remove(f);
        removed.push(f);
        console.log(`[install] removed duplicate plugin DLL: ${f}`);
      } catch (err) {
        console.warn(`[install] could not remove duplicate ${f}:`, err.message);
      }
    }
    return removed;
  }

  /**
   * Install a staged archive.
   *
   * `skipRemoval` is retained for IPC compatibility but is no longer honoured:
   * an existing copy of the same mod is always removed before the new archive
   * is extracted (see the removal loop below). Overwriting in place left stale
   * files from the previous version behind.
   */
  async installMod(filename, targetKey, modName, skipRemoval = false) {
    if (!this.gamePath) throw new Error('Game path not set');
    // Verify the game folder is really the game folder BEFORE writing anything.
    // Installing into a wrong folder appears to succeed — files copy fine — but
    // nothing ever loads in game, and that is very hard for a user to diagnose.
    if (!this.isGameExePresent()) {
      throw new Error(`"${GAME_EXE}" was not found in "${this.gamePath}". Set the game location to the folder containing it (for Xbox Game Pass that is the "Content" folder) before installing mods.`);
    }
    const archivePath = path.join(this.getStagingPath(), filename);
    if (!fs.existsSync(archivePath)) throw new Error(`Archive not found: ${filename}`);
    // targetKey is optional. The Downloaded Mods list no longer lets users pick a
    // destination — the manager decides from the archive's own structure below,
    // once the archive has been read. A caller may still pass one explicitly.
    if (targetKey && !TARGETS[targetKey]) throw new Error(`Invalid target: ${targetKey}`);

    const parsed = this._parseNexusFilename(filename);
    const name = modName || parsed.name;
    const version = parsed.version || '';
    // Remember the source archive's modified time so a later, newer archive of
    // the same mod (even with the same/no version) is recognized as an update
    // rather than a plain reinstall — compares archive-date to archive-date.
    let archiveMtime = 0;
    try { archiveMtime = fs.statSync(archivePath).mtimeMs; } catch {}

    // Security scan — block dangerous archives before installation
    const peek = await this.peekArchive(filename);
    // Blocklist — refuse before anything is written. Checked here rather than
    // only in the UI so no other code path (bulk install, a future import) can
    // slip one of these in.
    const blockedMod = this._findBlockedMod(filename, peek.entries);
    if (blockedMod) {
      const alt = blockedMod.useInstead ? ` Use ${blockedMod.useInstead} instead.` : '';
      throw new Error(`"${blockedMod.name}" cannot be installed. ${blockedMod.reason}${alt}`);
    }

    // Resolve the destination from the archive contents when the caller didn't
    // force one. peekArchive already ran _detectTarget over the real entry list,
    // so this is the same answer the list shows, decided by the backend.
    if (!targetKey) {
      targetKey = peek.suggestedTarget && TARGETS[peek.suggestedTarget] ? peek.suggestedTarget : 'plugins';
      console.log(`[install] "${filename}": detected target ${targetKey}`);
    }
    if (peek.security && !peek.security.safe) {
      return {
        success: false,
        blocked: true,
        error: `Security scan blocked: ${peek.security.blocked[0]}`,
        security: peek.security,
      };
    }

    // Direct archive scan for target detection — don't rely on peek.entries which may fail for large archives
    const archivePaths = await this._quickScanArchivePaths(archivePath);
    const bepInfo = this._findBepInExInArchive(archivePaths);

    // Safety: if archive contains BepInEx/ (at root or wrapped), force game_root
    if (bepInfo.found && targetKey !== 'game_root') {
      targetKey = 'game_root';
    }

    // Also check for plugins/ or patchers/ at root → should be bepinex target
    const hasPluginsRoot = archivePaths.some(p => /^plugins(\/|$)/i.test(p) || /^patchers(\/|$)/i.test(p));
    if (hasPluginsRoot && targetKey !== 'bepinex' && targetKey !== 'game_root') {
      targetKey = 'bepinex';
    }

    // Reverse guard: a plain plugin mod (a named folder holding a .dll, with no
    // BepInEx/ and no plugins//patchers/ of its own) must NOT be dropped at the
    // BepInEx root — that yields BepInEx/<Mod>/<Mod>.dll, which BepInEx never
    // loads because it only scans plugins/ and patchers/. Send it to plugins/.
    // Only the recognized BepInEx subfolder names are allowed to sit at the root.
    if (!bepInfo.found && !hasPluginsRoot && targetKey === 'bepinex') {
      const BEPINEX_SUBDIRS = ['plugins', 'patchers', 'config', 'core', 'cache', 'unity-libs', 'monomod'];
      const topLevel = new Set();
      let dllInNamedFolder = false;
      for (const raw of archivePaths) {
        const p = String(raw).replace(/\\/g, '/');
        const parts = p.split('/').filter(Boolean);
        if (parts.length === 0) continue;
        topLevel.add(parts[0].toLowerCase());
        if (parts.length >= 2 && /\.dll$/i.test(p)) dllInNamedFolder = true;
      }
      const touchesBepInExSubdir = [...topLevel].some(n => BEPINEX_SUBDIRS.includes(n));
      if (dllInNamedFolder && !touchesBepInExSubdir) {
        targetKey = 'plugins';
      }
    }

    // Some archives wrap content in a single folder that contains a plugins/
    // subfolder (e.g. "FogBeGone/plugins/FogBeGone.dll"). Treat the wrapper as
    // a BepInEx-prefix equivalent — extract and merge from the wrapper's contents
    // so the DLL lands at BepInEx/plugins/FogBeGone.dll rather than a nested mess
    // like BepInEx/plugins/<sanitized>/<wrapper>/plugins/<dll>.
    const topLevelFolders = new Set();
    const topLevelFiles = [];
    for (const p of archivePaths) {
      const first = p.split(/[\/\\]/)[0];
      if (p.includes('/') || p.includes('\\')) topLevelFolders.add(first);
      else topLevelFiles.push(first);
    }
    if (!bepInfo.found && topLevelFolders.size === 1) {
      const wrapper = [...topLevelFolders][0];
      const wrapperPrefix = wrapper + '/';
      const hasWrappedPlugins = archivePaths.some(p => {
        const np = p.replace(/\\/g, '/');
        return np.toLowerCase().startsWith(wrapperPrefix.toLowerCase() + 'plugins/')
            || np.toLowerCase().startsWith(wrapperPrefix.toLowerCase() + 'patchers/');
      });
      if (hasWrappedPlugins) {
        // Treat the wrapper folder as a BepInEx-equivalent prefix. Install to
        // BepInEx/ target with wrapper stripped — mirrors the bepInfo.prefix logic.
        targetKey = 'bepinex';
        bepInfo.found = true;
        bepInfo.prefix = wrapper;
      }
    }

    const target = TARGETS[targetKey];

    // Handle an existing copy of this mod: ALWAYS remove its files first, then
    // extract the new archive into a clean folder.
    //
    // This used to keep the old files and just overwrite them (skipRemoval),
    // which leaves stale files behind whenever a new version renames, splits or
    // drops a file: the old .dll/.png stays on disk forever, BepInEx may load
    // both, and mods misbehave in ways the user can only fix by hand. Removing
    // first makes the installed copy exactly what the new archive contains.
    //
    // Safe by construction — uninstallMod only deletes files this mod's own
    // archive was catalogued as owning, never directories, never files another
    // tracked mod also claims (reference counting), and never BepInEx configs,
    // so user settings and files dropped into a mod folder by hand survive.
    // It also clears any leftover disabled-mods copy, which the old path
    // orphaned on disk.
    const baseKey = this._baseName(name);
    let removedOldFiles = 0;
    let previousWasUntracked = false;
    for (const [oldId, m] of Array.from(this.mods)) {
      const mBase = this._baseName(m.name);
      if (m.filename === filename || mBase === baseKey) {
        try {
          // Report as its own phase so the UI can show "Removing old version"
          // instead of a progress bar that just says "Installing" throughout —
          // otherwise the removal is invisible and looks like it never ran.
          const res = await this.uninstallMod(oldId, (done, total) => {
            if (this.onProgress) this.onProgress(-1, done, total, 'removing');
          });
          removedOldFiles += res?.removed || 0;
          if (!res?.tracked) previousWasUntracked = true;
          console.log(`[install] "${m.name}": removed ${res?.removed || 0} of ${res?.tracked || 0} tracked files before extracting`);
        } catch (err) {
          // Never let cleanup block the install — drop the stale DB entry and
          // carry on; the extraction below overwrites what is still there.
          console.warn('[install] could not fully remove previous version:', err.message);
          this.mods.delete(oldId);
          this._saveDb();
        }
      }
    }

    // If a mod with this name already exists in the DB (case-insensitive),
    // reuse its id so we update the existing entry rather than creating a
    // duplicate. Handles cases where a mod was previously scan-imported with
    // a slightly different name casing (e.g. folder "Endgame Overhaul" on
    // disk vs DB entry "EndGame Overhaul"), or the user simply reinstalls.
    let id;
    const lowerName = (name || '').toLowerCase();
    for (const [existingId, existingMod] of this.mods) {
      if ((existingMod.name || '').toLowerCase() === lowerName) {
        id = existingId;
        break;
      }
    }
    if (!id) id = `mod_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
    let extractTo;
    const scatterInstall = (targetKey === 'game_root' || targetKey === 'bepinex');

    if (scatterInstall) {
      extractTo = targetKey === 'game_root' ? this.gamePath : path.join(this.gamePath, 'BepInEx');
      fs.ensureDirSync(extractTo);

      if (bepInfo.found && bepInfo.prefix) {
        // Archive has BepInEx inside a wrapper folder (e.g. "GPK 1986/BepInEx/...")
        // or a wrapper containing plugins/patchers directly (e.g. "FogBeGone/plugins/...").
        // In either case, merge the wrapper's contents into `extractTo`:
        //   - targetKey='game_root' → extractTo=gamePath → wrapper/BepInEx/... → gamePath/BepInEx/...
        //   - targetKey='bepinex'   → extractTo=gamePath/BepInEx → wrapper/plugins/... → gamePath/BepInEx/plugins/...
        const tempDir = path.join(this.getStagingPath(), '_temp_extract_' + Date.now());
        fs.ensureDirSync(tempDir);
        try {
          await this._extract(archivePath, tempDir, this.onProgress);

          // Merge everything from inside the wrapper to the chosen extract target
          const wrapperDir = path.join(tempDir, bepInfo.prefix);
          if (fs.existsSync(wrapperDir)) {
            this._mergeDir(wrapperDir, extractTo);
          }

          // Also merge files at temp root level (some archives put winhttp.dll alongside the wrapper).
          // For game_root target, loose root files go to gamePath (winhttp, doorstop, etc.).
          // For bepinex target, loose root files alongside the wrapper don't really happen in the
          // wrapped-plugins case — but if they do, gamePath is still the safest landing for them
          // since they aren't under plugins/patchers.
          for (const entry of fs.readdirSync(tempDir, { withFileTypes: true })) {
            if (entry.name === bepInfo.prefix) continue;
            const src = path.join(tempDir, entry.name);
            const dest = path.join(this.gamePath, entry.name);
            if (entry.isDirectory()) {
              fs.ensureDirSync(dest);
              this._mergeDir(src, dest);
            } else {
              fs.moveSync(src, dest, { overwrite: true });
            }
          }
        } finally {
          // Always clean up temp folder
          if (fs.existsSync(tempDir)) await fs.remove(tempDir);
        }
      }
    } else {
      // Check if the archive already contains a top-level folder
      // If so, extract directly to the target (e.g. plugins/) to avoid double-nesting
      // If it has loose files, wrap them in a named subfolder
      const archiveStructure = await this._getArchiveTopLevel(archivePath, filename);

      if (archiveStructure.hasSingleFolder) {
        // Archive has a folder like TextureReplacer/ — extract directly to plugins/
        // extractTo tracks the actual folder for toggle/uninstall
        const targetDir = path.join(this.gamePath, target.relative);
        fs.ensureDirSync(targetDir);
        await this._extract(archivePath, targetDir, this.onProgress);
        extractTo = path.join(targetDir, archiveStructure.folderName);
      } else if (archiveStructure.hasNamedFolders) {
        // Archive has multiple folders — extract directly to target
        extractTo = path.join(this.gamePath, target.relative);
        fs.ensureDirSync(extractTo);
        await this._extract(archivePath, extractTo, this.onProgress);
      } else {
        // Loose files at root — check if it's just DLLs
        const rootFiles = archivePaths.filter(p => p && !p.includes('/') && !p.endsWith('/'));
        const allDlls = rootFiles.length > 0 && rootFiles.every(p => p.toLowerCase().endsWith('.dll'));

        if (allDlls) {
          // A bare DLL archive normally belongs directly in plugins/. But some
          // mods only work from their own subfolder — TextureReplacer resolves
          // objects_textures next to its DLL, so plugins/TextureReplacer.dll
          // loads, shows up enabled in the F1 menu, and replaces nothing. The
          // known-mods list records the required folder; honour it.
          const requiredFolder = this._requiredPluginFolder(name, rootFiles);
          extractTo = requiredFolder
            ? path.join(this.gamePath, target.relative, requiredFolder)
            : path.join(this.gamePath, target.relative);
          if (requiredFolder) console.log(`[install] "${name}": DLL must live in plugins/${requiredFolder} — extracting there`);
          fs.ensureDirSync(extractTo);
          await this._extract(archivePath, extractTo, this.onProgress);
          // Track just the DLL filenames (not entire plugins folder)
          let looseDllFiles = rootFiles.map(p => p.replace(/\\/g, '/'));
          looseDllFiles = this._applyExcludeRules(looseDllFiles, this._findKnownModByName(name));
          const loosePrefab = looseDllFiles.some(f => f.toLowerCase().includes('_prefabloader'));
          const mod = { id, name, version, filename, targetKey, installTarget: targetKey, targetLabel: target.label, extractTo, enabled: true, core: this._isKnownCore(name) || loosePrefab, installedAt: new Date().toISOString(), archiveMtime, files: looseDllFiles, fileCount: looseDllFiles.length, looseDlls: !requiredFolder };
          this.mods.set(id, mod);
          this._saveDb();
          this._autoAssignGroupByName(id, name, looseDllFiles);
          const dupes = await this._sweepDuplicatePluginDlls(mod);
          if (this.getDeleteAfterInstall()) {
            const zipPath = path.join(this.getStagingPath(), filename);
            if (fs.existsSync(zipPath)) fs.removeSync(zipPath);
          }
          // Same shape as the main return path. This used to return
          // { success, mod }, which the IPC layer wrapped again — the renderer
          // then read r.mod.name off the wrapper and showed "undefined".
          return { ...mod, removedOldFiles, previousWasUntracked, removedDuplicates: dupes.length };
        } else {
          // Mixed loose files — wrap in a named subfolder (strip archive extensions from folder name)
          const safeName = this._sanitize(name).replace(/\.(zip|rar|7z)$/i, '');
          extractTo = path.join(this.gamePath, target.relative, safeName);
          fs.ensureDirSync(extractTo);
          await this._extract(archivePath, extractTo, this.onProgress);
        }
      }
    }

    // Only extract here for scatter installs without wrapper (wrapper case already extracted above)
    if (scatterInstall && !(bepInfo.found && bepInfo.prefix)) {
      await this._extract(archivePath, extractTo, this.onProgress);

    }

    // Build file list from archive contents (never scan disk — that picks up other mods' files)
    let files;
    const rawFiles = await this._catalogArchive(archivePath);

    if (scatterInstall && bepInfo.found && bepInfo.prefix) {
      // Strip wrapper prefix from cataloged paths
      const prefixLower = bepInfo.prefix.toLowerCase() + '/';
      files = rawFiles.map(f => {
        const fl = f.toLowerCase();
        if (fl.startsWith(prefixLower)) return f.substring(bepInfo.prefix.length + 1);
        return f;
      }).filter(f => f.length > 0);
    } else if (scatterInstall) {
      files = rawFiles;
    } else {
      // For plugin/patcher installs, make paths relative to extractTo
      // e.g. archive has "TextureReplacer/file.dll" → relative to extractTo (plugins/TextureReplacer/) = "file.dll"
      const extractToRel = path.relative(path.join(this.gamePath, target.relative), extractTo).replace(/\\/g, '/');
      if (extractToRel && extractToRel !== '.') {
        const prefix = extractToRel + '/';
        files = rawFiles
          .filter(f => f.startsWith(prefix) || f.replace(/\\/g, '/').startsWith(prefix))
          .map(f => f.substring(prefix.length));
      } else {
        files = rawFiles;
      }
    }

    // Apply global + per-mod exclude rules. Files matching these patterns
    // still exist on disk — they're just not OWNED by this mod, so uninstall
    // won't touch them. Prevents: shared phone-app icons, BepInEx configs,
    // BepInEx cache. See _getGlobalExcludePatterns for the full rule list.
    const knownEntry = this._findKnownModByName(name);
    files = this._applyExcludeRules(files, knownEntry);

    // AUTO-DERIVED TRACKING: trust the archive catalog as ground truth. Each
    // mod owns exactly the files its zip extracted. Reference counting at
    // uninstall/toggle time handles cross-mod shared files (e.g. DXPokemon
    // icons shared across many DraX Pokemon mods) — those files only get
    // removed when the LAST mod claiming them is uninstalled. We no longer
    // override with hardcoded DB.installedFiles lists because the manager
    // already knows exactly what each archive ships.
    //
    // We still need to normalize paths to gamePath-relative form across all
    // mods so cross-mod comparison in _filesClaimedByOtherMods works reliably.
    // Scatter targets (game_root/bepinex) are already gamePath-relative after
    // the wrapper-prefix-strip above. Non-scatter targets (plugins/patchers/
    // config) currently store extractTo-relative paths — prefix them with the
    // target's relative path so everything speaks the same coordinate system,
    // then switch targetKey to bepinex semantics for the same reason.
    // Where the files really went, kept for display. The block below rewrites
    // targetKey to 'game_root' for path bookkeeping only, which made the
    // Installed Mods list report "Game Root Folder" for a mod that went to
    // BepInEx/plugins — the install row and the installed row disagreed.
    const installTarget = targetKey;

    if (!scatterInstall && files.length > 0) {
      const targetRel = TARGETS[targetKey].relative.replace(/\\/g, '/');
      if (targetRel && targetRel !== '.') {
        // Account for any subfolder we wrapped the extract in (e.g. when archive
        // has mixed loose files, we extracted to plugins/<safeName>/). Compute
        // the prefix from extractTo's position under gamePath.
        const extractRel = path.relative(this.gamePath, extractTo).replace(/\\/g, '/');
        const prefix = extractRel ? extractRel + '/' : '';
        files = files.map(f => prefix + f);
      }
      // Switch to gamePath-relative scatter semantics so the toggle/uninstall
      // path resolver joins against gamePath and ref-counting can compare
      // canonical keys across mods.
      targetKey = 'game_root';
      extractTo = this.gamePath;
    }

    const hasPrefabloader = files.some(f => f.toLowerCase().includes('_prefabloader'));
    const mod = { id, name, version, filename, targetKey, installTarget, targetLabel: TARGETS[installTarget].label, extractTo, enabled: true, core: this._isKnownCore(name) || hasPrefabloader, installedAt: new Date().toISOString(), archiveMtime, files, fileCount: files.length };
    this.mods.set(id, mod);
    this._saveDb();
    this._autoAssignGroupByName(id, name, files);

    // Delete zip from staging after install if setting is enabled
    if (this.getDeleteAfterInstall()) {
      const zipPath = path.join(this.getStagingPath(), filename);
      if (fs.existsSync(zipPath)) fs.removeSync(zipPath);
    }

    // Installing is the other moment the staging folder gains a version, so keep
    // it trimmed here as well as on add.
    try { this.pruneStagingVersions(); } catch (err) { console.warn('[staging] prune failed:', err.message); }

    // Remove stray copies of this mod's own DLL left elsewhere under plugins/
    // (e.g. a hand-installed TextureReplacer.dll sitting in plugins/ next to the
    // proper plugins/TextureReplacer/ copy) — BepInEx would load one and skip
    // the other, leaving the mod silently inert.
    const removedDuplicates = (await this._sweepDuplicatePluginDlls(mod)).length;

    // Returned as a COPY: removedOldFiles/previousWasUntracked/removedDuplicates
    // are reporting fields for this install only and must not be persisted.
    return { ...mod, removedOldFiles, previousWasUntracked, removedDuplicates };
  }

  getDeleteAfterInstall() { return store.get('deleteAfterInstall', false); }
  setDeleteAfterInstall(val) { store.set('deleteAfterInstall', !!val); }

  /** Check if archive has a top-level folder or loose files */
  async _getArchiveTopLevel(archivePath, filename) {
    const topItems = new Set();
    const topFiles = [];

    // Directory entries must never count as loose root files. 7z and rar list a
    // mod's own folder as a bare entry with no trailing slash
    // ("StorageShelfExpanded"), so counting it as a file made hasSingleFolder
    // false and the installer wrapped the archive in a second folder of the same
    // name: plugins/StorageShelfExpanded/StorageShelfExpanded/. Zips happened to
    // escape this because their directory entries carry a trailing slash.
    if (/\.zip$/i.test(filename)) {
      const zipEntries = await this._listZipEntries(archivePath);
      for (const e of zipEntries) {
        const first = e.path.split('/')[0];
        topItems.add(first);
        if (!e.isDir && !e.path.includes('/')) topFiles.push(first);
      }
    } else if (/\.rar$/i.test(filename)) {
      const bin7z = this._getSystem7z();
      if (bin7z) {
        for (const e of await this._sevenList(archivePath, bin7z)) {
          const first = e.file.split(/[\/\\/]/)[0];
          topItems.add(first);
          if (!this._sevenEntryIsDir(e) && !e.file.includes('/') && !e.file.includes('\\')) topFiles.push(first);
        }
      }
    } else {
      for (const e of await this._sevenList(archivePath)) {
        const first = e.file.split(/[/\\]/)[0];
        topItems.add(first);
        if (!this._sevenEntryIsDir(e) && !e.file.includes('/') && !e.file.includes('\\')) topFiles.push(first);
      }
    }

    const hasSingleFolder = topItems.size === 1 && topFiles.length === 0;
    const hasNamedFolders = topFiles.length === 0 && topItems.size > 0;
    const hasLooseFiles = topFiles.length > 0;
    const folderName = hasSingleFolder ? [...topItems][0] : null;

    return { hasSingleFolder, hasNamedFolders, hasLooseFiles, folderName };
  }

  /** Normalize a file path to use correct BepInEx casing */
  _normalizePath(filePath) {
    return filePath
      .replace(/^bepinex\//i, 'BepInEx/')
      .replace(/^([^/]+\/)bepinex\//i, '$1BepInEx/')  // wrapper/bepinex → wrapper/BepInEx
      .replace(/\/bepinex\//gi, '/BepInEx/')
      .replace(/^BepInEx\/Plugins\//i, 'BepInEx/plugins/')
      .replace(/^BepInEx\/Patchers\//i, 'BepInEx/patchers/')
      .replace(/^BepInEx\/Config\//i, 'BepInEx/config/')
      .replace(/^BepInEx\/Core\//i, 'BepInEx/core/');
  }

  /**
   * Absolute path of the 7za binary that ships inside the app bundle.
   *
   * In the packaged app this lives under `app.asar.unpacked`. For the Windows
   * PORTABLE build that resolves to
   * `%TEMP%\Real-TCG-Overhaul-Mod-Manager\resources\app.asar.unpacked\...`,
   * because the portable stub unpacks the whole app into %TEMP% on every launch
   * and runs it from there.
   *
   * That folder is fair game for Windows Storage Sense, Disk Cleanup, "clean my
   * temp files" utilities and antivirus. When it is wiped while the manager is
   * STILL RUNNING, the .asar itself survives (Electron holds it open, so Windows
   * refuses to delete it) but 7za.exe is only touched at spawn time — so every
   * archive operation suddenly fails with `spawn ...7za.exe ENOENT` and keeps
   * failing until the app is restarted and unpacks itself again.
   *
   * Never spawn this path directly; go through _get7zBin().
   */
  _bundled7zPath() {
    let p = sevenBin.path7za;
    if (p.includes('app.asar')) p = p.replace('app.asar', 'app.asar.unpacked');
    return p;
  }

  /** Writable, NON-temporary folders the private 7za copy can live in, best first. */
  _stable7zDirs() {
    const dirs = [];
    try {
      const ud = require('electron').app?.getPath('userData');
      if (ud) dirs.push(path.join(ud, 'bin'));
    } catch { /* not running under Electron (test harness) */ }
    if (this.portablePath) dirs.push(path.join(this.portablePath, 'bin'));
    return dirs;
  }

  /**
   * On Linux/macOS the bundled 7za must carry the executable bit. It is often
   * lost when the app is packaged (or unpacked from an archive that doesn't
   * preserve POSIX modes), and the result is a bare "spawn ... EACCES" that
   * breaks every archive operation. Restore it cheaply.
   */
  _makeExecutable(p) {
    if (process.platform === 'win32') return;
    try {
      const mode = fs.statSync(p).mode;
      if (!(mode & 0o111)) fs.chmodSync(p, 0o755);
    } catch (err) {
      console.warn('[7z] could not set executable bit:', err.code || err.message);
    }
  }

  /**
   * Resolve the 7za binary to use, keeping a private copy OUTSIDE the volatile
   * unpack directory (see _bundled7zPath). The copy sits next to the settings
   * DB (userData), which nothing but this app ever touches, so archive
   * operations keep working for the whole session even if %TEMP% is emptied
   * underneath us.
   *
   * Pass force=true to re-provision after a spawn failed with ENOENT/EACCES.
   */
  _get7zBin(force = false) {
    if (!force && this._7zBinPath && fs.existsSync(this._7zBinPath)) return this._7zBinPath;
    this._7zBinPath = null;

    const src = this._bundled7zPath();
    const name = path.basename(src);
    let srcStat = null;
    try { srcStat = fs.statSync(src); } catch { /* unpack dir was cleaned */ }

    for (const dir of this._stable7zDirs()) {
      const dst = path.join(dir, name);
      try {
        let ok = fs.existsSync(dst);
        // (Re)copy when the private copy is missing, truncated, or from an older build.
        if (srcStat && (!ok || fs.statSync(dst).size !== srcStat.size)) {
          fs.ensureDirSync(dir);
          fs.copyFileSync(src, dst);
          ok = true;
        }
        if (ok) {
          this._makeExecutable(dst);
          this._7zBinPath = dst;
          return dst;
        }
      } catch (err) {
        console.warn('[7z] could not stage binary in', dir, '-', err.code || err.message);
      }
    }

    // No writable home for the copy — fall back to the bundled binary itself.
    if (srcStat) {
      this._makeExecutable(src);
      this._7zBinPath = src;
      return src;
    }
    // Bundled binary gone AND no copy survived: a full 7-Zip install, if the
    // user has one, does everything 7za does.
    const sys = this._getSystem7z();
    if (sys) {
      this._7zBinPath = sys;
      return sys;
    }
    return src; // let the spawn fail, with a decorated message
  }

  /** True when a 7za failure means "the binary isn't there / can't be run". */
  _is7zMissingError(err) {
    const s = `${err?.code || ''} ${err?.message || ''} ${err?.stderr || ''}`;
    return /ENOENT|EACCES|EPERM|not recognized|No such file/i.test(s);
  }

  /** Replace a raw `spawn ... ENOENT` with something the user can act on. */
  _decorate7zError(err, bin) {
    if (!this._is7zMissingError(err)) return err;
    const e = new Error(
      `Could not run the bundled 7-Zip helper (${bin}). ` +
      (process.platform === 'win32'
        ? 'Antivirus most likely quarantined 7za.exe, or a temp-file cleaner removed the app’s unpacked files. Add the Mod Manager to your antivirus exclusions, or install 7-Zip (https://7-zip.org), then restart the manager.'
        : 'Install p7zip with your package manager (e.g. "sudo apt install p7zip-full") and restart the manager.')
    );
    e.code = err?.code;
    return e;
  }

  /**
   * Run a 7za-backed operation. If the binary turns out to have vanished
   * mid-session (temp cleanup / AV quarantine), re-provision it and retry ONCE
   * instead of surfacing "spawn ...7za.exe ENOENT" and forcing an app restart.
   *
   * An explicitly supplied bin (system 7z / WinRAR paths) is used as-is.
   */
  async _run7z(bin, run) {
    const explicit = !!bin;
    const first = bin || this._get7zBin();
    try {
      return await run(first);
    } catch (err) {
      if (explicit || !this._is7zMissingError(err)) throw err;
      const fresh = this._get7zBin(true);
      if (!fresh || (fresh === first && !fs.existsSync(fresh))) throw this._decorate7zError(err, first);
      try {
        return await run(fresh);
      } catch (err2) {
        throw this._decorate7zError(err2, fresh);
      }
    }
  }

  /** Find full 7z on system (supports RAR, unlike 7za) */
  _getSystem7z() {
    const isWin = process.platform === 'win32';
    const exe = isWin ? '7z.exe' : '7z';
    // 1. Check for bundled 7z in app assets
    const bundledPaths = [
      path.join(__dirname, '../../assets/bin/' + exe),
      path.join(__dirname, '../assets/bin/' + exe),
    ];
    if (this.portablePath) bundledPaths.push(path.join(this.portablePath, exe));
    for (const p of bundledPaths) {
      if (fs.existsSync(p)) return p;
    }
    // 2. Check common install locations
    const candidates = isWin ? [
      'C:\\Program Files\\7-Zip\\7z.exe',
      'C:\\Program Files (x86)\\7-Zip\\7z.exe',
      'D:\\Program Files\\7-Zip\\7z.exe',
      'D:\\Program Files (x86)\\7-Zip\\7z.exe',
    ] : [
      // p7zip-full / 7zip packages on Linux, Homebrew on macOS
      '/usr/bin/7z', '/usr/local/bin/7z', '/usr/bin/7zz', '/usr/local/bin/7zz',
      '/opt/homebrew/bin/7z', '/usr/bin/7za', '/usr/local/bin/7za',
    ];
    for (const c of candidates) {
      if (fs.existsSync(c)) return c;
    }
    // 3. Check PATH
    const { execSync } = require('child_process');
    try {
      const cmd = isWin ? 'where 7z.exe' : 'command -v 7z || command -v 7zz';
      const found = execSync(cmd, { windowsHide: true, timeout: 3000, shell: isWin ? undefined : '/bin/sh' })
        .toString().trim().split('\n')[0].trim();
      if (found && fs.existsSync(found)) return found;
    } catch {}
    return null;
  }

  /** Find WinRAR's UnRAR.exe on system */
  /** Find a RAR-capable helper: UnRAR/WinRAR on Windows, unrar on Linux/macOS */
  _getWinRAR() {
    const isWin = process.platform === 'win32';
    const candidates = isWin ? [
      'C:\\Program Files\\WinRAR\\UnRAR.exe',
      'C:\\Program Files (x86)\\WinRAR\\UnRAR.exe',
      'D:\\Program Files\\WinRAR\\UnRAR.exe',
      'C:\\Program Files\\WinRAR\\WinRAR.exe',
    ] : [
      // WinRAR itself doesn't exist on Linux/macOS, but RARLAB ships the same
      // extractor as the `unrar` package (and Homebrew's unrar on macOS).
      '/usr/bin/unrar', '/usr/local/bin/unrar', '/opt/homebrew/bin/unrar',
      '/usr/bin/unrar-free', '/usr/local/bin/unrar-free',
    ];
    for (const c of candidates) {
      if (fs.existsSync(c)) return c;
    }
    const { execSync } = require('child_process');
    try {
      const cmd = isWin ? 'where UnRAR.exe' : 'command -v unrar || command -v unrar-free';
      const found = execSync(cmd, { windowsHide: true, timeout: 3000, shell: isWin ? undefined : '/bin/sh' })
        .toString().trim().split('\n')[0].trim();
      if (found && fs.existsSync(found)) return found;
    } catch {}
    return null;
  }

  /** Extract a RAR file using system 7z or WinRAR */
  /**
   * Run `7za x` and resolve when extraction finishes, guaranteeing the child
   * process and its stream are torn down.
   *
   * Same hazard as _sevenList: node-7z spawns 7za, which holds an open handle on
   * the archive. If the stream is abandoned (especially on the error path) the
   * child can outlive the call, and on Windows that leaked handle makes the .zip
   * undeletable until the whole app exits. Always reap it.
   */
  _sevenExtract(src, destDir, bin, onProgress, errPrefix) {
    return this._run7z(bin, b => this._sevenExtractOnce(src, destDir, b, onProgress, errPrefix));
  }

  _sevenExtractOnce(src, destDir, bin, onProgress, errPrefix) {
    return new Promise((resolve, reject) => {
      let settled = false;
      let s;
      const teardown = () => {
        try { if (s && s._childProcess && !s._childProcess.killed) s._childProcess.kill(); } catch {}
        try { if (s && typeof s.destroy === 'function') s.destroy(); } catch {}
        try { if (s && typeof s.removeAllListeners === 'function') s.removeAllListeners(); } catch {}
      };
      const finish = (err) => {
        if (settled) return;
        settled = true;
        teardown();
        if (err) reject(err); else resolve();
      };
      try {
        s = Seven.extractFull(src, destDir, { $bin: bin, yes: true, $spawnOptions: { windowsHide: true } });
      } catch (err) {
        return finish(err);
      }
      let done = 0;
      s.on('data', () => { done++; if (onProgress) onProgress(-1, done, -1); });
      s.on('end', () => finish(null));
      s.on('error', (err) => finish(new Error(`${errPrefix}: ${err.message || err.stderr || 'Unknown error'}`)));
    });
  }

  async _extractRar(src, dest, onProgress) {
    const tempDir = path.join(this.getStagingPath(), '_rar_temp_' + Date.now());
    fs.ensureDirSync(tempDir);
    try {
      const bin7z = this._getSystem7z();
      if (bin7z) {
        await this._sevenExtract(src, tempDir, bin7z, onProgress, 'RAR extraction failed');
      } else {
        const unrar = this._getWinRAR();
        if (!unrar) {
          throw new Error(process.platform === 'win32'
            ? 'Cannot extract RAR files. Install 7-Zip (https://7-zip.org) or WinRAR, or ask the mod author for a .zip.'
            : 'Cannot extract RAR files. Install p7zip or unrar with your package manager (e.g. "sudo pacman -S p7zip" / "sudo apt install p7zip-full unrar"), or convert the archive to .zip. On Steam Deck the system folder is read-only, so converting to .zip is usually easiest.');
        }
        const { execFile } = require('child_process');
        await new Promise((res, rej) => {
          // Trailing separator matters to unrar/WinRAR — it marks the argument as
          // a destination FOLDER. Must be "\" on Windows and "/" elsewhere.
          const destArg = tempDir + path.sep;
          const args = unrar.toLowerCase().includes('winrar')
            ? ['x', '-o+', '-ibck', src, destArg]
            : ['x', '-o+', '-y', src, destArg];
          execFile(unrar, args, { windowsHide: true, timeout: 300000 }, (err) => { if (err) rej(new Error(`RAR extraction failed: ${err.message}`)); else res(); });
        });
      }
      // Windows-packed archives can leave literal-backslash filenames on Linux
      // (Steam Deck) — rebuild real folders before merging.
      this._explodeBackslashEntries(tempDir);
      this._mergeDir(tempDir, dest);
    } finally {
      if (fs.existsSync(tempDir)) fs.removeSync(tempDir);
    }
  }

  /** List RAR archive contents using system 7z or WinRAR */
  async _listRar(archivePath) {
    const entries = [];
    const bin7z = this._getSystem7z();
    if (bin7z) {
      for (const e of await this._sevenList(archivePath, bin7z)) {
        entries.push({ file: e.file?.replace(/\\/g, '/'), size: e.size || 0, isDir: this._sevenEntryIsDir(e) });
      }
    } else {
      const winrar = this._getWinRAR();
      if (!winrar) return entries;
      const { execFile } = require('child_process');
      await new Promise((res, rej) => {
        execFile(winrar, ['lb', archivePath], { windowsHide: true, timeout: 30000 }, (err, stdout) => {
          if (err) { rej(err); return; }
          for (const line of stdout.split('\n')) {
            const f = line.trim();
            if (f) entries.push({ file: f.replace(/\\/g, '/'), size: 0, isDir: f.endsWith('\\') || f.endsWith('/') });
          }
          res();
        });
      });
    }
    return entries;
  }

  async _extract(src, dest, onProgress) {
    if (/\.zip$/i.test(src)) {
      try {
        await this._extractZip(src, dest, onProgress);
        return;
      } catch (err) {
        // node-stream-zip rejects Windows-style zips (backslash paths) as "Malicious entry".
        // Fall back to extract-zip (yauzl), which handles them fine.
        if (!/Malicious entry/.test(err.message || '')) throw err;
        await this._extractZipFallback(src, dest, onProgress);
        return;
      }
    } else if (/\.rar$/i.test(src)) {
      await this._extractRar(src, dest, onProgress);
      return;
    }
    await this._extract7z(src, dest, onProgress);
  }

  async _extractZip(src, dest, onProgress) {
    const StreamZip = require('node-stream-zip');
    // Callback API, NOT StreamZip.async — its close() awaits the open promise and
    // therefore CANNOT release the file descriptor when the archive fails to open.
    // Zips written by Windows tools use backslash separators, which this library
    // rejects as "Malicious entry"; we then fall back to 7z extraction, but the
    // handle from the failed open would stay open for the life of the app and
    // leave the .zip undeletable (EPERM on stat/unlink/rename) until it exits.
    // The callback API's close() is safe and idempotent on every path.
    const zip = new StreamZip({ file: src, storeEntries: true });
    const closeZip = () => { try { zip.close(); } catch { /* already closed */ } };
    try {
      const entries = await new Promise((resolve, reject) => {
        zip.on('ready', () => { try { resolve(zip.entries()); } catch (err) { reject(err); } });
        zip.on('error', reject);
      });

      const fileEntries = Object.values(entries).filter(e => !e.isDirectory);
      const total = fileEntries.length;
      const usePulse = total <= 5; // Too few files for meaningful percentage — pulse instead
      let done = 0;

      for (const entry of Object.values(entries)) {
        // Normalize BepInEx folder casing during extraction
        const normalized = this._normalizePath(entry.name.replace(/\\/g, '/'));
        const target = path.join(dest, normalized);
        if (entry.isDirectory) {
          fs.ensureDirSync(target);
        } else {
          fs.ensureDirSync(path.dirname(target));
          if (usePulse && onProgress) onProgress(-1, done, total);
          await new Promise((resolve, reject) => {
            zip.extract(entry, target, err => (err ? reject(err) : resolve()));
          });
          done++;
          if (onProgress) onProgress(usePulse ? -1 : Math.round((done / total) * 100), done, total);
        }
      }
    } finally {
      // Unconditional: the handle must never outlive this call, whether the
      // archive opened, failed to parse, or extraction threw part-way through.
      closeZip();
    }
  }

  /**
   * Rebuild real directories from entries whose names contain literal backslashes.
   *
   * Archives packed on Windows (WinRAR and friends) store paths like
   * "plugins\EnhancedPrefabLoader\EnhancedPrefabLoader.dll". Windows extractors
   * treat "\" as a separator, but on Linux (Steam Deck) it is a perfectly legal
   * filename character — so yauzl/7z create ONE flat file literally named
   * "plugins\EnhancedPrefabLoader\EnhancedPrefabLoader.dll" instead of nested
   * folders. BepInEx then never finds the plugin. Walk the freshly extracted
   * tree and move any such entry to the path it was meant to have.
   *
   * Runs on every platform: it is a no-op when the extractor already split the
   * path, and on Windows "\" can't appear in a filename anyway.
   */
  _explodeBackslashEntries(dir) {
    const collect = (d, rel, out) => {
      let entries;
      try { entries = fs.readdirSync(d, { withFileTypes: true }); } catch { return; }
      for (const e of entries) {
        const abs = path.join(d, e.name);
        const relPath = rel ? `${rel}/${e.name}` : e.name;
        if (e.isDirectory()) collect(abs, relPath, out);
        else out.push({ abs, relPath });
      }
    };
    const files = [];
    collect(dir, '', files);

    for (const { abs, relPath } of files) {
      if (!relPath.includes('\\')) continue;
      // Normalize separators, then drop any "." / ".." segments so a crafted
      // archive can't escape the extraction directory.
      const parts = relPath.replace(/\\/g, '/').split('/').filter(p => p && p !== '.' && p !== '..');
      if (parts.length === 0) continue;
      const target = path.join(dir, ...parts);
      if (path.resolve(target) === path.resolve(abs)) continue;
      if (!path.resolve(target).startsWith(path.resolve(dir) + path.sep)) continue;
      try {
        fs.ensureDirSync(path.dirname(target));
        fs.moveSync(abs, target, { overwrite: true });
      } catch (err) {
        console.warn('[explodeBackslash] could not relocate', relPath, err.code || err.message);
      }
    }

    // Remove directories left empty (or whose names still contain a backslash).
    const prune = (d) => {
      let entries;
      try { entries = fs.readdirSync(d, { withFileTypes: true }); } catch { return; }
      for (const e of entries) {
        if (e.isDirectory()) prune(path.join(d, e.name));
      }
      if (d !== dir) {
        try { if (fs.readdirSync(d).length === 0) fs.removeSync(d); } catch {}
      }
    };
    prune(dir);
  }

  /**
   * Fallback zip extraction using yauzl (via extract-zip).
   * Handles Windows-style zips with backslash path separators that
   * node-stream-zip rejects. Extracts to a temp dir and merges into dest
   * so BepInEx casing gets normalized via _mergeDir.
   */
  async _extractZipFallback(src, dest, onProgress) {
    const extract = require('extract-zip');
    const tempDir = path.join(this.getStagingPath(), '_zip_temp_' + Date.now());
    fs.ensureDirSync(tempDir);
    try {
      let done = 0;
      await extract(src, {
        dir: tempDir,
        onEntry: () => { done++; if (onProgress) onProgress(-1, done, -1); },
      });
      // Windows-packed archives can leave literal-backslash filenames on Linux
      // (Steam Deck) — rebuild real folders before merging.
      this._explodeBackslashEntries(tempDir);
      this._mergeDir(tempDir, dest);
    } finally {
      if (fs.existsSync(tempDir)) fs.removeSync(tempDir);
    }
  }

  async _extract7z(src, dest, onProgress) {
    // Extract to temp dir in staging folder, then merge into dest.
    // Merging (not moving the whole dir) preserves BepInEx casing normalization.
    const tempDir = path.join(this.getStagingPath(), '_7z_temp_' + Date.now());
    fs.ensureDirSync(tempDir);
    try {
      // Deliberately pass no $bin: _sevenExtract resolves it, so if the bundled
      // 7za went missing mid-session it is re-provisioned and retried once.
      await this._sevenExtract(src, tempDir, null, onProgress,
        'Extraction failed');
      // Windows-packed archives can leave literal-backslash filenames on Linux
      // (Steam Deck) — rebuild real folders before merging.
      this._explodeBackslashEntries(tempDir);
      this._mergeDir(tempDir, dest);
    } finally {
      if (fs.existsSync(tempDir)) fs.removeSync(tempDir);
    }
  }

  /** Recursively merge src directory into dest, never replacing directories */
  _mergeDir(src, dest) {
    for (const entry of fs.readdirSync(src, { withFileTypes: true })) {
      // Normalize BepInEx casing
      let name = entry.name;
      if (name.toLowerCase() === 'bepinex' && name !== 'BepInEx') name = 'BepInEx';
      if (name.toLowerCase() === 'plugins' && name !== 'plugins') name = 'plugins';
      if (name.toLowerCase() === 'patchers' && name !== 'patchers') name = 'patchers';
      if (name.toLowerCase() === 'config' && name !== 'config') name = 'config';
      if (name.toLowerCase() === 'core' && name !== 'core') name = 'core';

      const s = path.join(src, entry.name);
      const d = path.join(dest, name);

      if (entry.isDirectory()) {
        fs.ensureDirSync(d);
        this._mergeDir(s, d);
      } else {
        fs.ensureDirSync(path.dirname(d));
        fs.moveSync(s, d, { overwrite: true });
      }
    }
  }

  async _catalogArchive(archivePath) {
    const files = [];
    if (/\.zip$/i.test(archivePath)) {
      const zipEntries = await this._listZipEntries(archivePath);
      for (const e of zipEntries) { if (!e.isDir) files.push(e.path); }
    } else if (/\.rar$/i.test(archivePath)) {
      const rarEntries = await this._listRar(archivePath);
      for (const e of rarEntries) { if (!e.isDir) files.push(e.file); }
    } else {
      for (const e of await this._sevenList(archivePath)) {
        if (!this._sevenEntryIsDir(e)) files.push(e.file);
      }
    }
    // Always store forward-slash relative paths. Archives packed on Windows use
    // backslashes; on Linux (Steam Deck) those are NOT separators, so a tracked
    // path like "plugins\Foo\Foo.dll" would never match the extracted
    // "plugins/Foo/Foo.dll" and disable/uninstall would silently skip the file.
    return files.map(f => String(f).replace(/\\/g, '/')).filter(Boolean);
  }

  /**
   * Delete this mod's copy inside disabled-mods, whatever shape it was stored in.
   *
   * toggleMod names the folder `_sanitize(mod.name)`, but the old uninstall
   * cleanup looked for `basename(extractTo)` and only ran for non-scatter mods.
   * The two disagree whenever the folder name differs from the sanitized mod
   * name ("Big Mod" → "Big_Mod", renamed mods), and scatter mods (game_root /
   * bepinex) were never cleaned at all — so uninstalling or updating a disabled
   * mod orphaned its files. Check both names and both layouts.
   *
   * Only files this mod tracks are touched; shared files are never moved into
   * disabled-mods in the first place (reference counting in toggleMod), so
   * nothing another mod needs can be here.
   */
  async _removeDisabledCopy(mod) {
    const root = this._getDisabledPath();
    if (!root || !fs.existsSync(root)) return;
    const files = Array.isArray(mod.files) ? mod.files : [];
    const dirNames = new Set();
    if (mod.name) dirNames.add(this._sanitize(mod.name));
    if (mod.extractTo) dirNames.add(path.basename(mod.extractTo));

    for (const dirName of dirNames) {
      if (!dirName) continue;
      const disDir = path.join(root, dirName);
      if (!fs.existsSync(disDir)) continue;
      if (files.length === 0) {
        // No tracked files (legacy entry) — the whole folder was the mod.
        try { await fs.remove(disDir); } catch {}
        continue;
      }
      for (const f of files) {
        if (!f || f === '.' || f === '/') continue;
        const full = path.join(disDir, f);
        try { if (fs.existsSync(full) && fs.statSync(full).isFile()) await fs.remove(full); } catch {}
      }
      this._cleanEmpty(disDir);
      try { if (fs.readdirSync(disDir).length === 0) fs.rmdirSync(disDir); } catch {}
    }

    // Loose-DLL mods mirror their files at the disabled-mods root, not in a subfolder.
    if (mod.looseDlls) {
      for (const f of files) {
        if (!f || f === '.' || f === '/') continue;
        const full = path.join(root, f);
        try { if (fs.existsSync(full) && fs.statSync(full).isFile()) await fs.remove(full); } catch {}
      }
      this._cleanEmpty(root);
    }
  }

  async uninstallMod(modId, onProgress) {
    const mod = this.mods.get(modId);
    if (!mod) throw new Error(`Mod not found: ${modId}`);

    // Progress reporting (see toggleMod) — large mods take a moment to delete
    // every file, so emit (done, total) to drive a progress bar.
    const _total = Array.isArray(mod.files) ? mod.files.length : 0;
    let _done = 0;
    const _step = Math.max(1, Math.ceil(_total / 100));
    const tick = () => {
      _done++;
      if (onProgress && (_done % _step === 0 || _done >= _total)) {
        try { onProgress(_done, _total); } catch {}
      }
    };
    if (onProgress && _total > 0) { try { onProgress(0, _total); } catch {} }

    // Fix imported mods that were saved with wrong targetKey/extractTo
    // Imported mod files are always relative to gamePath
    if (mod.imported) {
      mod.targetKey = 'game_root';
      mod.extractTo = this.gamePath;
    }

    const isScatter = (mod.targetKey === 'game_root' || mod.targetKey === 'bepinex');

    // Reference counting: collect every file claimed by another tracked mod.
    // We must NOT delete a file that another tracked mod (enabled OR disabled)
    // also lists in its files. Shared icons like DXPokemon, HHPokemon, EGOShop
    // are typically referenced by many content packs; deleting them when one
    // mod is uninstalled would break every other mod that needs them. The file
    // is only deleted when the LAST claimant goes away.
    const claimedByOthers = this._filesClaimedByOtherMods(modId);

    // Protected paths that must NEVER be deleted
    const protectedNames = ['bepinex', 'plugins', 'patchers', 'config', 'core'];

    // Count what actually leaves the disk, so callers (and the user) can tell a
    // real removal apart from a no-op. Watching the game folder can't: when the
    // new version ships the same filenames, the files are deleted and recreated
    // at the same paths within the same second, which looks like an overwrite.
    let removed = 0;

    if (isScatter) {
      const base = mod.targetKey === 'game_root' ? this.gamePath : path.join(this.gamePath, 'BepInEx');
      // Untracked scatter entry (imported before file tracking existed): there is
      // no safe file list to delete from, and `base` is the game folder itself,
      // so wiping it wholesale is never an option. Report 0 instead of throwing
      // on `for (const f of undefined)`, which used to abort the whole uninstall.
      for (const f of (Array.isArray(mod.files) ? mod.files : [])) {
        tick();
        if (!f || f === '.' || f === '/') continue;
        // Never delete config files
        if (mod.targetKey === 'bepinex' && (f.toLowerCase().startsWith('config' + path.sep) || f.toLowerCase().startsWith('config/'))) continue;
        // Ref count: if another tracked mod claims this path, leave it alone.
        if (claimedByOthers.has(this._canonicalFileKey(mod, f))) continue;
        // When targetKey='bepinex' but the tracked path is gamePath-relative
        // (starts with "BepInEx/"), join against gamePath — not base — to avoid
        // doubling up the BepInEx segment. See toggleMod for the same resolver.
        const full = (mod.targetKey === 'bepinex' && /^BepInEx[\\/]/i.test(f))
          ? path.join(this.gamePath, f)
          : path.join(base, f);
        // Safety: skip if this is a protected directory
        if (protectedNames.includes(path.basename(full).toLowerCase()) && fs.existsSync(full) && fs.statSync(full).isDirectory()) continue;
        // Safety: only delete FILES, never directories
        if (fs.existsSync(full) && fs.statSync(full).isFile()) {
          await fs.remove(full);
          removed++;
        }
      }
      this._cleanEmpty(base);
    } else {
      if (mod.looseDlls) {
        // Loose DLL mod — delete individual files, not the folder
        for (const f of mod.files) {
          tick();
          if (claimedByOthers.has(this._canonicalFileKey(mod, f))) continue;
          const full = path.join(mod.extractTo, f);
          if (fs.existsSync(full) && fs.statSync(full).isFile()) { await fs.remove(full); removed++; }
        }
      } else if (mod.files && mod.files.length > 0) {
        // Delete only the files this mod installed, not the entire folder
        for (const f of mod.files) {
          tick();
          if (claimedByOthers.has(this._canonicalFileKey(mod, f))) continue;
          const full = path.join(mod.extractTo, f);
          if (fs.existsSync(full) && fs.statSync(full).isFile()) { await fs.remove(full); removed++; }
        }
        // Clean up empty directories left behind
        if (mod.extractTo && fs.existsSync(mod.extractTo)) {
          this._cleanEmpty(mod.extractTo);
          // Remove the mod's root folder only if it's now empty
          try { if (fs.readdirSync(mod.extractTo).length === 0) fs.rmdirSync(mod.extractTo); } catch {}
        }
      } else {
        // Fallback for old mods without file tracking
        if (mod.extractTo && fs.existsSync(mod.extractTo)) await fs.remove(mod.extractTo);
      }
    }
    // Remove any disabled-mods copy too, for EVERY install shape. Missing this
    // left the old version's files sitting in disabled-mods, and since updates
    // now uninstall before installing, that stale copy would come back the next
    // time the mod was disabled and re-enabled.
    await this._removeDisabledCopy(mod);
    this.mods.delete(modId); this._saveDb();
    return { success: true, removed, tracked: Array.isArray(mod.files) ? mod.files.length : 0 };
  }

  async toggleMod(modId, onProgress) {
    const mod = this.mods.get(modId);
    if (!mod) throw new Error(`Mod not found: ${modId}`);
    if (mod.core) throw new Error(`"${mod.name}" is a core mod and cannot be disabled.`);

    // Progress reporting: large mods (e.g. Pokemon Expansions ~906 files) take a
    // noticeable moment to move every file. Emit (done, total) so the UI can show
    // a progress bar instead of looking frozen. `tick()` is called once per file
    // considered (moved OR skipped) so the bar always reaches 100%. Throttled to
    // ~100 updates max to avoid flooding IPC on big mods.
    const _total = Array.isArray(mod.files) ? mod.files.length : 0;
    let _done = 0;
    const _step = Math.max(1, Math.ceil(_total / 100));
    const tick = () => {
      _done++;
      if (onProgress && (_done % _step === 0 || _done >= _total)) {
        try { onProgress(_done, _total); } catch {}
      }
    };
    if (onProgress && _total > 0) { try { onProgress(0, _total); } catch {} }

    // Fix imported mods that were saved with wrong targetKey/extractTo
    if (mod.imported) {
      mod.targetKey = 'game_root';
      mod.extractTo = this.gamePath;
    }

    // Ensure disabled-mods folder exists
    fs.ensureDirSync(this._getDisabledPath());

    const isScatter = (mod.targetKey === 'game_root' || mod.targetKey === 'bepinex');

    if (isScatter) {
      const baseDir = mod.targetKey === 'game_root' ? this.gamePath : path.join(this.gamePath, 'BepInEx');
      const disabledDir = path.join(this._getDisabledPath(), this._sanitize(mod.name));
      const protectedNames = ['bepinex', 'plugins', 'patchers', 'config', 'core'];

      // Helper: resolve a tracked file path. When targetKey='bepinex', `mod.files`
      // entries are conventionally relative to BepInEx/ (e.g. "plugins/Foo.dll").
      // But authoritative DB-derived paths are gamePath-relative (e.g.
      // "BepInEx/plugins/Foo.dll"). Detect which form each entry uses and join
      // against the correct base so both styles resolve correctly.
      const resolveSrc = (relFile) => {
        if (mod.targetKey === 'bepinex' && /^BepInEx[\\/]/i.test(relFile)) {
          return path.join(this.gamePath, relFile);
        }
        return path.join(baseDir, relFile);
      };
      const resolveDest = (root, relFile) => {
        // For the disabled-mods mirror, preserve the same relative form as on-disk.
        return path.join(root, relFile);
      };

      if (mod.enabled) {
        if (!mod.files || mod.files.length === 0) throw new Error(`No tracked files for "${mod.name}". Try reinstalling the mod.`);
        // Reference counting: any file claimed by another tracked mod stays in
        // place when this mod is disabled. Moving a shared file out would break
        // any other mod (enabled or disabled) that also lists it. When this
        // mod is later re-enabled, the move-back's existence check skips files
        // that weren't moved out (they're already at the destination).
        const claimedByOthers = this._filesClaimedByOtherMods(modId);
        fs.ensureDirSync(disabledDir);
        for (const relFile of mod.files) {
          tick();
          if (!relFile || relFile === '.' || relFile === '/') continue;
          if (claimedByOthers.has(this._canonicalFileKey(mod, relFile))) continue;
          const src = resolveSrc(relFile);
          // Safety: never move directories, only files
          if (!fs.existsSync(src) || !fs.statSync(src).isFile()) continue;
          // Safety: never move protected paths
          if (protectedNames.includes(path.basename(src).toLowerCase())) continue;
          const dest = resolveDest(disabledDir, relFile);
          fs.ensureDirSync(path.dirname(dest));
          await fs.move(src, dest, { overwrite: true });
        }
        this._cleanEmpty(baseDir);
        mod.enabled = false;
      } else {
        // Re-enable: move files back from disabled-mods. If disabledDir doesn't
        // exist, that can legitimately mean every tracked file was shared with
        // another mod (ref counting kept them in place during disable, so none
        // were moved out). In that case the files are already at their
        // destination — verify and succeed rather than throwing.
        if (!fs.existsSync(disabledDir)) {
          const allPresent = mod.files.every(rf => {
            if (!rf || rf === '.' || rf === '/') return true;
            const dest = resolveSrc(rf);
            return fs.existsSync(dest) && fs.statSync(dest).isFile();
          });
          if (allPresent) { mod.enabled = true; this._saveDb(); return mod; }
          throw new Error(`Disabled files for "${mod.name}" not found. Try reinstalling the mod.`);
        }
        for (const relFile of mod.files) {
          tick();
          if (!relFile || relFile === '.' || relFile === '/') continue;
          const src = resolveDest(disabledDir, relFile);
          if (!fs.existsSync(src) || !fs.statSync(src).isFile()) continue;
          const dest = resolveSrc(relFile);
          fs.ensureDirSync(path.dirname(dest));
          await fs.move(src, dest, { overwrite: true });
        }
        if (fs.existsSync(disabledDir)) await fs.remove(disabledDir);
        mod.enabled = true;
      }
    } else if (mod.looseDlls) {
      const disabledDir = path.join(this._getDisabledPath(), this._sanitize(mod.name));
      if (mod.enabled) {
        const claimedByOthers = this._filesClaimedByOtherMods(modId);
        fs.ensureDirSync(disabledDir);
        for (const f of mod.files) {
          tick();
          if (claimedByOthers.has(this._canonicalFileKey(mod, f))) continue;
          const src = path.join(mod.extractTo, f);
          if (!fs.existsSync(src) || !fs.statSync(src).isFile()) continue;
          const dest = path.join(disabledDir, f);
          await fs.move(src, dest, { overwrite: true });
        }
        mod.enabled = false;
      } else {
        if (!fs.existsSync(disabledDir)) {
          const allPresent = mod.files.every(f => {
            const dest = path.join(mod.extractTo, f);
            return fs.existsSync(dest) && fs.statSync(dest).isFile();
          });
          if (allPresent) { mod.enabled = true; this._saveDb(); return mod; }
          throw new Error(`Disabled files for "${mod.name}" not found. Try reinstalling the mod.`);
        }
        for (const f of mod.files) {
          tick();
          const src = path.join(disabledDir, f);
          if (!fs.existsSync(src) || !fs.statSync(src).isFile()) continue;
          const dest = path.join(mod.extractTo, f);
          await fs.move(src, dest, { overwrite: true });
        }
        if (fs.existsSync(disabledDir)) await fs.remove(disabledDir);
        mod.enabled = true;
      }
    } else {
      // Plugin/patcher/config mods: move only tracked files, not the whole folder
      const disabledDir = path.join(this._getDisabledPath(), this._sanitize(mod.name));
      if (mod.enabled) {
        if (!mod.files || mod.files.length === 0) throw new Error(`No tracked files for "${mod.name}". Try reinstalling the mod.`);
        const claimedByOthers = this._filesClaimedByOtherMods(modId);
        fs.ensureDirSync(disabledDir);
        for (const f of mod.files) {
          tick();
          if (claimedByOthers.has(this._canonicalFileKey(mod, f))) continue;
          const src = path.join(mod.extractTo, f);
          if (!fs.existsSync(src) || !fs.statSync(src).isFile()) continue;
          const dest = path.join(disabledDir, f);
          fs.ensureDirSync(path.dirname(dest));
          await fs.move(src, dest, { overwrite: true });
        }
        this._cleanEmpty(mod.extractTo);
        mod.enabled = false;
      } else {
        if (!fs.existsSync(disabledDir)) {
          const allPresent = mod.files.every(f => {
            const dest = path.join(mod.extractTo, f);
            return fs.existsSync(dest) && fs.statSync(dest).isFile();
          });
          if (allPresent) { mod.enabled = true; this._saveDb(); return mod; }
          throw new Error(`Disabled files for "${mod.name}" not found. Try reinstalling the mod.`);
        }
        fs.ensureDirSync(mod.extractTo);
        for (const f of mod.files) {
          tick();
          const src = path.join(disabledDir, f);
          if (!fs.existsSync(src) || !fs.statSync(src).isFile()) continue;
          const dest = path.join(mod.extractTo, f);
          fs.ensureDirSync(path.dirname(dest));
          await fs.move(src, dest, { overwrite: true });
        }
        if (fs.existsSync(disabledDir)) await fs.remove(disabledDir);
        mod.enabled = true;
      }
    }

    this._saveDb(); return mod;
  }

  markCore(modId, isCore) {
    const mod = this.mods.get(modId);
    if (!mod) throw new Error(`Mod not found: ${modId}`);
    mod.core = !!isCore;
    this._saveDb();
    return mod;
  }

  detectConflicts() {
    const fm = new Map(); const conflicts = [];
    // Only check plugin/patcher/config mods for conflicts — game_root and bepinex mods are expected to share files
    for (const [id, mod] of this.mods) {
      if (!mod.enabled || !mod.files) continue;
      if (mod.targetKey === 'game_root' || mod.targetKey === 'bepinex') continue;
      for (const f of mod.files) { const k = f.toLowerCase(); if (!fm.has(k)) fm.set(k, []); fm.get(k).push({ modId: id, modName: mod.name }); }
    }
    for (const [fp, ms] of fm) { if (ms.length > 1) conflicts.push({ file: fp, mods: ms }); }
    return conflicts;
  }

  getProfiles() { return store.get('profiles', []); }
  getActiveProfileId() { return store.get('activeProfileId', null); }
  createProfile(name) {
    const profiles = this.getProfiles();
    const p = { id: `profile_${Date.now()}`, name, createdAt: new Date().toISOString(), mods: Array.from(this.mods.values()).map(m => ({ id: m.id, enabled: m.enabled })) };
    profiles.push(p); store.set('profiles', profiles); return p;
  }
  async activateProfile(profileId, onProgress) {
    const profile = this.getProfiles().find(p => p.id === profileId);
    if (!profile) throw new Error('Profile not found');
    const states = new Map(profile.mods.map(m => [m.id, m.enabled]));
    // Determine which mods actually need to flip, so progress reflects real work.
    const toFlip = [];
    for (const [id, mod] of this.mods) {
      const s = states.get(id);
      if (s !== undefined && mod.enabled !== s) toFlip.push(id);
    }
    let i = 0;
    for (const id of toFlip) {
      i++;
      const mod = this.mods.get(id);
      if (onProgress) { try { onProgress({ current: i, total: toFlip.length, modName: mod ? mod.name : '', modId: id }); } catch {} }
      // Forward each mod's file-move progress too (same shape toggleMod emits).
      await this.toggleMod(id, (done, fileTotal) => {
        if (onProgress) { try { onProgress({ current: i, total: toFlip.length, modName: mod ? mod.name : '', modId: id, fileDone: done, fileTotal }); } catch {} }
      });
    }
    store.set('activeProfileId', profileId);
    return profile;
  }
  deleteProfile(id) {
    store.set('profiles', this.getProfiles().filter(p => p.id !== id));
    if (this.getActiveProfileId() === id) store.set('activeProfileId', null);
  }

  /**
   * The destination to SHOW for a mod. This must be the SAME answer the
   * Downloaded Mods row gives, which is _detectTarget's verdict on the archive
   * — recorded as installTarget at install time.
   *
   * Do NOT infer it from the tracked file paths: install rewrites those to
   * gamePath-relative form, so a plugins install and a game_root install both
   * end up looking like "BepInEx/plugins/...", and guessing from them relabels
   * game_root mods (Grading Overhaul, Phone Overhaul, content packs) as
   * plugins. Entries from before installTarget existed keep their stored
   * targetKey and correct themselves on the next install.
   */
  _displayTargetKey(mod) {
    if (mod.installTarget && TARGETS[mod.installTarget]) return mod.installTarget;
    return TARGETS[mod.targetKey] ? mod.targetKey : 'game_root';
  }

  /** Returns COPIES: targetLabel is corrected for display without touching the DB. */
  getInstalledMods() {
    return Array.from(this.mods.values()).map(m => ({
      ...m,
      targetLabel: TARGETS[this._displayTargetKey(m)].label,
    }));
  }

  async freshInstall() {
    if (!this.gamePath) throw new Error('Game path not set');

    // Resolve the BepInEx folder case-insensitively. Windows doesn't care, but
    // Linux (Steam Deck) does: a folder created as "bepinex" or "BepinEx" by a
    // hand-installed setup would make an exact-case lookup fail and abort the
    // whole reset with "BepInEx folder not found".
    let bepName = 'BepInEx';
    try {
      const match = fs.readdirSync(this.gamePath, { withFileTypes: true })
        .find(e => e.isDirectory() && e.name.toLowerCase() === 'bepinex');
      if (match) bepName = match.name;
    } catch {}
    const bepDir = path.join(this.gamePath, bepName);
    if (!fs.existsSync(bepDir)) throw new Error('BepInEx folder not found');

    // Delete everything in BepInEx/ EXCEPT the config/ folder
    for (const entry of fs.readdirSync(bepDir, { withFileTypes: true })) {
      if (entry.name.toLowerCase() === 'config') continue; // preserve configs
      const fullPath = path.join(bepDir, entry.name);
      await fs.remove(fullPath);
    }

    // Also delete BepInEx loader files from the game root. Doorstop ships as a
    // proxy DLL that Windows auto-loads: usually winhttp.dll, but pre-configured
    // packs commonly use version.dll instead — leaving that behind meant BepInEx
    // kept loading after a "fresh install". The run_bepinex.sh / doorstop_libs
    // entries cover native-Linux BepInEx layouts. Matched case-insensitively so
    // this behaves identically on Linux.
    const LOADER_NAMES = new Set([
      'winhttp.dll', 'version.dll', 'doorstop_config.ini', '.doorstop_version',
      'run_bepinex.sh', 'libdoorstop.so', 'libdoorstop_x64.so', 'libdoorstop_x86.so',
    ]);
    const LOADER_DIRS = new Set(['doorstop_libs']);
    try {
      for (const entry of fs.readdirSync(this.gamePath, { withFileTypes: true })) {
        const lower = entry.name.toLowerCase();
        if (entry.isDirectory() ? LOADER_DIRS.has(lower) : LOADER_NAMES.has(lower)) {
          await fs.remove(path.join(this.gamePath, entry.name));
        }
      }
    } catch (err) {
      console.warn('[freshInstall] loader cleanup:', err.code || err.message);
    }

    // Clear disabled-mods folder
    const disabledPath = this._getDisabledPath();
    if (fs.existsSync(disabledPath)) await fs.remove(disabledPath);
    fs.ensureDirSync(disabledPath);

    // Clear mod database
    this.mods.clear();
    this._saveDb();
    store.set('activeProfileId', null);

    return { success: true };
  }



  // ═══ CONFIG EDITOR ═══

  /** List all .cfg files in BepInEx/config/ */
  getConfigFiles() {
    if (!this.gamePath) return [];
    const configDir = path.join(this.gamePath, 'BepInEx', 'config');
    if (!fs.existsSync(configDir)) return [];
    return fs.readdirSync(configDir)
      .filter(f => f.endsWith('.cfg'))
      .map(f => ({ name: f, path: path.join(configDir, f) }));
  }

  /** Read and parse a .cfg file into sections with entries */
  readConfigFile(filename) {
    if (!this.gamePath) throw new Error('Game path not set');
    const configDir = path.join(this.gamePath, 'BepInEx', 'config');
    const filePath = path.join(configDir, filename);
    if (!fs.existsSync(filePath)) throw new Error('Config file not found');
    const raw = fs.readFileSync(filePath, 'utf8');
    const sections = [];
    let currentSection = null;
    let currentDesc = [];

    for (const line of raw.split('\n')) {
      const trimmed = line.trim();
      // Section header [SectionName]
      const sectionMatch = trimmed.match(/^\[(.+)\]$/);
      if (sectionMatch) {
        currentSection = { name: sectionMatch[1], entries: [], description: currentDesc.join('\n').trim() };
        sections.push(currentSection);
        currentDesc = [];
        continue;
      }
      // Comment line
      if (trimmed.startsWith('#') || trimmed.startsWith('//')) {
        // Check for ## Setting type and ## Default/Acceptable
        if (trimmed.startsWith('## ')) {
          currentDesc.push(trimmed.substring(3));
        } else if (trimmed.startsWith('# ')) {
          currentDesc.push(trimmed.substring(2));
        }
        continue;
      }
      // Key = Value
      const kvMatch = trimmed.match(/^([^=]+?)\s*=\s*(.*)$/);
      if (kvMatch && currentSection) {
        const key = kvMatch[1].trim();
        const value = kvMatch[2].trim();
        // Parse description for type hints
        const descText = currentDesc.join('\n');
        let type = 'string';
        let acceptable = null;
        if (/\bBoolean\b/i.test(descText) || value === 'true' || value === 'false') type = 'boolean';
        else if (/\bInt32\b|\bSingle\b|\bFloat\b|\bDouble\b/i.test(descText) || /^-?\d+(\.\d+)?$/.test(value)) type = 'number';
        const accMatch = descText.match(/Acceptable values?:\s*(.+)/i);
        if (accMatch) {
          const vals = accMatch[1].split(',').map(v => v.trim()).filter(Boolean);
          if (vals.length > 1) { type = 'select'; acceptable = vals; }
        }
        const rangeMatch = descText.match(/Acceptable value range:\s*From\s*(-?[\d.]+)\s*to\s*(-?[\d.]+)/i);
        let range = null;
        if (rangeMatch) { range = { min: parseFloat(rangeMatch[1]), max: parseFloat(rangeMatch[2]) }; type = 'number'; }
        currentSection.entries.push({ key, value, type, description: descText, acceptable, range });
        currentDesc = [];
      } else if (trimmed === '') {
        currentDesc = [];
      }
    }
    return { filename, sections };
  }

  /** Save a config value */
  saveConfigValue(filename, sectionName, key, newValue) {
    if (!this.gamePath) throw new Error('Game path not set');
    const configDir = path.join(this.gamePath, 'BepInEx', 'config');
    const filePath = path.join(configDir, filename);
    if (!fs.existsSync(filePath)) throw new Error('Config file not found');
    const raw = fs.readFileSync(filePath, 'utf8');
    const lines = raw.split('\n');
    let inSection = false;
    for (let i = 0; i < lines.length; i++) {
      const trimmed = lines[i].trim();
      if (trimmed.match(/^\[(.+)\]$/)) {
        inSection = trimmed === `[${sectionName}]`;
        continue;
      }
      if (inSection) {
        const kvMatch = trimmed.match(/^([^=]+?)\s*=\s*(.*)$/);
        if (kvMatch && kvMatch[1].trim() === key) {
          lines[i] = `${key} = ${newValue}`;
          break;
        }
      }
    }
    fs.writeFileSync(filePath, lines.join('\n'), 'utf8');
    return { success: true };
  }

  // ═══ PROFILE EXPORT/IMPORT ═══

  exportProfile(profileId) {
    const profile = this.getProfiles().find(p => p.id === profileId);
    if (!profile) throw new Error('Profile not found');
    const installedMods = this.getInstalledMods();
    return {
      version: 1,
      app: 'Real TCG Overhaul Mod Manager',
      exportedAt: new Date().toISOString(),
      profile: {
        name: profile.name,
        mods: profile.mods,
      },
      installedMods: installedMods.map(m => ({
        name: m.name, version: m.version, targetKey: m.targetKey, enabled: m.enabled, core: m.core,
      })),
    };
  }

  async importProfile(data) {
    if (!data || !data.profile) throw new Error('Invalid profile data');
    const name = data.profile.name + ' (imported)';
    const id = `profile_${Date.now()}`;
    const profiles = this.getProfiles();
    profiles.push({ id, name, mods: data.profile.mods, createdAt: new Date().toISOString() });
    store.set('profiles', profiles);
    return { success: true, name, id };
  }

  // ═══ DEPENDENCY CHECK ═══

  /** Check for missing dependencies among installed mods */
  checkDependencies() {
    const warnings = [];
    const installedNames = new Set();
    const installedFiles = new Set();

    for (const mod of this.mods.values()) {
      installedNames.add(mod.name.toLowerCase());
      if (mod.files) {
        for (const f of mod.files) {
          installedFiles.add(path.basename(f).toLowerCase());
        }
      }
    }

    // Known dependency map for TCG Card Shop Simulator mods
    const DEPENDENCIES = {
      'phone overhaul': ['enhancedprefabloader'],
      'holographic overhaul': ['enhancedprefabloader'],
      'grading overhaul': ['enhancedprefabloader'],
      'collection tracker': ['enhancedprefabloader'],
      'base expansions': ['enhancedprefabloader', 'holographic overhaul'],
      'neo expansions': ['enhancedprefabloader', 'holographic overhaul'],
      'gym expansions': ['enhancedprefabloader', 'holographic overhaul'],
      'pokemon expansions': ['enhancedprefabloader', 'holographic overhaul'],
      'pokemon accessories': ['enhancedprefabloader', 'phone overhaul'],
      'pokemon figurines': ['enhancedprefabloader'],
      'pokemon plushies': ['enhancedprefabloader'],
      'pokemon gameboy games': ['enhancedprefabloader'],
      'pokemon n64 games': ['enhancedprefabloader'],
      'pokemon statues': ['enhancedprefabloader'],
      'pokemon shop textures': ['texturereplacer'],
      'rtcgo shop textures': ['texturereplacer'],
      'rtcgo custom tv': ['enhancedprefabloader'],
      'rtcgo custom furniture': ['enhancedprefabloader'],
      'base set theme decks': ['enhancedprefabloader', 'phone overhaul'],
      'jungle theme decks': ['enhancedprefabloader', 'phone overhaul'],
      'fossil theme decks': ['enhancedprefabloader', 'phone overhaul'],
      'base set 2 theme decks': ['enhancedprefabloader', 'phone overhaul'],
      'team rocket theme decks': ['enhancedprefabloader', 'phone overhaul'],
      'gym heroes theme decks': ['enhancedprefabloader', 'phone overhaul'],
      'gym challenge theme decks': ['enhancedprefabloader', 'phone overhaul'],
      'pokemon adventures manga': ['enhancedprefabloader', 'phone overhaul'],
      'endgame overhaul': ['enhancedprefabloader'],
      'artexpander': ['texturereplacer'],
      'schwarz shelves set': ['enhancedprefabloader'],
      'showcase': ['enhancedprefabloader'],
    };

    // Display names for dependency mods
    const DEP_NAMES = {
      'enhancedprefabloader': 'Enhanced Prefab Loader',
      'holographic overhaul': 'Holographic Overhaul',
      'phone overhaul': 'Phone Overhaul',
      'texturereplacer': 'TextureReplacer',
    };

    for (const mod of this.mods.values()) {
      const modLower = mod.name.toLowerCase();
      const deps = DEPENDENCIES[modLower];
      if (!deps) continue;

      for (const dep of deps) {
        // Check if dependency is installed (by name match or DLL match)
        const depInstalled = [...this.mods.values()].some(m => {
          const mLower = m.name.toLowerCase().replace(/\s+/g, '');
          const depNorm = dep.replace(/\s+/g, '');
          return mLower.includes(depNorm) || depNorm.includes(mLower);
        });

        if (!depInstalled) {
          warnings.push({
            modId: mod.id,
            modName: mod.name,
            missingDep: DEP_NAMES[dep] || dep,
          });
        }
      }
    }

    // Auto-detect: any mod with _prefabloader in its files needs Enhanced Prefab Loader
    const hasEPL = [...this.mods.values()].some(m => {
      const n = m.name.toLowerCase().replace(/\s+/g, '');
      return n.includes('enhancedprefabloader');
    });
    if (!hasEPL) {
      for (const mod of this.mods.values()) {
        // Skip if this mod IS the prefab loader
        if (mod.name.toLowerCase().replace(/\s+/g, '').includes('enhancedprefabloader')) continue;
        // Skip if already warned via the hardcoded map
        if (warnings.some(w => w.modId === mod.id && w.missingDep === 'Enhanced Prefab Loader')) continue;
        // Check files for _prefabloader
        if (mod.files && mod.files.some(f => f.toLowerCase().includes('_prefabloader'))) {
          warnings.push({
            modId: mod.id,
            modName: mod.name,
            missingDep: 'Enhanced Prefab Loader',
          });
        }
      }
    }

    // Auto-detect: any mod with "Expansions" in name needs Holographic Overhaul
    const hasHoloOverhaul = [...this.mods.values()].some(m => {
      const n = m.name.toLowerCase().replace(/\s+/g, '');
      return n.includes('holographicoverhaul');
    });
    if (!hasHoloOverhaul) {
      for (const mod of this.mods.values()) {
        if (mod.name.toLowerCase().includes('holographic overhaul')) continue;
        if (warnings.some(w => w.modId === mod.id && w.missingDep === 'Holographic Overhaul')) continue;
        if (mod.name.toLowerCase().includes('expansion')) {
          warnings.push({ modId: mod.id, modName: mod.name, missingDep: 'Holographic Overhaul' });
        }
      }
    }

    // Auto-detect: any mod with Phone - Overhaul/ in its files needs Phone Overhaul
    const hasPhoneOverhaul = [...this.mods.values()].some(m => {
      const n = m.name.toLowerCase().replace(/\s+/g, '');
      return n.includes('phoneoverhaul');
    });
    if (!hasPhoneOverhaul) {
      for (const mod of this.mods.values()) {
        if (mod.name.toLowerCase().replace(/\s+/g, '').includes('phoneoverhaul')) continue;
        if (warnings.some(w => w.modId === mod.id && w.missingDep === 'Phone Overhaul')) continue;
        if (mod.files && mod.files.some(f => /phone\s*-\s*overhaul[/\\]/i.test(f))) {
          warnings.push({ modId: mod.id, modName: mod.name, missingDep: 'Phone Overhaul' });
        }
      }
    }

    // Auto-detect: any mod with Holographic Overhaul/ in its files needs Holographic Overhaul
    if (!hasHoloOverhaul) {
      for (const mod of this.mods.values()) {
        if (mod.name.toLowerCase().includes('holographic overhaul')) continue;
        if (warnings.some(w => w.modId === mod.id && w.missingDep === 'Holographic Overhaul')) continue;
        if (mod.files && mod.files.some(f => /holographic.overhaul[/\\]/i.test(f))) {
          warnings.push({ modId: mod.id, modName: mod.name, missingDep: 'Holographic Overhaul' });
        }
      }
    }

    // Auto-detect: any mod with TextureReplacer/ in its files needs TextureReplacer
    const hasTR = [...this.mods.values()].some(m => {
      const n = m.name.toLowerCase().replace(/\s+/g, '');
      return n.includes('texturereplacer');
    });
    if (!hasTR) {
      for (const mod of this.mods.values()) {
        if (mod.name.toLowerCase().replace(/\s+/g, '').includes('texturereplacer')) continue;
        if (warnings.some(w => w.modId === mod.id && w.missingDep === 'TextureReplacer')) continue;
        if (mod.files && mod.files.some(f => /texturereplacer[/\\]/i.test(f))) {
          warnings.push({ modId: mod.id, modName: mod.name, missingDep: 'TextureReplacer' });
        }
      }
    }

    return warnings;
  }


  /** Quick scan of archive paths for target detection — works reliably even for very large archives */
  async _quickScanArchivePaths(archivePath) {
    const paths = [];
    try {
      if (/\.zip$/i.test(archivePath)) {
        const zipEntries = await this._listZipEntries(archivePath);
        for (const e of zipEntries) paths.push(e.path);
      } else if (/\.rar$/i.test(archivePath)) {
        const rarEntries = await this._listRar(archivePath);
        for (const e of rarEntries) { if (e.file) paths.push(e.file.replace(/\\/g, '/')); }
      } else {
        for (const e of await this._sevenList(archivePath)) {
          if (e.file) paths.push(e.file.replace(/\\/g, '/'));
        }
      }
    } catch (err) {
      const fnLower = path.basename(archivePath).toLowerCase();
      if (fnLower.includes('bepinex')) paths.push('BepInEx/');
    }
    return paths;
  }

  _cleanEmpty(dir) {
    const protectedNames = ['bepinex', 'plugins', 'patchers', 'config', 'core'];
    try {
      for (const e of fs.readdirSync(dir, { withFileTypes: true })) {
        if (e.isDirectory()) {
          const f = path.join(dir, e.name);
          this._cleanEmpty(f);
          // Never remove protected BepInEx directories
          if (protectedNames.includes(e.name.toLowerCase())) continue;
          try { if (fs.readdirSync(f).length === 0) fs.rmdirSync(f); } catch {}
        }
      }
    } catch {}
  }
  _sanitize(n) { return n.replace(/[<>:"/\\|?*]/g, '_').replace(/\s+/g, '_').slice(0, 80); }
  _loadDb() { this.mods = new Map(Object.entries(store.get('installedMods', {}))); }
  _saveDb() { store.set('installedMods', Object.fromEntries(this.mods)); }

  // ═══ SCAN FOR UNTRACKED MODS ═══

  /** Recursively list all files under a directory, returning paths relative to base */
  _walkDir(dir, base) {
    const results = [];
    try {
      for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
        const full = path.join(dir, entry.name);
        if (entry.isDirectory()) {
          results.push(...this._walkDir(full, base));
        } else {
          results.push(path.relative(base, full).replace(/\\/g, '/'));
        }
      }
    } catch {}
    return results;
  }

  /** Known mod reference database — signature files used to identify installed mods */
  _getKnownModsDB() {
    return [
      { name: '(EN) hBP01 Blooming Radiance', filename: '(EN) hBP01 Blooming Radiance-928-1-3-1776474940.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp01_card_data_en'], installedFiles: ['BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp01_card_data_en', 'BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp01_card_data_en.json'] },
      { name: '(EN) hBP02 Quintet Spectrum', filename: '(EN) hBP02 Quintet Spectrum-928-1-3-1776474996.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp02_card_data_en'], installedFiles: ['BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp02_card_data_en', 'BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp02_card_data_en.json'] },
      { name: '(EN) hBP03 Elite Spark', filename: '(EN) hBP03 Elite Spark-928-1-3-1776475058.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp03_card_data_en'], installedFiles: ['BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp03_card_data_en', 'BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp03_card_data_en.json'] },
      { name: '(EN) hBP04 Curious Universe', filename: '(EN) hBP04 Curious Universe-928-1-3-1776475181.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp04_card_data_en'], installedFiles: ['BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp04_card_data_en', 'BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp04_card_data_en.json'] },
      { name: '(EN) hBP05 Enchant Regalia', filename: '(EN) hBP05 Enchant Regalia-928-1-3-1776475338.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp05_card_data_en'], installedFiles: ['BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp05_card_data_en', 'BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp05_card_data_en.json'] },
      { name: '(EN) Promos', filename: '(EN) Promos-928-1-3-1776474092.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG EN cards addon/promos_prefabloader/trade_card_data_en'], installedFiles: ['BepInEx/plugins/hololive OCG EN cards addon/promos_prefabloader/trade_card_data_en', 'BepInEx/plugins/hololive OCG EN cards addon/promos_prefabloader/trade_card_data_en.json'] },
      { name: '(EN) Start Decks', filename: '(EN) Start Decks-928-1-3-1776474050.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG EN cards addon/start_decks_prefabloader/start_cheer_card_data_en'], installedFiles: ['BepInEx/plugins/hololive OCG EN cards addon/start_decks_prefabloader/start_cheer_card_data_en', 'BepInEx/plugins/hololive OCG EN cards addon/start_decks_prefabloader/start_cheer_card_data_en.json'] },
      { name: '(JP) hBP01 Blooming Radiance', filename: '(JP) hBP01 Blooming Radiance-928-1-3-1776474179.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp01_card_data'], installedFiles: ['BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp01_card_data', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp01_card_data.json'] },
      { name: '(JP) hBP02 Quintet Spectrum', filename: '(JP) hBP02 Quintet Spectrum-928-1-3-1776474311.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp02_card_data'], installedFiles: ['BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp02_card_data', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp02_card_data.json'] },
      { name: '(JP) hBP03 Elite Spark', filename: '(JP) hBP03 Elite Spark-928-1-3-1776474376.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp03_card_data'], installedFiles: ['BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp03_card_data', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp03_card_data.json'] },
      { name: '(JP) hBP04 Curious Universe', filename: '(JP) hBP04 Curious Universe-928-1-3-1776474450.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp04_card_data'], installedFiles: ['BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp04_card_data', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp04_card_data.json'] },
      { name: '(JP) hBP05 Enchant Regalia', filename: '(JP) hBP05 Enchant Regalia-928-1-3-1776474786.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp05_card_data'], installedFiles: ['BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp05_card_data', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp05_card_data.json'] },
      { name: '(JP) hBP06 Ayakashi Vermilion', filename: '(JP) hBP06 Ayakashi Vermilion-928-1-3-1776474863.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp06_card_data'], installedFiles: ['BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp06_card_data', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp06_card_data.json'] },
      { name: '(JP) Promos', filename: '(JP) Promos-928-1-3-1776471989.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG JP cards addon/promos_prefabloader/basic_pack_1_data'], installedFiles: ['BepInEx/plugins/hololive OCG JP cards addon/promos_prefabloader/basic_pack_1_data', 'BepInEx/plugins/hololive OCG JP cards addon/promos_prefabloader/basic_pack_1_data.json', 'BepInEx/plugins/hololive OCG JP cards addon/promos_prefabloader/entry_pack_1_data', 'BepInEx/plugins/hololive OCG JP cards addon/promos_prefabloader/entry_pack_1_data.json', 'BepInEx/plugins/hololive OCG JP cards addon/promos_prefabloader/super_pack_1_data', 'BepInEx/plugins/hololive OCG JP cards addon/promos_prefabloader/super_pack_1_data.json', 'BepInEx/plugins/hololive OCG JP cards addon/promos_prefabloader/trade_card_data', 'BepInEx/plugins/hololive OCG JP cards addon/promos_prefabloader/trade_card_data.json'] },
      { name: '(JP) Start Decks', filename: '(JP) Start Decks-928-1-3-1776472068.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG JP cards addon/start_decks_prefabloader/hsd01_card_data'], installedFiles: ['BepInEx/plugins/hololive OCG JP cards addon/start_decks_prefabloader/hsd01_card_data', 'BepInEx/plugins/hololive OCG JP cards addon/start_decks_prefabloader/hsd01_card_data.json', 'BepInEx/plugins/hololive OCG JP cards addon/start_decks_prefabloader/start_cheer_card_data', 'BepInEx/plugins/hololive OCG JP cards addon/start_decks_prefabloader/start_cheer_card_data.json'] },
      { name: 'hololive OCG EN cards addon', filename: 'hololive OCG EN cards addon-928-1-3-1776475844.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp01_card_data_en'], installedFiles: ['BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp01_card_data_en', 'BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp01_card_data_en.json', 'BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp02_card_data_en', 'BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp02_card_data_en.json', 'BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp03_card_data_en', 'BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp03_card_data_en.json', 'BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp04_card_data_en', 'BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp04_card_data_en.json', 'BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp05_card_data_en', 'BepInEx/plugins/hololive OCG EN cards addon/booster_packs_prefabloader/hbp05_card_data_en.json'] },
      { name: 'hololive OCG JP cards addon', filename: 'hololive OCG JP cards addon-928-1-3-1776473973.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp01_card_data'], installedFiles: ['BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp01_card_data', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp01_card_data.json', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp02_card_data', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp02_card_data.json', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp03_card_data', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp03_card_data.json', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp04_card_data', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp04_card_data.json', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp05_card_data', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp05_card_data.json', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp06_card_data', 'BepInEx/plugins/hololive OCG JP cards addon/booster_packs_prefabloader/hbp06_card_data.json'] },
      { name: '000 SHOP FILE', filename: '000 - SHOP FILE-892-1-0-1773335414.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Phone - Overhaul/App Images/HHPokemon/HHPokeIcon.png'], installedFiles: ['BepInEx/plugins/Phone - Overhaul/App Images/HHPokemon/HHPokeIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/HHPokemon/TemplateInnerBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/HHPokemon/TemplateOuterBG.png'] },
      { name: '001 Ascended Heroes English Set', filename: '001 - Ascended Heroes English Set-892-1-3-1-1773561016.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/001AscendedHeroes_prefabLoader/hh_ascended_heroes'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/001AscendedHeroes_prefabLoader/hh_ascended_heroes', 'BepInEx/plugins/HH EPL Pokemons/001AscendedHeroes_prefabLoader/hh_ascended_heroes.json'] },
      { name: '00 Pokemon Shop Phone App', filename: '00 - Pokemon Shop Phone App-854-1-5-1776814742.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/IBG.png'], installedFiles: ['BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/MyIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/OBG.png'] },
      { name: '01 Base Expansions', filename: '01 - Base Expansions-854-1-4-1776567094.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/01 - Base Set_prefabloader/basesetexpansion'], contentPaths: ['BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket'], installedFiles: ['BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Alakazam0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Alakazam0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Blastoise1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Blastoise1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Chansey2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Chansey2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Charizard3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Charizard3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Clefairy4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Clefairy4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Gyarados5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Gyarados5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Hitmonchan6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Hitmonchan6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Machamp7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Machamp7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Magneton8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Magneton8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Mewtwo9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Mewtwo9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Nidoking10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Nidoking10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Ninetales11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Ninetales11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Poliwrath12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Poliwrath12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Raichu13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Raichu13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Venusaur14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Venusaur14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Zapdos15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionBaseSet/Zapdos15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Aerodactyl0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Aerodactyl0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Articuno1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Articuno1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Ditto2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Ditto2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Dragonite3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Dragonite3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Gengar4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Gengar4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Haunter5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Haunter5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Hitmonlee6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Hitmonlee6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Hypno7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Hypno7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Kabutops8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Kabutops8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Lapras9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Lapras9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Magneton10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Magneton10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Moltres11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Moltres11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Muk12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Muk12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Raichu13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Raichu13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Zapdos14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionFossil/Zapdos14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Clefable0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Clefable0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Electrode1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Electrode1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Flareon2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Flareon2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Jolteon3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Jolteon3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Kangaskhan4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Kangaskhan4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Mr. Mime5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Mr. Mime5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Nidoqueen6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Nidoqueen6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Pidgeot7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Pidgeot7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Pinsir8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Pinsir8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Scyther9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Scyther9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Snorlax10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Snorlax10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Vaporeon11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Vaporeon11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Venomoth12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Venomoth12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Victreebel13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Victreebel13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Vileplume14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Vileplume14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Wigglytuff15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionJungle/Wigglytuff15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Alakazam0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Alakazam0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Arbok1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Arbok1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Blastoise2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Blastoise2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Charizard3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Charizard3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Dragonite4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Dragonite4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Dugtrio5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Dugtrio5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Golbat6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Golbat6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Gyarados7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Gyarados7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Hypno8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Hypno8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Machamp9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Machamp9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Magneton10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Magneton10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Raichu82/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Raichu82/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Slowbro11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Slowbro11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Vileplume12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Vileplume12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Weezing13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Dark Weezing13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Here Comes Team Rocket!14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Here Comes Team Rocket!14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Rainbow Energy16/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Rainbow Energy16/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Rocket\'s Sneak Attack15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionTeamRocket/Rocket\'s Sneak Attack15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Alakazam0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Alakazam0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Blastoise1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Blastoise1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Chansey2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Chansey2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Charizard3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Charizard3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Clefairy4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Clefairy4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Gyarados5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Gyarados5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Hitmonchan6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Hitmonchan6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Machamp7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Machamp7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Magneton8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Magneton8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Mewtwo9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Mewtwo9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Nidoking10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Nidoking10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Ninetales11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Ninetales11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Poliwrath12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Poliwrath12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Raichu13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Raichu13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Venusaur14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Venusaur14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Zapdos15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet/Zapdos15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Alakazam0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Alakazam0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Ancient Mew130/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Ancient Mew130/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Blastoise1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Blastoise1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Chansey2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Chansey2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Charizard3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Charizard3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Clefable4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Clefable4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Clefairy5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Clefairy5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Gyarados6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Gyarados6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Hitmonchan7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Hitmonchan7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Magneton8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Magneton8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Mewtwo9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Mewtwo9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Nidoking10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Nidoking10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Nidoqueen11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Nidoqueen11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Ninetales12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Ninetales12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Pidgeot13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Pidgeot13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Poliwrath14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Poliwrath14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Raichu15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Raichu15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Scyther16/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Scyther16/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Venusaur17/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Venusaur17/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Wigglytuff18/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Wigglytuff18/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Zapdos19/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/BaseSet2/Zapdos19/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Aerodactyl0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Aerodactyl0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Articuno1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Articuno1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Ditto2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Ditto2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Dragonite3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Dragonite3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Gengar4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Gengar4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Haunter5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Haunter5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Hitmonlee6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Hitmonlee6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Hypno7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Hypno7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Kabutops8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Kabutops8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Lapras9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Lapras9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Magneton10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Magneton10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Moltres11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Moltres11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Muk12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Muk12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Raichu13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Raichu13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Zapdos14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Fossil/Zapdos14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Clefable0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Clefable0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Electrode1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Electrode1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Flareon2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Flareon2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Jolteon3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Jolteon3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Kangaskhan4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Kangaskhan4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Mr. Mime5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Mr. Mime5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Nidoqueen6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Nidoqueen6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Pidgeot7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Pidgeot7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Pinsir8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Pinsir8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Scyther9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Scyther9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Snorlax10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Snorlax10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Vaporeon11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Vaporeon11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Venomoth12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Venomoth12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Victreebel13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Victreebel13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Vileplume14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Vileplume14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Wigglytuff15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/Jungle/Wigglytuff15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Alakazam0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Alakazam0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Arbok1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Arbok1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Blastoise2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Blastoise2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Charizard3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Charizard3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Dragonite4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Dragonite4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Dugtrio5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Dugtrio5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Golbat6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Golbat6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Gyarados7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Gyarados7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Hypno8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Hypno8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Machamp9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Machamp9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Magneton10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Magneton10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Raichu82/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Raichu82/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Slowbro11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Slowbro11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Vileplume12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Vileplume12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Weezing13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Dark Weezing13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Here Comes Team Rocket!14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Here Comes Team Rocket!14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Rainbow Energy16/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Rainbow Energy16/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Rocket\'s Sneak Attack15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/TeamRocket/Rocket\'s Sneak Attack15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Birthday Pikachu23/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Birthday Pikachu23/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Cool Porygon14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Cool Porygon14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Dark Persian16/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Dark Persian16/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Eevee10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Eevee10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Entei33/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Entei33/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Meowth9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Meowth9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Mew8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Mew8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Pichu34/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Pichu34/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Venusaur12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/WotCPromos/Venusaur12/Settings.txt', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/MyIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/OBG.png', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/01 - Base Set_prefabloader/basesetexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/01 - Base Set_prefabloader/basesetexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/02 - 1st Edition Base Set_prefabloader/1steditionbasesetexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/02 - 1st Edition Base Set_prefabloader/1steditionbasesetexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/03 - Jungle_prefabloader/jungleexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/03 - Jungle_prefabloader/jungleexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/04 - 1st Edition Jungle_prefabloader/1steditionjungleexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/04 - 1st Edition Jungle_prefabloader/1steditionjungleexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/05 - Fossil_prefabloader/fossilexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/05 - Fossil_prefabloader/fossilexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/06 - 1st Edition Fossil_prefabloader/1steditionfossilexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/06 - 1st Edition Fossil_prefabloader/1steditionfossilexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/07 - Base Set 2_prefabloader/baseset2expansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/07 - Base Set 2_prefabloader/baseset2expansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/08 - Team Rocket_prefabloader/teamrocketexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/08 - Team Rocket_prefabloader/teamrocketexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/09 - 1st Edition Team Rocket_prefabloader/1steditionteamrocketexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/09 - 1st Edition Team Rocket_prefabloader/1steditionteamrocketexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/10 - Wotc Promos_prefabloader/wotcpromos', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/10 - Wotc Promos_prefabloader/wotcpromos.json'] },
      { name: '01 Base Theme Decks', filename: '01 - Base Theme Decks-856-1-2-1776635476.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/01 - Base Theme Decks/01 - Base Theme Decks_prefabloader/basethemedecks'], installedFiles: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/01 - Base Theme Decks/01 - Base Theme Decks_prefabloader/basethemedecks', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/01 - Base Theme Decks/01 - Base Theme Decks_prefabloader/basethemedecks.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/01 - Base Theme Decks/02 - Jungle Theme Decks_prefabloader/junglethemedecks', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/01 - Base Theme Decks/02 - Jungle Theme Decks_prefabloader/junglethemedecks.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/01 - Base Theme Decks/03 - Fossil Theme Decks_prefabloader/fossilthemedecks', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/01 - Base Theme Decks/03 - Fossil Theme Decks_prefabloader/fossilthemedecks.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/01 - Base Theme Decks/04 - Base Set 2 Theme Decks_prefabloader/baseset2themedecks', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/01 - Base Theme Decks/04 - Base Set 2 Theme Decks_prefabloader/baseset2themedecks.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/01 - Base Theme Decks/05 - Team Rocket Theme Decks_prefabloader/teamrocketthemedecks', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/01 - Base Theme Decks/05 - Team Rocket Theme Decks_prefabloader/teamrocketthemedecks.json'] },
      { name: '01 Pokemon Adventures Manga', filename: '01 - Pokemon Adventures Manga-856-1-2-1776635629.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/100 - Manga/01 - Pokemon Adventures_prefabloader/pokemonmanga'], installedFiles: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/100 - Manga/01 - Pokemon Adventures_prefabloader/pokemonmanga', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/100 - Manga/01 - Pokemon Adventures_prefabloader/pokemonmanga.json'] },
      { name: '01 Pokemon Plushies', filename: '01 - Pokemon Plushies-870-1-1-1775689582.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Figurines/01 - Plushies/01 - Plushies_prefabloader/pokemonplushies'], installedFiles: ['BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/MyIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/OBG.png', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Figurines/01 - Plushies/01 - Plushies_prefabloader/pokemonplushies', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Figurines/01 - Plushies/01 - Plushies_prefabloader/pokemonplushies.json'] },
      { name: '02 Gym Expansions', filename: '02 - Gym Expansions-854-1-4-1776567304.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/02 - Gym/01 - Gym Heroes_prefabloader/gymheroesexpansion'], contentPaths: ['BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge'], installedFiles: ['BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Blaine\'s Arcanine0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Blaine\'s Arcanine0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Blaine\'s Charizard1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Blaine\'s Charizard1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Blaine16/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Blaine16/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Brock\'s Ninetales2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Brock\'s Ninetales2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Erika\'s Venusaur3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Erika\'s Venusaur3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Giovanni\'s Gyarados4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Giovanni\'s Gyarados4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Giovanni\'s Machamp5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Giovanni\'s Machamp5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Giovanni\'s Nidoking6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Giovanni\'s Nidoking6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Giovanni\'s Persian7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Giovanni\'s Persian7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Giovanni17/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Giovanni17/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Koga\'s Beedrill8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Koga\'s Beedrill8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Koga\'s Ditto9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Koga\'s Ditto9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Koga18/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Koga18/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Lt. Surge\'s Raichu10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Lt. Surge\'s Raichu10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Misty\'s Golduck11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Misty\'s Golduck11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Misty\'s Gyarados12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Misty\'s Gyarados12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Rocket\'s Mewtwo13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Rocket\'s Mewtwo13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Rocket\'s Zapdos14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Rocket\'s Zapdos14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Sabrina\'s Alakazam15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Sabrina\'s Alakazam15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Sabrina19/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymChallenge/Sabrina19/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Blaine\'s Moltres0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Blaine\'s Moltres0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Brock\'s Rhydon1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Brock\'s Rhydon1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Brock14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Brock14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Erika\'s Clefable2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Erika\'s Clefable2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Erika\'s Dragonair3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Erika\'s Dragonair3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Erika\'s Vileplume4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Erika\'s Vileplume4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Erika15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Erika15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Lt. Surge\'s Electabuzz5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Lt. Surge\'s Electabuzz5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Lt. Surge\'s Fearow6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Lt. Surge\'s Fearow6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Lt. Surge\'s Magneton7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Lt. Surge\'s Magneton7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Lt. Surge16/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Lt. Surge16/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Misty\'s Seadra8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Misty\'s Seadra8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Misty\'s Tentacruel9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Misty\'s Tentacruel9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Misty17/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Misty17/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Rocket\'s Hitmonchan10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Rocket\'s Hitmonchan10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Rocket\'s Moltres11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Rocket\'s Moltres11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Rocket\'s Scyther12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Rocket\'s Scyther12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Sabrina\'s Gengar13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Sabrina\'s Gengar13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Sabrina\'s Gengar132/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/Sabrina\'s Gengar132/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/The Rocket\'s Trap18/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionGymHeroes/The Rocket\'s Trap18/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Blaine\'s Arcanine0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Blaine\'s Arcanine0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Blaine\'s Charizard1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Blaine\'s Charizard1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Blaine16/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Blaine16/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Brock\'s Ninetales2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Brock\'s Ninetales2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Erika\'s Venusaur3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Erika\'s Venusaur3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Giovanni\'s Gyarados4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Giovanni\'s Gyarados4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Giovanni\'s Machamp5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Giovanni\'s Machamp5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Giovanni\'s Nidoking6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Giovanni\'s Nidoking6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Giovanni\'s Persian7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Giovanni\'s Persian7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Giovanni17/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Giovanni17/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Koga\'s Beedrill8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Koga\'s Beedrill8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Koga\'s Ditto9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Koga\'s Ditto9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Koga18/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Koga18/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Lt. Surge\'s Raichu10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Lt. Surge\'s Raichu10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Misty\'s Golduck11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Misty\'s Golduck11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Misty\'s Gyarados12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Misty\'s Gyarados12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Rocket\'s Mewtwo13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Rocket\'s Mewtwo13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Rocket\'s Zapdos14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Rocket\'s Zapdos14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Sabrina\'s Alakazam15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Sabrina\'s Alakazam15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Sabrina19/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymChallenge/Sabrina19/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Blaine\'s Moltres0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Blaine\'s Moltres0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Brock\'s Rhydon1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Brock\'s Rhydon1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Brock14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Brock14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Erika\'s Clefable2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Erika\'s Clefable2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Erika\'s Dragonair3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Erika\'s Dragonair3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Erika\'s Vileplume4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Erika\'s Vileplume4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Erika15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Erika15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Lt. Surge\'s Electabuzz5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Lt. Surge\'s Electabuzz5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Lt. Surge\'s Fearow6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Lt. Surge\'s Fearow6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Lt. Surge\'s Magneton7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Lt. Surge\'s Magneton7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Lt. Surge16/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Lt. Surge16/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Misty\'s Seadra8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Misty\'s Seadra8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Misty\'s Tentacruel9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Misty\'s Tentacruel9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Misty17/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Misty17/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Rocket\'s Hitmonchan10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Rocket\'s Hitmonchan10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Rocket\'s Moltres11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Rocket\'s Moltres11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Rocket\'s Scyther12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Rocket\'s Scyther12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Sabrina\'s Gengar13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Sabrina\'s Gengar13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Sabrina\'s Gengar132/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/Sabrina\'s Gengar132/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/The Rocket\'s Trap18/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/GymHeroes/The Rocket\'s Trap18/Settings.txt', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/MyIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/OBG.png', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/02 - Gym/01 - Gym Heroes_prefabloader/gymheroesexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/02 - Gym/01 - Gym Heroes_prefabloader/gymheroesexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/02 - Gym/02 - 1st Edition Gym Heroes_prefabloader/1steditiongymheroesexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/02 - Gym/02 - 1st Edition Gym Heroes_prefabloader/1steditiongymheroesexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/02 - Gym/03 - Gym Challenge_prefabloader/gymchallengeexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/02 - Gym/03 - Gym Challenge_prefabloader/gymchallengeexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/02 - Gym/04 - 1st Edition Gym Challenge_prefabloader/1steditiongymchallengeexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/02 - Gym/04 - 1st Edition Gym Challenge_prefabloader/1steditiongymchallengeexpansion.json'] },
      { name: '02 Gym Theme Decks', filename: '02 - Gym Theme Decks-856-1-2-1776635570.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/02 - Gym Theme Decks/01 - Gym Heroes Theme Decks_prefabloader/gymheroesthemedecks'], installedFiles: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/02 - Gym Theme Decks/01 - Gym Heroes Theme Decks_prefabloader/gymheroesthemedecks', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/02 - Gym Theme Decks/01 - Gym Heroes Theme Decks_prefabloader/gymheroesthemedecks.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/02 - Gym Theme Decks/02 - Gym Challenge Theme Decks_prefabloader/gymchallengethemedecks', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Accessories/02 - Gym Theme Decks/02 - Gym Challenge Theme Decks_prefabloader/gymchallengethemedecks.json'] },
      { name: '02 Pokemon Shop Textures', filename: '02 - Pokemon Shop Textures-872-1-0-1772223553.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/CardMachine_Large_mtl_BC.png'], walkDir: 'BepInEx/plugins/TextureReplacer/objects_textures', installedFiles: ['BepInEx/config/shaklin.TextureReplacer.cfg', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/CardMachine_Large_mtl_BC.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/CardMachine_Medium_mtl_BC.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/CardMachine_Small_mtl_BC.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/playtable/T_3dCardModel.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/playtable/T_3dCardModel2.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/playtable/T_3dCardModel3.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/playtable/T_3dCardModel4.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/playtable/T_3dCardModel5.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/playtable/T_3dCardModel6.png', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/CardDisplayBigWallWhite_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/CardDisplayBig_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/CardDisplaySmall_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/CardDisplayTable_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/CardDisplayTable_glass_edge.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/CardDisplayWall3X2_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/CardDisplayWall4X2_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/CardDisplayWallBig_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/CardDisplayWallElectronicBig_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/CardDisplayWallElectronicBig_glass2.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/CardDisplayWallElectronicSmall_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/CardDisplayWallElectronicSmall_glass2.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/CardDisplayWallElectronic_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/CardDisplayWallElectronic_glass2.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/CardDisplayWallSmall_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/PersonalShelfBig_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/PersonalShelfHuge_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/PersonalShelfSmall_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/StoreBFront_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/glass/StoreFront_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/CardBack.png', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/DiscordButton.png', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/EmptyCardSlot.png', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/GameStartBG_Blur.png', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/LibraryLogo.png', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/LoadingScreen_ScreenGrp_LoadingScreen_ScreenGrp_BG_(null-sprite).png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/buydecor/Phone_BuyDecoration_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/buydecor/Phone_BuyDecoration_over.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/customerreview/Icon_ImageIcon_StarGrade_l_Off.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/customerreview/Icon_ImageIcon_StarGrade_l_On.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/customerreview/Phone_CustomerReview_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/customerreview/Phone_CustomerReview_icon_left.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/customerreview/Phone_CustomerReview_icon_right.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/customerreview/Phone_CustomerReview_over.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/customerreview/UI_StarFill.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/customerreview/UI_StarFillBG.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/DestinyCardIcon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/expandshop/Phone_ExpandShop_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/expandshop/Phone_ExpandShop_over.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/furniture/Phone_Furniture_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/furniture/Phone_Furniture_over.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/gameevent/Phone_GameEvent_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/gameevent/Phone_GameEvent_over.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/grading/Phone_Grading_back.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/grading/Phone_Grading_back2.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/grading/Phone_Grading_over.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/grading/Phone_icontext.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/hiring/Phone_Hiring_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/hiring/Phone_Hiring_over.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/MobileButtonsText.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/MobileProviderText.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/MobileUIText.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/NetworkIcon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/PhoneBatteryFillIcon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/PhoneBatteryIcon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/pricecheck/Phone_PriceCheck_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/pricecheck/Phone_PriceCheck_icon_small.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/pricecheck/Phone_PriceCheck_over.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/rentbill/Phone_RentBill_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/rentbill/Phone_RentBill_over.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/restock/Phone_Restock_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/restock/Phone_Restock_over.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/restockboardgame/Phone_RestockBoardGame_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/restockboardgame/Phone_RestockBoardGame_over.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/scannerorder/Phone_ScannerOrder_over.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/settings/Phone_Setting_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/settings/Phone_Setting_over.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/WifiIcon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Billboard.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/CardboardBox_20x20x10_Albedo.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/CardboardBox_20x20x20_Albedo.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/CardboardBox_30x20x20_Albedo.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/CollectionIcon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/credit_card_D.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/docbinder02o_binder_Mat_BaseColor.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/GameTitle.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Icon_MenuIcon02_Shop.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/mcp_building_32_billboards_d.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/PO_PlayTableA1.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/T_BinderPage.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/T_CuttingGridMat.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/T_NoEntranceSign.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/T_OpenCloseSign.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/T_PaperBagAlbedoClosed.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/T_PaperBagAlbedoOpen.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/wood1.png'] },
      { name: '02 Pokemon Gameboy Games', filename: '02 - Pokemon Gameboy Games-870-1-1-1775689625.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Figurines/02 - Games/01 - Gameboy Games_prefabloader/pokemongbgames'], installedFiles: ['BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/MyIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/OBG.png', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Figurines/02 - Games/01 - Gameboy Games_prefabloader/pokemongbgames', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Figurines/02 - Games/01 - Gameboy Games_prefabloader/pokemongbgames.json'] },
      { name: '03 Neo Expansions', filename: '03 - Neo Expansions-854-1-4-1776567607.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/01 - Neo Genesis_prefabloader/neogenesisexpansion'], contentPaths: ['BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny'], installedFiles: ['BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Ampharos0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Ampharos0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Crobat1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Crobat1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Donphan2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Donphan2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Espeon3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Espeon3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Feraligatr4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Feraligatr4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Gengar5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Gengar5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Houndoom6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Houndoom6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Porygon27/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Porygon27/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Scizor8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Scizor8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Typhlosion9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Typhlosion9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Tyranitar10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Dark Tyranitar10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Light Arcanine11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Light Arcanine11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Light Azumarill12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Light Azumarill12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Light Dragonite13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Light Dragonite13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Light Togetic14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Light Togetic14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Miracle Energy15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Miracle Energy15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Celebi105/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Celebi105/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Charizard106/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Charizard106/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Kabutops107/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Kabutops107/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Mew113/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Mew113/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Mewtwo108/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Mewtwo108/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Noctowl109/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Noctowl109/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Raichu110/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Raichu110/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Steelix111/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Steelix111/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Tyranitar112/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDestiny/Shining Tyranitar112/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Espeon0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Espeon0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Forretress1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Forretress1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Hitmontop2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Hitmontop2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Houndoom3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Houndoom3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Houndour4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Houndour4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Kabutops5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Kabutops5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Magnemite6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Magnemite6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Politoed7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Politoed7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Poliwrath8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Poliwrath8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Scizor9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Scizor9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Smeargle10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Smeargle10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Tyranitar11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Tyranitar11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Umbreon12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Umbreon12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Unown [A]13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Unown [A]13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Ursaring14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Ursaring14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Wobbuffet15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Wobbuffet15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Yanma16/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoDiscovery/Yanma16/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Ampharos0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Ampharos0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Azumarill1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Azumarill1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Bellossom2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Bellossom2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Feraligatr3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Feraligatr3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Feraligatr4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Feraligatr4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Heracross5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Heracross5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Jumpluff6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Jumpluff6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Kingdra7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Kingdra7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Lugia8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Lugia8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Meganium10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Meganium10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Meganium9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Meganium9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Metal Energy18/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Metal Energy18/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Pichu11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Pichu11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Skarmory12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Skarmory12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Slowking13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Slowking13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Steelix14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Steelix14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Togetic15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Togetic15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Typhlosion16/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Typhlosion16/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Typhlosion17/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoGenesis/Typhlosion17/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Ampharos0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Ampharos0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Blissey1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Blissey1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Celebi2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Celebi2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Crobat3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Crobat3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Delibird4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Delibird4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Entei5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Entei5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Ho-oh6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Ho-oh6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Houndoom7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Houndoom7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Jumpluff8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Jumpluff8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Magneton9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Magneton9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Misdreavus10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Misdreavus10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Porygon211/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Porygon211/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Raikou12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Raikou12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Shining Gyarados64/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Shining Gyarados64/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Shining Magikarp65/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Shining Magikarp65/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Suicune13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/1stEditionNeoRevelation/Suicune13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Ampharos0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Ampharos0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Crobat1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Crobat1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Donphan2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Donphan2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Espeon3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Espeon3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Feraligatr4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Feraligatr4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Gengar5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Gengar5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Houndoom6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Houndoom6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Porygon27/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Porygon27/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Scizor8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Scizor8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Typhlosion9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Typhlosion9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Tyranitar10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Dark Tyranitar10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Light Arcanine11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Light Arcanine11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Light Azumarill12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Light Azumarill12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Light Dragonite13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Light Dragonite13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Light Togetic14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Light Togetic14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Miracle Energy15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Miracle Energy15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Celebi105/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Celebi105/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Charizard106/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Charizard106/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Kabutops107/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Kabutops107/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Mew113/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Mew113/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Mewtwo108/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Mewtwo108/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Noctowl109/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Noctowl109/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Raichu110/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Raichu110/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Steelix111/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Steelix111/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Tyranitar112/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDestiny/Shining Tyranitar112/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Espeon0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Espeon0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Forretress1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Forretress1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Hitmontop2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Hitmontop2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Houndoom3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Houndoom3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Houndour4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Houndour4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Kabutops5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Kabutops5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Magnemite6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Magnemite6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Politoed7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Politoed7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Poliwrath8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Poliwrath8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Scizor9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Scizor9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Smeargle10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Smeargle10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Tyranitar11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Tyranitar11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Umbreon12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Umbreon12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Unown [A]13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Unown [A]13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Ursaring14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Ursaring14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Wobbuffet15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Wobbuffet15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Yanma16/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoDiscovery/Yanma16/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Ampharos0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Ampharos0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Azumarill1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Azumarill1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Bellossom2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Bellossom2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Feraligatr3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Feraligatr3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Feraligatr4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Feraligatr4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Heracross5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Heracross5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Jumpluff6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Jumpluff6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Kingdra7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Kingdra7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Lugia8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Lugia8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Meganium10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Meganium10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Meganium9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Meganium9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Metal Energy18/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Metal Energy18/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Pichu11/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Pichu11/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Skarmory12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Skarmory12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Slowking13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Slowking13/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Steelix14/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Steelix14/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Togetic15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Togetic15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Typhlosion16/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Typhlosion16/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Typhlosion17/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoGenesis/Typhlosion17/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Ampharos0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Ampharos0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Blissey1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Blissey1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Celebi2/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Celebi2/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Crobat3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Crobat3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Delibird4/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Delibird4/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Entei5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Entei5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Ho-oh6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Ho-oh6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Houndoom7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Houndoom7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Jumpluff8/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Jumpluff8/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Magneton9/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Magneton9/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Misdreavus10/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Misdreavus10/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Porygon211/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Porygon211/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Raikou12/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Raikou12/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Shining Gyarados64/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Shining Gyarados64/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Shining Magikarp65/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Shining Magikarp65/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Suicune13/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/NeoRevelation/Suicune13/Settings.txt', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/MyIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/OBG.png', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/01 - Neo Genesis_prefabloader/neogenesisexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/01 - Neo Genesis_prefabloader/neogenesisexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/02 - 1st Edition Neo Genesis_prefabloader/1steditionneogenesisexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/02 - 1st Edition Neo Genesis_prefabloader/1steditionneogenesisexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/03 - Neo Discovery_prefabloader/neodiscoveryexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/03 - Neo Discovery_prefabloader/neodiscoveryexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/04 - 1st Edition Neo Discovery_prefabloader/1steditionneodiscoveryexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/04 - 1st Edition Neo Discovery_prefabloader/1steditionneodiscoveryexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/05 - Neo Revelation_prefabloader/neorevelationexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/05 - Neo Revelation_prefabloader/neorevelationexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/06 - 1st Edition Neo Revelation_prefabloader/1steditionneorevelationexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/06 - 1st Edition Neo Revelation_prefabloader/1steditionneorevelationexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/07 - Neo Destiny_prefabloader/neodestinyexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/07 - Neo Destiny_prefabloader/neodestinyexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/08 - 1st Edition Neo Destiny_prefabloader/1steditionneodestinyexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/08 - 1st Edition Neo Destiny_prefabloader/1steditionneodestinyexpansion.json'] },
      { name: '03 Pokemon N64 Games', filename: '03 - Pokemon N64 Games-870-1-1-1775689656.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Figurines/02 - Games/02 - N64 Games_prefabloader/pokemonn64games'], installedFiles: ['BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/MyIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/OBG.png', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Figurines/02 - Games/02 - N64 Games_prefabloader/pokemonn64games', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Figurines/02 - Games/02 - N64 Games_prefabloader/pokemonn64games.json'] },
      { name: '031 Pokemon GO English', filename: '031 - Pokemon GO English v1.1-892-1-1-1775915881.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/031PokemonGO_prefabLoader/031hh_pokemon_go'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/031PokemonGO_prefabLoader/031hh_pokemon_go', 'BepInEx/plugins/HH EPL Pokemons/031PokemonGO_prefabLoader/031hh_pokemon_go.json'] },
      { name: '04 Legendary Collection Expansion', filename: '04 - Legendary Collection Expansion-854-1-4-1776654721.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/04 - Legendary Collection_prefabloader/legendarycollectionexpansion'], installedFiles: ['BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/MyIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/OBG.png', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/04 - Legendary Collection_prefabloader/legendarycollectionexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/04 - Legendary Collection_prefabloader/legendarycollectionexpansion.json'] },
      { name: '04 Pokemon Statues', filename: '04 - Pokemon Statues-872-1-0-1773025326.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Statues/01 - Pokemon Statues_prefabloader/pokemonstatues'], installedFiles: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Statues/01 - Pokemon Statues_prefabloader/pokemonstatues', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Statues/01 - Pokemon Statues_prefabloader/pokemonstatues.json'] },
      { name: 'Yu-Gi-Oh Manga', filename: '01 - Yu-Gi-Oh Manga-988-1-0-1777174436.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/YuGiOh Accessories/01 - Manga_prefabloader/yugiohmanga'], installedFiles: ['BepInEx/plugins/Real TCG Overhaul/YuGiOh Accessories/01 - Manga_prefabloader/yugiohmanga', 'BepInEx/plugins/Real TCG Overhaul/YuGiOh Accessories/01 - Manga_prefabloader/yugiohmanga.json'] },
      { name: 'Yu-Gi-Oh Starter Decks', filename: '02 - Yu-Gi-Oh Starter Decks-988-1-0-1777174499.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/YuGiOh Accessories/02 - StarterDecks_prefabloader/yugiohstarterdecks'], installedFiles: ['BepInEx/plugins/Real TCG Overhaul/YuGiOh Accessories/02 - StarterDecks_prefabloader/yugiohstarterdecks', 'BepInEx/plugins/Real TCG Overhaul/YuGiOh Accessories/02 - StarterDecks_prefabloader/yugiohstarterdecks.json'] },
      { name: 'Yu-Gi-Oh Structure Decks', filename: '03 - Yu-Gi-Oh Structure Decks-988-1-0-1777174552.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/YuGiOh Accessories/03 - StructureDecks_prefabloader/yugiohstructuredecks'], installedFiles: ['BepInEx/plugins/Real TCG Overhaul/YuGiOh Accessories/03 - StructureDecks_prefabloader/yugiohstructuredecks', 'BepInEx/plugins/Real TCG Overhaul/YuGiOh Accessories/03 - StructureDecks_prefabloader/yugiohstructuredecks.json'] },
      { name: '00 YuGiOh Shop Phone App', filename: '00 - YuGiOh Shop Phone App-987-1-0-1777149896.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Phone - Overhaul/App Images/Yu-Gi-Oh Shop/IBG.png'], installedFiles: ['BepInEx/plugins/Phone - Overhaul/App Images/Yu-Gi-Oh Shop/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Yu-Gi-Oh Shop/MyIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Yu-Gi-Oh Shop/OBG.png'] },
      { name: '01 Legend Of Blue Eyes White Dragon Expansion', filename: '01 - Legend Of Blue Eyes White Dragon Expansion-987-1-0-1777153769.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/YuGiOh Cards/2002/01 - Legend of Blue Eyes White Dragon_prefabloader/legendofblueeyeswhitedragon'], contentPaths: ['BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon'], installedFiles: ['BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Aqua Madoor27/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Aqua Madoor27/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Armed Ninja106/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Armed Ninja106/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Blue-Eyes White Dragon1/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Blue-Eyes White Dragon1/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Celtic Guardian7/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Celtic Guardian7/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Charubin the Fire Knight15/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Charubin the Fire Knight15/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Curse of Dragon66/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Curse of Dragon66/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Dark Hole52/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Dark Hole52/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Dark Magician5/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Dark Magician5/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Darkfire Dragon19/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Darkfire Dragon19/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Dragon Capture Jar45/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Dragon Capture Jar45/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Dragoness the Wicked Knight86/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Dragoness the Wicked Knight86/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Exodia the Forbidden One124/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Exodia the Forbidden One124/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Final Flame100/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Final Flame100/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Fissure57/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Fissure57/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Flame Ghost29/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Flame Ghost29/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Flame Swordsman3/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Flame Swordsman3/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Flower Wolf107/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Flower Wolf107/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Fusionist22/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Fusionist22/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Gaia the Dragon Champion125/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Gaia the Dragon Champion125/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Gaia The Fierce Knight6/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Gaia The Fierce Knight6/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Giant Soldier of Stone68/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Giant Soldier of Stone68/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Goblin\'s Secret Remedy99/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Goblin\'s Secret Remedy99/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Gravedigger Ghoul65/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Gravedigger Ghoul65/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Hane-Hane110/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Hane-Hane110/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Karbonala Warrior67/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Karbonala Warrior67/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Left Arm of the Forbidden One123/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Left Arm of the Forbidden One123/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Left Leg of the Forbidden One121/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Left Leg of the Forbidden One121/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Man-Eater Bug108/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Man-Eater Bug108/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Metal Dragon102/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Metal Dragon102/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Monster Reborn118/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Monster Reborn118/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Mystical Elf62/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Mystical Elf62/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Polymerization59/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Polymerization59/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Pot of Greed119/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Pot of Greed119/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Raigeki53/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Raigeki53/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Reaper of the Cards71/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Reaper of the Cards71/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Red-Eyes Black Dragon70/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Red-Eyes Black Dragon70/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Right Arm of the Forbidden One122/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Right Arm of the Forbidden One122/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Right Leg of the Forbidden One120/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Right Leg of the Forbidden One120/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Spirit of the Harp78/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Spirit of the Harp78/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Stop Defense95/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Stop Defense95/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Swords of Revealing Light101/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Swords of Revealing Light101/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Trap Hole58/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Trap Hole58/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Tri-Horned Dragon0/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Tri-Horned Dragon0/Settings.txt', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Two-Pronged Attack61/Foiltex3.png', 'BepInEx/plugins/Holographic Overhaul/Holographics/LegendofBlueEyesWhiteDragon/Two-Pronged Attack61/Settings.txt', 'BepInEx/plugins/Real TCG Overhaul/YuGiOh Cards/2002/01 - Legend of Blue Eyes White Dragon_prefabloader/legendofblueeyeswhitedragon', 'BepInEx/plugins/Real TCG Overhaul/YuGiOh Cards/2002/01 - Legend of Blue Eyes White Dragon_prefabloader/legendofblueeyeswhitedragon.json'] },
      { name: '05 e Card Series Expansions', filename: '05 - e-Card Series Expansions-854-1-5-1776655401.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/05 - e-Card Series/01 - Expedition Base Set_prefabloader/expeditionexpansion'], installedFiles: ['BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/MyIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Pokemon Shop/OBG.png', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/05 - e-Card Series/01 - Expedition Base Set_prefabloader/expeditionexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/05 - e-Card Series/01 - Expedition Base Set_prefabloader/expeditionexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/05 - e-Card Series/02 - Aquapolis_prefabloader/aquapolisexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/05 - e-Card Series/02 - Aquapolis_prefabloader/aquapolisexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/05 - e-Card Series/03 - Skyridge_prefabloader/skyridgeexpansion', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/05 - e-Card Series/03 - Skyridge_prefabloader/skyridgeexpansion.json'] },
      { name: '053(2) Ultra Prism English ORIGINAL SATURATION', filename: '053(2) - Ultra Prism English ORIGINAL SATURATION-892-1-1-1775916699.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/053UltraPrism_prefabLoader/053hh_ultra prism_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/053UltraPrism_prefabLoader/053hh_ultra prism_eng', 'BepInEx/plugins/HH EPL Pokemons/053UltraPrism_prefabLoader/053hh_ultra prism_eng.json'] },
      { name: '056 Dragon Majesty English HIGH SATURATION', filename: '056 - Dragon Majesty English HIGH SATURATION-892-1-1-1775917080.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/056DragonMajesty_prefabLoader/056hh_dragon majesty_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/056DragonMajesty_prefabLoader/056hh_dragon majesty_eng', 'BepInEx/plugins/HH EPL Pokemons/056DragonMajesty_prefabLoader/056hh_dragon majesty_eng.json'] },
      { name: '079(2) Emerging Powers ORIGINAL SATURATION', filename: '079(2) - Emerging Powers ORIGINAL SATURATION-892-1-1-1775917745.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/079EmergingPowers_prefabLoader/079hh_emerging powers'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/079EmergingPowers_prefabLoader/079hh_emerging powers', 'BepInEx/plugins/HH EPL Pokemons/079EmergingPowers_prefabLoader/079hh_emerging powers.json'] },
      { name: '081 Next Destinies HIGH SATURATION', filename: '081 - Next Destinies HIGH SATURATION-892-1-1-1775918755.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/081NextDestinies_prefabLoader/081hh_next destinies'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/081NextDestinies_prefabLoader/081hh_next destinies', 'BepInEx/plugins/HH EPL Pokemons/081NextDestinies_prefabLoader/081hh_next destinies.json'] },
      { name: '087 Plasma Freeze HIGH SATURATION', filename: '087 - Plasma Freeze HIGH SATURATION-892-1-0-1775827772.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/087PlasmaFreeze_prefabLoader/087hh_plasma freeze'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/087PlasmaFreeze_prefabLoader/087hh_plasma freeze', 'BepInEx/plugins/HH EPL Pokemons/087PlasmaFreeze_prefabLoader/087hh_plasma freeze.json'] },
      { name: '088 Plasma Blast HIGH SATURATION', filename: '088 - Plasma Blast HIGH SATURATION-892-1-0-1775828093.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/088PlasmaBlast_prefabLoader/088hh_plasma blast'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/088PlasmaBlast_prefabLoader/088hh_plasma blast', 'BepInEx/plugins/HH EPL Pokemons/088PlasmaBlast_prefabLoader/088hh_plasma blast.json'] },
      { name: '089 Legendary Treasures HIGH SATURATION', filename: '089 - Legendary Treasures HIGH SATURATION-892-1-0-1775828460.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/089LegendaryTreasures_prefabLoader/089hh_legendary treasures'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/089LegendaryTreasures_prefabLoader/089hh_legendary treasures', 'BepInEx/plugins/HH EPL Pokemons/089LegendaryTreasures_prefabLoader/089hh_legendary treasures.json'] },
      { name: '1997 Pokemon Cardass Set (Pocket Monsters)', filename: '1997 Pokemon Cardass Set (Pocket Monsters)-942-1-1776071433.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Brainrot/_prefabloader/97cardass'], installedFiles: ['BepInEx/plugins/Brainrot/_prefabloader/97cardass', 'BepInEx/plugins/Brainrot/_prefabloader/97cardass.json', 'BepInEx/plugins/Phone - Overhaul/App Images/Brainrot/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Brainrot/Sahur.png'] },
      { name: '1997 Topsun PocketMonsters', filename: '1997 Topsun PocketMonsters-942-1-1776122111.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Brainrot/_prefabloader/97 topsun'], installedFiles: ['BepInEx/plugins/Brainrot/_prefabloader/97 topsun', 'BepInEx/plugins/Brainrot/_prefabloader/97 topsun.json', 'BepInEx/plugins/Phone - Overhaul/App Images/Brainrot/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Brainrot/Sahur.png'] },
      { name: '2004 Topps Pokemon', filename: '2004 Topps Pokemon-942-1-1776141066.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Brainrot/_prefabloader/04topps'], installedFiles: ['BepInEx/plugins/Brainrot/_prefabloader/04topps', 'BepInEx/plugins/Brainrot/_prefabloader/04topps.json', 'BepInEx/plugins/Phone - Overhaul/App Images/Brainrot/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Brainrot/Sahur.png'] },
      { name: '2007 Topps Halo', filename: '2007 Topps Halo-942-1-1776315733.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Brainrot/_prefabloader/07halotopps'], installedFiles: ['BepInEx/plugins/Brainrot/_prefabloader/07halotopps', 'BepInEx/plugins/Brainrot/_prefabloader/07halotopps.json', 'BepInEx/plugins/Phone - Overhaul/App Images/Brainrot/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Brainrot/Sahur.png'] },
      { name: '2025 Brainrot', filename: '2025 Brainrot-942-3-1775759406.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Brainrot/_prefabloader/bbrainrot'], installedFiles: ['BepInEx/plugins/Brainrot/_prefabloader/bbrainrot', 'BepInEx/plugins/Brainrot/_prefabloader/bbrainrot.json', 'BepInEx/plugins/Phone - Overhaul/App Images/Brainrot/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Brainrot/Sahur.png'] },
      { name: '40K prefabloader', filename: '40K_prefabloader-894-1-01-1773421797.7z', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/40KNO__prefabLoader/40k'], installedFiles: ['BepInEx/plugins/40KNO__prefabLoader/40k', 'BepInEx/plugins/40KNO__prefabLoader/40k.json', 'BepInEx/plugins/Phone - Overhaul/App Images/TCGShop/Icon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/TCGShop/Inner BG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/TCGShop/Outer BG.png'] },
      { name: 'Amiibo variety set', filename: 'Amiibo variety set 1-942-1-1775345314.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Brainrot/_prefabloader/amiibov1'], installedFiles: ['BepInEx/plugins/Brainrot/_prefabloader/amiibov1', 'BepInEx/plugins/Brainrot/_prefabloader/amiibov1.json', 'BepInEx/plugins/Phone - Overhaul/App Images/Brainrot/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Brainrot/Sahur.png'] },
      { name: 'Auto Pricer', filename: 'Auto Pricer-921-1-1-1774297612.zip', targetKey: 'plugins', core: false, signature: ['BepInEx/plugins/Auto Pricer/AutoPricer.dll'], installedFiles: ['BepInEx/plugins/AutoPricer.dll'] },
      { name: 'BepInEx with Configuration Manager', filename: 'BepInEx with Configuration Manager-2-5-4-23-2-1726589034.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/core/0Harmony.dll'], installedFiles: ['.doorstop_version', 'BepInEx/cache/chainloader_typeloader.dat', 'BepInEx/cache/harmony_interop_cache.dat', 'BepInEx/config/BepInEx.cfg', 'BepInEx/config/com.bepis.bepinex.configurationmanager.cfg', 'BepInEx/core/0Harmony.dll', 'BepInEx/core/0Harmony.xml', 'BepInEx/core/0Harmony20.dll', 'BepInEx/core/BepInEx.dll', 'BepInEx/core/BepInEx.Harmony.dll', 'BepInEx/core/BepInEx.Harmony.xml', 'BepInEx/core/BepInEx.Preloader.dll', 'BepInEx/core/BepInEx.Preloader.xml', 'BepInEx/core/BepInEx.xml', 'BepInEx/core/HarmonyXInterop.dll', 'BepInEx/core/Mono.Cecil.dll', 'BepInEx/core/Mono.Cecil.Mdb.dll', 'BepInEx/core/Mono.Cecil.Pdb.dll', 'BepInEx/core/Mono.Cecil.Rocks.dll', 'BepInEx/core/MonoMod.RuntimeDetour.dll', 'BepInEx/core/MonoMod.RuntimeDetour.xml', 'BepInEx/core/MonoMod.Utils.dll', 'BepInEx/core/MonoMod.Utils.xml', 'BepInEx/plugins/ConfigurationManager/ConfigurationManager.dll', 'BepInEx/plugins/ConfigurationManager/ConfigurationManager.xml', 'BepInEx/plugins/ConfigurationManager/LICENSE', 'BepInEx/plugins/ConfigurationManager/README.md', 'changelog.txt', 'doorstop_config.ini', 'winhttp.dll'] },
      { name: 'Blue Archive TCG Main File', filename: 'Blue Archive TCG Main File-943-1-0-1775044609.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/bluearchive_prefabloader/bluearchive'], installedFiles: ['BepInEx/plugins/bluearchive_prefabloader/bluearchive', 'BepInEx/plugins/bluearchive_prefabloader/bluearchive.json', 'BepInEx/plugins/Phone - Overhaul/App Images/SCHALE/SCHALEIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/SCHALE/SCHALEInnerBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/SCHALE/SCHALEOuterBG.png'] },
      { name: 'Boxing 2024 Topps Chrome', filename: 'Boxing 2024 Topps Chrome-935-1-1775504009.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/boxing24chrome'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/boxing24chrome', 'BepInEx/plugins/BSports/grails_prefabloader/boxing24chrome.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'CardWars', filename: 'CardWars-918-3-0-1776634186.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/AdventureTime_prefabLoader/cardwars'], installedFiles: ['BepInEx/plugins/AdventureTime_prefabLoader/cardwars', 'BepInEx/plugins/AdventureTime_prefabLoader/cardwars.json', 'BepInEx/plugins/Phone - Overhaul/App Images/AdventureTimeCW/Icon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/AdventureTimeCW/InnerBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/AdventureTimeCW/OuterBG.png'] },
      { name: 'Chaotic Mod', filename: 'Chaotic Mod-969-1-1776299809.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Chaotic/_prefabloader/mybundles'], installedFiles: ['BepInEx/plugins/Chaotic/_prefabloader/mybundles', 'BepInEx/plugins/Chaotic/_prefabloader/mybundles.json'] },
      { name: 'Civil Draft', filename: 'Civil Draft 1.2.2-916-1-2-2-1776031066.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/CivilDraft_prefabloader/civildraft'], installedFiles: ['BepInEx/plugins/CivilDraft_prefabloader/civildraft', 'BepInEx/plugins/CivilDraft_prefabloader/civildraft.json', 'BepInEx/plugins/CivilDraft_prefabloader/civildraft.meta', 'BepInEx/plugins/Phone - Overhaul/App Images/CivilDraftShop/CivilDraftIcon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Billboard.png'] },
      { name: 'Collection Tracker', filename: 'Collection Tracker v1.0.8-867-1-0-8-1776206951.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/CollectionTracker/CollectionTracker.dll'], installedFiles: ['BepInEx/plugins/CollectionTracker/CollectionTracker.dll', 'BepInEx/plugins/Phone - Overhaul/App Images/CollectionTracker/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/CollectionTracker/MyIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/CollectionTracker/OBG.png'] },
      { name: 'Controller Support', filename: 'Controller Support-893-1-1-1-1773588401.7z', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/ControllerSupport/ControllerSupport.dll'], installedFiles: ['BepInEx/plugins/ControllerSupport/ControllerSupport.dll'] },
      { name: 'Customer Manager', filename: 'Customer Manager 1.0.0-975-1-0-1-1776486302.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Customer Manager/CustomerManager.dll'], installedFiles: ['BepInEx/plugins/Customer Manager/CustomerManager.dll'] },
      { name: 'EndGame Overhaul', filename: 'EndGame Overhaul-873-2-2-1-1776504065.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Endgame Overhaul/Endgame Overhaul.dll'], contentPaths: ['BepInEx/plugins/Phone - Overhaul/App Images/EGOShop'], installedFiles: ['BepInEx/plugins/Endgame Overhaul/Endgame Overhaul.dll', 'BepInEx/plugins/Endgame Overhaul/v*.txt', 'BepInEx/plugins/Phone - Overhaul/App Images/EGOShop/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/EGOShop/Icon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/EGOShop/OBG.png'] },
      { name: 'EnhancedPrefabLoader', filename: 'EnhancedPrefabLoader-496-6-0-1-1776601336.zip', targetKey: 'bepinex', core: true, signature: ['BepInEx/plugins/EnhancedPrefabLoader/EnhancedPrefabLoader.API.dll'], installedFiles: ['BepInEx/plugins/EnhancedPrefabLoader/EnhancedPrefabLoader.API.dll', 'BepInEx/plugins/EnhancedPrefabLoader/EnhancedPrefabLoader.dll', 'BepInEx/plugins/EnhancedPrefabLoader/Scripts.dll', 'BepInEx/patchers/EnhancedPrefabLoader/CollectionWeaver.dll', 'BepInEx/patchers/EnhancedPrefabLoader/EnhancedPrefabLoaderPrepatch.dll'] },
      { name: 'F1 2023 Chrome', filename: 'F1 2023 Chrome-935-2-1775514678.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/f1_prefabloader/f12023v1'], installedFiles: ['BepInEx/plugins/BSports/f1_prefabloader/f12023v1', 'BepInEx/plugins/BSports/f1_prefabloader/f12023v1.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'F1 2025 Sapphire', filename: 'F1 2025 Sapphire-935-1-1775944148.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/f1_prefabloader/f1sap'], installedFiles: ['BepInEx/plugins/BSports/f1_prefabloader/f1sap', 'BepInEx/plugins/BSports/f1_prefabloader/f1sap.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'Fair Price', filename: 'Fair Price-920-1-0-1774162912.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/FairPrice/FairPrice.dll'], installedFiles: ['BepInEx/plugins/FairPrice/FairPrice.dll'] },
      { name: 'Fast Opening Pack', filename: 'Fast Opening Pack V2-913-1-2-1-1774107704.zip', targetKey: 'plugins', core: false, signature: ['BepInEx/plugins/FastPackOpening.dll'], installedFiles: ['BepInEx/plugins/FastPackOpening.dll'] },
      { name: 'FGO Mod', filename: 'FGO Mod 3.0-754-3-7-1-1774070880.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/FGOmod/_prefabLoader/fgomod'], installedFiles: ['BepInEx/plugins/FGOmod/_prefabLoader/fgomod', 'BepInEx/plugins/FGOmod/_prefabLoader/fgomod.json', 'BepInEx/plugins/Phone - Overhaul/App Images/FGOtcg/FGOIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/FGOtcg/FGOInnerBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/FGOtcg/FGOOuterBG.png'] },
      { name: 'FIFA 2014 World Cup', filename: 'FIFA 2014 World Cup-935-1-1775769355.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/12wc'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/12wc', 'BepInEx/plugins/BSports/grails_prefabloader/12wc.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'FIFA 2023 Donruss Optic', filename: 'FIFA 2023 Donruss Optic-935-1-1776657049.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/donruss23soccerfifa'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/donruss23soccerfifa', 'BepInEx/plugins/BSports/grails_prefabloader/donruss23soccerfifa.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'Fire Emblem Cipher Set', filename: 'Fire Emblem Cipher Set-907-1-0-0-1773604210.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/ElmaSchwarzExpansions/FECipherSet_prefabloader/fecipher'], installedFiles: ['BepInEx/plugins/ElmaSchwarzExpansions/FECipherSet_prefabloader/fecipher', 'BepInEx/plugins/ElmaSchwarzExpansions/FECipherSet_prefabloader/fecipher.json'] },
      { name: 'Fortnite 2019 Panini', filename: 'Fortnite 2019 Panini-942-1-1775539030.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Brainrot/_prefabloader/fortv1'], installedFiles: ['BepInEx/plugins/Brainrot/_prefabloader/fortv1', 'BepInEx/plugins/Brainrot/_prefabloader/fortv1.json', 'BepInEx/plugins/Phone - Overhaul/App Images/Brainrot/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Brainrot/Sahur.png'] },
      { name: 'Game Speed Ajuster', filename: 'Game Speed Ajuster v1.0.1-915-1-0-1-1774369965.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/GameSpeedAjuster.dll'], installedFiles: ['BepInEx/plugins/GameSpeedAjuster.dll'] },
      { name: 'Game Stats', filename: 'Game Stats-903-2-2-1-1773773830.7z', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/GameStats/GameStats.dll'], installedFiles: ['BepInEx/plugins/GameStats/icon_card_sales.png', 'BepInEx/plugins/GameStats/icon_item_sales.png', 'BepInEx/plugins/GameStats/icon_pack_opening.png', 'BepInEx/plugins/GameStats/icon_shop_overview.png', 'BepInEx/plugins/Phone - Overhaul/App Images/com.gs.stats/Icon.png', 'BepInEx/plugins/GameStats/GameStats.dll'] },
      { name: 'Garbage Pail Kids 2013 Chrome', filename: 'Garbage Pail Kids 2013 Chrome-937-1-1775931918.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/GPK/1985_prefabloader/gpk2013'], installedFiles: ['BepInEx/plugins/GPK/1985_prefabloader/gpk2013', 'BepInEx/plugins/GPK/1985_prefabloader/gpk2013.json', 'BepInEx/plugins/Phone - Overhaul/App Images/GPK/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/GPK/GPKlogo.png'] },
      { name: 'GPK', filename: 'GPK 1986-937-2-1775338122.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/GPK/1985_prefabloader/gpk1986'], installedFiles: ['BepInEx/plugins/GPK/1985_prefabloader/gpk1986', 'BepInEx/plugins/GPK/1985_prefabloader/gpk1986.json', 'BepInEx/plugins/Phone - Overhaul/App Images/GPK/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/GPK/GPKlogo.png'] },
      { name: 'GPK1985Set', filename: 'GPK1985Set-937-2-1775186704.rar', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/GPK/1985_prefabloader/blissgbk'], installedFiles: ['BepInEx/plugins/GPK/1985_prefabloader/blissgbk', 'BepInEx/plugins/GPK/1985_prefabloader/blissgbk.json', 'BepInEx/plugins/Phone - Overhaul/App Images/GPK/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/GPK/GPKlogo.png'] },
      { name: 'Grading Overhaul', filename: 'Grading Overhaul-612-3-3-0-1776331062.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Grading Overhaul/Grading Overhaul.dll', 'BepInEx/plugins/GradeDataLifeSaver.dll'], installedFiles: ['BepInEx/plugins/GradeDataLifeSaver.dll', 'BepInEx/plugins/Grading Overhaul/ExpansionList.json', 'BepInEx/plugins/Grading Overhaul/Grading Overhaul.dll', 'BepInEx/plugins/Grading Overhaul/v*.txt'] },
      { name: 'Grails', filename: 'Grails-935-1-1774929876.rar', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/sportsgrails'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/sportsgrails', 'BepInEx/plugins/BSports/grails_prefabloader/sportsgrails.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'Gundam Items prefabloader', filename: 'Gundam Items_prefabloader-884-1-01-1773422322.7z', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/GundamItemsNO__prefabLoader/gundamitems'], installedFiles: ['BepInEx/plugins/GundamItemsNO__prefabLoader/gundamitems', 'BepInEx/plugins/GundamItemsNO__prefabLoader/gundamitems.json', 'BepInEx/plugins/Phone - Overhaul/App Images/GundamABtcg/GABIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/GundamABtcg/GABInnerBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/GundamABtcg/GABOuterBG.png'] },
      { name: 'Holographic Overhaul', filename: 'Holographic Overhaul-617-3-0-7-1775952266.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Holographic Overhaul/Holographic Overhaul.dll'], installedFiles: ['BepInEx/plugins/Holographic Overhaul/GlobalDefaults.txt', 'BepInEx/plugins/Holographic Overhaul/holofoil', 'BepInEx/plugins/Holographic Overhaul/Holographic Overhaul.dll', 'BepInEx/plugins/Holographic Overhaul/Holographics/ExpansionName/CardNameCardNumber/Settings.txt.txt', 'BepInEx/plugins/Holographic Overhaul/SettingsTxtFileExample.txt', 'BepInEx/plugins/Holographic Overhaul/v*.txt'] },
      { name: 'Hololive Pack Opener Machine', filename: 'Hololive Pack Opener Machine-923-0-1-1774278374.rar', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/TextureReplacer/objects_textures/furniture/machines/CardMachine_Large_mtl_BC.png'], installedFiles: ['BepInEx/plugins/TextureReplacer/objects_textures/furniture/machines/CardMachine_Large_mtl_BC.png', 'BepInEx/plugins/TextureReplacer/objects_textures/furniture/machines/CardMachine_Large_mtl_N.png', 'BepInEx/plugins/TextureReplacer/objects_textures/furniture/machines/CardMachine_Medium_mtl_BC.png', 'BepInEx/plugins/TextureReplacer/objects_textures/furniture/machines/CardMachine_Medium_mtl_N.png', 'BepInEx/plugins/TextureReplacer/objects_textures/furniture/machines/CardMachine_Small_mtl_BC.png', 'BepInEx/plugins/TextureReplacer/objects_textures/furniture/machines/CardMachine_Small_mtl_N.png', 'BepInEx/plugins/TextureReplacer/objects_textures/furniture/machines/Large_MS_Specular.png', 'BepInEx/plugins/TextureReplacer/objects_textures/furniture/machines/Medium_MS_Specular.png', 'BepInEx/plugins/TextureReplacer/objects_textures/furniture/machines/PO_AutoPackOpener1.png', 'BepInEx/plugins/TextureReplacer/objects_textures/furniture/machines/PO_AutoPackOpener2.png', 'BepInEx/plugins/TextureReplacer/objects_textures/furniture/machines/PO_AutoPackOpener3.png', 'BepInEx/plugins/TextureReplacer/objects_textures/furniture/machines/Small_MS.png', 'BepInEx/plugins/TextureReplacer/objects_textures/furniture/machines/Small_MS_Specular.png', 'BepInEx/Plugin'] },
      { name: 'Main File', filename: 'Main File-932-1-1-1774930294.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Elder Scrolls/_prefabloader/elder scrolls legends'], installedFiles: ['BepInEx/plugins/Elder Scrolls/_prefabloader/elder scrolls legends', 'BepInEx/plugins/Elder Scrolls/_prefabloader/elder scrolls legends.json', 'BepInEx/plugins/Phone - Overhaul/App Images/Elder Scrolls/ElderEmpty.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Elder Scrolls/ElderPhone.png'] },
      { name: 'Manga Akasaka Set', filename: 'Manga - Akasaka Set-909-1-0-0-1773605069.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/ElmaSchwarzExpansions/MangaSet_prefabloader/manga-akasaka'], installedFiles: ['BepInEx/plugins/ElmaSchwarzExpansions/MangaSet_prefabloader/manga-akasaka', 'BepInEx/plugins/ElmaSchwarzExpansions/MangaSet_prefabloader/manga-akasaka.json'] },
      { name: 'Manga China in Japan Set', filename: 'Manga - China in Japan Set-909-1-0-0-1773605124.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/ElmaSchwarzExpansions/MangaSet_prefabloader/manga-china'], installedFiles: ['BepInEx/plugins/ElmaSchwarzExpansions/MangaSet_prefabloader/manga-china', 'BepInEx/plugins/ElmaSchwarzExpansions/MangaSet_prefabloader/manga-china.json'] },
      { name: 'Manga Princess Set', filename: 'Manga - Princess Set-909-1-0-0-1773605182.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/ElmaSchwarzExpansions/MangaSet_prefabloader/manga-princess'], installedFiles: ['BepInEx/plugins/ElmaSchwarzExpansions/MangaSet_prefabloader/manga-princess', 'BepInEx/plugins/ElmaSchwarzExpansions/MangaSet_prefabloader/manga-princess.json'] },
      { name: 'Manga Shoujo Set', filename: 'Manga - Shoujo Set-909-1-0-0-1773605238.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/ElmaSchwarzExpansions/MangaSet_prefabloader/manga-shoujo'], installedFiles: ['BepInEx/plugins/ElmaSchwarzExpansions/MangaSet_prefabloader/manga-shoujo', 'BepInEx/plugins/ElmaSchwarzExpansions/MangaSet_prefabloader/manga-shoujo.json'] },
      { name: 'Manga Yuri Set', filename: 'Manga - Yuri Set-909-1-0-0-1773605340.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/ElmaSchwarzExpansions/MangaSet_prefabloader/manga-yuri'], installedFiles: ['BepInEx/plugins/ElmaSchwarzExpansions/MangaSet_prefabloader/manga-yuri', 'BepInEx/plugins/ElmaSchwarzExpansions/MangaSet_prefabloader/manga-yuri.json'] },
      { name: 'MLB 2024 Allen Ginter', filename: 'MLB 2024 Allen Ginter-935-1-1776408193.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/allenginter24'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/allenginter24', 'BepInEx/plugins/BSports/grails_prefabloader/allenginter24.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'MLB 2024 Chrome', filename: 'MLB 2024 Chrome-935-1-1776207980.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/mllb24chrome'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/mllb24chrome', 'BepInEx/plugins/BSports/grails_prefabloader/mllb24chrome.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'MLB 2024 Signatures', filename: 'MLB 2024 Signatures-935-1-1775106590.rar', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/mlb2024cs'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/mlb2024cs', 'BepInEx/plugins/BSports/grails_prefabloader/mlb2024cs.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'MLB 2024 Stars (Very Small Pack)', filename: 'MLB 2024 Stars (Very Small Pack)-935-1-1776408212.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/mlbstars24'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/mlbstars24', 'BepInEx/plugins/BSports/grails_prefabloader/mlbstars24.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'MLB 2025 Pristine', filename: 'MLB 2025 Pristine-935-1-1775872852.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/mlbpristine25'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/mlbpristine25', 'BepInEx/plugins/BSports/grails_prefabloader/mlbpristine25.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'MLS 2024 Chrome', filename: 'MLS 2024 Chrome-935-2-1775340810.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/mls2024chrome'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/mls2024chrome', 'BepInEx/plugins/BSports/grails_prefabloader/mls2024chrome.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NASCAR 2021 Panini Prism', filename: 'NASCAR 2021 Panini Prism-935-1-1776363298.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/21nascarpaniniprism'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/21nascarpaniniprism', 'BepInEx/plugins/BSports/grails_prefabloader/21nascarpaniniprism.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NBA 2020 Panini Prism', filename: 'NBA 2020 Panini Prism-935-2-1775487465.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/pan2020nba'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/pan2020nba', 'BepInEx/plugins/BSports/grails_prefabloader/pan2020nba.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NBA 2021 Origins Pt1', filename: 'NBA 2021 Origins Pt1-935-1-1776047737.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/2021 origins nba'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/2021 origins nba', 'BepInEx/plugins/BSports/grails_prefabloader/2021 origins nba.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NBA 2023 Court Kings', filename: 'NBA 2023 Court Kings-935-1-1776243881.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/courtking23'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/courtking23', 'BepInEx/plugins/BSports/grails_prefabloader/courtking23.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NBA 2024 Panini Signatures', filename: 'NBA 2024 Panini Signatures-935-1-1775351313.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/paninisig24nba'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/paninisig24nba', 'BepInEx/plugins/BSports/grails_prefabloader/paninisig24nba.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NFL 2023 Chrome Topps', filename: 'NFL 2023 Chrome Topps 1974-935-1-1774959950.rar', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/20231973_prefabloader/2024nfl1954'], installedFiles: ['BepInEx/plugins/BSports/20231973_prefabloader/2024nfl1954', 'BepInEx/plugins/BSports/20231973_prefabloader/2024nfl1954.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NFL 2023 National Treasures', filename: 'NFL 2023 National Treasures-935-1-1775662093.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/23nt'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/23nt', 'BepInEx/plugins/BSports/grails_prefabloader/23nt.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NFL 2024 Panini Origins (Part One)', filename: 'NFL 2024 Panini Origins (Part One)-935-1-1775813662.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/nfl24origins'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/nfl24origins', 'BepInEx/plugins/BSports/grails_prefabloader/nfl24origins.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NFL 2024 Veteran Signatures', filename: 'NFL 2024 Veteran Signatures-935-1-1775607986.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/vet24sig'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/vet24sig', 'BepInEx/plugins/BSports/grails_prefabloader/vet24sig.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NFL 2025 Donruss Optic', filename: 'NFL 2025 Donruss Optic-935-1-1775326147.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/donruss25'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/donruss25', 'BepInEx/plugins/BSports/grails_prefabloader/donruss25.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NFL 2025 Panini Phoenix', filename: 'NFL 2025 Panini Phoenix-935-1-1776117700.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/2025 panini phoenix'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/2025 panini phoenix', 'BepInEx/plugins/BSports/grails_prefabloader/2025 panini phoenix.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NFL 2025 Rookie and Stars', filename: 'NFL 2025 Rookie and Stars-935-1-1776282508.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/25rookiesandstars'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/25rookiesandstars', 'BepInEx/plugins/BSports/grails_prefabloader/25rookiesandstars.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NHL 2023 Upper Deck', filename: 'NHL 2023 Upper Deck-935-1-1775926906.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/nhl2023ud'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/nhl2023ud', 'BepInEx/plugins/BSports/grails_prefabloader/nhl2023ud.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NHL 2025 Encore', filename: 'NHL 2025 Encore-935-1-1775662122.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/nhl25en'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/nhl25en', 'BepInEx/plugins/BSports/grails_prefabloader/nhl25en.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NHL 2025 Upper Deck', filename: 'NHL 2025 Upper Deck-935-1-1775556087.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/nhlud25v2'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/nhlud25v2', 'BepInEx/plugins/BSports/grails_prefabloader/nhlud25v2.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'NHL 2025 Upper Deck Black Diamond', filename: 'NHL 2025 Upper Deck Black Diamond-935-1-1775111399.rar', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/nhlbd25'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/nhlbd25', 'BepInEx/plugins/BSports/grails_prefabloader/nhlbd25.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'No Collision Placement', filename: 'No Collision Placement V1.0.1-973-2-1776583552.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/NoCollisionPlacement/NoCollisionPlacement.dll'], installedFiles: ['BepInEx/plugins/NoCollisionPlacement/NoCollisionPlacement.dll', 'BepInEx/plugins/NoCollisionPlacement/README.txt'] },
      { name: 'PackContentsEnhanced', filename: 'PackContentsEnhanced-91-1-2-2-1776673242.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/PackContentsEnhanced.dll'], installedFiles: ['BepInEx/plugins/PackContentsEnhanced.dll'] },
      { name: 'PGA 2024 Panini Prism', filename: 'PGA 2024 Panini Prism-935-1-1775507031.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/pga24prism'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/pga24prism', 'BepInEx/plugins/BSports/grails_prefabloader/pga24prism.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'Phone Overhaul', filename: 'Phone - Overhaul-685-1-7-1-1776562945.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Phone - Overhaul/Phone - Overhaul.dll'], installedFiles: ['BepInEx/config/munch.PhoneOverhaul.cfg', 'BepInEx/plugins/Phone - Overhaul/App Images/Example - Use Appid/Icon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Example - Use Appid/Inner BG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Example - Use Appid/Outer BG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/PTCGO_Discord/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/PTCGO_Discord/Icon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/PTCGO_Discord/OBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Wallpapers/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Wallpapers/Icon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/Wallpapers/OBG.png', 'BepInEx/plugins/Phone - Overhaul/Base Game Assets/panel-inside_white.png', 'BepInEx/plugins/Phone - Overhaul/Base Game Assets/panel-inside_yellow.png', 'BepInEx/plugins/Phone - Overhaul/Phone - Overhaul.dll', 'BepInEx/plugins/Phone - Overhaul/v*.txt', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/Default/Black Hole.mp4', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/Default/Black Hole_Icon.png', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/Default/Icon.png', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Ash.mp4', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Ash_Icon.png', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Bulbasaur.mp4', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Bulbasaur_Icon.png', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Gengar.mp4', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Gengar_Icon.png', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/icon.png', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Mega Charizard.mp4', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Mega Charizard_Icon.png', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Mega Rayquaza.mp4', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Mega Rayquaza_Icon.png', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Pikachu.mp4', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Pikachu_Icon.png', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/RayquazaCloud.mp4', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/RayquazaCloud_Icon.png', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Squirtle.mp4', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Squirtle_Icon.png', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Umbreon.mp4', 'BepInEx/plugins/Phone - Overhaul/Wallpaper/PTCGO_Pokemon/Umbreon_Icon.png'] },
      { name: 'PokedoodlesCards', filename: 'PokedoodlesCards-934-2-1775771686.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Pokedoodles Cards/01 - No-Scalp Series_prefabloader/pokedoodlesnoscalp'], installedFiles: ['BepInEx/plugins/Binder Overhaul/Pokedoodles/Pokedoodles.txt', 'BepInEx/plugins/Phone - Overhaul/App Images/PokedoodlesShop/PDShopIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/PokedoodlesShop/PDShopInnerBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/PokedoodlesShop/PDShopOuterBG.png', 'BepInEx/plugins/Pokedoodles Cards/01 - No-Scalp Series_prefabloader/pokedoodlesnoscalp', 'BepInEx/plugins/Pokedoodles Cards/01 - No-Scalp Series_prefabloader/pokedoodlesnoscalp.json'] },
      { name: 'Pokemon SV01 SV Base Set addon by DraX', filename: 'Pokemon SV01 - SV Base Set addon by DraX-902-1-0-1776478555.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-01svi'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-01svi', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-01svi.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'Pokemon SV02 Paldea Evolved addon by DraX', filename: 'Pokemon SV02 - Paldea Evolved addon by DraX-902-1-0-1776478599.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-02pal'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-02pal', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-02pal.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'Pokemon SV03 Obsidian Flame addon by DraX', filename: 'Pokemon SV03 - Obsidian Flame addon by DraX-902-1-0-1776478640.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-03obf'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-03obf', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-03obf.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'Pokemon SV04 151 addon by DraX', filename: 'Pokemon SV04 - 151 addon by DraX-902-1-0-1776478686.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-04mew'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-04mew', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-04mew.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'Pokemon SV05 Paradox Rift addon by DraX', filename: 'Pokemon SV05 - Paradox Rift addon by DraX-902-1-0-1776478731.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-05par'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-05par', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-05par.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'Pokemon SV06 Paldean Fates addon by DraX', filename: 'Pokemon SV06 - Paldean Fates addon by DraX-902-1-0-1776478765.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-06paf'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-06paf', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-06paf.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'Pokemon SV07 Temporal Forces addon by DraX', filename: 'Pokemon SV07 - Temporal Forces addon by DraX-902-1-0-1776478809.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-07tef'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-07tef', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-07tef.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'Pokemon SV08 Twilight Masquerade addon by DraX', filename: 'Pokemon SV08 - Twilight Masquerade addon by DraX-902-1-0-1776478847.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-08twm'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-08twm', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-08twm.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'Pokemon SV09 Shrouded Fable addon by DraX', filename: 'Pokemon SV09 - Shrouded Fable addon by DraX-902-1-0-1776478877.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-09sfa'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-09sfa', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-09sfa.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'Pokemon SV10 Stellar Crown addon by DraX', filename: 'Pokemon SV10 - Stellar Crown addon by DraX-902-1-0-1776478930.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-10scr'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-10scr', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-10scr.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'Pokemon SV11 Surging Sparks addon by DraX', filename: 'Pokemon SV11 - Surging Sparks addon by DraX-902-1-0-1776478978.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-11ssp'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-11ssp', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-11ssp.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'Pokemon SV12 Prismatic Evolutions addon by DraX', filename: 'Pokemon SV12 - Prismatic Evolutions addon by DraX-902-1-0-1776479023.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-12pre'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-12pre', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-12pre.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'Pokemon SV13 Journey Together addon by DraX', filename: 'Pokemon SV13 - Journey Together addon by DraX-902-1-0-1776479063.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-13jtg'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-13jtg', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-13jtg.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'Pokemon SV14 Destined Rivals addon by DraX', filename: 'Pokemon SV14 - Destined Rivals addon by DraX-902-1-0-1776479111.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-14dri'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-14dri', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-14dri.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'Pokemon SV15 Black Bolt addon by DraX', filename: 'Pokemon SV15 - Black Bolt addon by DraX-902-1-0-1776479162.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-15blk'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-15blk', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-15blk.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'Pokemon SV16 White Flare addon by DraX', filename: 'Pokemon SV16 - White Flare addon by DraX-902-1-0-1776479207.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-16wht'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-16wht', 'BepInEx/plugins/DXCardSets_prefabLoader/dxpokemon-sv-16wht.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXPokemon/DraX\'s Pokestop.png'] },
      { name: 'PokemonTCGCoopEnhancer', filename: 'PokemonTCGCoopEnhancer-904-1-1773563624.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/PokemonTCGCoopEnhancer.dll'], installedFiles: ['BepInEx/plugins/PokemonTCGCoopEnhancer.dll'] },
      { name: 'Premier League 2022 Panini Select', filename: 'Premier League 2022 Panini Select-935-1-1776667372.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/22paniislectpremleague'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/22paniislectpremleague', 'BepInEx/plugins/BSports/grails_prefabloader/22paniislectpremleague.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'Premier League 2026 Topps Chrome', filename: 'Premier League 2026 Topps Chrome-935-1-1775543811.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/premleague26'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/premleague26', 'BepInEx/plugins/BSports/grails_prefabloader/premleague26.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'PTCGO Economics', filename: 'PTCGO Economics-958-1-5-0-1776503872.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/PTCGO Economics/PTCGO Economics.dll'], installedFiles: ['BepInEx/plugins/PTCGO Economics/PTCGO Economics.dll', 'BepInEx/plugins/PTCGO Economics/v*.txt'] },
      { name: 'PTCGO Extras', filename: 'PTCGO Extras-905-1-4-0-1776563127.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/PTCGO Extras/PTCGO Extras.dll'], installedFiles: ['BepInEx/plugins/PTCGO Extras/PTCGO Extras.dll', 'BepInEx/plugins/PTCGO Extras/v*.txt'] },
      { name: 'PTCGO IitzSamurai Compatibility Patch', filename: 'PTCGO IitzSamurai Compatibility Patch V1.0.0-968-1-0-0-1776570141.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Binder Overhaul/PTCGO/Base Set.txt'], installedFiles: ['BepInEx/plugins/Binder Overhaul/PTCGO/Base Set.txt', 'BepInEx/plugins/Binder Overhaul/PTCGO/BaseSet2.txt', 'BepInEx/plugins/Binder Overhaul/PTCGO/Fossil.txt', 'BepInEx/plugins/Binder Overhaul/PTCGO/Gym Challenge.txt', 'BepInEx/plugins/Binder Overhaul/PTCGO/Gym Heroes.txt', 'BepInEx/plugins/Binder Overhaul/PTCGO/Jungle.txt', 'BepInEx/plugins/Binder Overhaul/PTCGO/Neo Discovery.txt', 'BepInEx/plugins/Binder Overhaul/PTCGO/Neo Genesis.txt', 'BepInEx/plugins/Binder Overhaul/PTCGO/Neo Revelation.txt', 'BepInEx/plugins/Binder Overhaul/PTCGO/Team Rockett.txt', 'BepInEx/plugins/Binder Overhaul/PTCGO/WoTC Promos.txt', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/01 - Base Set_prefabloader/basesetexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/02 - 1st Edition Base Set_prefabloader/1steditionbasesetexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/03 - Jungle_prefabloader/jungleexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/04 - 1st Edition Jungle_prefabloader/1steditionjungleexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/05 - Fossil_prefabloader/fossilexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/06 - 1st Edition Fossil_prefabloader/1steditionfossilexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/07 - Base Set 2_prefabloader/baseset2expansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/08 - Team Rocket_prefabloader/teamrocketexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/09 - 1st Edition Team Rocket_prefabloader/1steditionteamrocketexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/01 - Base/10 - Wotc Promos_prefabloader/wotcpromos.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/02 - Gym/01 - Gym Heroes_prefabloader/gymheroesexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/02 - Gym/02 - 1st Edition Gym Heroes_prefabloader/1steditiongymheroesexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/02 - Gym/03 - Gym Challenge_prefabloader/gymchallengeexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/02 - Gym/04 - 1st Edition Gym Challenge_prefabloader/1steditiongymchallengeexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/01 - Neo Genesis_prefabloader/neogenesisexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/02 - 1st Edition Neo Genesis_prefabloader/1steditionneogenesisexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/03 - Neo Discovery_prefabloader/neodiscoveryexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/04 - 1st Edition Neo Discovery_prefabloader/1steditionneodiscoveryexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/05 - Neo Revelation_prefabloader/neorevelationexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/06 - 1st Edition Neo Revelation_prefabloader/1steditionneorevelationexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/07 - Neo Destiny_prefabloader/neodestinyexpansion.json', 'BepInEx/plugins/Real TCG Overhaul/Pokemon Cards/03 - Neo/08 - 1st Edition Neo Destiny_prefabloader/1steditionneodestinyexpansion.json', 'BepInEx/plugins/Shop Overhaul/PTCGO/01 - Base Set/Base Set Expansions.txt', 'BepInEx/plugins/Shop Overhaul/PTCGO/01 - Base Set/Fossil Expansions.txt', 'BepInEx/plugins/Shop Overhaul/PTCGO/01 - Base Set/Jungle Expansions.txt', 'BepInEx/plugins/Shop Overhaul/PTCGO/01 - Base Set/Team Rocket Expansion.txt', 'BepInEx/plugins/Shop Overhaul/PTCGO/02 - Gym/Gym Challenge Expansions.txt', 'BepInEx/plugins/Shop Overhaul/PTCGO/02 - Gym/Gym Heroes Expansions.txt', 'BepInEx/plugins/Shop Overhaul/PTCGO/03 - Neo/Neo Destiny Expansions.txt', 'BepInEx/plugins/Shop Overhaul/PTCGO/03 - Neo/Neo Discovery Expansions.txt', 'BepInEx/plugins/Shop Overhaul/PTCGO/03 - Neo/Neo Genesis Expansions.txt', 'BepInEx/plugins/Shop Overhaul/PTCGO/03 - Neo/Neo Revelation Expansions.txt', 'BepInEx/plugins/Shopping List Overhaul/PTCGO/01 - Base Set/Base Set Expansions.txt', 'BepInEx/plugins/Shopping List Overhaul/PTCGO/01 - Base Set/Fossil Expansions.txt', 'BepInEx/plugins/Shopping List Overhaul/PTCGO/01 - Base Set/Jungle Expansions.txt', 'BepInEx/plugins/Shopping List Overhaul/PTCGO/01 - Base Set/Team Rocket Expansions.txt', 'BepInEx/plugins/Shopping List Overhaul/PTCGO/02 - Gym/Gym Challenge Expansions.txt', 'BepInEx/plugins/Shopping List Overhaul/PTCGO/02 - Gym/Gym Heroes Expansions.txt', 'BepInEx/plugins/Shopping List Overhaul/PTCGO/03 - Neo/Neo Destiny Expansions.txt', 'BepInEx/plugins/Shopping List Overhaul/PTCGO/03 - Neo/Neo Discovery Expansions.txt', 'BepInEx/plugins/Shopping List Overhaul/PTCGO/03 - Neo/Neo Genesis Expansions.txt', 'BepInEx/plugins/Shopping List Overhaul/PTCGO/03 - Neo/Neo Revelation Expansions.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/01 - Base/WoTC Promos.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/01 - Base Set/1stEditionBaseSet.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/01 - Base Set/1stEditionFossil.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/01 - Base Set/1stEditionJungle.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/01 - Base Set/1stEditionTeamRocket.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/01 - Base Set/BaseSet.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/01 - Base Set/BaseSet2.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/01 - Base Set/Fossil.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/01 - Base Set/Jungle.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/01 - Base Set/TeamRocket.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/02 - Gym/1stEditionGymChallenge.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/02 - Gym/1stEditionGymHeroes.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/02 - Gym/GymChallenge.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/02 - Gym/GymHeroes.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/03 - Neo/1stEditionNeoDestiny.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/03 - Neo/1stEditionNeoDiscovery.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/03 - Neo/1stEditionNeoGenesis.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/03 - Neo/1stEditionNeoRevelation.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/03 - Neo/NeoDestiny.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/03 - Neo/NeoDiscovery.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/03 - Neo/NeoGenesis.txt', 'BepInEx/plugins/Trading Overhaul/PTCGO/03 - Neo/NeoRevelation.txt'] },
      { name: 'RCE Shop Sign', filename: 'RCE Shop Sign-916-1-0-1773990910.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/TextureReplacer/objects_textures/Billboard.png'], installedFiles: ['BepInEx/plugins/TextureReplacer/objects_textures/Billboard.png'] },
      { name: 'Road Trip Adventure Cards', filename: 'Road Trip Adventure Cards-927-2-1775772261.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Road Trip Adventure Cards/01 - Road Trip Adventure_prefabloader/roadtripadventurecards'], installedFiles: ['BepInEx/plugins/Binder Overhaul/RoadTripAdventure/Road Trip Adventure.txt', 'BepInEx/plugins/Phone - Overhaul/App Images/RTA Qs Shop/QsShopIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/RTA Qs Shop/QsShopInnerBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/RTA Qs Shop/QsShopOuterBG.png', 'BepInEx/plugins/Road Trip Adventure Cards/01 - Road Trip Adventure_prefabloader/roadtripadventurecards', 'BepInEx/plugins/Road Trip Adventure Cards/01 - Road Trip Adventure_prefabloader/roadtripadventurecards.json', 'BepInEx/plugins/Road Trip Adventure Cards/02 - Road Trip Adventure First Edition_prefabloader/roadtripadventurefirsteditioncards', 'BepInEx/plugins/Road Trip Adventure Cards/02 - Road Trip Adventure First Edition_prefabloader/roadtripadventurefirsteditioncards.json'] },
      { name: 'RTCGO Custom TV', filename: 'RTCGO Custom TV-895-1-4-1776621055.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Extra/deno.exe'], installedFiles: ['BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Extra/deno.exe', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Extra/ffmpeg.exe', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Extra/yt-dlp.exe', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/README.txt', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/RTCGOCustomScripts.dll', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/rtcgocustomtv', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/rtcgocustomtv.json', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Videos/01 Default.mp4'] },
      { name: 'Schwarz Shelves Set', filename: 'Schwarz Shelves Set-908-1-0-1-1773610474.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/ElmaSchwarzExpansions/ShelvingSet_prefabloader/shelvingaddon'], installedFiles: ['BepInEx/plugins/ElmaSchwarzExpansions/ShelvingSet_prefabloader/shelvingaddon', 'BepInEx/plugins/ElmaSchwarzExpansions/ShelvingSet_prefabloader/shelvingaddon.json'] },
      { name: 'Schwarz Shop App Data', filename: 'Schwarz Shop App Data-909-1-0-0-1773605389.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Phone - Overhaul/App Images/SchwarzShop/SSIcon.png'], installedFiles: ['BepInEx/plugins/Phone - Overhaul/App Images/SchwarzShop/SSIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/SchwarzShop/SSInnerBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/SchwarzShop/SSOuterBG.png'] },
      { name: 'Self Checkout', filename: 'Self Checkout-922-1-3-1774230637.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/SelfCheckout.dll'], installedFiles: ['BepInEx/plugins/SelfCheckout.dll'] },
      { name: 'Soccer 2023 Panini Obsidan', filename: 'Soccer 2023 Panini Obsidan-935-1-1776670516.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/2023paniniprismso'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/2023paniniprismso', 'BepInEx/plugins/BSports/grails_prefabloader/2023paniniprismso.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'TCGSimPC', filename: 'TCGSimPC-946-4-1775269967.zip', targetKey: 'plugins', core: false, signature: ['BepInEx/plugins/TCGSimPC.dll'], installedFiles: ['BepInEx/plugins/TCGSimPC.dll'] },
      { name: 'Tennis 2024 Topps Chrome', filename: 'Tennis 2024 Topps Chrome-935-1-1775878083.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/tennis24chrome'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/tennis24chrome', 'BepInEx/plugins/BSports/grails_prefabloader/tennis24chrome.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'TextureReplacer', filename: 'TextureReplacer 1.6.0-69-1-6-0-1776185937.zip', targetKey: 'plugins', core: false, signature: ['BepInEx/plugins/TextureReplacer/TextureReplacer.dll'], installedFiles: ['BepInEx/plugins/TextureReplacer/TextureReplacer.dll'] },
      { name: 'UFC 2022 Donruss Optic', filename: 'UFC 2022 Donruss Optic-935-1-1776491878.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/ufcoptic22'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/ufcoptic22', 'BepInEx/plugins/BSports/grails_prefabloader/ufcoptic22.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'UFC 2023 Panini Prism', filename: 'UFC 2023 Panini Prism-935-2-1775841030.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/ufcpan23'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/ufcpan23', 'BepInEx/plugins/BSports/grails_prefabloader/ufcpan23.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'UFC 2023 Panini Select', filename: 'UFC 2023 Panini Select-935-1-1775813632.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/ufc24panselect'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/ufc24panselect', 'BepInEx/plugins/BSports/grails_prefabloader/ufc24panselect.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'UnionArenamod', filename: 'UnionArenamod-756-1-3-3-1776228219.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/UnionArenaJP__prefabLoader/un2.5dimension'], installedFiles: ['BepInEx/plugins/Phone - Overhaul/App Images/UnionArena/UNIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/UnionArena/UNInnerBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/UnionArena/UNOuterBG.png', 'BepInEx/plugins/UnionArenaJP__prefabLoader/un2.5dimension', 'BepInEx/plugins/UnionArenaJP__prefabLoader/un2.5dimension.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/ungakuenimastcg', 'BepInEx/plugins/UnionArenaJP__prefabLoader/ungakuenimastcg.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/ungameratcg', 'BepInEx/plugins/UnionArenaJP__prefabLoader/ungameratcg.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unimasshinytcg', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unimasshinytcg.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unkamenridertcg', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unkamenridertcg.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unkinnikumantcg', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unkinnikumantcg.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unmadokatcg', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unmadokatcg.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unmatoslavetcg', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unmatoslavetcg.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unmonogataritcg', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unmonogataritcg.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unnikketcg', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unnikketcg.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unshytcg', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unshytcg.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unsololeveling', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unsololeveling.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unsyndualitynoirtcg', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unsyndualitynoirtcg.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/untalesarisetcg', 'BepInEx/plugins/UnionArenaJP__prefabLoader/untalesarisetcg.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/untoloverumod', 'BepInEx/plugins/UnionArenaJP__prefabLoader/untoloverumod.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unundeadulmod', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unundeadulmod.json', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unwinnerpack', 'BepInEx/plugins/UnionArenaJP__prefabLoader/unwinnerpack.json'] },
      { name: 'Unset 1 Unglued', filename: 'Unset 1 - Unglued-900-1-1-1776041954.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxmtg-ugl'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxmtg-ugl', 'BepInEx/plugins/DXCardSets_prefabLoader/dxmtg-ugl.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXMTG/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXMTG/MTG Shop Icon.png'] },
      { name: 'Unset 2 Unhinged', filename: 'Unset 2 - Unhinged-900-1-1-1776041988.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxmtg-unh'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxmtg-unh', 'BepInEx/plugins/DXCardSets_prefabLoader/dxmtg-unh.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXMTG/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXMTG/MTG Shop Icon.png'] },
      { name: 'Unset 3 Unstable', filename: 'Unset 3 - Unstable-900-1-1-1776042044.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxmtg-unt'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxmtg-unt', 'BepInEx/plugins/DXCardSets_prefabLoader/dxmtg-unt.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXMTG/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXMTG/MTG Shop Icon.png'] },
      { name: 'Unset 4 Unfinity', filename: 'Unset 4 - Unfinity-900-1-1-1776042190.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxmtg-unf'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxmtg-unf', 'BepInEx/plugins/DXCardSets_prefabLoader/dxmtg-unf.json', 'BepInEx/plugins/Phone - Overhaul/App Images/DXMTG/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/DXMTG/MTG Shop Icon.png'] },
      { name: 'Utility Menu', filename: 'Utility Menu-890-3-0-3-1775790415.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Ultimate Menu.dll'], installedFiles: ['BepInEx/plugins/Ultimate Menu.dll'] },
      { name: 'Vanilla Collection Tracker', filename: 'Vanilla Collection Tracker-899-1-5-1773714590.7z', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/BaseGameCollection/BaseGameCollections.dll'], installedFiles: ['BepInEx/plugins/Phone - Overhaul/App Images/com.bgc.collections/Icon.png', 'BepInEx/plugins/BaseGameCollection/BaseGameCollections.dll'] },
      { name: 'Wall Grids', filename: 'Wall Grids-889-1-0-5-1775701730.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Wall Grids/Wall Grids.dll'], installedFiles: ['BepInEx/plugins/Wall Grids/v*.txt', 'BepInEx/plugins/Wall Grids/Wall Grids.dll'] },
      { name: 'WNBA 2024 Panini Donruss', filename: 'WNBA 2024 Panini Donruss-935-1-1775874712.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/wnba24donruss'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/wnba24donruss', 'BepInEx/plugins/BSports/grails_prefabloader/wnba24donruss.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'Worker AI Overhaul', filename: 'Worker AI Overhaul-924-1-3-8-1776504231.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Worker AI Overhaul/Worker AI Overhaul.dll'], installedFiles: ['BepInEx/plugins/Worker AI Overhaul/v*.txt', 'BepInEx/plugins/Worker AI Overhaul/Worker AI Overhaul.dll'] },
      { name: 'Worker Shelf Access', filename: 'Worker Shelf Access-926-1-1774390885.zip', targetKey: 'plugins', core: false, signature: ['BepInEx/plugins/WorkerShelfAccess/ShelfWorkerAccess.dll'], installedFiles: ['BepInEx/plugins/ShelfWorkerAccess.dll'] },
      { name: 'WWE 2023 Panini Prism', filename: 'WWE 2023 Panini Prism-935-1-1776303037.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/wwe23prism'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/wwe23prism', 'BepInEx/plugins/BSports/grails_prefabloader/wwe23prism.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: 'WWE 2024 Panini National Treasures', filename: 'WWE 2024 Panini National Treasures-935-1-1775376848.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BSports/grails_prefabloader/wwe24nt'], installedFiles: ['BepInEx/plugins/BSports/grails_prefabloader/wwe24nt', 'BepInEx/plugins/BSports/grails_prefabloader/wwe24nt.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BSports/SportLogo.png'] },
      { name: '003 Lorcana Into the Inklands addon by DraX', filename: '003 Lorcana - Into the Inklands addon by DraX-750-1-0-1775961648.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-003'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-003', 'BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-003.json'] },
      { name: '004 Lorcana Ursula\'s Return addon by DraX', filename: '004 Lorcana - Ursula\'s Return addon by DraX-750-1-0-1775961674.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-004'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-004', 'BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-004.json'] },
      { name: '005 Lorcana Shimmering Skies addon by DraX', filename: '005 Lorcana - Shimmering Skies addon by DraX-750-1-0-1775961707.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-005'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-005', 'BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-005.json'] },
      { name: '006 Lorcana Azurite Sea addon by DraX', filename: '006 Lorcana - Azurite Sea addon by DraX-750-1-0-1775961740.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-006'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-006', 'BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-006.json'] },
      { name: '007 Lorcana Archazia\'s Island addon by DraX', filename: '007 Lorcana - Archazia\'s Island addon by DraX-750-1-0-1775961771.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-007'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-007', 'BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-007.json'] },
      { name: '008 Lorcana Reign of Jafar addon by DraX', filename: '008 Lorcana - Reign of Jafar addon by DraX-750-1-0-1775961881.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-008'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-008', 'BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-008.json'] },
      { name: '009 Lorcana Fabled addon by DraX', filename: '009 Lorcana - Fabled addon by DraX-750-1-0-1775961925.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-009'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-009', 'BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-009.json'] },
      { name: '01 Alpha addon by DraX', filename: '01 - Alpha addon by DraX-814-1-0-1776304262.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxsorcery-01alp'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxsorcery-01alp', 'BepInEx/plugins/DXCardSets_prefabLoader/dxsorcery-01alp.json'] },
      { name: '01 Dawn Of Ashes addon by DraX', filename: '01 - Dawn Of Ashes addon by DraX-759-1-0-1776219114.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-01doa1st'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-01doa1st', 'BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-01doa1st.json'] },
      { name: '01 MetaZoo Base Set', filename: '01 - MetaZoo Base Set-794-1-0-1776041772.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxmetazoo-mz1'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxmetazoo-mz1', 'BepInEx/plugins/DXCardSets_prefabLoader/dxmetazoo-mz1.json'] },
      { name: '010 Lorcana Whispers in the Well addon by DraX', filename: '010 Lorcana - Whispers in the Well addon by DraX-750-1-0-1775961980.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-010'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-010', 'BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-010.json'] },
      { name: '011 Lorcana Winterspell addon by DraX', filename: '011 Lorcana - Winterspell addon by DraX-750-1-0-1775962019.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-011'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-011', 'BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-011.json'] },
      { name: '02 Arthurian Legends addon by DraX', filename: '02 - Arthurian Legends addon by DraX-814-1-0-1776304295.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxsorcery-02art'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxsorcery-02art', 'BepInEx/plugins/DXCardSets_prefabLoader/dxsorcery-02art.json'] },
      { name: '02 Fractured Crown addon by DraX', filename: '02 - Fractured Crown addon by DraX-759-1-0-1776219147.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-02ftc'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-02ftc', 'BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-02ftc.json'] },
      { name: '02 Torrential Tides', filename: '02 - Torrential Tides-794-1-0-1776041825.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxmetazoo-mz2'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxmetazoo-mz2', 'BepInEx/plugins/DXCardSets_prefabLoader/dxmetazoo-mz2.json'] },
      { name: '03 Alchemical Revolution addon by DraX', filename: '03 - Alchemical Revolution addon by DraX-759-1-0-1776219177.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-03alc'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-03alc', 'BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-03alc.json'] },
      { name: '03 Gothic addon by DraX', filename: '03 - Gothic addon by DraX-814-1-0-1776304378.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxsorcery-03got'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxsorcery-03got', 'BepInEx/plugins/DXCardSets_prefabLoader/dxsorcery-03got.json'] },
      { name: '03 Secret Shadows', filename: '03 - Secret Shadows-794-1-0-1776041870.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxmetazoo-mz3'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxmetazoo-mz3', 'BepInEx/plugins/DXCardSets_prefabLoader/dxmetazoo-mz3.json'] },
      { name: '04 Mercurial Heart', filename: '04 - Mercurial Heart-759-1-0-1776219223.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-04mrc'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-04mrc', 'BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-04mrc.json'] },
      { name: '05 Mortal Ambition addon by DraX', filename: '05 - Mortal Ambition addon by DraX-759-1-0-1776219288.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-05amb'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-05amb', 'BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-05amb.json'] },
      { name: '06 Abyssal Heaven addon by DraX', filename: '06 - Abyssal Heaven addon by DraX-759-1-0-1776219340.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-06hvn'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-06hvn', 'BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-06hvn.json'] },
      { name: '07 Distorted Reflections addon by DraX', filename: '07 - Distorted Reflections addon by DraX-759-1-0-1776219367.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-07dtr'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-07dtr', 'BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-07dtr.json'] },
      { name: '08 Phantom Monarchs addon by DraX', filename: '08 - Phantom Monarchs addon by DraX-759-1-0-1776219398.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-08ptm'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-08ptm', 'BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-08ptm.json'] },
      { name: '09 Radiant Origins addon by DraX', filename: '09 - Radiant Origins addon by DraX-759-1-0-1776562912.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-09rdo'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-09rdo', 'BepInEx/plugins/DXCardSets_prefabLoader/dxgrandarchive-09rdo.json'] },
      { name: '240 Standing Card Display by DraX', filename: '240 Standing Card Display by DraX-838-1-8-1776052780.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXFurnitureDecor_prefabLoader/240cardstand'], installedFiles: ['BepInEx/plugins/DXFurnitureDecor_prefabLoader/240cardstand', 'BepInEx/plugins/DXFurnitureDecor_prefabLoader/240cardstand.json'] },
      { name: 'ArcadeMon', filename: 'ArcadeMon-860-1-2d-1773562389.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/patchers/ArcadeCabinet_prefabLoader/Objects_arcade.json'], installedFiles: ['BepInEx/patchers/ArcadeCabinet_prefabLoader/Objects_arcade.json', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeCabinet.dll', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeCabinet_prefabLoader/arcade.json', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/IdleVideos/videoplayback.mp4', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/IdleVideos/videoplayback10.mp4', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/IdleVideos/videoplayback11.mp4', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/IdleVideos/videoplayback12.mp4', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/IdleVideos/videoplayback13.mp4', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/IdleVideos/videoplayback2.mp4', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/IdleVideos/videoplayback3.mp4', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/IdleVideos/videoplayback4.mp4', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/IdleVideos/videoplayback5.mp4', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/IdleVideos/videoplayback6.mp4', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/IdleVideos/videoplayback7.mp4', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/IdleVideos/videoplayback8.mp4', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/IdleVideos/videoplayback9.mp4', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/Libretro/bios/.keep', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/Libretro/cores/.keep', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/Libretro/roms/gb/.keep', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/Libretro/roms/gba/.keep', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/Libretro/roms/gbc/.keep', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/Libretro/roms/genesis/.keep', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/Libretro/roms/mame/.keep', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/Libretro/roms/mastersystem/.keep', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/Libretro/roms/n64/.keep', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/Libretro/roms/nes/.keep', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/Libretro/roms/snes/.keep', 'BepInEx/plugins/ArcadeCabinetMod/ArcadeRuntime/Libretro/saves/.keep'] },
      { name: 'Arknights TCG Main File', filename: 'Arknights TCG Main File-868-1-1-2-1775038159.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Arknights_prefabloader/arknights'], installedFiles: ['BepInEx/plugins/Arknights_prefabloader/arknights', 'BepInEx/plugins/Arknights_prefabloader/arknights.json'] },
      { name: 'ArtExpander', filename: 'ArtExpander-417-3-8-1-1774887099.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/ArtExpander/ArtExpander.dll'], installedFiles: ['BepInEx/plugins/ArtExpander/ArtExpander.dll'] },
      { name: 'Auto Lights', filename: 'Auto Lights-467-0-3-0-1732658267.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/AutoLights.dll'], installedFiles: ['BepInEx/plugins/AutoLights.dll'] },
      { name: 'BepInEx Mod Manager', filename: 'BepInEx Mod Manager-698-1-2-1772153791.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BepInEx Mod Manager/BepInEx Mod Manager.dll'], installedFiles: ['BepInEx/plugins/BepInEx Mod Manager/BepInEx Mod Manager.dll', 'BepInEx/plugins/BepInEx Mod Manager/v*.txt', 'BepInEx/plugins/Phone - Overhaul/App Images/BepInEx Converter/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BepInEx Converter/Icon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BepInEx Converter/OBG.png'] },
      { name: 'Binder Overhaul', filename: 'Binder Overhaul-744-2-5-0-1776562841.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Binder Overhaul/Binder Overhaul.dll'], installedFiles: ['BepInEx/plugins/Binder Overhaul/Binder Overhaul.dll', 'BepInEx/plugins/Binder Overhaul/v*.txt'] },
      { name: 'BlazblueADWmod', filename: 'BlazblueADWmod-843-1-1-1-1775918470.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/BlazblueADW__prefabLoader/bbdwtcg'], installedFiles: ['BepInEx/plugins/BlazblueADW__prefabLoader/bbdwtcg', 'BepInEx/plugins/BlazblueADW__prefabLoader/bbdwtcg.json', 'BepInEx/plugins/Phone - Overhaul/App Images/BBDWShop/BlazblueIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BBDWShop/BlazblueInnerBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/BBDWShop/BlazblueOuterBG.png'] },
      { name: 'BlazblueVideo', filename: 'BlazblueVideo-843-1-0-1774469397.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Videos/BlazBlue - Calamity Trigger (Intro).mp4'], installedFiles: ['BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Videos/BlazBlue - Calamity Trigger (Intro).mp4', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Videos/BlazBlue - Continuum Shift (Intro).mp4', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Videos/Blazblue Alter Memory Opening.mp4', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Videos/BLAZBLUE ALTERNATIVE DARKWAR OP.mp4', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Videos/Blazblue Chrono Phantasma Opening.mp4', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Videos/BLAZBLUE CHRONOPHANTASMA EXTEND OP.mp4', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Videos/Blazblue Continuum Shift Extend Opening.mp4', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Videos/Blazblue Cross Tag Battle - Opening.mp4', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Videos/BlazBlue; Continuum Shift 2 - Opening.mp4', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Videos/BLAZBLUE　CENTRALFICTION  OP.mp4', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Videos/XBLAZE CODE：EMBRYO　OP.mp4', 'BepInEx/plugins/Real TCG Overhaul/RTCGO Custom Furniture/01 - Custom TV_prefabloader/Videos/XBLAZE LOST：MEMORIES OP.mp4'] },
      { name: 'BP01 AdventOfGenesis', filename: 'BP01-AdventOfGenesis-766-1-0-1776042541.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp01'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp01', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp01.json'] },
      { name: 'BP02 ReignOfBahamut', filename: 'BP02-ReignOfBahamut-766-1-0-1776042697.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp02'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp02', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp02.json'] },
      { name: 'BP03 FlameOfLaevateinn', filename: 'BP03-FlameOfLaevateinn-766-1-0-1776042693.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp03'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp03', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp03.json'] },
      { name: 'BP04 CosmicMythos', filename: 'BP04-CosmicMythos-766-1-0-1776042747.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp04'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp04', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp04.json'] },
      { name: 'BP05 OmensEternal', filename: 'BP05-OmensEternal-766-1-0-1776042761.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp05'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp05', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp05.json'] },
      { name: 'BP06 ParagonsOfTheColosseum', filename: 'BP06-ParagonsOfTheColosseum-766-1-0-1776043029.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp06'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp06', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp06.json'] },
      { name: 'BP07 VerdantSteel', filename: 'BP07-VerdantSteel-766-1-0-1776043031.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp07'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp07', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp07.json'] },
      { name: 'BP08 Alterchaotica', filename: 'BP08-Alterchaotica-766-1-0-1776043033.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp08'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp08', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp08.json'] },
      { name: 'BP09 DuetOfDawnAndDusk', filename: 'BP09-DuetOfDawnAndDusk-766-1-0-1776043034.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp09'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp09', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp09.json'] },
      { name: 'BP10 GodsOfTheArcana', filename: 'BP10-GodsOfTheArcana-766-1-0-1776043036.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp10'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp10', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp10.json'] },
      { name: 'BP11 BulletOfFate', filename: 'BP11-BulletOfFate-766-1-0-1776043038.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp11'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp11', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp11.json'] },
      { name: 'BP12 Worldreaver\'sDescent', filename: 'BP12-Worldreaver\'sDescent-766-1-0-1776043040.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp12'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp12', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp12.json'] },
      { name: 'BP13 DominionOfDarkness', filename: 'BP13-DominionOfDarkness-766-1-0-1776043042.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp13'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp13', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp13.json'] },
      { name: 'BP14 BanquetOfDreams', filename: 'BP14-BanquetOfDreams-766-1-0-1776043043.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp14'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp14', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp14.json'] },
      { name: 'BP15 TrialOfOmens', filename: 'BP15-TrialOfOmens-766-1-0-1776043046.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp15'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp15', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp15.json'] },
      { name: 'BP16 New World Genesis', filename: 'BP16-New World Genesis-766-1-0-1776043048.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp16'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp16', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-bp16.json'] },
      { name: 'BulkSelling', filename: 'BulkSelling-34-0-3-0-1756587142.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/BulkSelling.dll'], installedFiles: ['BepInEx/plugins/BulkSelling.dll'] },
      { name: 'Card Table Overhaul', filename: 'Card Table - Overhaul-626-2-3-3-1776562893.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Card Table Overhaul/Card Table Overhaul.dll'], installedFiles: ['BepInEx/plugins/Card Table Overhaul/Card Table Overhaul.dll', 'BepInEx/plugins/Card Table Overhaul/v*.txt'] },
      { name: 'Card Targeting AI Overhaul', filename: 'Card Targeting - AI Overhaul-646-2-0-0-1775701152.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Card Targeting AI Patch/Card Targeting AI Patch.dll'], installedFiles: ['BepInEx/plugins/Card Targeting AI Patch/Card Targeting AI Patch.dll', 'BepInEx/plugins/Card Targeting AI Patch/v*.txt'] },
      { name: 'CashOrCard', filename: 'CashOrCard 1.0.3-71-1-0-3-1764483371.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/CashOrCard.dll'], installedFiles: ['BepInEx/plugins/CashOrCard.dll'] },
      { name: 'CP01 Umamusume Pretty Derby', filename: 'CP01-Umamusume-Pretty Derby-769-1-0-1776128595.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-cp01'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-cp01', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-cp01.json'] },
      { name: 'CP02 THE IDOLMASTER CINDERELLA GIRLS', filename: 'CP02-THE IDOLMASTER CINDERELLA GIRLS-769-1-0-1776128641.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-cp02'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-cp02', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-cp02.json'] },
      { name: 'CP03 Cardfight Vanguard', filename: 'CP03-Cardfight Vanguard-769-1-0-1776128685.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-cp03'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-cp03', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-cp03.json'] },
      { name: 'Crouch Overhaul', filename: 'Crouch Overhaul-788-1-1-1772153420.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Crouch Overhaul/Crouch Overhaul.dll'], installedFiles: ['BepInEx/plugins/Crouch Overhaul/Crouch Overhaul.dll', 'BepInEx/plugins/Crouch Overhaul/v*.txt'] },
      { name: 'CustomTitleScreen', filename: 'CustomTitleScreen 1.1.0-738-1-1-0-1763717286.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/CustomTitleScreen/CustomTitleScreen.dll'], installedFiles: ['BepInEx/plugins/CustomTitleScreen/CustomTitleScreen.dll'] },
      { name: 'DeadmanCrossmod', filename: 'DeadmanCrossmod-842-1-1-1-1775918589.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DeadmanCross__prefabLoader/deadmancrosstcg'], installedFiles: ['BepInEx/plugins/DeadmanCross__prefabLoader/deadmancrosstcg', 'BepInEx/plugins/DeadmanCross__prefabLoader/deadmancrosstcg.json'] },
      { name: 'Digimon Card Game', filename: 'Digimon Card Game-885-1-0-1772713920.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HHDigimon_prefabLoader/digimons tcg'], installedFiles: ['BepInEx/plugins/HHDigimon_prefabLoader/digimons tcg', 'BepInEx/plugins/HHDigimon_prefabLoader/digimons tcg.json'] },
      { name: 'Draw Distance Overhaul', filename: 'Draw Distance Overhaul-699-1-2-1772153595.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Draw Distance Overhaul/Draw Distance Overhaul.dll'], installedFiles: ['BepInEx/plugins/Draw Distance Overhaul/Draw Distance Overhaul.dll', 'BepInEx/plugins/Draw Distance Overhaul/v*.txt'] },
      { name: 'ECP01 EX Umamusume Pretty Derby', filename: 'ECP01-EX-Umamusume-Pretty Derby-769-1-0-1776128721.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-ecp01'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-ecp01', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-ecp01.json'] },
      { name: 'Enhanced Number Formatter', filename: 'Enhanced Number Formatter-755-1-0-1-1767187581.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/EnhancedNumberFormatter.dll'], installedFiles: ['BepInEx/plugins/EnhancedNumberFormatter.dll'] },
      { name: 'EnhancedWorkbenches', filename: 'EnhancedWorkbenches-448-3-0-1-1768890990.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/EnhancedWorkbench/EnhancedWorkbenches.dll'], installedFiles: ['BepInEx/plugins/EnhancedWorkbench/EnhancedWorkbenches.dll'] },
      { name: 'Faster Interactions Overhaul', filename: 'Faster Interactions Overhaul-611-1-3-2-1772538263.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Faster Interactions/Faster Interactions.dll'], installedFiles: ['BepInEx/plugins/Faster Interactions/Faster Interactions.dll', 'BepInEx/plugins/Faster Interactions/v*.txt'] },
      { name: 'FECipher prefabloader', filename: 'FECipher_prefabloader-857-0-2-1771961432.7z', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/fireemblemtcg__prefabLoader/FECipher_prefabloader'], installedFiles: ['BepInEx/plugins/fireemblemtcg__prefabLoader/FECipher_prefabloader', 'BepInEx/patchers/fireemblemtcg__prefabLoader/Objects_fireemblemtcg.json', 'BepInEx/plugins/fireemblemtcg__prefabLoader/fireemblemtcg.json', 'BepInEx/plugins/Phone - Overhaul/App Images/FECipherShop/FECipherIcon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/FECipherShop/FECipherInner BG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/FECipherShop/FECipherOuter BG.png'] },
      { name: 'Final Fantasy TCG', filename: 'Final Fantasy TCG 1.1-785-1-1-1768936326.rar', targetKey: 'game_root', core: true, signature: ['BepInEx/patchers/HHFinalFantasy_prefabLoader/Objects_hhfinalfantasytc.json'], installedFiles: ['BepInEx/patchers/HHFinalFantasy_prefabLoader/Objects_hhfinalfantasytc.json', 'BepInEx/plugins/HHFinalFantasy_prefabLoader/hhfinalfantasytc.json', 'BepInEx/plugins/Phone - Overhaul/App Images/HHFFTCG_shop/mogpod_ff_crystal_circle_solid_v5.png', 'BepInEx/plugins/Phone - Overhaul/App Images/HHFFTCG_shop/TemplateInnerBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/HHFFTCG_shop/TemplateOuterBG.png'] },
      { name: 'Fog Be Gone', filename: 'Fog Be Gone-628-2-0-65-1765173710.7z', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/FogBeGone.dll'], installedFiles: ['BepInEx/plugins/FogBeGone.dll'] },
      { name: 'Furniture and Decor addon by DraX', filename: 'Furniture and Decor addon by DraX-838-1-7-1776047112.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXFurnitureDecor_prefabLoader/dxfurnituredecor'], installedFiles: ['BepInEx/plugins/DXFurnitureDecor_prefabLoader/dxfurnituredecor', 'BepInEx/plugins/DXFurnitureDecor_prefabLoader/dxfurnituredecor.json'] },
      { name: 'GundamArsenalmod', filename: 'GundamArsenalmod-837-1-3-1-1775922994.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/GundamAB__prefabLoader/gundamarsenalmod'], installedFiles: ['BepInEx/plugins/GundamAB__prefabLoader/gundamarsenalmod', 'BepInEx/plugins/GundamAB__prefabLoader/gundamarsenalmod.json'] },
      { name: 'IdolWarsmod', filename: 'IdolWarsmod-848-1-1-1-1775923189.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/IdolWars_prefabLoader/idolwarmod'], installedFiles: ['BepInEx/plugins/IdolWars_prefabLoader/idolwarmod', 'BepInEx/plugins/IdolWars_prefabLoader/idolwarmod.json'] },
      { name: 'Item Targeting AI Patch', filename: 'Item Targeting AI Patch-653-3-0-0-1775701385.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Item Targeting AI Patch/Item Targeting AI Patch.dll'], installedFiles: ['BepInEx/plugins/Item Targeting AI Patch/Item Targeting AI Patch.dll', 'BepInEx/plugins/Item Targeting AI Patch/v*.txt'] },
      { name: 'KRGanbaLegendsMod', filename: 'KRGanbaLegendsMod-757-1-6-1-1775919386.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/KRGanbaLegendsMod/_prefabLoader/ganbalegendstcg'], installedFiles: ['BepInEx/plugins/KRGanbaLegendsMod/_prefabLoader/ganbalegendstcg', 'BepInEx/plugins/KRGanbaLegendsMod/_prefabLoader/ganbalegendstcg.json'] },
      { name: 'Lorcana Enchanted Wall Displays by DraX', filename: 'Lorcana Enchanted Wall Displays by DraX-750-1-1-1771456621.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXFurnitureDecor_prefabLoader/dxechantedcase'], installedFiles: ['BepInEx/plugins/DXFurnitureDecor_prefabLoader/dxechantedcase', 'BepInEx/plugins/DXFurnitureDecor_prefabLoader/dxechantedcase.json'] },
      { name: 'Lorcana Rise of the Floodborn addon by DraX', filename: 'Lorcana - Rise of the Floodborn addon by DraX-750-1-0-1775961517.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-002'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-002', 'BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-002.json'] },
      { name: 'Lorcana The First Chapter addon by DraX', filename: 'Lorcana - The First Chapter addon by DraX-750-1-0-1775961407.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-001'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-001', 'BepInEx/plugins/DXCardSets_prefabLoader/dxlorcana-001.json'] },
      { name: 'Marvel VS.', filename: 'Marvel VS.-858-1-2-1-1775869376.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/MarvelVs_prefabloader/Cards.meta'], installedFiles: ['BepInEx/plugins/MarvelVs_prefabloader/Cards.meta', 'BepInEx/plugins/MarvelVs_prefabloader/Cards.meta.json'] },
      { name: 'medievalshelf', filename: 'medievalshelf-853-1-1-0-1771279503.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/patchers/medievalshelf_prefabloader/Objects_shelfbundle.json'], installedFiles: ['BepInEx/patchers/medievalshelf_prefabloader/Objects_shelfbundle.json', 'BepInEx/plugins/medievalshelf_prefabloader/shelfbundle.json'] },
      { name: 'Music Battlers EPL Pack', filename: 'Music Battlers EPL Pack-840-1-0-1-1773611291.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/ElmaSchwarzExpansions/MusicBattlersSet_prefabloader/musicbattlers'], installedFiles: ['BepInEx/plugins/ElmaSchwarzExpansions/MusicBattlersSet_prefabloader/musicbattlers', 'BepInEx/plugins/ElmaSchwarzExpansions/MusicBattlersSet_prefabloader/musicbattlers.json'] },
      { name: 'Night Manager', filename: 'Night Manager-852-1-1-0-1772335793.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Night Manager/NightManager.dll'], installedFiles: ['BepInEx/plugins/Night Manager/NightManager.dll'] },
      { name: 'NotForSale', filename: 'NotForSale-812-0-6-0-1769631959.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/NotForSale.dll'], installedFiles: ['BepInEx/plugins/NotForSale.dll'] },
      { name: 'OreCollectionMod', filename: 'OreCollectionMod-861-1-1-1-1775919477.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/OreCollection_prefabLoader/orecollectionmod'], installedFiles: ['BepInEx/plugins/OreCollection_prefabLoader/orecollectionmod', 'BepInEx/plugins/OreCollection_prefabLoader/orecollectionmod.json'] },
      { name: 'Pack Opener Utilities', filename: 'Pack Opener Utilities-865-1-3-2-1775787379.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/PackOpenerUtilities.dll'], installedFiles: ['BepInEx/plugins/PackOpenerUtilities.dll'] },
      { name: 'PlayableTableTop Online', filename: 'PlayableTableTop-Online-881-2-6-4-1773304829.7z', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/IndulgeEplApi'], installedFiles: ['BepInEx/plugins/Phone - Overhaul/App Images/CardMarket/Icon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/CoOp/Icon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/PlayerHomes/Icon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/PokeDecor/Icon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/PokemonTCG/Icon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/PokeRadio/Icon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/WonderTrade/Icon.png', 'BepInEx/plugins/PlayerHomes/editor.json', 'BepInEx/plugins/PokeDecor/Data/custom_posters.json', 'BepInEx/plugins/PokeDecor/Data/Posters/chobin-pokemon.gif', 'BepInEx/plugins/PokeDecor/Data/Posters/EJywBUX.gif', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_01.jpeg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_02.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_03.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_04.jpeg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_05.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_06.jpg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_07.jpeg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_08.jpg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_09.jpg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_10.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_11.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_12.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_13.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_14.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_15.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_16.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_17.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_18.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_19.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_20.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_21.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_22.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_23.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_24.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_25.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_26.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_27.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_28.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_29.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_30.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_31.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_32.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_33.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_34.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_35.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_36.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_37.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_38.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_39.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_40.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_41.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_42.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_43.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_44.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_45.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_46.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_47.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_48.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_49.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_50.jpg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_51.jpg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_52.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_53.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_54.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_55.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_56.jpg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_57.jpg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_58.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_59.jpeg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_60.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_61.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_62.jpg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_63.jpg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_64.jpeg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_65.jpeg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_66.jpeg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_67.jpeg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_68.jpeg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_69.jpeg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_70.jpeg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_71.jpeg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_72.jpeg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_73.jpeg', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_74.png', 'BepInEx/plugins/PokeDecor/Data/Posters/Icon_Poster_75.png', 'BepInEx/plugins/PokeDecor/Data/Posters/pokemon-quagsire.gif', 'BepInEx/plugins/PokeDecor/Data/Posters/super-mario-sunshine-piantas.gif', 'BepInEx/plugins/PokeDecor/Data/Posters/super-smash-bros-melee-smash-bros.gif', 'BepInEx/plugins/PokeDecor/poster_ids.json', 'BepInEx/plugins/PokeManga/Data/Manga/manga_config.json', 'BepInEx/plugins/PokemonEmulator/Overlays/gbc.png', 'BepInEx/plugins/PokemonEmulator/Overlays/gbc_purple.png', 'BepInEx/plugins/PokemonEmulator/Overlays/gb_classic.png', 'BepInEx/plugins/PokemonTCGBattle/Data/base2_gameplay.json', 'BepInEx/plugins/PokemonTCGBattle/Data/base3_gameplay.json', 'BepInEx/plugins/PokemonTCGBattle/Data/base4_gameplay.json', 'BepInEx/plugins/PokemonTCGBattle/Data/base5_gameplay.json', 'BepInEx/plugins/PokemonTCGBattle/Data/base_set_gameplay.json', 'BepInEx/plugins/PokemonTCGBattle/Data/gym1_gameplay.json', 'BepInEx/plugins/PokemonTCGBattle/Data/gym2_gameplay.json', 'BepInEx/plugins/PokemonTCGBattle/Data/playmat.png', 'BepInEx/plugins/PokemonTCGBattle/tcg_icon.png', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/01. Opening Movie (Gold & Silver - Part 1).mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/05 Duel.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/06. Title Screen (Crystal).mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/1-01. Opening Movie - Setting out on a Journey in the Hoenn Region.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/1-02. Theme Of Pallet Town.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/1-03. Professor Oak.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/1-05. Littleroot Town.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/1-07. May.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/1-09. Theme Of Pewter City.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/1-12. Viridian Forest.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/1-13. Pok�mon Center.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/1-20. Pok�mon Gym.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/1-23. Theme Of Vermillion City.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/1-30. Cycling.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/1-33. Theme of Celadon City.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/1-34. Casino.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/1-38. Surf.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/1-45. ~Ending~.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/13. Pok�mon Lab.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/21. Cherrygrove City.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/23 Challenge Hall.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/28. Rival Appears! 1.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/36. Violet City.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/41. Pok�mon Gym.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/53. Azalea Town.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/56. Battle! (Team Rocket).mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/58. Goldenrod City.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/59. Pok�mon Communication Center.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/66. Pok�gear Radio (Pok�mon March).mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/67. Pok�gear Radio (Pok�mon Lullaby).mp3', 'BepInEx/plugins/PokeRadio/Stations/Gameboy/92. Team Rocket HQ.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gamecube/04 - Pok�mon HQ Lab.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gamecube/pc-08-normal_battle.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gamecube/pc-10-pokemon_center.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gamecube/pc-11-es_cade\'s_theme.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gamecube/pc-13-cipher_syndicate\'s_theme.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gamecube/pc-14-pyrite_town.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gamecube/pc-17-miror_b.\'s_retro_groove.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gamecube/pc-19-agate_village.mp3', 'BepInEx/plugins/PokeRadio/Stations/Gamecube/pc-24-shadow_pokemon_lab.mp3', 'BepInEx/plugins/PokeRadio/Stations/N64/01. A Mysterious Sighting.mp3', 'BepInEx/plugins/PokeRadio/Stations/N64/01_-_title_screen.mp3', 'BepInEx/plugins/PokeRadio/Stations/N64/05_-_mystery_gift.mp3', 'BepInEx/plugins/PokeRadio/Stations/N64/06. Professor Oak\'s Laboratory.mp3', 'BepInEx/plugins/PokeRadio/Stations/N64/09. Beach.mp3', 'BepInEx/plugins/PokeRadio/Stations/N64/12. Pok�mon Report.mp3', 'BepInEx/plugins/PokeRadio/Stations/N64/13_-_prof._oak\'s_lab.mp3', 'BepInEx/plugins/PokeRadio/Stations/N64/14_-_stadium.mp3', 'BepInEx/plugins/PokeRadio/Stations/N64/16_-_minigames.mp3', 'BepInEx/plugins/PokeRadio/Stations/N64/22. Valley.mp3', 'BepInEx/plugins/PokeRadio/Stations/N64/33. Jigglypuff\'s Song.mp3', 'BepInEx/plugins/PokeRadio/Stations/N64/62_-_johto_castle.mp3', 'BepInEx/plugins/PokeRadio/Stations/N64/63_-_kanto_castle.mp3', 'BepInEx/plugins/PokeRadio/Stations/N64/Super Mario 64 Soundtrack - Dire, Dire Docks - RadiatorRampardos (128k).mp3', 'BepInEx/plugins/PokeRadio/Stations/TV/My Best Friends.mp3', 'BepInEx/plugins/PokeRadio/Stations/TV/Pokemon World Lyrics (Full Song) - MegaFAKEtroll (128k).mp3', 'BepInEx/plugins/PokeRadio/Stations/TV/Pok�mon Theme - Pok�mon - Topic (128k).mp3', 'BepInEx/plugins/PokeRadio/Stations/TV/Pok�rap (Kanto Version) - Pok�mon - Topic (128k).mp3', 'BepInEx/plugins/PokeRadio/Stations/TV/Viridian City - Pok�mon - Topic (128k).mp3', 'BepInEx/plugins/IndulgeEplApi/EplApi.dll', 'BepInEx/plugins/PlayerHomes/PlayerHomes.dll', 'BepInEx/plugins/PokeDecor/PokeDecor.dll', 'BepInEx/plugins/PokeManga/PokeManga.dll', 'BepInEx/plugins/PokemonEmulator/PokemonEmulator.dll', 'BepInEx/plugins/PokemonTCGBattle/PokemonTCGMod.dll', 'BepInEx/plugins/PokeRadio/PokeRadio.dll'] },
      { name: 'Poke Stop Pokemon Theme Pack', filename: 'Poke Stop Pokemon Theme Pack-250-1-58-1775074746.7z', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/CustomTitleScreen/Poke Stop'], installedFiles: ['BepInEx/plugins/SoundReplacer/sounds/BGM_ShopDay.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopDay1.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopDay10.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopDay11.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopDay2.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopDay3.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopDay4.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopDay5.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopDay6.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopDay7.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopDay8.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopDay9.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopMenu.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopMenu2.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopNight.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopNight1.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopNight10.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopNight11.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopNight2.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopNight3.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopNight4.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopNight5.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopNight6.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopNight7.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopNight8.ogg', 'BepInEx/plugins/SoundReplacer/sounds/BGM_ShopNight9.ogg', 'BepInEx/plugins/SoundReplacer/sounds/SFX_CardReveal.ogg', 'BepInEx/plugins/SoundReplacer/sounds/SFX_CardReveal0.ogg', 'BepInEx/plugins/SoundReplacer/sounds/SFX_FinalizeCard.ogg', 'BepInEx/plugins/SoundReplacer/sounds/TCG Trading Card Simulator - Soundlist.txt', 'BepInEx/plugins/TextureReplacer/objects_meshes/Bin.obj', 'BepInEx/plugins/TextureReplacer/objects_meshes/CardMachine_Large_low.obj', 'BepInEx/plugins/TextureReplacer/objects_meshes/CardTable.obj', 'BepInEx/plugins/TextureReplacer/objects_meshes/counter.obj', 'BepInEx/plugins/TextureReplacer/objects_meshes/shelving.obj', 'BepInEx/plugins/TextureReplacer/objects_meshes/SmallShelfA.obj', 'BepInEx/plugins/TextureReplacer/objects_meshes/Table1_Half.obj', 'BepInEx/plugins/TextureReplacer/objects_textures/Binders/Icon_8_UP_Album.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Binders/Icon_ItemCollectionBook.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Binders/Icon_PremiumCollectionBook.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Binders/T_8_UP_Album.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Binders/T_BinderBook.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Binders/T_BinderBookAllStar.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/CardMachine_Large_mtl_BC.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/CardMachine_Large_mtl_N.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/CardMachine_Medium_mtl_BC.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/CardMachine_Medium_mtl_N.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/CardMachine_Small_mtl_BC.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/CardMachine_Small_mtl_N.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/Large_MS_Specular.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/Medium_MS_Specular.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/PO_AutoPackOpener1.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/PO_AutoPackOpener2.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/PO_AutoPackOpener3.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/Small_MS.png', 'BepInEx/plugins/TextureReplacer/objects_textures/cards/machines/Small_MS_Specular.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Card Display Table/CardDisplayTable.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Card Display Table/CardDisplayTable_plastic.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Card Display Table/Glass_02_MS.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Card Display Table/Glass_Auto_02_MS.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Card Display Table/Glass_MS.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Card Table/CardTable_bottom.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Card Table/CardTable_metal.jpg', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Card Table/CardTable_metal.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Card Table/CardTable_plastic.jpg', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Card Table/CardTable_plastic.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Card Table/CardTable_top.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Card Table/CardTable_top1.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Card Table/metal.jpg', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/carpet.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CashRegister/CashRegister_button.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CashRegister/CashRegister_lambert1 - Copy.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CashRegister/CashRegister_lambert1.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CashRegister/CashRegister_lambert2 - Copy.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CashRegister/CashRegister_lambert2.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CashRegister/CashRegister_lambert3.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CashRegister/CashRegister_lambert4.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CashRegister/CashRegister_lambert5.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CashRegister/CashRegister_lambert6.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CashRegister/CashRegister_plastic.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CashRegister/CashRegister_Screen.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CashRegister/CashRegister_silver.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CashRegister/T_MoneyCoin.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CashRegister/T_MoneyCoinGBP.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CheckoutCounter.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Checkout Counter/CheckoutCounter_metal.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Metal Shelfs/mesh_supermarketshelf-3_AlbedoTransparency.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Play Table/PlayTable_metal.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Play Table/PO_PlayTableA1.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Play Table/wood1.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Sided Shelf Double/SidedShelfDouble.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Sided Shelf Single/SidedShelfSingle.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Small Cabinet/SmallCabinet_metal_medium.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Small Cabinet/SmallCabinet_side.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Trash Bin/TrashBin_metal.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Trash Bin/TrashBin_plastic.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/3x2/CardDisplayWall3X2_metal_dark.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/3x2/CardDisplayWall3X2_metal_mediumdark.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/3x2/PO_CardWallDisplay3X2.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/4x2/CardDisplayWall4X2_metal_dark.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/4x2/CardDisplayWall4X2_metal_mediumdark.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/4x2/PO_CardWallDisplay1.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/Huge Wall Black/CardDisplayWallBig_metal_beige.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/Huge Wall Black/CardDisplayWallBig_metal_dark.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/Huge Wall Black/CardDisplayWallBig_metal_medium.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/Huge Wall Black/CardDisplayWallBig_metal_mediumdark.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/Huge Wall Black/PO_CardWallDisplayBig.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/Huge Wall Card Display - White/CardDisplayBigWallWhite_metal_beige.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/Huge Wall Card Display - White/CardDisplayBigWallWhite_metal_medium.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/Huge Wall Card Display - White/CardDisplayBigWallWhite_metal_mediumdark.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/Huge Wall Card Display - White/PO_CardWallDisplayBigWhite.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/mini wall display/CardDisplayWallSmall_metal_dark.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/mini wall display/CardDisplayWallSmall_metal_mediumdark.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Wall Displays/mini wall display/PO_CardWallDisplaySmall.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Workbench/alt 2.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Workbench/alt1.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Workbench/mesh_supermarketshelf-1_AlbedoTransparency.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Workbench/T_CuttingGridMat.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Workbench/Workbench_metal_dark.jpg', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Workbench/Workbench_metal_dark.png', 'BepInEx/plugins/TextureReplacer/objects_textures/Furniture/Workbench/Workbench_metal_medium.jpg', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/61-2XgvmZOL._AC_SX679_-removebg-preview-removebg-preview-removebg-preview.png', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/Buttons/ForAll/All_BG2_Color.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/Buttons/ForAll/All_BG3_Color.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/Buttons/ForAll/All_BG4_Color.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/Buttons/ForAll/All_BG5_Color.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/Buttons/ForAll/All_BGBlack.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/Buttons/ForAll/All_BGBorder.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/Buttons/ForAll/All_BGHighlight.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/Buttons/ForAll/All_BGMidtone.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/Buttons/ForAll/All_BG_Color.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/Buttons/ForAll/All_BtnInteraction.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/Buttons/ForAll/All_Menu_Text_Color.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/DiscordButton.png', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/GameStartBG_Blur.png', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/GameTitle.png', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/LibraryLogo.png', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/LoadingScreen_ScreenGrp_LoadingScreen_ScreenGrp_BG_(null-sprite).png', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/Roadmap/Roadmap_BG.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/Roadmap/Roadmap_Frame.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/Roadmap/Roadmap_Text_Color.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/menu/Roadmap/Roadmap_Text_Seperator_Color.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/CollectionIcon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/credit_card_D.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/docbinder02o_binder_Mat_BaseColor.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/ExclaimationEmoticon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/GameStartBG_Blur.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/GameTitle.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Icon_8_UP_Album.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Icon_Checkout.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Icon_ImageIcon_Ranking.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Icon_ItemCollectionBook.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Icon_MenuIcon02_Shop.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Icon_PremiumCollectionBook.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Input_Keyboard/KeyboardBtn.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Input_Keyboard/KeyboardBtn_Enter.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Input_Keyboard/KeyboardBtn_Shift.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Input_Keyboard/KeyboardBtn_Spacebar.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Input_Keyboard/KeyboardBtn_Tab.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Input_Keyboard/LeftMouseClick.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Input_Keyboard/LeftMouseHold.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Input_Keyboard/MiddleMouseScroll.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Input_Keyboard/RightMouseClick.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Input_Keyboard/RightMouseHold.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/LibraryLogo.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/Road_Sign_A.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/T_8_UP_Album.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/T_BinderBook.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/T_BinderBookAllStar.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/T_BinderPage.png', 'BepInEx/plugins/TextureReplacer/objects_textures/misc/T_CuttingGridMat.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/buydecor/Phone_BuyDecoration_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/customerreview/Phone_CustomerReview_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/customerreview/Phone_CustomerReview_icon_left.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/customerreview/Phone_CustomerReview_icon_right.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/gameevent/Phone_GameEvent_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/hiring/Phone_Hiring_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/MobileProviderText.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/NetworkIcon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/rentbill/Phone_RentBill_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/restock/Phone_Restock_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/restockboardgame/Phone_RestockBoardGame_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/scannerorder/Phone_ScannerOrder_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/phone/settings/Phone_Setting_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/posters/Icon_Poster 3.png', 'BepInEx/plugins/TextureReplacer/objects_textures/posters/Poster_3.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Billboard.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/CardboardBox_20x20x10_Albedo.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/CardboardBox_20x20x20_Albedo.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/CardboardBox_30x20x20_Albedo.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/ceiling/0_ceiling_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/ceiling/0_ceiling_mainTexture.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/ceiling/0_ceiling_normalMap.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/CollectionIcon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/CounterClosedSign.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/credit_card_D.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/docbinder02o_binder_Mat_BaseColor.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/door wood.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Doors/A/StoreFront_crank_metal_bright.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Doors/A/StoreFront_doorframe_wood.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Doors/A/StoreFront_door_wood.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Doors/A/StoreFront_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Doors/A/StoreFront_metal_bright.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Doors/A/StoreFront_metal_dark.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Doors/B/StoreBFront_crank_metal_bright.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Doors/B/StoreBFront_doorframe2_wood.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Doors/B/StoreBFront_doorframe_wood.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Doors/B/StoreBFront_door_wood.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Doors/B/StoreBFront_glass.txt', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Doors/B/StoreBFront_metal_bright.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Doors/Inner/door wood.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/ExclaimationEmoticon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/ExclaimEmoticon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Floor/0_floor_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Floor/0_floor_mainTexture.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Floor/0_floor_normalMap.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Floor/0_floor_roughnessMap.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Floor/1_floor_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Floor/1_floor_mainTexture.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Floor/1_floor_normalMap.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Floor/1_floor_roughnessMap.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Front Door/Shop A/StoreFront_crank_metal_bright.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Front Door/Shop A/StoreFront_crank_metal_bright1.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Front Door/Shop A/StoreFront_doorframe_wood.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Front Door/Shop A/StoreFront_door_wood.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Front Door/Shop A/StoreFront_metal_bright.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Front Door/Shop A/StoreFront_metal_bright1.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Front Door/Shop A/StoreFront_metal_dark.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Front Door/Shop B/StoreBFront_crank_metal_bright.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Front Door/Shop B/StoreBFront_doorframe2_wood.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Front Door/Shop B/StoreBFront_doorframe_wood.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Front Door/Shop B/StoreBFront_door_wood.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Front Door/Shop B/StoreBFront_metal_bright.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Icon_Checkout.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Icon_MenuIcon02_Shop.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Input_Keyboard/KeyboardBtn.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Input_Keyboard/KeyboardBtn_Enter.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Input_Keyboard/KeyboardBtn_Shift.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Input_Keyboard/KeyboardBtn_Spacebar.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Input_Keyboard/KeyboardBtn_Tab.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Input_Keyboard/LeftMouseClick.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Input_Keyboard/LeftMouseHold.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Input_Keyboard/MiddleMouseScroll.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Input_Keyboard/RightMouseClick.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Input_Keyboard/RightMouseHold.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/mcp_building_32_billboards_d.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Misc/Icon_ImageIcon_Ranking.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Misc/Road_Sign_A.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/PO_PlayTableA1.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/StoreBFront_wall_stone_o.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/StoreFront_brickwallBottom_outside.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/StoreFront_brickwallBottom_outside_m.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/StoreFront_brickwallBottom_outside_n.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/StoreFront_brickwall_outside.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/StoreFront_brickwall_outside_m.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/StoreFront_brickwall_outside_n.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/StoreFront_metal_dark.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/T_BinderPage.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/T_CuttingGridMat.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/T_NoEntranceSign.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/T_OpenCloseSign.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/T_PaperBagAlbedoClosed.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/T_PaperBagAlbedoOpen.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/0_wallbar_mainTexture.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/0_wallbar_roughnessMap.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/0_wall_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/0_wall_mainTexture.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/0_wall_roughnessMap.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/1_wall_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/1_wall_mainTexture.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/1_wall_normalMap.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/1_wall_roughnessMap.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/2_wall_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/2_wall_mainTexture.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/2_wall_normalMap.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/2_wall_roughnessMap.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/3_wall_icon.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/3_wall_mainTexture.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/3_wall_normalMap.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/Wall/3_wall_roughnessMap.png', 'BepInEx/plugins/TextureReplacer/objects_textures/shop/wood1.png'] },
      { name: 'PriceSlinger', filename: 'PriceSlinger-880-1-1-2-1772673553.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/PriceSlinger.dll'], installedFiles: ['BepInEx/plugins/PriceSlinger.dll'] },
      { name: 'PutItBack', filename: 'PutItBack-449-1-1-3-1762363991.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/PutItBack/PutItBack.dll'], installedFiles: ['BepInEx/plugins/PutItBack/PutItBack.dll'] },
      { name: 'Reviews Matter', filename: 'Reviews Matter-654-1-4-0-1776563189.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/ReviewsMatter.dll', 'BepInEx/plugins/ReviewsMatter/ReviewsMatter.dll', 'BepInEx/plugins/Reviews Matter/ReviewsMatter.dll'], installedFiles: ['BepInEx/plugins/Reviews Matter/Reviews Matter.dll', 'BepInEx/plugins/Reviews Matter/v*.txt'] },
      { name: 'Riftbound 01 Origins addon by DraX', filename: 'Riftbound - 01 - Origins addon by DraX-761-1-0-1776479287.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxriftbound-01ogn'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxriftbound-01ogn', 'BepInEx/plugins/DXCardSets_prefabLoader/dxriftbound-01ogn.json'] },
      { name: 'Riftbound 02 Spiritforged addon by DraX', filename: 'Riftbound - 02 - Spiritforged addon by DraX-761-1-1-1776724583.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxriftbound-02sfd'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxriftbound-02sfd', 'BepInEx/plugins/DXCardSets_prefabLoader/dxriftbound-02sfd.json'] },
      { name: 'Riftbound 03 Unleashed addon by DraX', filename: 'Riftbound - 03 - Unleashed addon by DraX-761-1-1-1776724630.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxriftbound-03unl'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxriftbound-03unl', 'BepInEx/plugins/DXCardSets_prefabLoader/dxriftbound-03unl.json'] },
      { name: 'Scanner Overhaul', filename: 'Scanner Overhaul-689-1-2-1772154124.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Scanner Overhaul/Scanner Overhaul.dll'], installedFiles: ['BepInEx/plugins/Scanner Overhaul/Scanner Overhaul.dll', 'BepInEx/plugins/Scanner Overhaul/v*.txt'] },
      { name: 'Shop Overhaul', filename: 'Shop Overhaul-874-1-6-0-1775701607.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Shop Overhaul/Shop Overhaul.dll'], installedFiles: ['BepInEx/plugins/Shop Overhaul/Shop Overhaul.dll', 'BepInEx/plugins/Shop Overhaul/v*.txt'] },
      { name: 'Shopping List Overhaul', filename: 'Shopping List Overhaul-875-2-1-0-1776504174.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Phone - Overhaul/App Images/ShoppingList/IBG.png'], installedFiles: ['BepInEx/plugins/Phone - Overhaul/App Images/ShoppingList/IBG.png', 'BepInEx/plugins/Phone - Overhaul/App Images/ShoppingList/Icon.png', 'BepInEx/plugins/Phone - Overhaul/App Images/ShoppingList/OBG.png', 'BepInEx/plugins/Shopping List Overhaul/Shopping List Overhaul.dll', 'BepInEx/plugins/Shopping List Overhaul/v*.txt'] },
      { name: 'Showcase', filename: 'Showcase-408-2-2-1771694431.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Showcase.dll'], installedFiles: ['BepInEx/plugins/Showcase.dll'] },
      { name: 'SinglesSlinger', filename: 'SinglesSlinger-836-1-9-4-1773074299.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/SinglesSlinger.dll'], installedFiles: ['BepInEx/plugins/SinglesSlinger.dll'] },
      { name: 'Small Item Shelf by DraX', filename: 'Small Item Shelf by DraX-838-1-4-1776047444.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXFurnitureDecor_prefabLoader/dx22smallitemshelf'], installedFiles: ['BepInEx/plugins/DXFurnitureDecor_prefabLoader/dx22smallitemshelf', 'BepInEx/plugins/DXFurnitureDecor_prefabLoader/dx22smallitemshelf.json'] },
      { name: 'Smelly Customers AI Patch', filename: 'Smelly Customers AI Patch-633-2-0-6-1774102884.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Smelly Customers AI Patch/Smelly Customer AI Patch.dll'], installedFiles: ['BepInEx/plugins/Smelly Customers AI Patch/Smelly Customer AI Patch.dll', 'BepInEx/plugins/Smelly Customers AI Patch/v*.txt'] },
      { name: 'SP01 Seaside Memories', filename: 'SP01-Seaside Memories-769-1-0-1776128774.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-sp01'], installedFiles: ['BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-sp01', 'BepInEx/plugins/DXCardSets_prefabLoader/dxshadowverse-sp01.json'] },
      { name: 'Trading Overhaul', filename: 'Trading Overhaul-876-1-5-4-1776563254.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Trading Overhaul/Trading Overhaul.dll'], installedFiles: ['BepInEx/plugins/Trading Overhaul/Trading Overhaul.dll', 'BepInEx/plugins/Trading Overhaul/v*.txt'] },
      { name: 'VCTCGCSSv2.0', filename: 'VCTCGCSSv2.0-849-2-0-1771104502.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/patchers/VCard/_prefabloader/Objects_vcard.json'], installedFiles: ['BepInEx/patchers/VCard/_prefabloader/Objects_vcard.json', 'BepInEx/plugins/Phone - Overhaul/App Images/VCard/Clear.png', 'BepInEx/plugins/Phone - Overhaul/App Images/VCard/Site Logo.png', 'BepInEx/plugins/Phone - Overhaul/App Images/VCard/VCard Shop Logo.png', 'BepInEx/plugins/VCard/_prefabloader/vcard.json'] },
      { name: 'Vintage Magic The Gathering Card Overhaul', filename: 'Vintage Magic The Gathering Card Overhaul v3.4-491-3-4-1772309026.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/MTGExpansion/_prefabloader/mtgexpansion'], installedFiles: ['BepInEx/plugins/MTGExpansion/_prefabloader/mtgexpansion', 'BepInEx/plugins/MTGExpansion/_prefabloader/mtgexpansion.json'] },
      { name: 'Warehouse Shelf Overhaul', filename: 'Warehouse Shelf Overhaul-690-1-2-1772153969.zip', targetKey: 'game_root', core: false, signature: ['BepInEx/plugins/Warehouse Shelf Overhaul/v*.txt'], installedFiles: ['BepInEx/plugins/Warehouse Shelf Overhaul/v*.txt', 'BepInEx/plugins/Warehouse Shelf Overhaul/Warehouse Shelf Overhaul.dll'] },
      { name: 'Wingback Chairs and Play Table by DraX', filename: 'Wingback Chairs and Play Table  by DraX-838-1-3-1776047607.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/DXFurnitureDecor_prefabLoader/wingbacktables'], installedFiles: ['BepInEx/plugins/DXFurnitureDecor_prefabLoader/wingbacktables', 'BepInEx/plugins/DXFurnitureDecor_prefabLoader/wingbacktables.json'] },
      { name: '002 Mega Evolution English', filename: '002 - Mega Evolution English-892-1-2-1773521145.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/002MegaEvolution_prefabLoader/hh_mega_evolution'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/002MegaEvolution_prefabLoader/hh_mega_evolution', 'BepInEx/plugins/HH EPL Pokemons/002MegaEvolution_prefabLoader/hh_mega_evolution.json'] },
      { name: '003 Phantasmal Flames English', filename: '003 - Phantasmal Flames English-892-1-1-1773521598.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/003PhantasmalFlames_prefabLoader/hh_phantasmal_flames'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/003PhantasmalFlames_prefabLoader/hh_phantasmal_flames', 'BepInEx/plugins/HH EPL Pokemons/003PhantasmalFlames_prefabLoader/hh_phantasmal_flames.json'] },
      { name: '004 Ninja Spinner', filename: '004 - Ninja Spinner-892-1-1-1773559037.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/004NinjaSpinner_prefabLoader/004hh_ninja_spinner'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/004NinjaSpinner_prefabLoader/004hh_ninja_spinner', 'BepInEx/plugins/HH EPL Pokemons/004NinjaSpinner_prefabLoader/004hh_ninja_spinner.json'] },
      { name: '005 Nullifying Zero (Nihil Zero) Japanese', filename: '005 - Nullifying Zero (Nihil Zero) Japanese-892-1-2-1773589036.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/005NullifyingZero_prefabLoader/005hh_nullifying_zero'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/005NullifyingZero_prefabLoader/005hh_nullifying_zero', 'BepInEx/plugins/HH EPL Pokemons/005NullifyingZero_prefabLoader/005hh_nullifying_zero.json'] },
      { name: '006 Start Deck 100 Battle Collection Japanese', filename: '006 - Start Deck 100 Battle Collection Japanese-892-1-2-1773592397.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/006StartDeck100BattleCollection_prefabLoader/006hh_startdeck100battlecoll'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/006StartDeck100BattleCollection_prefabLoader/006hh_startdeck100battlecoll', 'BepInEx/plugins/HH EPL Pokemons/006StartDeck100BattleCollection_prefabLoader/006hh_startdeck100battlecoll.json'] },
      { name: '007 Mega Dream EX Japanese', filename: '007 - Mega Dream EX Japanese-892-1-0-1773525350.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/007MegaDreamEX_prefabLoader/007hh_mega_dream_ex_set'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/007MegaDreamEX_prefabLoader/007hh_mega_dream_ex_set', 'BepInEx/plugins/HH EPL Pokemons/007MegaDreamEX_prefabLoader/007hh_mega_dream_ex_set.json'] },
      { name: '008 Inferno X Japanese', filename: '008 - Inferno X Japanese-892-1-1-1773559777.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/008InfernoX_prefabLoader/008hh_inferno_x_jap'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/008InfernoX_prefabLoader/008hh_inferno_x_jap', 'BepInEx/plugins/HH EPL Pokemons/008InfernoX_prefabLoader/008hh_inferno_x_jap.json'] },
      { name: '009 Mega Brave Japanese', filename: '009 - Mega Brave Japanese-892-1-0-1773525670.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/009MegaBrave_prefabLoader/009hh_mega_brave'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/009MegaBrave_prefabLoader/009hh_mega_brave', 'BepInEx/plugins/HH EPL Pokemons/009MegaBrave_prefabLoader/009hh_mega_brave.json'] },
      { name: '010 Mega Symphonia Japanese', filename: '010 - Mega Symphonia Japanese-892-1-0-1773525796.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/010MegaSymphonia_prefabLoader/010hh_mega_symphonia_jap'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/010MegaSymphonia_prefabLoader/010hh_mega_symphonia_jap', 'BepInEx/plugins/HH EPL Pokemons/010MegaSymphonia_prefabLoader/010hh_mega_symphonia_jap.json'] },
      { name: '012 Black Bolt English', filename: '012 - Black Bolt English-892-1-0-1773573754.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/012BlackBolt_prefabLoader/012hh_black_bolt_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/012BlackBolt_prefabLoader/012hh_black_bolt_eng', 'BepInEx/plugins/HH EPL Pokemons/012BlackBolt_prefabLoader/012hh_black_bolt_eng.json'] },
      { name: '013 White Flare English', filename: '013 - White Flare English-892-1-0-1773574669.rar', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/013WhiteFlare_prefabLoader/013hh_white_flare_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/013WhiteFlare_prefabLoader/013hh_white_flare_eng', 'BepInEx/plugins/HH EPL Pokemons/013WhiteFlare_prefabLoader/013hh_white_flare_eng.json'] },
      { name: '014 Destined Rivals Englins', filename: '014 - Destined Rivals Englins-892-1-0-1773608489.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/014Destined Rivals_prefabLoader/014_destined rivals_english'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/014Destined Rivals_prefabLoader/014_destined rivals_english', 'BepInEx/plugins/HH EPL Pokemons/014Destined Rivals_prefabLoader/014_destined rivals_english.json'] },
      { name: '015 Journey Together English', filename: '015 - Journey Together English-892-1-0-1773609299.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/015JourneyTogether_prefabLoader/015hh journey together eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/015JourneyTogether_prefabLoader/015hh journey together eng', 'BepInEx/plugins/HH EPL Pokemons/015JourneyTogether_prefabLoader/015hh journey together eng.json'] },
      { name: '016 Prismatic Evolution English', filename: '016 - Prismatic Evolution English-892-1-1-1775574103.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/016PrismaticEvolution_prefabLoader/016hh_prismatic evolution eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/016PrismaticEvolution_prefabLoader/016hh_prismatic evolution eng', 'BepInEx/plugins/HH EPL Pokemons/016PrismaticEvolution_prefabLoader/016hh_prismatic evolution eng.json'] },
      { name: '017 Surging Sparks English', filename: '017 - Surging Sparks English-892-1-0-1773686879.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/017SurgingSparks_prefabLoader/017_surging_sparks_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/017SurgingSparks_prefabLoader/017_surging_sparks_eng', 'BepInEx/plugins/HH EPL Pokemons/017SurgingSparks_prefabLoader/017_surging_sparks_eng.json'] },
      { name: '018 Stellar Crown English', filename: '018 - Stellar Crown English-892-1-0-1773691552.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/018StellarCrown_prefabLoader/018hh_stellar_crown_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/018StellarCrown_prefabLoader/018hh_stellar_crown_eng', 'BepInEx/plugins/HH EPL Pokemons/018StellarCrown_prefabLoader/018hh_stellar_crown_eng.json'] },
      { name: '019 Shrouded Fable English', filename: '019 - Shrouded Fable English-892-1-0-1773694622.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/019ShroudedFable_prefabLoader/019hh_shrouded_fable_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/019ShroudedFable_prefabLoader/019hh_shrouded_fable_eng', 'BepInEx/plugins/HH EPL Pokemons/019ShroudedFable_prefabLoader/019hh_shrouded_fable_eng.json'] },
      { name: '020 Twilight Masquerade English', filename: '020 - Twilight Masquerade English-892-1-0-1773695872.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/020TwilightMasquerade_prefabLoader/020hh_twilight_masquerade_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/020TwilightMasquerade_prefabLoader/020hh_twilight_masquerade_eng', 'BepInEx/plugins/HH EPL Pokemons/020TwilightMasquerade_prefabLoader/020hh_twilight_masquerade_eng.json'] },
      { name: '021 Temporal Forces English', filename: '021 - Temporal Forces English-892-1-0-1773780815.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/021TemporalForces_prefabLoader/021hh_temporal_forces_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/021TemporalForces_prefabLoader/021hh_temporal_forces_eng', 'BepInEx/plugins/HH EPL Pokemons/021TemporalForces_prefabLoader/021hh_temporal_forces_eng.json'] },
      { name: '022 Paldean Fates English', filename: '022 - Paldean Fates English-892-1-0-1773781297.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/022PaldeanFates_prefabLoader/022hh_paldean_fates_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/022PaldeanFates_prefabLoader/022hh_paldean_fates_eng', 'BepInEx/plugins/HH EPL Pokemons/022PaldeanFates_prefabLoader/022hh_paldean_fates_eng.json'] },
      { name: '023 Paradox Rift English', filename: '023 - Paradox Rift English-892-1-0-1773781943.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/023ParadoxRift_prefabLoader/023hh_paradox_rift_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/023ParadoxRift_prefabLoader/023hh_paradox_rift_eng', 'BepInEx/plugins/HH EPL Pokemons/023ParadoxRift_prefabLoader/023hh_paradox_rift_eng.json'] },
      { name: '024 Scarlet and Violet 151 English', filename: '024 - Scarlet and Violet 151 English-892-1-0-1773782443.rar', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/024Scarlet&Violet151_prefabLoader/024hh_scarlet_violet_151_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/024Scarlet&Violet151_prefabLoader/024hh_scarlet_violet_151_eng', 'BepInEx/plugins/HH EPL Pokemons/024Scarlet&Violet151_prefabLoader/024hh_scarlet_violet_151_eng.json'] },
      { name: '025 Obsidian Flames English', filename: '025 - Obsidian Flames English-892-1-0-1773866404.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/025ObsidianFlames_prefabLoader/025hh_obsidian_flames_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/025ObsidianFlames_prefabLoader/025hh_obsidian_flames_eng', 'BepInEx/plugins/HH EPL Pokemons/025ObsidianFlames_prefabLoader/025hh_obsidian_flames_eng.json'] },
      { name: '026 Paldea Evolved English', filename: '026 - Paldea Evolved English-892-1-0-1773867150.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/026PaldeaEvolved_prefabLoader/026hh_paldea_evolved_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/026PaldeaEvolved_prefabLoader/026hh_paldea_evolved_eng', 'BepInEx/plugins/HH EPL Pokemons/026PaldeaEvolved_prefabLoader/026hh_paldea_evolved_eng.json'] },
      { name: '027 Scarlet And Violet English', filename: '027 - Scarlet And Violet English-892-1-0-1773868351.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/027ScarletAndViolet_prefabLoader/027hh_scarlet_and_violet_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/027ScarletAndViolet_prefabLoader/027hh_scarlet_and_violet_eng', 'BepInEx/plugins/HH EPL Pokemons/027ScarletAndViolet_prefabLoader/027hh_scarlet_and_violet_eng.json'] },
      { name: '028 Crown Zenith English', filename: '028 - Crown Zenith English-892-1-0-1773952940.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/028CrownZenith_prefabLoader/028hh_crown_zenith_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/028CrownZenith_prefabLoader/028hh_crown_zenith_eng', 'BepInEx/plugins/HH EPL Pokemons/028CrownZenith_prefabLoader/028hh_crown_zenith_eng.json'] },
      { name: '029 Silver Tempest English', filename: '029 - Silver Tempest English-892-1-1-1774010452.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/029SilverTempest_prefabLoader/029hh_silver_tempest_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/029SilverTempest_prefabLoader/029hh_silver_tempest_eng', 'BepInEx/plugins/HH EPL Pokemons/029SilverTempest_prefabLoader/029hh_silver_tempest_eng.json'] },
      { name: '030 Lost Origin English', filename: '030 - Lost Origin English-892-1-0-1773953872.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/030LostOrigin_prefabLoader/030hh_lost_origin_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/030LostOrigin_prefabLoader/030hh_lost_origin_eng', 'BepInEx/plugins/HH EPL Pokemons/030LostOrigin_prefabLoader/030hh_lost_origin_eng.json'] },
      { name: '032 Astral Radiance English', filename: '032 - Astral Radiance English-892-1-0-1773954562.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/032AstralRadiance_prefabLoader/032hh_astralradiance_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/032AstralRadiance_prefabLoader/032hh_astralradiance_eng', 'BepInEx/plugins/HH EPL Pokemons/032AstralRadiance_prefabLoader/032hh_astralradiance_eng.json'] },
      { name: '033 Brilliant Stars English', filename: '033 - Brilliant Stars English-892-1-0-1773954980.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/033BrilliantStars_prefabLoader/033hh_brilliant_stars_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/033BrilliantStars_prefabLoader/033hh_brilliant_stars_eng', 'BepInEx/plugins/HH EPL Pokemons/033BrilliantStars_prefabLoader/033hh_brilliant_stars_eng.json'] },
      { name: '034 Fusion Strike English', filename: '034 - Fusion Strike English-892-1-0-1774129397.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/034FusionStrike_prefabLoader/034hh_fusion_strike_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/034FusionStrike_prefabLoader/034hh_fusion_strike_eng', 'BepInEx/plugins/HH EPL Pokemons/034FusionStrike_prefabLoader/034hh_fusion_strike_eng.json'] },
      { name: '035 Celebrations English', filename: '035 - Celebrations English-892-1-0-1774129499.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/035Celebrations_prefabLoader/035hh_celebration_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/035Celebrations_prefabLoader/035hh_celebration_eng', 'BepInEx/plugins/HH EPL Pokemons/035Celebrations_prefabLoader/035hh_celebration_eng.json'] },
      { name: '036 Evolving Skies English', filename: '036 - Evolving Skies English-892-1-0-1774130024.rar', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/036EvolvingSkies_prefabLoader/036hh_evolving_skies_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/036EvolvingSkies_prefabLoader/036hh_evolving_skies_eng', 'BepInEx/plugins/HH EPL Pokemons/036EvolvingSkies_prefabLoader/036hh_evolving_skies_eng.json'] },
      { name: '037 Chilling Reign English', filename: '037 - Chilling Reign English-892-1-0-1774130736.rar', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/037ChillingReign_prefabLoader/037hh_chilling_reign_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/037ChillingReign_prefabLoader/037hh_chilling_reign_eng', 'BepInEx/plugins/HH EPL Pokemons/037ChillingReign_prefabLoader/037hh_chilling_reign_eng.json'] },
      { name: '038 Battle Styles English', filename: '038 - Battle Styles English-892-1-1-1774200292.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/038BattleStyles_prefabLoader/038hh_battle_styles_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/038BattleStyles_prefabLoader/038hh_battle_styles_eng', 'BepInEx/plugins/HH EPL Pokemons/038BattleStyles_prefabLoader/038hh_battle_styles_eng.json'] },
      { name: '039 Shining Fates English', filename: '039 - Shining Fates English-892-1-1-1774200735.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/039ShiningFates_prefabLoader/039hh_shining_fates_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/039ShiningFates_prefabLoader/039hh_shining_fates_eng', 'BepInEx/plugins/HH EPL Pokemons/039ShiningFates_prefabLoader/039hh_shining_fates_eng.json'] },
      { name: '040 Vivid Voltage English', filename: '040 - Vivid Voltage English-892-1-1-1774201224.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/040VividVoltage_prefabLoader/040hh_vivid_voltage_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/040VividVoltage_prefabLoader/040hh_vivid_voltage_eng', 'BepInEx/plugins/HH EPL Pokemons/040VividVoltage_prefabLoader/040hh_vivid_voltage_eng.json'] },
      { name: '041 Champions Path English', filename: '041 - Champions Path English-892-1-1-1774201465.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/041ChampionsPath_prefabLoader/041hh_champion_path_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/041ChampionsPath_prefabLoader/041hh_champion_path_eng', 'BepInEx/plugins/HH EPL Pokemons/041ChampionsPath_prefabLoader/041hh_champion_path_eng.json'] },
      { name: '042 Darkness Ablaze English', filename: '042 - Darkness Ablaze English-892-1-0-1774211603.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/042DarknessAblaze_prefabLoader/042hh_darkness_ablaze_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/042DarknessAblaze_prefabLoader/042hh_darkness_ablaze_eng', 'BepInEx/plugins/HH EPL Pokemons/042DarknessAblaze_prefabLoader/042hh_darkness_ablaze_eng.json'] },
      { name: '043 Rebel Clash English', filename: '043 - Rebel Clash English-892-1-0-1774378624.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/043RebelClash_prefabLoader/043hh_rebel_clash_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/043RebelClash_prefabLoader/043hh_rebel_clash_eng', 'BepInEx/plugins/HH EPL Pokemons/043RebelClash_prefabLoader/043hh_rebel_clash_eng.json'] },
      { name: '044 Sword And Shield English', filename: '044 - Sword And Shield English-892-1-0-1774379940.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/044SwordAndShield_prefabLoader/044hh_sword_and_shield_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/044SwordAndShield_prefabLoader/044hh_sword_and_shield_eng', 'BepInEx/plugins/HH EPL Pokemons/044SwordAndShield_prefabLoader/044hh_sword_and_shield_eng.json'] },
      { name: '045 Trick Or Trade Set English', filename: '045 - Trick Or Trade Set English-892-1-3-1775592263.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/045TrickOrTrade_prefabLoader/045hh_trick or trade_set'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/045TrickOrTrade_prefabLoader/045hh_trick or trade_set', 'BepInEx/plugins/HH EPL Pokemons/045TrickOrTrade_prefabLoader/045hh_trick or trade_set.json'] },
      { name: '046 Perfect Order English', filename: '046 - Perfect Order English-892-1-0-1774632910.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/046PerfectOrder_prefabLoader/046hh_perfect_order_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/046PerfectOrder_prefabLoader/046hh_perfect_order_eng', 'BepInEx/plugins/HH EPL Pokemons/046PerfectOrder_prefabLoader/046hh_perfect_order_eng.json'] },
      { name: '048 Sun And Moon English HIGH SATURATION', filename: '048 - Sun And Moon English HIGH SATURATION-892-1-1775785031.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/048SunMoon_prefabLoader/048hh_sunmoonbase_english'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/048SunMoon_prefabLoader/048hh_sunmoonbase_english', 'BepInEx/plugins/HH EPL Pokemons/048SunMoon_prefabLoader/048hh_sunmoonbase_english.json'] },
      { name: '048(2) Sun and Moon ORIGINAL SATURATION', filename: '048(2) - Sun and Moon ORIGINAL SATURATION-892-1-0-1775785660.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/048SunMoon_prefabLoader/048hh_sunmoonbase_english'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/048SunMoon_prefabLoader/048hh_sunmoonbase_english', 'BepInEx/plugins/HH EPL Pokemons/048SunMoon_prefabLoader/048hh_sunmoonbase_english.json'] },
      { name: '049 Guardians Rising English HIGH SATURATION', filename: '049 - Guardians Rising English HIGH SATURATION-892-1-0-1775785954.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/049GuardiansRising_prefabLoader/049hh_guardians rising_english'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/049GuardiansRising_prefabLoader/049hh_guardians rising_english', 'BepInEx/plugins/HH EPL Pokemons/049GuardiansRising_prefabLoader/049hh_guardians rising_english.json'] },
      { name: '049(2) Guardians Rising English ORIGINAL SATURAT', filename: '049(2) - Guardians Rising English ORIGINAL SATURAT-892-1-0-1775786238.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/049GuardiansRising_prefabLoader/049hh_guardians rising_english'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/049GuardiansRising_prefabLoader/049hh_guardians rising_english', 'BepInEx/plugins/HH EPL Pokemons/049GuardiansRising_prefabLoader/049hh_guardians rising_english.json'] },
      { name: '050 Burning Shadows Englins HIGH SATURATION', filename: '050 - Burning Shadows Englins HIGH SATURATION-892-1-0-1775786559.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/050BurningShadows_prefabLoader/050hh_burning shadows_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/050BurningShadows_prefabLoader/050hh_burning shadows_eng', 'BepInEx/plugins/HH EPL Pokemons/050BurningShadows_prefabLoader/050hh_burning shadows_eng.json'] },
      { name: '050(2) Burning Shadows Englins ORIGINAL SATURATI', filename: '050(2) - Burning Shadows Englins ORIGINAL SATURATI-892-1-0-1775786856.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/050BurningShadows_prefabLoader/050hh_burning shadows_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/050BurningShadows_prefabLoader/050hh_burning shadows_eng', 'BepInEx/plugins/HH EPL Pokemons/050BurningShadows_prefabLoader/050hh_burning shadows_eng.json'] },
      { name: '051 Shining Legends English HIGH SATURATION', filename: '051 - Shining Legends English HIGH SATURATION-892-1-0-1775787010.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/051ShiningLegends_prefabLoader/051hh_shininglegends_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/051ShiningLegends_prefabLoader/051hh_shininglegends_eng', 'BepInEx/plugins/HH EPL Pokemons/051ShiningLegends_prefabLoader/051hh_shininglegends_eng.json'] },
      { name: '051(2) Shining Legends English ORIGINAL SATURATI', filename: '051(2) - Shining Legends English ORIGINAL SATURATI-892-1-0-1775787146.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/051ShiningLegends_prefabLoader/051hh_shininglegends_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/051ShiningLegends_prefabLoader/051hh_shininglegends_eng', 'BepInEx/plugins/HH EPL Pokemons/051ShiningLegends_prefabLoader/051hh_shininglegends_eng.json'] },
      { name: '052 Crimson Invasion English HIGH SATURATION', filename: '052 - Crimson Invasion English HIGH SATURATION-892-1-0-1775787402.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/052CrimsonInvasion_prefabLoader/052hh_crimson invasion_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/052CrimsonInvasion_prefabLoader/052hh_crimson invasion_eng', 'BepInEx/plugins/HH EPL Pokemons/052CrimsonInvasion_prefabLoader/052hh_crimson invasion_eng.json'] },
      { name: '052(2) Crimson Invasion English ORIGINAL SATURAT', filename: '052(2) - Crimson Invasion English ORIGINAL SATURAT-892-1-0-1775787628.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/052CrimsonInvasion_prefabLoader/052hh_crimson invasion_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/052CrimsonInvasion_prefabLoader/052hh_crimson invasion_eng', 'BepInEx/plugins/HH EPL Pokemons/052CrimsonInvasion_prefabLoader/052hh_crimson invasion_eng.json'] },
      { name: '053 Ultra Prism English HIGH SATURATION', filename: '053 - Ultra Prism English HIGH SATURATION-892-1-0-1775787951.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/053UltraPrism_prefabLoader/053hh_ultra prism_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/053UltraPrism_prefabLoader/053hh_ultra prism_eng', 'BepInEx/plugins/HH EPL Pokemons/053UltraPrism_prefabLoader/053hh_ultra prism_eng.json'] },
      { name: '054 Forbidden Light English HIGH SATURATION', filename: '054 - Forbidden Light English HIGH SATURATION-892-1-0-1775788659.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/054ForbiddenLight_prefabLoader/054hh_forbidden light_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/054ForbiddenLight_prefabLoader/054hh_forbidden light_eng', 'BepInEx/plugins/HH EPL Pokemons/054ForbiddenLight_prefabLoader/054hh_forbidden light_eng.json'] },
      { name: '054(2) Forbidden Light English ORIGINAL SATURATI', filename: '054(2) - Forbidden Light English ORIGINAL SATURATI-892-1-0-1775788902.rar', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/054ForbiddenLight_prefabLoader/054hh_forbidden light_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/054ForbiddenLight_prefabLoader/054hh_forbidden light_eng', 'BepInEx/plugins/HH EPL Pokemons/054ForbiddenLight_prefabLoader/054hh_forbidden light_eng.json'] },
      { name: '055 Celestial Storm English HIGH SATURATION', filename: '055 - Celestial Storm English HIGH SATURATION-892-1-0-1775789243.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/055CelestialStorm_prefabLoader/055hh_celestial storm_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/055CelestialStorm_prefabLoader/055hh_celestial storm_eng', 'BepInEx/plugins/HH EPL Pokemons/055CelestialStorm_prefabLoader/055hh_celestial storm_eng.json'] },
      { name: '055(2) Celestial Storm English ORIGINAL SATURATI', filename: '055(2) - Celestial Storm English ORIGINAL SATURATI-892-1-0-1775789622.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/055CelestialStorm_prefabLoader/055hh_celestial storm_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/055CelestialStorm_prefabLoader/055hh_celestial storm_eng', 'BepInEx/plugins/HH EPL Pokemons/055CelestialStorm_prefabLoader/055hh_celestial storm_eng.json'] },
      { name: '056(2) Dragon Majesty English ORIGINAL SATURATIO', filename: '056(2) - Dragon Majesty English ORIGINAL SATURATIO-892-1-0-1775789947.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/056DragonMajesty_prefabLoader/056hh_dragon majesty_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/056DragonMajesty_prefabLoader/056hh_dragon majesty_eng', 'BepInEx/plugins/HH EPL Pokemons/056DragonMajesty_prefabLoader/056hh_dragon majesty_eng.json'] },
      { name: '057 Lost Thunder English HIGH SATURATION', filename: '057 - Lost Thunder English HIGH SATURATION-892-1-0-1775790446.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/057LostThunder_prefabLoader/057hh_lost thunder_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/057LostThunder_prefabLoader/057hh_lost thunder_eng', 'BepInEx/plugins/HH EPL Pokemons/057LostThunder_prefabLoader/057hh_lost thunder_eng.json'] },
      { name: '057(2) Lost Thunder English ORIGINAL SATURATION', filename: '057(2) - Lost Thunder English ORIGINAL SATURATION-892-1-0-1775790826.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/057LostThunder_prefabLoader/057hh_lost thunder_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/057LostThunder_prefabLoader/057hh_lost thunder_eng', 'BepInEx/plugins/HH EPL Pokemons/057LostThunder_prefabLoader/057hh_lost thunder_eng.json'] },
      { name: '058 Team Up English HIGH SATURATION', filename: '058 - Team Up English HIGH SATURATION-892-1-0-1775791198.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/058TeamUp_prefabLoader/058hh_team up_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/058TeamUp_prefabLoader/058hh_team up_eng', 'BepInEx/plugins/HH EPL Pokemons/058TeamUp_prefabLoader/058hh_team up_eng.json'] },
      { name: '058(2) Team Up English ORIGINAL SATURATION', filename: '058(2) - Team Up English ORIGINAL SATURATION-892-1-0-1775791564.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/058TeamUp_prefabLoader/058hh_team up_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/058TeamUp_prefabLoader/058hh_team up_eng', 'BepInEx/plugins/HH EPL Pokemons/058TeamUp_prefabLoader/058hh_team up_eng.json'] },
      { name: '059 Detective Pikachu English HIGH SATURATION', filename: '059 - Detective Pikachu English HIGH SATURATION-892-1-0-1775791631.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/059DetectivePikachu_prefabLoader/059hh_detective pikachu_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/059DetectivePikachu_prefabLoader/059hh_detective pikachu_eng', 'BepInEx/plugins/HH EPL Pokemons/059DetectivePikachu_prefabLoader/059hh_detective pikachu_eng.json'] },
      { name: '059(2) Detective Pikachu English ORIGINAL SATURA', filename: '059(2) - Detective Pikachu English ORIGINAL SATURA-892-1-0-1775791700.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/059DetectivePikachu_prefabLoader/059hh_detective pikachu_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/059DetectivePikachu_prefabLoader/059hh_detective pikachu_eng', 'BepInEx/plugins/HH EPL Pokemons/059DetectivePikachu_prefabLoader/059hh_detective pikachu_eng.json'] },
      { name: '060 Unbroken Bonds English HIGH SATURATION', filename: '060 - Unbroken Bonds English HIGH SATURATION-892-1-0-1775792133.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/060UnbrokenBonds_prefabLoader/060hh_unbroken bonds_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/060UnbrokenBonds_prefabLoader/060hh_unbroken bonds_eng', 'BepInEx/plugins/HH EPL Pokemons/060UnbrokenBonds_prefabLoader/060hh_unbroken bonds_eng.json'] },
      { name: '060(2) Unbroken Bonds English ORIGINAL SATURATIO', filename: '060(2) - Unbroken Bonds English ORIGINAL SATURATIO-892-1-0-1775792515.rar', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/060UnbrokenBonds_prefabLoader/060hh_unbroken bonds_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/060UnbrokenBonds_prefabLoader/060hh_unbroken bonds_eng', 'BepInEx/plugins/HH EPL Pokemons/060UnbrokenBonds_prefabLoader/060hh_unbroken bonds_eng.json'] },
      { name: '061 Unified Minds English HIGH SATURATION', filename: '061 - Unified Minds English HIGH SATURATION-892-1-0-1775793176.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/061UnifiedMinds_prefabLoader/061hh_unified minds_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/061UnifiedMinds_prefabLoader/061hh_unified minds_eng', 'BepInEx/plugins/HH EPL Pokemons/061UnifiedMinds_prefabLoader/061hh_unified minds_eng.json'] },
      { name: '061(2) Unified Minds English ORIGINAL SATURATION', filename: '061(2) - Unified Minds English ORIGINAL SATURATION-892-1-0-1775798775.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/061UnifiedMinds_prefabLoader/061hh_unified minds_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/061UnifiedMinds_prefabLoader/061hh_unified minds_eng', 'BepInEx/plugins/HH EPL Pokemons/061UnifiedMinds_prefabLoader/061hh_unified minds_eng.json'] },
      { name: '062 Hidden Fates English HIGH SATURATION', filename: '062 - Hidden Fates English HIGH SATURATION-892-1-0-1775799332.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/062HiddenFates_prefabLoader/062hh_hidden fates_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/062HiddenFates_prefabLoader/062hh_hidden fates_eng', 'BepInEx/plugins/HH EPL Pokemons/062HiddenFates_prefabLoader/062hh_hidden fates_eng.json'] },
      { name: '062(2) Hidden Fates English ORIGINAL SATURATION', filename: '062(2) - Hidden Fates English ORIGINAL SATURATION-892-1-0-1775799622.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/062HiddenFates_prefabLoader/062hh_hidden fates_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/062HiddenFates_prefabLoader/062hh_hidden fates_eng', 'BepInEx/plugins/HH EPL Pokemons/062HiddenFates_prefabLoader/062hh_hidden fates_eng.json'] },
      { name: '063 Cosmic Eclipse English HIGH SATURATION', filename: '063 - Cosmic Eclipse English HIGH SATURATION-892-1-0-1775800385.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/063CosmicEclipse_prefabLoader/063hh_cosmic eclipse_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/063CosmicEclipse_prefabLoader/063hh_cosmic eclipse_eng', 'BepInEx/plugins/HH EPL Pokemons/063CosmicEclipse_prefabLoader/063hh_cosmic eclipse_eng.json'] },
      { name: '063(2) Cosmic Eclipse English ORIGINAL SATURATIO', filename: '063(2) - Cosmic Eclipse English ORIGINAL SATURATIO-892-1-0-1775800956.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/063CosmicEclipse_prefabLoader/063hh_cosmic eclipse_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/063CosmicEclipse_prefabLoader/063hh_cosmic eclipse_eng', 'BepInEx/plugins/HH EPL Pokemons/063CosmicEclipse_prefabLoader/063hh_cosmic eclipse_eng.json'] },
      { name: '064 XY English HIGH SATURATION', filename: '064 - XY English HIGH SATURATION-892-1-0-1775801815.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/064XY_prefabLoader/064hh_xy_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/064XY_prefabLoader/064hh_xy_eng', 'BepInEx/plugins/HH EPL Pokemons/064XY_prefabLoader/064hh_xy_eng.json'] },
      { name: '064(2) XY English ORIGINAL SATURATION', filename: '064(2) - XY English ORIGINAL SATURATION-892-1-0-1775802558.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/064XY_prefabLoader/064hh_xy_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/064XY_prefabLoader/064hh_xy_eng', 'BepInEx/plugins/HH EPL Pokemons/064XY_prefabLoader/064hh_xy_eng.json'] },
      { name: '065 Flashfire English HIGH SATURATION', filename: '065 - Flashfire English HIGH SATURATION.zip-892-1-0-1775802830.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/065Flashfire_prefabLoader/065hh_flashfire_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/065Flashfire_prefabLoader/065hh_flashfire_eng', 'BepInEx/plugins/HH EPL Pokemons/065Flashfire_prefabLoader/065hh_flashfire_eng.json'] },
      { name: '065(2) Flashfire English ORIGINAL SATURATION', filename: '065(2) - Flashfire English ORIGINAL SATURATION-892-1-0-1775803134.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/065Flashfire_prefabLoader/065hh_flashfire_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/065Flashfire_prefabLoader/065hh_flashfire_eng', 'BepInEx/plugins/HH EPL Pokemons/065Flashfire_prefabLoader/065hh_flashfire_eng.json'] },
      { name: '066 Furious Fists English HIGH SATURATION', filename: '066 - Furious Fists English HIGH SATURATION-892-1-0-1775803666.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/066FuriousFists_prefabLoader/066hh_furious fists_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/066FuriousFists_prefabLoader/066hh_furious fists_eng', 'BepInEx/plugins/HH EPL Pokemons/066FuriousFists_prefabLoader/066hh_furious fists_eng.json'] },
      { name: '066(2) Furious Fists English ORIGINAL SATURATION', filename: '066(2) - Furious Fists English ORIGINAL SATURATION-892-1-0-1775804013.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/066FuriousFists_prefabLoader/066hh_furious fists_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/066FuriousFists_prefabLoader/066hh_furious fists_eng', 'BepInEx/plugins/HH EPL Pokemons/066FuriousFists_prefabLoader/066hh_furious fists_eng.json'] },
      { name: '067 Phantom Forces English HIGH SATURATION', filename: '067 - Phantom Forces English HIGH SATURATION-892-1-0-1775804380.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/067PhantomForces_prefabLoader/067hh_phantom forces_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/067PhantomForces_prefabLoader/067hh_phantom forces_eng', 'BepInEx/plugins/HH EPL Pokemons/067PhantomForces_prefabLoader/067hh_phantom forces_eng.json'] },
      { name: '067(2) Phantom Forces English ORIGINAL SATURATIO', filename: '067(2) - Phantom Forces English ORIGINAL SATURATIO-892-1-0-1775804710.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/067PhantomForces_prefabLoader/067hh_phantom forces_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/067PhantomForces_prefabLoader/067hh_phantom forces_eng', 'BepInEx/plugins/HH EPL Pokemons/067PhantomForces_prefabLoader/067hh_phantom forces_eng.json'] },
      { name: '068 Primal Clash English HIGH SATURATION', filename: '068 - Primal Clash English HIGH SATURATION-892-1-0-1775805151.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/068PrimalClash_prefabLoader/068hh_primal clash_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/068PrimalClash_prefabLoader/068hh_primal clash_eng', 'BepInEx/plugins/HH EPL Pokemons/068PrimalClash_prefabLoader/068hh_primal clash_eng.json'] },
      { name: '068(2) Primal Clash English ORIGINAL SATURATION', filename: '068(2) - Primal Clash English ORIGINAL SATURATION-892-1-0-1775806255.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/068PrimalClash_prefabLoader/068hh_primal clash_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/068PrimalClash_prefabLoader/068hh_primal clash_eng', 'BepInEx/plugins/HH EPL Pokemons/068PrimalClash_prefabLoader/068hh_primal clash_eng.json'] },
      { name: '069 Double Crisis English HIGH SATURATION', filename: '069 - Double Crisis English HIGH SATURATION-892-1-0-1775806405.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/069DoubleCrisis_prefabLoader/069hh_double crisis_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/069DoubleCrisis_prefabLoader/069hh_double crisis_eng', 'BepInEx/plugins/HH EPL Pokemons/069DoubleCrisis_prefabLoader/069hh_double crisis_eng.json'] },
      { name: '069(2) Double Crisis English ORIGINAL SATURATION', filename: '069(2) - Double Crisis English ORIGINAL SATURATION-892-1-0-1775806520.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/069DoubleCrisis_prefabLoader/069hh_double crisis_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/069DoubleCrisis_prefabLoader/069hh_double crisis_eng', 'BepInEx/plugins/HH EPL Pokemons/069DoubleCrisis_prefabLoader/069hh_double crisis_eng.json'] },
      { name: '070 Roaring Skies English HIGH SATURATION', filename: '070 - Roaring Skies English HIGH SATURATION-892-1-0-1775806859.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/070RoaringSkies_prefabLoader/070hh_roaring skies_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/070RoaringSkies_prefabLoader/070hh_roaring skies_eng', 'BepInEx/plugins/HH EPL Pokemons/070RoaringSkies_prefabLoader/070hh_roaring skies_eng.json'] },
      { name: '070(2) Roaring Skies English ORIGINAL SATURATION', filename: '070(2) - Roaring Skies English ORIGINAL SATURATION-892-1-0-1775807243.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/070RoaringSkies_prefabLoader/070hh_roaring skies_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/070RoaringSkies_prefabLoader/070hh_roaring skies_eng', 'BepInEx/plugins/HH EPL Pokemons/070RoaringSkies_prefabLoader/070hh_roaring skies_eng.json'] },
      { name: '071 Ancient Origins English HIGH SATURATION', filename: '071 - Ancient Origins English HIGH SATURATION-892-1-0-1775807661.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/071AncientOrigins_prefabLoader/071hh_ancient origins_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/071AncientOrigins_prefabLoader/071hh_ancient origins_eng', 'BepInEx/plugins/HH EPL Pokemons/071AncientOrigins_prefabLoader/071hh_ancient origins_eng.json'] },
      { name: '071(2) Ancient Origins English ORIGINAL SATURATI', filename: '071(2) - Ancient Origins English ORIGINAL SATURATI-892-1-0-1775808313.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/071AncientOrigins_prefabLoader/071hh_ancient origins_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/071AncientOrigins_prefabLoader/071hh_ancient origins_eng', 'BepInEx/plugins/HH EPL Pokemons/071AncientOrigins_prefabLoader/071hh_ancient origins_eng.json'] },
      { name: '072 BREAKthrough English HIGH SATURATION', filename: '072 - BREAKthrough English HIGH SATURATION-892-1-0-1775808935.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/072BREAKthrough_prefabLoader/072hh_breakthrough_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/072BREAKthrough_prefabLoader/072hh_breakthrough_eng', 'BepInEx/plugins/HH EPL Pokemons/072BREAKthrough_prefabLoader/072hh_breakthrough_eng.json'] },
      { name: '072(2) BREAKthrough English ORIGINAL SATURATION', filename: '072(2) - BREAKthrough English ORIGINAL SATURATION-892-1-0-1775809490.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/072BREAKthrough_prefabLoader/072hh_breakthrough_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/072BREAKthrough_prefabLoader/072hh_breakthrough_eng', 'BepInEx/plugins/HH EPL Pokemons/072BREAKthrough_prefabLoader/072hh_breakthrough_eng.json'] },
      { name: '073 BREAKpoint English HIGH SATURATION', filename: '073 - BREAKpoint English HIGH SATURATION-892-1-0-1775809984.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/073BREAKpoint_prefabLoader/073hh_breakpoint_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/073BREAKpoint_prefabLoader/073hh_breakpoint_eng', 'BepInEx/plugins/HH EPL Pokemons/073BREAKpoint_prefabLoader/073hh_breakpoint_eng.json'] },
      { name: '073(2) BREAKpoint English ORIGINAL SATURATION', filename: '073(2) - BREAKpoint English ORIGINAL SATURATION-892-1-0-1775810502.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/073BREAKpoint_prefabLoader/073hh_breakpoint_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/073BREAKpoint_prefabLoader/073hh_breakpoint_eng', 'BepInEx/plugins/HH EPL Pokemons/073BREAKpoint_prefabLoader/073hh_breakpoint_eng.json'] },
      { name: '074 Generations English HIGH SATURATION', filename: '074 - Generations English HIGH SATURATION-892-1-0-1775811136.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/074Generations_prefabLoader/074hh_generations_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/074Generations_prefabLoader/074hh_generations_eng', 'BepInEx/plugins/HH EPL Pokemons/074Generations_prefabLoader/074hh_generations_eng.json'] },
      { name: '074(2) Generations English ORIGINAL SATURATION', filename: '074(2) - Generations English ORIGINAL SATURATION-892-1-0-1775811604.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/074Generations_prefabLoader/074hh_generations_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/074Generations_prefabLoader/074hh_generations_eng', 'BepInEx/plugins/HH EPL Pokemons/074Generations_prefabLoader/074hh_generations_eng.json'] },
      { name: '075 Fates Collide English HIGH SATURATION', filename: '075 - Fates Collide English HIGH SATURATION-892-1-0-1775812244.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/075FatesCollide_prefabLoader/075hh_fates collide_neg'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/075FatesCollide_prefabLoader/075hh_fates collide_neg', 'BepInEx/plugins/HH EPL Pokemons/075FatesCollide_prefabLoader/075hh_fates collide_neg.json'] },
      { name: '075(2) Fates Collide English ORIGINAL SATURATION', filename: '075(2) - Fates Collide English ORIGINAL SATURATION-892-1-0-1775812729.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/075FatesCollide_prefabLoader/075hh_fates collide_neg'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/075FatesCollide_prefabLoader/075hh_fates collide_neg', 'BepInEx/plugins/HH EPL Pokemons/075FatesCollide_prefabLoader/075hh_fates collide_neg.json'] },
      { name: '076 Steam Siege English HIGH SATURATION', filename: '076 - Steam Siege English HIGH SATURATION-892-1-0-1775813205.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/076SteamSiege_prefabLoader/076hh_steam siege_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/076SteamSiege_prefabLoader/076hh_steam siege_eng', 'BepInEx/plugins/HH EPL Pokemons/076SteamSiege_prefabLoader/076hh_steam siege_eng.json'] },
      { name: '076(2) Steam Siege English ORIGINAL SATURATION', filename: '076(2) - Steam Siege English ORIGINAL SATURATION-892-1-0-1775813719.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/076SteamSiege_prefabLoader/076hh_steam siege_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/076SteamSiege_prefabLoader/076hh_steam siege_eng', 'BepInEx/plugins/HH EPL Pokemons/076SteamSiege_prefabLoader/076hh_steam siege_eng.json'] },
      { name: '077 Evolutions English HIGH SATURATION', filename: '077 - Evolutions English HIGH SATURATION-892-1-0-1775814296.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/077Evolutions_prefabLoader/077hh_evolutions_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/077Evolutions_prefabLoader/077hh_evolutions_eng', 'BepInEx/plugins/HH EPL Pokemons/077Evolutions_prefabLoader/077hh_evolutions_eng.json'] },
      { name: '077(2) Evolutions English ORIGINAL SATURATION', filename: '077(2) - Evolutions English ORIGINAL SATURATION-892-1-0-1775814937.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/077Evolutions_prefabLoader/077hh_evolutions_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/077Evolutions_prefabLoader/077hh_evolutions_eng', 'BepInEx/plugins/HH EPL Pokemons/077Evolutions_prefabLoader/077hh_evolutions_eng.json'] },
      { name: '078 Black And White HIGH SATURATION', filename: '078 - Black And White HIGH SATURATION-892-1-0-1775816395.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/078BlackAndWhite_prefabLoader/078hh_black and white_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/078BlackAndWhite_prefabLoader/078hh_black and white_eng', 'BepInEx/plugins/HH EPL Pokemons/078BlackAndWhite_prefabLoader/078hh_black and white_eng.json'] },
      { name: '078(2) Black And White ORIGINAL SATURATION', filename: '078(2) - Black And White ORIGINAL SATURATION-892-1-0-1775817782.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/078BlackAndWhite_prefabLoader/078hh_black and white_eng'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/078BlackAndWhite_prefabLoader/078hh_black and white_eng', 'BepInEx/plugins/HH EPL Pokemons/078BlackAndWhite_prefabLoader/078hh_black and white_eng.json'] },
      { name: '079 Emerging Powers HIGH SATURATION', filename: '079 - Emerging Powers HIGH SATURATION-892-1-0-1775818794.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/079EmergingPowers_prefabLoader/079hh_emerging powers'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/079EmergingPowers_prefabLoader/079hh_emerging powers', 'BepInEx/plugins/HH EPL Pokemons/079EmergingPowers_prefabLoader/079hh_emerging powers.json'] },
      { name: '080 Noble Victories HIGH SATURATIN', filename: '080 - Noble Victories HIGH SATURATIN-892-1-0-1775820932.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/080NobleVictories_prefabLoader/080hh_noble victories'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/080NobleVictories_prefabLoader/080hh_noble victories', 'BepInEx/plugins/HH EPL Pokemons/080NobleVictories_prefabLoader/080hh_noble victories.json'] },
      { name: '081(2) Next Destinies ORIGINAL SATURATION', filename: '081(2) - Next Destinies ORIGINAL SATURATION-892-1-0-1775821320.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/081NextDestinies_prefabLoader/081hh_next destinies'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/081NextDestinies_prefabLoader/081hh_next destinies', 'BepInEx/plugins/HH EPL Pokemons/081NextDestinies_prefabLoader/081hh_next destinies.json'] },
      { name: '082 Dark Explorers HIGH SATURATION', filename: '082 - Dark Explorers HIGH SATURATION-892-1-0-1775826242.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/082DarkExplorers_prefabLoader/082hh_dark explorers'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/082DarkExplorers_prefabLoader/082hh_dark explorers', 'BepInEx/plugins/HH EPL Pokemons/082DarkExplorers_prefabLoader/082hh_dark explorers.json'] },
      { name: '082(2) Dark Explorers ORIGINAL SATURATION', filename: '082(2) - Dark Explorers ORIGINAL SATURATION-892-1-0-1775826390.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/082DarkExplorers_prefabLoader/082hh_dark explorers'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/082DarkExplorers_prefabLoader/082hh_dark explorers', 'BepInEx/plugins/HH EPL Pokemons/082DarkExplorers_prefabLoader/082hh_dark explorers.json'] },
      { name: '083 Dragon Exalted HIGH SATURATION', filename: '083 - Dragon Exalted HIGH SATURATION-892-1-0-1775826549.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/083DragonExalted_prefabLoader/083hh_dragons exalted'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/083DragonExalted_prefabLoader/083hh_dragons exalted', 'BepInEx/plugins/HH EPL Pokemons/083DragonExalted_prefabLoader/083hh_dragons exalted.json'] },
      { name: '083(2) Dragon Exalted ORIGINAL SATURATION', filename: '083(2) - Dragon Exalted ORIGINAL SATURATION-892-1-0-1775826732.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/083DragonExalted_prefabLoader/083hh_dragons exalted'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/083DragonExalted_prefabLoader/083hh_dragons exalted', 'BepInEx/plugins/HH EPL Pokemons/083DragonExalted_prefabLoader/083hh_dragons exalted.json'] },
      { name: '084 Dargon Vault HIGH SATURATION', filename: '084 - Dargon Vault HIGH SATURATION-892-1-0-1775826817.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/084DargonVault_prefabLoader/084hh_dragon vault'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/084DargonVault_prefabLoader/084hh_dragon vault', 'BepInEx/plugins/HH EPL Pokemons/084DargonVault_prefabLoader/084hh_dragon vault.json'] },
      { name: '084(2) Dargon Vault ORIGINAL SATURATION', filename: '084(2) - Dargon Vault ORIGINAL SATURATION-892-1-0-1775826852.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/084DargonVault_prefabLoader/084hh_dragon vault'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/084DargonVault_prefabLoader/084hh_dragon vault', 'BepInEx/plugins/HH EPL Pokemons/084DargonVault_prefabLoader/084hh_dragon vault.json'] },
      { name: '085(2) Boundaries Crossed ORIGINAL SATURATION', filename: '085(2) - Boundaries Crossed ORIGINAL SATURATION-892-1-0-1775827206.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/085BoundariesCrossed_prefabLoader/085hh_boundaries crossed'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/085BoundariesCrossed_prefabLoader/085hh_boundaries crossed', 'BepInEx/plugins/HH EPL Pokemons/085BoundariesCrossed_prefabLoader/085hh_boundaries crossed.json'] },
      { name: '086 Plasma Storm HIGH SATURATION', filename: '086 - Plasma Storm HIGH SATURATION-892-1-0-1775827359.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/086PlasmaStorm_prefabLoader/086hh_plasma storm'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/086PlasmaStorm_prefabLoader/086hh_plasma storm', 'BepInEx/plugins/HH EPL Pokemons/086PlasmaStorm_prefabLoader/086hh_plasma storm.json'] },
      { name: '086(2) Plasma Storm ORIGINAL SATURATION', filename: '086(2) - Plasma Storm ORIGINAL SATURATION-892-1-0-1775827504.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/086PlasmaStorm_prefabLoader/086hh_plasma storm'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/086PlasmaStorm_prefabLoader/086hh_plasma storm', 'BepInEx/plugins/HH EPL Pokemons/086PlasmaStorm_prefabLoader/086hh_plasma storm.json'] },
      { name: '087(2) Plasma Freeze ORIGINAL SATURATION', filename: '087(2) - Plasma Freeze ORIGINAL SATURATION-892-1-0-1775827908.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/087PlasmaFreeze_prefabLoader/087hh_plasma freeze'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/087PlasmaFreeze_prefabLoader/087hh_plasma freeze', 'BepInEx/plugins/HH EPL Pokemons/087PlasmaFreeze_prefabLoader/087hh_plasma freeze.json'] },
      { name: '088(2) Plasma Blast ORIGINAL SATURATION', filename: '088(2) - Plasma Blast ORIGINAL SATURATION-892-1-0-1775828254.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/088PlasmaBlast_prefabLoader/088hh_plasma blast'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/088PlasmaBlast_prefabLoader/088hh_plasma blast', 'BepInEx/plugins/HH EPL Pokemons/088PlasmaBlast_prefabLoader/088hh_plasma blast.json'] },
      { name: '089(2) Legendary Treasures ORIGINAL SATURATION', filename: '089(2) - Legendary Treasures ORIGINAL SATURATION-892-1-0-1775828604.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/089LegendaryTreasures_prefabLoader/089hh_legendary treasures'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/089LegendaryTreasures_prefabLoader/089hh_legendary treasures', 'BepInEx/plugins/HH EPL Pokemons/089LegendaryTreasures_prefabLoader/089hh_legendary treasures.json'] },
      { name: 'Boundaries Crossed HIGH SATURATION', filename: 'Boundaries Crossed HIGH SATURATION-892-1-0-1775827023.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/085BoundariesCrossed_prefabLoader/085hh_boundaries crossed'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/085BoundariesCrossed_prefabLoader/085hh_boundaries crossed', 'BepInEx/plugins/HH EPL Pokemons/085BoundariesCrossed_prefabLoader/085hh_boundaries crossed.json'] },
      { name: 'Noble Victories ORIGINAL SATURATIN', filename: 'Noble Victories ORIGINAL SATURATIN-892-1-0-1775821067.zip', targetKey: 'game_root', core: true, signature: ['BepInEx/plugins/HH EPL Pokemons/080NobleVictories_prefabLoader/080hh_noble victories'], installedFiles: ['BepInEx/plugins/HH EPL Pokemons/080NobleVictories_prefabLoader/080hh_noble victories', 'BepInEx/plugins/HH EPL Pokemons/080NobleVictories_prefabLoader/080hh_noble victories.json'] },
    ];
  }

  /** Scan game directory for known mods not tracked by the manager */
  scanUntracked() {
    if (!this.gamePath) return [];

    // Build set of already-tracked mod names (case-insensitive)
    const trackedNames = new Set();
    for (const mod of this.mods.values()) {
      trackedNames.add(mod.name.toLowerCase());
    }

    const found = [];
    const knownDB = this._getKnownModsDB();

    // Pre-compute: top-level-of-plugins container folder set for Phase 2.
    // A container folder is any BepInEx/plugins/<Folder>/ that holds content
    // from 2+ known mods (e.g. "Real TCG Overhaul", "HH EPL Pokemons").
    // Phase 2's "unknown mod" scan must NOT treat these as standalone mods —
    // they're just parent folders for multiple mods whose siblings are
    // already handled in Phase 1.
    const containerFolders = new Set();
    const pluginTopCounts = new Map();
    for (const k of knownDB) {
      for (const sig of k.signature) {
        const sp = sig.split('/');
        if (sp.length >= 3 && sp[0].toLowerCase() === 'bepinex' && sp[1].toLowerCase() === 'plugins') {
          const top = sp[2].toLowerCase();
          const set = pluginTopCounts.get(top) || new Set();
          set.add(k.name);
          pluginTopCounts.set(top, set);
        }
      }
    }
    for (const [folder, modSet] of pluginTopCounts.entries()) {
      if (modSet.size >= 2) containerFolders.add(folder);
    }

    // Pre-compute: for every OTHER known mod, the path segments that uniquely
    // identify its files. When walking a candidate mod's directory, we exclude
    // anything that matches another known mod's signature path — this prevents
    // a mod from slurping its siblings' files when they share a parent folder.
    // E.g. "BepInEx/plugins/HH EPL Pokemons/001A..." and "...002M..." share
    // "HH EPL Pokemons/" as a parent, so walking the parent would grab both.
    //
    // Two modes of foreignness:
    //   (a) file lives under another mod's EXCLUSIVE folder (deeper than any
    //       shared container) → foreign
    //   (b) file IS another mod's exact signature path, or lies under the
    //       signature path (e.g. signature is a file AND that file+sibling
    //       metadata files form a set) → foreign
    // For mods that share a parent folder (like DXCardSets_prefabLoader/ with
    // 70 DraX SV/XY/etc. sets inside), (a) alone isn't enough because the
    // "exclusive folder" of each is the shared parent. (b) handles that.
    const isForeignPath = (relPath, ownSigPrefix, ownName) => {
      const lower = relPath.toLowerCase();
      for (const other of knownDB) {
        if (ownName && other.name.toLowerCase() === ownName.toLowerCase()) continue;
        for (const otherSig of other.signature) {
          const otherLower = otherSig.toLowerCase();
          // (b) File IS another mod's exact signature, or lies inside a folder
          // named like another mod's signature file (companion .json/.meta files).
          if (lower === otherLower) return true;
          if (lower.startsWith(otherLower + '.') || lower.startsWith(otherLower + '/')) return true;
          // (a) File lies under another mod's EXCLUSIVE folder (deeper than any
          // shared container we'd walk).
          const otherSigParts = otherSig.split('/');
          if (otherSigParts.length < 2) continue;
          const otherFolder = otherSigParts.slice(0, -1).join('/').toLowerCase();
          if (!otherFolder) continue;
          // Skip shared top-level containers — many standalone plugins live
          // directly under BepInEx/plugins/ with sig files like
          // "BepInEx/plugins/Foo.dll". Their sig parent is "BepInEx/plugins"
          // which is NOT an exclusive folder marker. Without this skip, any
          // file inside ANY subfolder of BepInEx/plugins/ would be flagged
          // foreign — dropping every legitimate file during walk.
          if (otherFolder === 'bepinex/plugins' || otherFolder === 'bepinex') continue;
          // Skip if the "other folder" is our own walk prefix — we share a
          // parent with that mod, so folder-level exclusion is useless. Rule
          // (b) above already handled file-level exclusion for this case.
          if (otherFolder + '/' === ownSigPrefix.toLowerCase()) continue;
          if (lower.startsWith(otherFolder + '/')) return true;
        }
        // (c) File lies under another mod's declared contentPaths — used for
        // cross-folder mods that install content INTO another framework mod's
        // folder. e.g. "01 Base Expansions" has sig under Real TCG Overhaul/
        // but also owns BaseSet/, Fossil/, etc. subfolders inside Holographic
        // Overhaul/Holographics/. Without this, a scan of Holographic Overhaul
        // would slurp 01 Base Expansions' 340 content files.
        if (Array.isArray(other.contentPaths)) {
          for (const cp of other.contentPaths) {
            const cpLower = cp.toLowerCase();
            if (lower === cpLower || lower.startsWith(cpLower + '/')) return true;
          }
        }
      }
      return false;
    };

    // Phase 1: Check known mods by signature files
    const claimedSigs = new Set(); // lower-cased sig files already claimed by a found mod
    for (const known of knownDB) {
      // Skip if already tracked
      if (trackedNames.has(known.name.toLowerCase())) continue;

      const isBepInExMod = known.name.toLowerCase().includes('bepinex') && known.core;
      // Guard: for non-BepInEx mods, ignore signatures rooted in BepInEx/config or
      // BepInEx/cache — those are user-editable and auto-regenerated respectively,
      // and survive uninstallation, which would cause false positives (ghost mods).
      const isSharedPath = (p) => {
        const lower = p.toLowerCase();
        return lower.startsWith('bepinex/config/') || lower.startsWith('bepinex/cache/');
      };
      const effectiveSigs = isBepInExMod
        ? known.signature
        : known.signature.filter(s => !isSharedPath(s));
      if (effectiveSigs.length === 0) continue;

      // De-dup: if another mod already claimed this EXACT signature file, skip.
      // Handles cases like HIGH/ORIGINAL SATURATION variants that share a
      // signature file (same files on disk, just different Nexus uploads —
      // only one can be tracked because both would own the same files).
      // The DB is ordered so the canonical variant (the first one) wins.
      //
      // We dedup on the exact sig file, NOT the sig parent folder — because
      // mods in shared-parent layouts (DraX card sets under DXCardSets_prefabLoader/,
      // HH sets under HH EPL Pokemons/) have DIFFERENT sig files but share
      // a parent. Those are legitimate siblings and must all be detected.
      if (!isBepInExMod) {
        const alreadyClaimed = effectiveSigs.some(s => claimedSigs.has(s.toLowerCase()));
        if (alreadyClaimed) continue;
      }

      // Check if signature files exist on disk
      const sigMatch = effectiveSigs.some(sig => {
        const full = path.join(this.gamePath, sig);
        return fs.existsSync(full);
      });

      if (sigMatch) {
        // Walk the mod's main folder to get all its files.
        let files = [];
        const firstSig = known.signature[0];
        const parts = firstSig.split('/');

        // AUTHORITATIVE TRACKING: if the DB entry declares installedFiles, that
        // list IS the ground truth — it's the exact set of files the mod's zip
        // installs. Use it directly and skip all walker/filter guesswork. Only
        // files that actually exist on disk are kept (a file missing on disk
        // means the user manually removed it). This prevents framework mods
        // like Phone Overhaul from slurping content-mod assets that happen to
        // live in their folder, and guarantees Disable moves the complete set.
        if (Array.isArray(known.installedFiles) && known.installedFiles.length > 0) {
          files = this._resolveInstalledFiles(known.installedFiles);
        } else if (isBepInExMod) {
          // Walk BepInEx/ directory — include everything except other mods' plugin folders
          const bepDir = path.join(this.gamePath, 'BepInEx');
          if (fs.existsSync(bepDir) && fs.statSync(bepDir).isDirectory()) {
            files = this._walkDir(bepDir, this.gamePath).filter(f => {
              const lower = f.toLowerCase();
              // Exclude config/ (user settings) and cache/ (auto-regenerated) — these survive uninstall
              if (lower.startsWith('bepinex/config/') || lower.startsWith('bepinex/cache/')) return false;
              // Exclude plugin subfolders that aren't ConfigurationManager (those are other mods)
              if (lower.startsWith('bepinex/plugins/') && !lower.startsWith('bepinex/plugins/configurationmanager/')) return false;
              return true;
            });
          }
          // Also include root-level BepInEx files
          const rootBepFiles = ['.doorstop_version', 'doorstop_config.ini', 'winhttp.dll', 'changelog.txt'];
          for (const rf of rootBepFiles) {
            const full = path.join(this.gamePath, rf);
            if (fs.existsSync(full) && fs.statSync(full).isFile()) {
              files.push(rf);
            }
          }
        } else {
          // Walk the DEEPEST folder unique to this mod — the immediate parent of
          // the signature file. For a signature like
          //   BepInEx/plugins/HH EPL Pokemons/001AscendedHeroes_prefabLoader/hh_ascended_heroes
          // we walk .../001AscendedHeroes_prefabLoader/, NOT .../HH EPL Pokemons/
          // (which is shared with 119 other HH mods and would slurp them all).
          //
          // Some mods need a wider walk scope than sig's immediate parent — e.g.
          // a mod whose signature lives several folders deep inside its own
          // unique tree. Those entries can set `walkDir` to a path that contains
          // the full mod footprint. We still apply the foreign-path filter so
          // sibling mods' files aren't included.
          //
          // Special case: when the signature's parent is `BepInEx/plugins` itself
          // (a top-level DLL or folder directly under plugins/), there's no
          // unique subfolder to walk — walking plugins/ would grab every other
          // plugin's content. In that case we just track the signature file(s).
          const sigParentParts = parts.slice(0, -1);
          const sigParentStr = sigParentParts.join('/').toLowerCase();
          const plainTopLevelPlugin = !known.walkDir && (
            sigParentStr === 'bepinex/plugins' || sigParentStr === 'bepinex'
          );
          const walkParts = known.walkDir ? known.walkDir.split('/') : sigParentParts;
          let walked = false;
          if (!plainTopLevelPlugin && walkParts.length > 0) {
            const modRoot = path.join(this.gamePath, ...walkParts);
            if (fs.existsSync(modRoot) && fs.statSync(modRoot).isDirectory()) {
              const ownPrefix = walkParts.join('/') + '/';
              const rawFiles = this._walkDir(modRoot, this.gamePath);
              // Defensive filter: even though we're walking the tightest scope,
              // if another known mod's signature-folder happens to be NESTED under
              // this one, exclude those files from this mod's tracked list.
              files = rawFiles.filter(f => !isForeignPath(f, ownPrefix, known.name));
              walked = true;
            }
          }
          // For loose DLLs (signature is just a filename at game root), top-level
          // plugin DLLs, or when no walkable directory exists, fall back to
          // the signature files themselves.
          if (!walked || files.length === 0) files = known.signature;

          // Union ALL existing signature files into the tracked list. Handles
          // mods like Grading Overhaul whose signature points at a subfolder
          // file (for walking) but also needs its top-level helper DLL
          // (GradeDataLifeSaver.dll) tracked. Files are only added if they
          // actually exist on disk, and duplicates are naturally deduped.
          const trackedSet = new Set(files.map(f => f.toLowerCase()));
          for (const sig of known.signature) {
            const sigFull = path.join(this.gamePath, sig);
            if (fs.existsSync(sigFull) && !trackedSet.has(sig.toLowerCase())) {
              files.push(sig);
              trackedSet.add(sig.toLowerCase());
            }
          }

          // Union declared contentPaths: folders outside the mod's own sig
          // tree where the mod installs content (e.g. iiTzSamurai expansions
          // have sigs under Real TCG Overhaul/ but install 300+ files into
          // Holographic Overhaul/Holographics/BaseSet/, Fossil/, etc.).
          // Walking each declared path and unioning the files ensures the
          // full install footprint is tracked, so Disable/Uninstall moves
          // the complete set.
          if (Array.isArray(known.contentPaths)) {
            for (const cp of known.contentPaths) {
              const cpFull = path.join(this.gamePath, cp);
              try {
                if (!fs.existsSync(cpFull)) continue;
                const stat = fs.statSync(cpFull);
                if (stat.isDirectory()) {
                  const walked = this._walkDir(cpFull, this.gamePath);
                  for (const f of walked) {
                    if (!trackedSet.has(f.toLowerCase())) {
                      files.push(f);
                      trackedSet.add(f.toLowerCase());
                    }
                  }
                } else if (stat.isFile()) {
                  if (!trackedSet.has(cp.toLowerCase())) {
                    files.push(cp);
                    trackedSet.add(cp.toLowerCase());
                  }
                }
              } catch {}
            }
          }

          // Apply global + per-mod exclude rules (shared icons, BepInEx
          // configs/cache). Keeps uninstall surgical.
          files = this._applyExcludeRules(files, known);
        }

        // Always use game_root — scanned files are relative to gamePath
        found.push({
          name: known.name,
          version: null,
          filename: known.filename,
          location: known.signature[0].split('/').slice(0, -1).join('/') || known.signature[0],
          targetKey: 'game_root',
          targetLabel: 'Game Root Folder',
          core: known.core,
          files,
          fileCount: files.length,
          known: true,
        });

        // Record this signature's files as claimed. Subsequent known mods
        // whose signature points at the SAME file (HIGH/ORIGINAL SATURATION
        // variants that share assets) will be skipped by the de-dup check above.
        if (!isBepInExMod) {
          for (const s of effectiveSigs) claimedSigs.add(s.toLowerCase());
        }
      }
    }

    // Phase 2: Generic scan for completely unknown mods
    const allTracked = new Set();
    for (const mod of this.mods.values()) {
      if (!mod.files) continue;
      for (const f of mod.files) {
        if (mod.targetKey === 'game_root') allTracked.add(f.toLowerCase());
        else if (mod.targetKey === 'bepinex') allTracked.add(('BepInEx/' + f).toLowerCase());
        else if (mod.extractTo) allTracked.add(path.relative(this.gamePath, path.join(mod.extractTo, f)).replace(/\\/g, '/').toLowerCase());
      }
    }
    // Also mark found known mods as tracked
    for (const f of found) {
      for (const file of f.files) allTracked.add(file.toLowerCase());
    }

    // containerFolders was pre-computed at the top of this method and is used
    // below in Phase 2 to skip parent folders shared by multiple known mods.

    // Pre-compute: folders already "owned" by a Phase 1 find — the sig parent
    // of every found mod. Phase 2 must skip these (case-insensitive) so we
    // don't double-import a known mod as a second unknown entry just because
    // the folder name on disk has different casing than the DB signature.
    const phase1ClaimedFolderNames = new Set();
    for (const f of found) {
      const locParts = (f.location || '').split('/');
      if (locParts.length >= 3 && locParts[0].toLowerCase() === 'bepinex' && locParts[1].toLowerCase() === 'plugins') {
        phase1ClaimedFolderNames.add(locParts[2].toLowerCase());
      }
      // Also look at each file's first plugin-subfolder path segment
      for (const file of (f.files || [])) {
        const fp = file.split('/');
        if (fp.length >= 3 && fp[0].toLowerCase() === 'bepinex' && fp[1].toLowerCase() === 'plugins') {
          phase1ClaimedFolderNames.add(fp[2].toLowerCase());
        }
      }
    }

    // Also skip BepInEx/plugins/<folder> subfolders that contain files already
    // tracked by an EXISTING mod in this.mods (not just Phase 1 finds).
    // Example: EndGame Overhaul is already tracked with
    // "BepInEx/plugins/Endgame Overhaul/Endgame Overhaul.dll" in its files list.
    // The user updates the mod so their on-disk version text file is now
    // "v2.2.1.txt" instead of the DB's "v2.0.10.txt" — that stray file is an
    // orphan from a tracked mod, not an untracked mod. Skip the whole folder.
    const trackedPluginFolders = new Set();
    for (const mod of this.mods.values()) {
      if (!mod.files) continue;
      for (const f of mod.files) {
        const parts = f.split('/');
        if (parts.length >= 3 && parts[0].toLowerCase() === 'bepinex' && parts[1].toLowerCase() === 'plugins') {
          trackedPluginFolders.add(parts[2].toLowerCase());
        }
      }
    }

    const skipDirs = new Set(['core', 'cache', 'config', 'configurationmanager']);
    const pluginsDir = path.join(this.gamePath, 'BepInEx', 'plugins');
    if (fs.existsSync(pluginsDir)) {
      for (const entry of fs.readdirSync(pluginsDir, { withFileTypes: true })) {
        const lowerName = entry.name.toLowerCase();
        if (skipDirs.has(lowerName)) continue;
        if (!entry.isDirectory()) continue;
        // Skip container folders — they're not standalone mods, they just hold
        // content from multiple known mods (e.g. Real TCG Overhaul, HH EPL Pokemons).
        if (containerFolders.has(lowerName)) continue;
        // Skip folders already claimed by a Phase 1 find (case-insensitive).
        // Without this, "Endgame Overhaul" on disk (lower g) would re-import
        // as a phantom second mod when Phase 1 already matched DB entry
        // "EndGame Overhaul" (cap G) via its signature inside this folder.
        if (phase1ClaimedFolderNames.has(lowerName)) continue;
        // Skip folders that already contain files from an EXISTING tracked mod.
        // Those files in the folder belong to the tracked mod (even if some are
        // orphans like a newer version's text file that isn't in the DB's
        // authoritative installedFiles list). This prevents phantom re-imports
        // whenever a user updates a mod and the new zip has a different version
        // sidecar or extra files not captured in the DB entry.
        if (trackedPluginFolders.has(lowerName)) continue;
        const full = path.join(pluginsDir, entry.name);
        const files = this._walkDir(full, this.gamePath);
        const untracked = files.filter(f => !allTracked.has(f.toLowerCase()));
        // Dedup against Phase 1 finds — case-insensitive name match.
        if (untracked.length > 0 && !found.some(f => f.name.toLowerCase() === lowerName)) {
          found.push({
            name: entry.name,
            version: null,
            filename: null,
            location: 'BepInEx/plugins/' + entry.name,
            targetKey: 'game_root',
            targetLabel: 'Game Root Folder',
            core: false,
            files: untracked,
            fileCount: untracked.length,
            known: false,
          });
        }
      }
    }

    return found;
  }

  /** Import an untracked mod into the manager's database */
  importUntracked(name, files, targetKey, version, filename, core) {
    // Case-insensitive name collision → update existing entry (don't duplicate)
    let id;
    const lowerName = (name || '').toLowerCase();
    for (const [existingId, existingMod] of this.mods) {
      if ((existingMod.name || '').toLowerCase() === lowerName) {
        id = existingId;
        break;
      }
    }
    if (!id) id = 'mod_' + Date.now() + '_' + Math.random().toString(36).substr(2, 6);
    // Apply global + per-mod exclude rules so imported mods don't claim
    // files that belong to another standalone mod or are BepInEx-regenerated.
    files = this._applyExcludeRules(files, this._findKnownModByName(name));
    const hasPrefabloader = files.some(f => f.toLowerCase().includes('_prefabloader'));
    const target = TARGETS[targetKey];
    const isCore = core !== undefined ? core : (this._isKnownCore(name) || hasPrefabloader);

    // Imported mods always use game_root — scanned files are relative to gamePath
    const mod = {
      id,
      name,
      version: version || null,
      filename: filename || null,
      targetKey: 'game_root',
      targetLabel: 'Game Root Folder',
      extractTo: this.gamePath,
      enabled: true,
      core: isCore,
      installedAt: new Date().toISOString(),
      files,
      fileCount: files.length,
      imported: true,
    };
    this.mods.set(id, mod);
    this._saveDb();
    this._autoAssignGroupByName(id, name, files);
    return mod;
  }

  // ═══ MOD GROUPS ═══

  /**
   * Rules for auto-assigning freshly-installed/imported mods to groups.
   * First match wins. Rules can match on:
   *   - namePattern: regex against the mod name
   *   - pathPattern: regex against any file path in the mod
   *   - names: explicit Set of lower-cased mod names (exact match)
   * Patterns are case-insensitive. Group is created if missing.
   * Order matters when multiple rules could match — keep specific first.
   */
  _autoGroupRules() {
    // Explicit mod list for authors whose mods share no common name prefix or
    // file-path signature. Each name is a canonical DB name (lower-cased for
    // comparison). Keep this list in sync with the DB's entries for the author.
    const MUNCHMATOAST_NAMES = new Set([
      'ptcgo iitzsamurai compatibility patch',
      'ptcgo economics',
      'worker ai overhaul',
      'ptcgo extras',
      'wall grids',
      'endgame overhaul',
      'shop overhaul',
      'shopping list overhaul',
      'trading overhaul',
      'crouch overhaul',
      'binder overhaul',
      'draw distance overhaul',
      'bepinex mod manager',
      'warehouse shelf overhaul',
      'scanner overhaul',
      'reviews matter',
      'item targeting ai patch',
      'card targeting ai overhaul',     // aka "Card Targeting AI Patch"
      'smelly customers ai patch',
      'card table overhaul',
      'faster interactions overhaul',   // aka "Faster Interactions"
    ]);

    const KNARF247_NAMES = new Set([
      'customer manager',
      "rce's civil draft tcg",
      'the shop manager',
      'marvel tcg - marvel vs. system',
      'marvel vs.',                     // canonical DB name for the same mod
      'night manager',
      'warehouse manager',
    ]);

    // iiTzSamurai's mods — "Real TCG Overhaul" content packs (expansions, theme
    // decks, figurines, textures, manga). All live under Real TCG Overhaul/
    // or TextureReplacer/. Mods are numbered by category and each one is a
    // standalone pack, so match by explicit name list.
    const IITZSAMURAI_NAMES = new Set([
      '00 pokemon shop phone app',
      '00 yugioh shop phone app',
      '01 base expansions',
      '01 base theme decks',
      '01 legend of blue eyes white dragon expansion',
      '01 pokemon adventures manga',
      '01 pokemon plushies',
      '01 yu gi oh manga',
      '02 gym expansions',
      '02 gym theme decks',
      '02 pokemon gameboy games',
      '02 pokemon shop textures',
      '02 yu gi oh starter decks',
      '03 neo expansions',
      '03 pokemon n64 games',
      '03 yu gi oh structure decks',
      '04 legendary collection expansion',
      '04 pokemon statues',
      '05 e card series expansions',
      'pokemon accessories',
      'pokemon expansions',
      'pokemon figurines',
      'pokemon shop app',
      'pokemon statues',
      'rtcgo custom tv',
      'yu gi oh shop phone app',
      'yu-gi-oh manga',
      'yu-gi-oh starter decks',
      'yu-gi-oh structure decks',
    ]);

    // The 6 mods the game stack depends on. Grouped together and pinned to
    // the top of the Installed Mods list via displayGroups in InstalledMods.jsx.
    // Accepts common name variants (e.g. "Phone Overhaul" vs "Phone - Overhaul").
    const MAIN_CORE_NAMES = new Set([
      'bepinex',
      'bepinex with configuration manager',
      'pre-configured bepinex with configuration manager',
      'enhanced prefab loader',
      'enhancedprefabloader',
      'phone overhaul',
      'phone - overhaul',
      'grading overhaul',
      'holographic overhaul',
      'collection tracker',
      'collectiontracker',
    ]);

    return [
      // Main Core Mods takes precedence over everything — these 6 framework mods
      // are grouped together at the top so the user can see critical deps at a glance.
      { names: MAIN_CORE_NAMES, groupName: 'Main Core Mods' },
      { namePattern: /\bdrax\b/i, groupName: 'DraX Mods' },
      // HellHound: identified by file-path markers because their mod names are
      // generic (numbered card sets like "001 Ascended Heroes"). Their content
      // ships under one of these path patterns at a segment boundary:
      //   HH  (folder with space), HHPokemon (folder), hh_ (file prefix)
      { pathPattern: /(^|[\\/])(HH\s|HHPokemon|hh_)/i, groupName: 'HellHound Mods' },
      // Munchmatoast: mods have no shared naming convention or path signature
      // (each is a standalone plugin). Match by explicit name list against the
      // known canonical titles.
      { names: MUNCHMATOAST_NAMES, groupName: 'Munchmatoast Mods' },
      // Knarf247: same approach — standalone plugins with no shared signature.
      // Includes alternate name for Marvel VS. which ships as "Marvel TCG -
      // Marvel Vs. System" on Nexus but is tracked internally as "Marvel VS."
      { names: KNARF247_NAMES, groupName: 'Knarf247 Mods' },
      // iiTzSamurai: Real TCG Overhaul content packs. Match by name list (covers
      // the phone-app mods that only drop icons into Phone Overhaul) OR by path —
      // every card/accessory/figurine/statue/expansion pack installs files under a
      // "Real TCG Overhaul/" folder (sometimes nested inside Holographic Overhaul's
      // Holographics tree), so the path pattern future-proofs new packs without
      // needing to extend the name list each release.
      { names: IITZSAMURAI_NAMES, pathPattern: /(^|[\\/])Real TCG Overhaul[\\/]/i, groupName: 'iiTzSamurai Mods' },
    ];
  }

  /**
   * If the mod matches an auto-group rule, place it in that group.
   * Creates the group on demand. Called from installMod and importUntracked.
   * Respects user intent: only auto-assigns when the mod has no group yet,
   * never moves mods the user has manually placed elsewhere.
   */
  _autoAssignGroupByName(modId, name, files) {
    if (!modId) return;
    const rules = this._autoGroupRules();
    const fileList = Array.isArray(files) ? files : [];
    const lowerName = (name || '').toLowerCase();
    const rule = rules.find(r => {
      if (r.namePattern && name && r.namePattern.test(name)) return true;
      if (r.pathPattern && fileList.some(f => r.pathPattern.test(f))) return true;
      if (r.names && lowerName && r.names.has(lowerName)) return true;
      return false;
    });
    if (!rule) return;

    const groups = this.getModGroups();

    // Skip if this mod is already in some group (user may have placed it)
    if (groups.some(g => g.modIds.includes(modId))) return;

    let target = groups.find(g => g.name === rule.groupName);
    if (!target) target = this.createModGroup(rule.groupName);
    this.setModGroup(modId, target.id);
  }

  getModGroups() { return store.get('modGroups', []); }
  createModGroup(name) {
    const groups = this.getModGroups();
    const id = 'group_' + Date.now();
    groups.push({ id, name, modIds: [] });
    store.set('modGroups', groups);
    return { id, name, modIds: [] };
  }
  deleteModGroup(id) {
    store.set('modGroups', this.getModGroups().filter(g => g.id !== id));
  }
  renameModGroup(id, name) {
    const groups = this.getModGroups();
    const g = groups.find(g => g.id === id);
    if (g) { g.name = name; store.set('modGroups', groups); }
  }
  /**
   * Rename an installed mod's display name. This changes the name used for
   * display and, going forward, for same-mod matching on reinstall (via
   * _baseName). The mod's stable `id` is unchanged, so file tracking, grouping
   * membership, enabled state, and everything keyed on id are unaffected.
   */
  renameMod(modId, newName) {
    const mod = this.mods.get(modId);
    if (!mod) throw new Error(`Mod not found: ${modId}`);
    const clean = String(newName == null ? '' : newName).replace(/\s+/g, ' ').trim();
    if (!clean) throw new Error('Name cannot be empty');
    mod.name = clean;
    this._saveDb();
    return mod;
  }
  reorderModGroups(orderedIds) {
    const groups = this.getModGroups();
    const map = new Map(groups.map(g => [g.id, g]));
    const reordered = orderedIds.map(id => map.get(id)).filter(Boolean);
    // Append any groups not in the list (safety)
    for (const g of groups) { if (!orderedIds.includes(g.id)) reordered.push(g); }
    store.set('modGroups', reordered);
  }
  setModGroup(modId, groupId) {
    const groups = this.getModGroups();
    for (const g of groups) { g.modIds = g.modIds.filter(id => id !== modId); }
    if (groupId) {
      const target = groups.find(g => g.id === groupId);
      if (target) target.modIds.push(modId);
    }
    store.set('modGroups', groups);
  }
  setModsGroup(modIds, groupId) {
    const groups = this.getModGroups();
    for (const g of groups) { g.modIds = g.modIds.filter(id => !modIds.includes(id)); }
    if (groupId) {
      const target = groups.find(g => g.id === groupId);
      if (target) target.modIds.push(...modIds);
    }
    store.set('modGroups', groups);
  }

  /**
   * Re-applies auto-group rules to all currently-tracked mods. Useful after
   * new rules are added (e.g. introducing a "Munchmatoast Mods" group after
   * those mods were already imported, so they weren't auto-grouped originally).
   *
   * Respects user intent the same way _autoAssignGroupByName does: mods that
   * the user has manually placed in a group are NOT moved. Only ungrouped
   * mods get assigned.
   *
   * Returns { success, assigned, total } where `assigned` is the number of
   * previously-ungrouped mods that landed in a group this run.
   */
  regroupAllMods() {
    let assigned = 0;
    const total = this.mods.size;
    for (const [id, mod] of this.mods.entries()) {
      const groups = this.getModGroups();
      const alreadyGrouped = groups.some(g => g.modIds.includes(id));
      if (alreadyGrouped) continue;
      // _autoAssignGroupByName is a no-op for mods that are already grouped,
      // so we can just call it. It reads fresh groups on each call which
      // keeps state consistent as we iterate.
      const before = this.getModGroups().some(g => g.modIds.includes(id));
      this._autoAssignGroupByName(id, mod.name, mod.files);
      const after = this.getModGroups().some(g => g.modIds.includes(id));
      if (!before && after) assigned++;
    }
    return { success: true, assigned, total };
  }
}

module.exports = new ModManager();
